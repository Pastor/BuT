// Файл с намеренной синтаксической ошибкой.
// Используется для проверки обработки ошибок парсинга при импорте.

model BrokenModel {
    state BadState {
        if (true)       // ОШИБКА: if недопустим внутри тела состояния
    }
}
