# Узаконенный долг гейта scripts/check-book-traces.py.
#
# Трассы, которые прогоном не воспроизводятся по устройству самого примера.
# Список - ратчет: запись можно только удалять, добавлять - нет.
#
# Запись, которая начала сходиться, роняет прогон: иначе долг растёт молча.
#
# Формат: путь#номер-трассы-в-файле | причина
#
# Сегодня долга нет: последняя запись (выбег вентилятора, 180 000 тактов) снята
# фичей 0523 - прогон больше не обрывается на последнем шаге сценария.
