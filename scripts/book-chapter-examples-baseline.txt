# Узаконенный долг проверки scripts/check-book-chapter-examples.py.
#
# Блоки глав, у которых обещание не проверяется прогоном по устройству самого
# примера. Список - ратчет: запись можно только удалять (дополнив пример до
# самодостаточного), добавлять - нет.
#
# Запись, которая начала проходить, роняет прогон: иначе долг растёт молча.
#
# Формат: путь#номер-блока-в-файле | причина
#
# Сегодня долга нет: обе записи сняты - пример, показывавший ошибку
# закомментированной строкой, разделён на отказ и обход, а перечень мест записи
# отделён от самого отказа.
