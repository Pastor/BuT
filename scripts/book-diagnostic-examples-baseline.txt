# Узаконенный долг проверки scripts/check-book-diagnostic-examples.py.
#
# Пары "пример -> ожидаемый вывод", которые прогоном не воспроизводятся, потому
# что пример намеренно не является программой. Список - ратчет: запись можно
# только удалять (дополнив пример до самодостаточного), добавлять - нет.
#
# Запись, которая начала воспроизводиться, проверка объявляет протухшей и роняет
# прогон: иначе долг растёт молча и перестаёт быть долгом.
#
# Формат: ключ | причина
#
# Сегодня долга нет: обе записи (`SE-062`, `SY-005`) сняты - пределы
# вложенности показаны настоящим входом вместо многоточия.
