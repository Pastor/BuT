/// Фикстура: композиция функций `f -> g` внутри одной модели.
/// r = f(5) = g(5) + 10 = (5 + 1) + 10 = 16.
var r: u8 := 0;
fn g(x: u8) -> u8 { return x + 1; }
fn f(x: u8) -> u8 { return g(x) + 10; }
start Idle {
    always { r := f(5); }
}
