// Переполнение `+` над q(8,8) - перенос, а не насыщение
// перенос идёт к ширине формата). max repr = 32767 (127.996...); 32767 + 1 = 32768 -> -32768.
model FixedWrap {
    var big: q(8, 8) := 127.99609375;
    var one: q(8, 8) := 0.00390625;
    start S0 { always { big := big + one; } }
}
start Entry = FixedWrap;
