/// Инвариант модели нарушается. c растёт, P = c = 0 ложно со 2-го
/// такта. Симулятор обязан остановиться (SIM-025), как assert()->abort() в C.
var c: u8 := 0;
invariant P = c = 0;
start A { always { c := c + 1; } ref B; }
state B { ref A; }
