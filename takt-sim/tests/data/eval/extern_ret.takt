/// Внешняя функция со значением.
///
/// Тела нет, поэтому вычислить значение нельзя. Ожидается отказ SIM-019,
/// а не тихий ноль (решение ADR 0025).
extern fn calc() -> u8;

var x: u8 := 0;

start Idle {
    always { x := calc(); }
}
