/// Смешение целого и вещественного в условии.
///
/// Без вычислителя роняет эталон: unwrap() на None в extract_number.
/// Ожидается: 1 + 2.5 = 3.5 > 3 -> переход в Hot, без паники.
var t: u8 := 1;

start Idle {
    ref Hot: t + 2.5 > 3;
}

state Hot {
}
