/// Сравнение с вариантом перечисления.
///
/// Без вычислителя `EnumVariant` даёт Err, поэтому `mode = Manual` при
/// mode=Manual было ложным.
enum Mode {
    Auto = 0,
    Manual = 1
}

var mode: Mode := Manual;

start Idle {
    ref Hot: mode = Manual;
}

state Hot {
}
