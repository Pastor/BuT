/// Деление на ноль.
///
/// В C это UB. Симулятор обязан отказать с диагностикой (SIM-001), а не
/// паниковать и не подставлять тихий ноль.
var d: u8 := 0;
var x: u8 := 1;

start Idle {
    always { x := 10 / d; }
}
