// Применение фичи 0469: подключает файл, чья модель носит имя файла.
//
// Наблюдаемое - порт `beat` библиотеки: трасса 1, 2, 3, 3 возможна только
// тогда, когда тикает модель `Clash`, а не её одноимённый контейнер.
import "clash.takt";

start Main = Clash;
