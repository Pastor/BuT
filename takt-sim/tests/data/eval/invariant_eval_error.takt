/// Условие инварианта само не вычисляется (индекс за границей
/// массива, SIM-010). Это ошибка достоверности, а не "инвариант ложен" -
/// обязана дать Failed даже в мягком режиме (R4).
var arr: [u8;2] := {0, 0};
invariant Q = arr[5] = 0;
start A { ref A; }
