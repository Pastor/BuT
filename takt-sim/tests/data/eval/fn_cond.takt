/// Вызов функции в условии перехода.
///
/// Без вычислителя роняет эталон: unimplemented!() в predicate.rs.
var t: u8 := 0;

fn ready(v: u8) -> u8 {
    return 1;
}

start Idle {
    ref Hot: ready(t) > 0;
}

state Hot {
}
