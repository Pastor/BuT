/// Скобки в условии перехода.
///
/// Без вычислителя `Parenthesis` возвращается невычисленным, поэтому `(t + 1) > 2`
/// при t=5 было ложным и модель навсегда оставалась в Idle.
var t: u8 := 5;

start Idle {
    ref Hot: (t + 1) > 2;
}

state Hot {
}
