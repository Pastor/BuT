/// Скобки в условии перехода.
///
/// До фичи 0025 `Parenthesis` возвращался невычисленным, поэтому `(t + 1) > 2`
/// при t=5 было ложным и модель навсегда оставалась в Idle.
var t: u8 := 5;

start Idle {
    ref Hot: (t + 1) > 2;
}

state Hot {
}
