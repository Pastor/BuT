[package]
name = "but-simulators"
version = "0.1.0"
authors = ["Pastor <viruszold@gmail.com>"]
edition = "2021"
license = "BSD"

[dependencies]
but-grammar = { path = "../grammar/but" }
indexmap = "2"
thiserror = "2"
