# Фича: Переименование языка Lam → Takt (крейты, тулы, расширение, документация)

- **Номер:** 0100
- **Статус:** ГОТОВО
- **Зависит от:** нет (жёстких зависимостей; см. анализ — рекомендация брать
  **до** [вынос оставшихся нарушителей размера модуля (ядро семантики + пришпиленные)](0099-module-size-core.md) из-за общих файлов и реестра размера)
- **Связанные issue (анализ):** новая фича (запрос заказчика 2026-07-24)
- **Приоритет / Tier:** **Tier 3** (крупный сквозной рефакторинг идентичности; не
  багфикс). **Обратно несовместим** (расширение файлов, имена бинарников/крейтов)
  — обоснование по своду в [анализе](0100-language-rename-takt.md#анализ).

## Стадии жизненного цикла

Все стадии — **разделы этой карточки**: «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог».
Отдельным артефактом остаются только исправления —
[`docs/fixes/`](../fixes/README.md) (при необходимости `0100-YY-*`).

## Краткое описание

Переименовать язык проекта **Lam → Takt** сквозным образом: имя языка, расширение
исходников, имена бинарников/крейтов, идентификаторы кода, LSP, грамматику,
плагин IntelliJ, все примеры, документацию, скрипты и проверки. Ключевые слова и
семантика языка **не меняются** — переименовывается только идентичность
(название, расширение, тулчейн-имена).

- **Имя языка:** `Takt` (возможная расшифровка — **T**yped, **A**utomata,
  **K**nown **T**iming; финальный бэкроним утверждает заказчик на стадии ADR).
- **Расширение файлов:** `.lam` → `.takt`.
- **Компилятор (бинарник):** `lamc` → `taktc`.
- **LSP (бинарник):** `lam-lsp` → предположительно `takt-lsp` (уточнить в ADR).
- **Крейт:** `takt-lang` (какой из двух крейтов так называется и как быть со
  вторым — решает ADR; сейчас крейты `grammar` и `simulation`).

## Карта переименования (предварительная, уточняется в ADR/анализе)

| Было | Стало | Примечание |
|---|---|---|
| Язык `Lam` (Language of Automata Models) | Язык `Takt` (Typed, Automata, Known Timing) | бэкроним — за ADR |
| `.lam` | `.takt` | расширение исходников (**breaking**) |
| `lamc` | `taktc` | бинарник компилятора (`grammar/src/bin/lamc.rs` → `taktc.rs`) |
| `lam-lsp` / `lam_lsp` | `takt-lsp` / `takt_lsp` | LSP-бинарник (`bin/lam_lsp.rs`) |
| крейт `grammar` | `takt-lang` (?) | имя крейта компилятора; путь импорта `grammar::…` → `takt_lang::…` |
| крейт `simulation` | ? | второе имя крейта — за ADR (напр. `takt-sim`) |
| `LANGUAGE_VERSION` (0085), якорь `**Версия языка: X.Y.Z**` в README | без изменения номера, но контекст «Takt» | проверка `check-language-version.sh` держать зелёным |
| плагин `extensions/intellij-lam/`, пакет `org/lam/`, классы `Lam*` | `intellij-takt/`, `org/takt/`, `Takt*` | IntelliJ-плагин целиком |
| `Lam.ebnf` | `Takt.ebnf` | грамматика EBNF |
| `examples/*.lam` (244 файла), `run_simulations.sh` матчинг | `examples/*.takt` | примеры + скрипты |

## Объём (метрики на 2026-07-24, ориентир для декомпозиции)

- **244** файла `.lam` (переименование + правки перекрёстных ссылок/`import`);
- **~324** трекнутых файла содержат слово `Lam`;
- **~414** файлов содержат `.lam`/`lamc`/`lam-lsp`/`lam_lsp`;
- плагин IntelliJ — отдельное дерево (`extensions/intellij-lam/`, Kotlin,
  собственный `LamKeywordSyncTest`/round-trip);
- проверки `precheck.sh`/CI, скрипты (`run_simulations.sh`, `check-*.sh`), сравнение
  порождённого кода (`examples/generated/`).

## Развилки — решены ([ADR](0100-language-rename-takt.md#архитектура-adr), заказчик 2026-07-24)

1. **Обратная совместимость** → **жёсткий разрыв**: принимается
   только `.takt`, `.lam` не распознаётся (проект до 1.0, внешних потребителей
   нет). Обоснование — драйвер 2 ADR.
2. **Имена крейтов** → **полный ренейм**: `grammar`→`takt-lang`,
   `simulation`→`takt-sim`, **с** обновлением всех путей импорта
   (`grammar::`→`takt_lang::`, `simulation::`→`takt_sim::`).
3. **Версия языка** → **подъём `0.4.0`→`0.5.0`**, тег `v0.5.0`.
4. **Бэкроним `Takt`** → **Typed, Automata, Known Timing** (официально).
5. **Репозиторий/релизные артефакты** → упоминания в коде правятся; фактический
   переезд `github.com/Pastor/BuT` — **вне фичи** (административный, позже).
6. **Детерминизм/снапшоты** → `examples/generated/` меняются **законно** (имена в
   `#include`); эталоны пересобираются в слое примеров (0100-04), детерминизм-проверка
   подтверждает стабильность.

## Стратегия (черновик, финализируется в ADR)

Механическое сквозное переименование **декомпозировать** по слоям,
каждый слой — отдельная подзадача `0100-YY` с зелёным `precheck.sh`:
крейты/бинарники → расширение и лексер/CLI → идентификаторы кода и пути импорта →
примеры и генерация → плагин IntelliJ → документация (README/README.typ/CLAUDE.md/
CHANGES.md/docs) → скрипты/проверки. Каждая — по возможности чистое переименование с
сохранением поведения; изменения вывода (имена в `#include`) — под сверку.

> Фича зарегистрирована **2026-07-24 по запросу заказчика**; далее проходит
> жизненный цикл по своду (ADR → анализ → разработка). Текст выше —
> **фиксация запроса и предварительный объём**, а не принятое решение: карта имён,
> обратная совместимость, имена крейтов и подъём версии определяются на стадиях
> 2–3.

## Архитектура (ADR)

- **Status:** Accepted
- **Date:** 2026-07-24
- **Authors:** Архитектор (+ решения заказчика по развилкам)
- **Related issues:** [Фича](0100-language-rename-takt.md); затрагивает
  инварианты ADR (константа версии языка), ADR (`bin/lamc` пришпилен к
  baseline размера), процессные проверки `precheck.sh`/CI.

### Context

Проект переименовывает язык **Lam** (Language of Automata Models) в **Takt**
(бэкроним — **T**yped, **A**utomata, **K**nown **T**iming). Это смена
**идентичности**, а не синтаксиса/семантики: ключевые слова, грамматика
(`grammar.lalrpop`), семантические проходы и генераторы кода **не меняются** —
меняются только имена, расширение файлов и тексты.

Текущее состояние (метрики 2026-07-24): язык зовётся `Lam`, расширение `.lam`
(**244** файла), компилятор `lamc`, LSP `lam-lsp`, крейты `grammar` и
`simulation`, плагин IntelliJ `extensions/intellij-lam/` (пакет `org/lam`, классы
`Lam*`), грамматика `Lam.ebnf`. Слово `Lam` встречается в **~324** трекнутых
файлах, токены `.lam`/`lamc`/`lam-lsp` — в **~414**.

> **(диаграмма PlantUML) неприменимо:** ADR **не** меняет синтаксис,
> семантику языка и потоки данных между компонентами — только имена и тексты.
> Конвейер компиляции (`.takt` → лексер → LALRPOP → AST → семантика → генераторы)
> структурно тождествен прежнему. Схема не требуется.

### Decision Drivers

1. **Единый источник идентичности.** После ренейма не должно остаться двух имён
   языка (дрейф «Lam в коде, Takt в доке» — тот же класс проблемы, что 0085
   закрыла для версии). Ренейм — сквозной или его нет.
2. **Обратная совместимость.** Смена расширения и имён бинарников/
   крейтов **ломает** пользовательские сборки и скрипты. Проект **до 1.0**,
   гарантированных внешних потребителей нет — цена разрыва мала, цена
   половинчатой миграции (два расширения навсегда) велика.
3. **Прослеживаемость и зелёные проверки.** Каждый слой ренейма оставляет
   `precheck.sh` зелёным (сборка/тесты/детерминизм/размер модулей/версия-якорь).
4. **Управляемость диффа.** ~324 файла — механическая правка; нужна декомпозиция
   по слоям, а не один гигантский коммит.

### Considered Options

#### Option A. Сквозной жёсткий ренейм (полный)

Переименовать **всё**: расширение `.lam`→`.takt` (только `.takt` принимается),
бинарники `lamc`→`taktc`/`lam-lsp`→`takt-lsp`, крейты `grammar`→`takt-lang` и
`simulation`→`takt-sim` **вместе с путями импорта** (`grammar::`→`takt_lang::`),
идентификаторы кода, плагин, EBNF, все примеры и документацию. Версия языка
`0.4.0`→`0.5.0` (тег `v0.5.0`).

**Pros:**
- Единая идентичность, ноль остаточного `Lam`; крейт `takt-lang` реально
  появляется (требование заказчика).
- Чистая история: после закрытия слово `Lam` остаётся только в архивных
  артефактах стадий (карточки закрытых фич — исторический контекст).

**Cons:**
- Крупнейший дифф проекта; правка путей импорта — сотни мест.
- Снапшоты `examples/generated/` **законно** меняются (`#include "X.h"` при смене
  имён примеров) — нужна согласованная пересборка эталонов.

#### Option B. Ренейм с `.lam`-алиасом (мягкая миграция)

Как A, но компилятор/LSP/симулятор продолжают принимать `.lam` наравне с `.takt`
на переходный период.

**Pros:**
- Не ломает существующие пользовательские файлы разом.

**Cons:**
- Два расширения — источник дрейфа (драйвер 1); тесты/примеры двоятся или
  фиксируют старое имя. Для до-1.0-проекта без внешних потребителей цена
  совместимости не оправдана.

#### Option C. Только внешние имена (минимальный churn)

Переименовать бинарники (`taktc`/`takt-lsp`) и расширение, но **оставить**
внутренние имена крейтов `grammar`/`simulation` и пути импорта.

**Pros:**
- Минимальный дифф; пути импорта не трогаются.

**Cons:**
- Крейт `takt-lang` **не появляется** (прямо противоречит запросу заказчика);
  внутри код остаётся «Lam-проектом». Идентичность половинчата.

### Decision

Принимается **Option A** (сквозной жёсткий ренейм) — по решениям заказчика от
2026-07-24:

- **Расширение:** жёсткий разрыв — принимается **только `.takt`**; все 244 файла
  переименованы, `.lam` больше не распознаётся (драйвер 2, до-1.0).
- **Крейты и импорты:** полный ренейм — `grammar`→`takt-lang`, `simulation`→
  `takt-sim`, **все пути импорта обновляются** (`grammar::`→`takt_lang::`,
  `simulation::`→`takt_sim::`). Имя пакета крейта = `takt-lang`/`takt-sim`, имя
  для `use` = `takt_lang`/`takt_sim` (Rust заменяет `-`→`_`).
- **Бинарники:** `lamc`→`taktc` (`bin/lamc.rs`→`bin/taktc.rs`), `lam-lsp`→
  `takt-lsp` (`bin/lam_lsp.rs`→`bin/takt_lsp.rs`).
- **Плагин IntelliJ:** `extensions/intellij-lam`→`intellij-takt`, пакет
  `org/lam`→`org/takt`, классы `Lam*`→`Takt*`, `LamFileType` распознаёт `.takt`.
- **Грамматика EBNF:** `Lam.ebnf`→`Takt.ebnf`.
- **Версия языка:** `0.4.0`→`0.5.0`, коммит подъёма помечается тегом `v0.5.0`: ренейм — изменение, связанное с языком (новая идентичность и
  расширение), хотя ключевые слова/семантика неизменны.
- **Бэкроним:** официально — **Typed, Automata, Known Timing**.

**Вне кода (отметить, не выполнять в рамках фичи):** переезд репозитория
`github.com/Pastor/BuT` и имена релизных артефактов CI — решение заказчика позднее;
константа/URL репозитория в коде правится, сам переезд — административный.

### Consequences

#### Положительные

- Единая идентичность **Takt** во всём проекте; крейт `takt-lang` появляется.
- `check-language-version.sh` (0085) остаётся источником истины версии; ренейм
  синхронно правит контекст `LANGUAGE_VERSION` и якорь README.
- Детерминизм-проверка продолжает охранять вывод: после законной пересборки снапшотов
  `examples/generated/` дифф снова становится нулевым.

#### Отрицательные / Action items

- **Крупный дифф**, декомпозируется анализом на подзадачи `0100-YY` по слоям;
  каждый слой — зелёный `precheck.sh`.
- **Снапшоты `examples/generated/` меняются законно** — пересобрать эталоны в том
  же коммите, где переименованы примеры; детерминизм-проверка подтвердит стабильность.
- **Пути импорта** (`grammar::`) — массовая механическая правка; риск — пропуск в
  файле под фичей `lsp` (ловит только `--all-features`, т.е. `precheck.sh`).
- **`bin/lamc.rs` пришпилен к baseline размера** (реестр `module-size-baseline.txt`
  — запись `grammar/src/bin/lamc.rs`): при переименовании файла запись реестра
  обязана переехать на путь нового крейта/бинарника (иначе проверка размера покраснеет
  на «новом нарушителе»). То же — все пути в реестре с `grammar/`/`simulation/`.
- **Внешний URL/имя репозитория** в текстах правится, но фактический переезд —
  вне фичи.

#### Acceptance criteria

1. `git grep -nw Lam` и `git grep -n '\.lam\b\|lamc\|lam-lsp\|lam_lsp'` по коду,
   скриптам, примерам и **активной** документации дают **0** (архивные карточки
   закрытых фич — исторический контекст, из проверки исключены явным списком).
2. Сборка `cargo build --bin taktc` и `--features lsp --bin takt-lsp` — успешна;
   старые имена `lamc`/`lam-lsp` не существуют.
3. Крейты называются `takt-lang`/`takt-sim`; `cargo test` зелёный, пути импорта
   обновлены.
4. `examples/*.takt` (244), `.lam` в дереве **отсутствует**; `run_simulations.sh`
   и все скрипты работают на `.takt`.
5. `precheck.sh` зелёный (в т.ч. `check-module-size.sh` с переехавшими путями,
   `check-language-version.sh` на `0.5.0`, детерминизм на пересобранных
   снапшотах, `check-links.py`).
6. Версия языка `0.5.0`, коммит подъёма помечен тегом `v0.5.0`.
7. Плагин IntelliJ собирается (`./gradlew --offline test`) под именем Takt,
   распознаёт `.takt`.

## Анализ

### Цель и контекст

Реализовать сквозной ренейм **Lam → Takt** по [ADR](0100-language-rename-takt.md#архитектура-adr)
(Option A, жёсткий разрыв): расширение `.lam`→`.takt`, бинарники `lamc`→`taktc`/
`lam-lsp`→`takt-lsp`, крейты `grammar`→`takt-lang`/`simulation`→`takt-sim`
(с путями импорта), плагин IntelliJ, EBNF, все примеры и документация; версия
языка `0.4.0`→`0.5.0` (тег `v0.5.0`). **Синтаксис и семантика не меняются** —
ключевые слова, грамматика и генераторы тождественны; меняются имена и тексты.

### Зависимости фичи

- **Зависит от:** **нет** (жёстких зависимостей). Фича не ждёт закрытия другой:
  ренейм механический и самодостаточный.
- **Влияние на порядок разработки:**
  - **Пересечение с [вынос оставшихся нарушителей размера модуля (ядро семантики + пришпиленные)](0099-module-size-core.md)** (вынос ядра
    семантики + пришпиленных): обе трогают `semantic/*` и **реестр
    `module-size-baseline.txt`** (0100 переезжает пути `grammar/`→новый крейт,
    0099 дробит те же файлы). Рекомендация аналитика (критерий 4 своду):
    **брать 0100 перед 0099** — тогда 0099 сразу работает на переименованных
    путях, а не переписывает свои результаты. Не блокировка, а последовательность.
  - Ренейм делает **устаревшими упоминания** `Lam`/`.lam`/`lamc` в карточках
    **незакрытых** фич и `FEATURES.md`; их правит слой документации (0100-06).
    Карточки **закрытых** фич — исторический контекст, не правятся (см. A1).
  - Фича **не в статусе `ЗАБЛОКИРОВАНА`** (зависимостей нет).

### Требования и проверяемые условия

- **R1. Полнота ренейма.** В коде, скриптах, примерах и **активной** документации
  не остаётся `Lam`/`.lam`/`lamc`/`lam-lsp`/`lam_lsp` (кроме явного списка
  исторических артефактов — карточки/ADR/анализ/отчёты закрытых фич).
- **R2. Сборка и поведение.** `cargo build --bin taktc` и `--features lsp --bin
  takt-lsp` успешны; `cargo test` зелёный; поведение компилятора/симулятора/LSP
  **неизменно** (переименование — не смена логики).
- **R3. Расширение.** Компилятор/LSP/симулятор/форматтер принимают **только**
  `.takt`; `.lam` не распознаётся (жёсткий разрыв). Импорт `import "x.takt"`.
- **R4. Крейты и пути.** Крейты `takt-lang`/`takt-sim`; пути импорта
  `takt_lang::`/`takt_sim::` во всех потребителях; публичный API по составу
  неизменен (только имя крейта).
- **R5. Версия.** Версия языка `0.5.0` (константа `LANGUAGE_VERSION` + якорь
  README синхронны, проверка `check-language-version.sh` зелёный); коммит подъёма
  помечен тегом `v0.5.0`.
- **R6. Проверки.** `precheck.sh` зелёный на каждом слое: `check-module-size.sh`
  (пути реестра переехали), детерминизм (снапшоты `examples/generated/`
  пересобраны законно), `check-links.py` (переименованные файлы не оставили битых
  ссылок — свод).
- **R7. Плагин IntelliJ.** Собирается и тестируется (`./gradlew --offline test`)
  под именем Takt, `TaktFileType` распознаёт `.takt`.

### Декомпозиция на подзадачи (по слоям; порядок — снизу вверх по зависимостям сборки)

| № | Слой | Содержание | Проверка |
|---|---|---|---|
| 0100-01 | Крейты + пути импорта | `Cargo.toml` (workspace/deps): `grammar`→`takt-lang`, `simulation`→`takt-sim`; массово `grammar::`→`takt_lang::`, `simulation::`→`takt_sim::` в коде/тестах/доктестах. **Переезд путей в `module-size-baseline.txt`** | `cargo build`/`cargo test`; `check-module-size.sh` |
| 0100-02 | Бинарники | `bin/lamc.rs`→`bin/taktc.rs` (`[[bin]] name="taktc"`), `bin/lam_lsp.rs`→`bin/takt_lsp.rs` (`name="takt-lsp"`); usage/`eprintln!` CLI (`lamc …`→`taktc …`); baseline-запись `lamc.rs`→`taktc.rs` | `cargo build --bin taktc`/`--bin takt-lsp`; тесты CLI |
| 0100-03 | Идентификаторы кода (текст) | Текст в коде/комментариях/доках (`Lam`→`Takt`) + Cargo `description`/`keywords`. **Расширение перенесено в 0100-04** (атомарно связано с файлами — см. корректировку ниже) | `cargo build --all-features`; `precheck.sh` |
| 0100-04 | Расширение + примеры + фикстуры + генерация | **Расширение `.lam`→`.takt` атомарно**: точка распознавания (`taktc.rs:476 e=="lam"`→`"takt"`), `examples/**/*.lam`→`.takt` (244) + внутренние `import`, 235 фикстур `tests/data/**/*.lam` + пути в тест-коде, карты `.map`/`.plc.map`, globы precheck `examples/*.lam`, **пересборка снапшотов `examples/generated/`**, `run_simulations.sh` | `run_simulations.sh`; детерминизм-проверка; `examples_scenario_tests`; проба компиляции `.takt` |

> **Корректировка декомпозиции (при разработке 0100-03):**
> «расширение» перенесено из 0100-03 в 0100-04. Причина: логика расширения — одна
> точка (`taktc.rs:476`, обход каталога у `fmt`), а всё остальное (`compile`/
> `verify`/тесты) берёт **явные** пути; расширение атомарно связано с 244
> `.lam`-файлами, globами precheck и импортами внутри примеров — флип в отрыве от
> файлов даёт битое промежуточное состояние. 0100-03 сведён к чистому тексту.
| 0100-05 | Плагин IntelliJ | `extensions/intellij-lam`→`intellij-takt`, пакет `org/lam`→`org/takt`, классы `Lam*`→`Takt*`, `plugin.xml`/ресурсы, `TaktFileType`→`.takt`, корпус тестов `.takt` (`LamPsiCorpusTest`/`LamKeywordSyncTest`) | `./gradlew --offline test` |
| 0100-06 | Документация + версия | `README.md`/`README.typ`/`CLAUDE.md`/`CHANGES.md`, `Lam.ebnf`→`Takt.ebnf`, активные `docs/`; подъём версии `0.4.0`→`0.5.0` (константа + якорь README) + **тег `v0.5.0`**; grep-проверка R1 | `check-language-version.sh`; `check-links.py`; grep-проверка R1 |

Каждая подзадача: переименовать/поправить → обновить реестры → зелёный целевой
прогон → зелёный `precheck.sh` → коммит. Плагин (0100-05) — вне `precheck.sh`/CI,
проверяется локально (`./gradlew`).

### Критерии приёмки и способ проверки

| # | Критерий | Способ проверки |
|---|---|---|
| A1 | Ноль остаточного `Lam`/`.lam`/`lamc` вне исторических артефактов | `git grep -nw Lam` + `git grep -nE '\.lam\b\|lamc\|lam-lsp\|lam_lsp'` с исключением списка закрытых карточек/ADR/анализа/отчётов |
| A2 | Бинарники переименованы, сборка успешна | `cargo build --bin taktc`; `cargo build --features lsp --bin takt-lsp` |
| A3 | Крейты `takt-lang`/`takt-sim`, тесты зелёные | `cargo test -- --test-threads=1`; `cargo test --features lsp` |
| A4 | Только `.takt` распознаётся; `.lam` нет | проба: `taktc compile x.takt` ок, `x.lam` — не подхватывается каталогом |
| A5 | `examples/*.takt` (244), `.lam` в дереве отсутствует | `git ls-files '*.lam' | wc -l` = 0; `'*.takt'` = 244 |
| A6 | `precheck.sh` зелёный (размер/детерминизм/версия/ссылки) | `./scripts/precheck.sh` EXIT=0 |
| A7 | Версия `0.5.0`, тег `v0.5.0` | `check-language-version.sh`; `git tag -l v0.5.0` |
| A8 | Плагин под именем Takt собирается | `./gradlew --offline test` (локально) |

### Особенности по обратной функциональности

свод: **сознательный обрыв обратной совместимости** (обоснование в ADR,
драйвер 2): расширение `.lam`, имена `lamc`/`lam-lsp`, имена крейтов и пути
импорта меняются жёстко. Проект **до 1.0**, гарантированных внешних потребителей
нет; цена половинчатой миграции (два расширения навсегда) выше цены разрыва.
Поведение языка (разбор, семантика, генерация, симуляция) **неизменно** — ренейм
не задевает функциональность, только идентичность.

### Риски и зависимости

- **Пропуск пути импорта под фичей `lsp`** — обычная `cargo build` его не видит;
  ловит `precheck.sh` (`--all-features`). Снижение: каждый слой гоняет полный
  precheck.
- **Снапшоты `examples/generated/` меняются законно** (`#include "имя.h"` при
  смене имён примеров) — риск спутать с содержательной регрессией. Снижение:
  пересобрать эталоны в том же коммите (0100-04), детерминизм-проверка подтвердит
  стабильность повторного прогона.
- **Реестр размера модуля** содержит пути `grammar/`/`simulation/` — при ренейме
  крейтов записи обязаны переехать синхронно, иначе проверка краснеет (0100-01/02).
- **Пересечение с 0099** (общие файлы + реестр) — рекомендация брать 0100 раньше
  (см. «Влияние на порядок»).
- **Массовость правки** (~324 файла) — риск ручной ошибки; снижение —
  механические `git mv` + `grep -rl … | xargs sed` по слоям с немедленным
  precheck, а не одна большая правка.

## Разработка

### Задача

#### Что было

Два крейта в каталогах `grammar/` (пакет `grammar`, бинарники `lamc`/`lam-lsp`) и
`simulation/` (пакет `simulation`, бинарник `simulation`); пути импорта
`grammar::…` (147 вхождений, включая доктесты/бины) и `simulation::…` (в бинарнике
и интеграционных тестах). Скрипты-проверки и реестр размера ссылались на каталоги
`grammar/`/`simulation/` жёстко.

#### Что сделано (первый слой ренейма, [ADR](0100-language-rename-takt.md#архитектура-adr) Option A)

- **Каталоги:** `git mv grammar takt-lang`, `git mv simulation takt-sim` (476
  переименований).
- **Манифесты:** корневой `Cargo.toml` `members = ["takt-lang", "takt-sim"]`;
  `[package] name` → `takt-lang`/`takt-sim`; зависимость `takt-sim` →
  `takt-lang = { path = "../takt-lang" }`.
- **Пути импорта:** массово `grammar::`→`takt_lang::`, `simulation::`→`takt_sim::`
  во всех `.rs` крейтов (Rust: имя пакета `takt-lang` → `use`-имя `takt_lang`).
- **Ловушка (защищена):** `lib.rs` объявляет **`mod grammar`** —
  LALRPOP-генерируемый модуль парсера (`include!(…/grammar.rs)`), и
  `grammar::SourceUnitParser` (`lib.rs:117`) ссылается на **него**, а не на крейт.
  Единственная такая ссылка **возвращена** после массовой замены (модуль остаётся
  `grammar`, файл `grammar.lalrpop`/`grammar.rs` — тоже: это грамматика **языка**,
  не имя крейта).
- **Скрипты-проверки:** `check-module-size.sh`/`check-diagnostic-codes.sh`/
  `check-exhaustive-nodes.sh`/`check-language-version.sh` — пути `grammar/`→
  `takt-lang/`, `simulation/`→`takt-sim/`; комментарий `grammar::version`→
  `takt_lang::version`. `.gitignore` — пути артефактов `/grammar/`→`/takt-lang/`.
- **Реестр размера** `module-size-baseline.txt` — 6 путей переехали на
  `takt-lang/…` (файл `bin/lamc.rs` пока с прежним именем — ренейм бинарников —
  слой 0100-02). **`bin/lamc.rs` 1993→1994** — не рост долга, а **фактический
  размер перемещённого файла**: удлинение `grammar::`→`takt_lang::` в строке 726
  (`construct_model`) сделало её 101 символ, и `cargo fmt` штатно перенёс `{` на
  свою строку. Короткого реэкспорта `construct_model` нет, любой импорт добавил бы
  строку — держать ровно 1993 нечем. Запись переехала на новый путь
  (`grammar/`→`takt-lang/`), поэтому это **пере-регистрация по факту**, а не правка
  числа на стабильном пути; файл пришпилен и будет переименован в `taktc.rs`
  (0100-02) с очередной перезаписью записи.
- **Markdown-ссылки**: 9 битых ссылок в `docs/features/{0041,0045,
  0057,0059}` вида «ссылка на `../../grammar/src/…`» → `takt-lang/` — **только цели ссылок**
  (проза/бэктики с именами каталогов — слой документации 0100-06).

**Вне объёма слоя (следующие подзадачи):** имена бинарников `lamc`/`lam-lsp`/
`simulation` (0100-02); расширение `.lam`→`.takt` и тексты в коде (0100-03);
`description="Lam"`/`keywords` в манифестах, README/CLAUDE/CHANGES-проза, плагин
IntelliJ (0100-05/06); подъём версии (0100-06).

#### Проверки

- `cargo build` / `cargo build --features lsp --bin lam-lsp` / `cargo check
  --all-features --all-targets` — успешно.
- `./scripts/precheck.sh` — **зелёный** (fmt/clippy/тесты/доктесты/примеры/
  детерминизм/`check-module-size` на переехавших путях/`check-language-version`/
  `check-links`). Поведение неизменно — ренейм не трогает логику.

### Задача

#### Что было

Бинарники `lamc` (`src/bin/lamc.rs`), `lam-lsp` (`src/bin/lam_lsp.rs`) в
`takt-lang`, `simulation` (`src/bin/simulation.rs`) в `takt-sim`. Имена
фигурировали в `[[bin]]`-блоках, `env!("CARGO_BIN_EXE_…")` тестов, usage-строках
CLI, LSP-идентичности (`ServerInfo.name`, `source` диагностик), лог-префиксах,
скриптах и реестре размера.

#### Что сделано (слой 2 ренейма)

- **Файлы бинарников** (`git mv`): `lamc.rs`→`taktc.rs`, `lam_lsp.rs`→`takt_lsp.rs`,
  `simulation.rs`→`takt_sim.rs`.
- **Манифесты `[[bin]]`:** `taktc`/`takt-lsp` (в `takt-lang`), `takt-sim` (в
  `takt-sim`); пути файлов обновлены.
- **`env!("CARGO_BIN_EXE_…")`** — Cargo формирует имя переменной из **имени
  бинарника дословно** (дефис **не** заменяется на подчёркивание, вопреки первой
  догадке): `CARGO_BIN_EXE_lamc`→`CARGO_BIN_EXE_taktc` (3 теста `takt-lang/tests`),
  `CARGO_BIN_EXE_simulation`→**`CARGO_BIN_EXE_takt-sim`** (с дефисом!) в
  `takt-sim/tests/sim/diagnostics_tests.rs`. Первый прогон с `_takt_sim` дал
  `environment variable not defined at compile time` — Cargo определяет её как
  `CARGO_BIN_EXE_takt-sim`. Без верного имени тест не компилируется (E-101).
- **Функциональная ссылка на файл:** `tests/lsp_goto_tests.rs:220` читает
  `src/bin/lam_lsp.rs` в времени выполнения → `src/bin/takt_lsp.rs` (иначе тест падает
  «файл не найден»).
- **LSP-идентичность:** `ServerInfo.name` и `source` диагностик `"lam-lsp"`→
  `"takt-lsp"` (`bin/takt_lsp.rs`, `lsp/diagnostics.rs`, тест `lsp/mod.rs`),
  лог-префиксы `[lam-lsp]`→`[takt-lsp]`.
- **Usage-строки CLI** и упоминания инструментов в коде/комментариях/тестах:
  сплошь `lamc`→`taktc`, `lam-lsp`→`takt-lsp`, `lam_lsp`→`takt_lsp`; перечисление
  бинарников `(taktc, simulation)`→`(taktc, takt-sim)`.
- **Скрипты:** `precheck.sh` `--bin taktc`/`--bin takt-lsp`; `run_simulations.sh`
  `BINARY=target/debug/takt-sim`, `--bin takt-sim`.
- **Реестр размера:** запись `bin/lamc.rs`→`bin/taktc.rs` (1994).

**Решение по симулятору:** бинарник `simulation`→`takt-sim` (завершение «всех
тулов» из запроса; крейт уже `takt-sim`). ADR явно называл `taktc`/`takt-lsp` —
симулятор переименован по духу решения (не «Lam», но тул проекта).

**Вне объёма (следующие слои):** расширение `.lam`→`.takt` и имя языка `Lam`
в коде (0100-03); README/CLAUDE/docs-проза, плагин IntelliJ (`LamLspBinary` ищет
старый бинарник — 0100-05); подъём версии (0100-06).

#### Проверки

- `cargo build --bin taktc` / `--features lsp --bin takt-lsp` / `--bin takt-sim`
  — успешно.
- `cargo test --test lsp_goto_tests` (11) и `--test cli_warnings_tests` (4) —
  passed (`CARGO_BIN_EXE_taktc` и чтение файла работают).
- `./scripts/precheck.sh` — зелёный (размер на переехавшем пути `taktc.rs`).

### Задача

#### Корректировка декомпозиции

Анализ относил к 0100-03 **и** текст в коде, **и** расширение `.lam`→`.takt`.
Проработка вскрыла, что расширение **атомарно связано с 244 файлами**: логика
расширения — **одна** точка (`taktc.rs:476`, `e == "lam"` в обходе каталога у
`fmt`), а precheck компилирует примеры **явным globом** `examples/*.lam` (шелл),
плюс 235 фикстур `tests/data/**/*.lam` и `import "…​.lam"` **внутри** примеров.
Флип расширения в отрыве от файлов даёт битое промежуточное состояние. Поэтому:

- **0100-03** — **только текст** `Lam`→`Takt` в коде (поведение неизменно);
- **расширение `.lam`→`.takt` перенесено в 0100-04** — атомарно с ренеймом всех
  `.lam`-файлов (примеры + фикстуры), globов precheck, `run_simulations.sh`,
  снапшотов `examples/generated/`, импортов внутри примеров и путей фикстур в
  тестах.

#### Что сделано

- **`\bLam\b` → `Takt`** во всех `.rs` крейтов `takt-lang`/`takt-sim` (292
  вхождения в 78 файлах). Проверено заранее: **все** вхождения — слово в
  **комментариях/доках** (`//!`/`///`/`//`); в `.rs` **нет** ни строк-литералов
  `"Lam"` (логика/ассерты), ни `Lam`-префиксных идентификаторов, ни строки
  «Language of Automata Models». Регистр значим — `\bLam\b` **не** трогает
  расширение `.lam`.
- **Cargo.toml** обоих крейтов: `description = "Lam"`→`"Takt"`, `keywords[0]`
  `"Lam"`→`"Takt"`.

Поведение компилятора/симулятора/LSP **байт-в-байт неизменно** (правились только
комментарии и метаданные пакета). `taktc.rs` остался 1994 (комментарии `fmt` не
переносит).

**Вне объёма (следующие слои):** расширение `.lam`→`.takt` и файлы (0100-04);
README/CLAUDE/`docs/*.md`-проза, `Lam.ebnf`, README.typ (0100-06); плагин
IntelliJ (0100-05); подъём версии (0100-06). Внутренние шелл-переменные вроде
`$LAMC` в скриптах — косметика, слой документации/скриптов.

#### Проверки

- `cargo build --all-features` — успешно.
- `./scripts/precheck.sh` — зелёный (текст-правка поведение не меняет).

### Задача

#### Что было

Расширение исходников — `.lam`: **244** файла (8 примеров + ~235 фикстур
`tests/data/**`), логика распознавания в `taktc.rs:476` (обход каталога у `fmt`)
и в 9 местах тестов (`ext == "lam"`), 375 хардкод-ссылок `".lam"` в 37 тест-`.rs`,
импорты `import "x.lam"` внутри 20 примеров/фикстур, генератор эмитил `.lam` в
комментариях снапшотов, скрипты globили `examples/*.lam`, `.plc.map` ссылались на
`.lam`.

#### Что сделано (атомарный флип, [ADR](0100-language-rename-takt.md#архитектура-adr) — жёсткий разрыв)

- **Файлы:** `git mv` всех **244** `.lam`→`.takt` (циклом по `git ls-files`).
- **Контент исходников `.takt`:** `import "x.lam"`→`import "x.takt"` и любые `.lam`
  внутри (20 файлов с импортами).
- **`.rs` обоих крейтов:** `.lam`→`.takt` (375 хардкод-путей фикстур + генератор-
  строки, эмитящие `.lam`) и extension-проверки `"lam"`→`"takt"` (9 мест:
  `taktc.rs:476`, `lexer`/`parser`/`format`/`verify`/`address_export`/
  `lsp_formatting_conformance`/`c_stub` тесты).
- **Скрипты:** `precheck.sh` (globы `examples/*.takt`, `basename … .takt`,
  `sv-mmio`, сообщения, комментарии-имена примеров), `run_simulations.sh` (поиск
  модели по `${candidate}.takt`).
- **Карты:** `examples/*.plc.map` — `.lam`→`.takt`.
- **Снапшоты `examples/generated/`:** пересобраны компилятором. Дифф **минимален —
  8 файлов, только `.lam`→`.takt`** в эмитируемых комментариях (`sv/*.sv`,
  `sv-mmio`, `rust/elevator.rs`): «служебный порт цели sv: в.takt его нет»,
  «в исходнике.takt». Кодоген по существу неизменен (проверено: строки диффа без
  `.takt`/`.lam` отсутствуют).

#### Ключевые проверки согласованности

- Логика расширения — **одна** точка в компиляторе (`taktc.rs:476`, `fmt`
  каталога); `compile`/`verify` берут явные пути. Тесты-обходы каталогов
  (`ext == "takt"`) синхронны с переименованными фикстурами.
- `$LAMC="./target/debug/taktc"` (0100-02) — уже верен.

**Вне объёма:** плагин IntelliJ (`LamFileType` распознаёт `.lam`, корпус-тест) —
[переименование языка Lam → Takt (крейты, тулы, расширение, документация)](0100-language-rename-takt.md)-05; README/CLAUDE/`docs/*.md`, `Takt.ebnf`, README.typ, подъём версии —
[переименование языка Lam → Takt (крейты, тулы, расширение, документация)](0100-language-rename-takt.md)-06.

#### Проверки

- `cargo build --all-features`; целевые `lexer`/`parser`/`format`/`verify` тесты
  (28+ passed) — обходы каталогов и пути фикстур согласованы.
- Пересборка снапшотов: дифф только `.lam`→`.takt` (8 файлов).
- `./scripts/precheck.sh` — зелёный (fmt примеров на `.takt`, детерминизм,
  прогон всех тестов, `run_simulations` вне precheck).

### Задача

#### Корректировка объёма

Анализ называл слой 0100-05 «плагин IntelliJ». При проработке обнаружено **второе**
редакторное расширение — **Zed** (`extensions/zed-lam/`), пропущенное и в
исходной декомпозиции фичи. Оба — редакторный тул языка; слой расширен до
«**редакторные расширения**» (IntelliJ + Zed). Ни то, ни другое не входит в
`precheck.sh`/CI (IntelliJ собирается `./gradlew`, Zed — WASM-крейт вне
workspace).

#### Что сделано — IntelliJ (`extensions/intellij-lam` → `intellij-takt`)

- **Каталог/пакет:** `intellij-lam`→`intellij-takt`, `org/lam/intellij`→
  `org/takt/intellij` (main + test).
- **Файлы:** 45 `Lam*.kt`→`Takt*.kt`; ресурсы `icons/lam.svg`→`takt.svg`,
  `META-INF/lam-lsp4ij.xml`→`takt-lsp4ij.xml`.
- **Контент:** `\bLam`→`Takt` (классы + слово; проверено — нет `Lam`+строчная,
  риска `Lambda` нет), `org.lam`→`org.takt`, `.lam`→`.takt`, `lam-lsp`→`takt-lsp`,
  `intellij-lam`→`intellij-takt`, `Language of Automata Models`→`Typed, Automata,
  Known Timing`. plugin.xml `<id>org.takt.intellij</id>`/`<name>Takt</name>`/тип
  файла `.takt`; `gradle.properties` `pluginGroup=org.takt`; `settings`
  `rootProject.name="intellij-takt"`.
- **Функциональные пропуски, пойманные проверкой:**
  - `TaktFileType.getDefaultExtension()` возвращал голое `"lam"` (без точки — не
    поймано `.lam`-паттерном) → `"takt"`.
  - Тесты плагина ссылались на каталог Rust-проекта **`grammar/`** (переименован в
    0100-01!): `TaktKeywordSyncTest` (`grammar/src/parser/lexer.rs`),
    `TaktPsiCorpusTest` (`grammar/tests/data`, детект корня по `grammar/`) →
    `takt-lang/`. Плюс `lamc`→`taktc`, storage настроек `lam.xml`→`takt.xml`.
- **Installer-скрипты** `extensions/install-rustrover-plugin.{sh,ps1}`:
  `intellij-lam`→`intellij-takt`, `Lam`→`Takt`.

#### Что сделано — Zed (`extensions/zed-lam` → `zed-takt`)

- Каталог `zed-lam`→`zed-takt`, `languages/lam`→`languages/takt`.
- `extension.toml`: `id="takt"`, `name="Takt"`, `[language_servers.takt-lsp]`,
  `language="Takt"`, описание/`.takt`. `config.toml`: `path_suffixes=["takt"]`,
  `language_servers=["takt-lsp"]`. `Cargo.toml` `name="zed-takt"`. `src/lib.rs`:
  поиск бинарника `takt-lsp`, путь установки `takt-lang --bin takt-lsp`.
  `scripts/install.sh`: `EXT_ID="takt"`, `languages/takt`.

#### Проверки

- **IntelliJ:** `./gradlew --offline test` — **BUILD SUCCESSFUL** (компиляция
  переименованных пакетов/классов, тесты; корпус-тест нашёл `.takt` в `examples/`
  и `takt-lang/tests/data/`).
- **Rust-precheck не затронут:** поверхность 0100-05 — только `extensions/` + одна
  markdown-ссылка `README.md` (`extensions/intellij-lam`→`intellij-takt`); ни одного файла `takt-lang`/`takt-sim`/`scripts`/`examples` не изменено →
  Rust-проверки идентичны зелёному 0100-04. `check-links` и `check-language-version`
  — зелёные.

**Вне объёма:** README/CLAUDE/`docs/*.md`-проза, `Takt.ebnf`, README.typ, подъём
версии — 0100-06. Историческая хроника `CHANGES.md` (упоминания `intellij-lam`/
`zed-lam` в прошедших записях) **не переписывается** — как карточки закрытых фич.

### Задача

#### Что сделано (финальный слой)

- **Версия языка `0.4.0` → `0.5.0`** ([ADR](0100-language-rename-takt.md#архитектура-adr)): константа `takt_lang::LANGUAGE_VERSION` (`version.rs`) + якорь
  `**Версия языка: 0.5.0**` README; проверка `check-language-version.sh` зелёный.
  Коммит помечен тегом **`v0.5.0`**. Тег `v0.4.0` в истории отсутствовал (был
  только `v0.3.0`) — пре-существующий пробел, к фиче не относится (кандидат).
- **`Lam.ebnf` → `Takt.ebnf`** (`git mv` + содержимое) + ссылка в README.
- **Проза живых документов** (`\bLam\b`→`Takt`, `.lam`→`.takt`, `lamc`→`taktc`,
  `lam-lsp`→`takt-lsp`, `Language of Automata Models`→`Typed, Automata, Known
  Timing`, крейт `` `grammar` ``→`` `takt-lang` ``, `grammar::`→`takt_lang::`,
  `grammar/`→`takt-lang/`): `README.md`, `README.typ`, `CLAUDE.md`, `docs/RULE.md`,
  `docs/CODE.md`, `FEATURES.md`, `docs/diagnostics/README.md`, `.zed/settings.json`,
  `.github/workflows/ci.yml`, скрипты `precheck.sh`/`check-language-version.sh`.
- **Крейт-имена в комментариях кода/плагина/тестов** `` `grammar` ``/
  `` `simulation` `` → `` `takt-lang` ``/`` `takt-sim` `` (не попали в 0100-01/03/05
  — это не `Lam`, не путь, не `::`).
- **Примеры `examples/*.takt`** и рукописная обвязка `examples/generated/` (tb/
  `Cargo.toml`/`lib.rs`): `Lam`→`Takt`, `lamc`→`taktc`. Снапшоты пересобраны — дифф
  только эти токены (кодоген по существу неизменен).

#### Грамматика A1 (нет остаточного `Lam`) — с документированными исключениями

Проверка `git grep -nEw 'Lam|lamc|lam-lsp'` (и `` `grammar` ``/`.lam`) по коду,
скриптам, примерам и **активной** документации — **чисто**. Исключения (замысел,
не долг):

- **Исторические артефакты** — `docs/{features,adr,analyze,reports,development,
  fixes,tests}/**`, `CHANGES.md`, `.claude/report_ru.html`: датированные записи,
  описывающие прошлое под тогдашними именами (как карточки закрытых фич). Имя
  файла `0081-lamc-print-warnings.md` — часть исторического slug, не переименован
  (ссылки на него сохраняют путь).
- **`takt-lang/src/lib.rs:117`** `grammar::SourceUnitParser` — **не** крейт, а
  локальный LALRPOP-модуль `mod grammar`.
- **Тест-фикстуры `tests/data/**/*.{takt,st}`** — комментарии со старыми именами
  (`lamc`, `языка Lam`) оставлены: часть LSP/goto-фикстур **позиционно-связана** с
  ассертами тестов (смена длины `Lam`→`Takt` сдвинула бы байтовые офсеты и сломала
  проверки). Внутренние, не user-facing. Кандидат на аккуратный проход.

#### Проверки

- `check-links` (после отката ссылок на исторический `0081-lamc-*`),
  `check-language-version` (0.5.0) — зелёные.
- `./scripts/precheck.sh` — зелёный (version.rs/скрипты/примеры затронуты →
  Rust-проверки прогнаны; снапшоты пересобраны).
- Тег `v0.5.0` на коммите подъёма версии.

## Тест-план

### Область и цель

Фича — **сквозной ренейм идентичности** (ADR, Option A): имена, расширение,
тексты. **Синтаксис и семантика языка не меняются** (ключевые слова, грамматика,
проходы, генераторы тождественны). Поэтому (примеры/контрпримеры
языка) неприменимо в части «новая семантика»: проверка — **отсутствие регресса
поведения** и **полнота ренейма**. Критерии — A1–A8 [анализа](0100-language-rename-takt.md#анализ).

### Проверки (условие → ожидаемый результат)

| # | Проверка | Предусловие | Ожидаемый результат | R/A |
|---|---|---|---|---|
| T1 | Полнота ренейма | после 0100-06 | `git grep -nEw 'Lam\|lamc\|lam-lsp'` по коду/скриптам/примерам/активным докам = **0** (исключения: историч. артефакты, `.claude`, `lib.rs:117` LALRPOP, tests/data-фикстуры) | A1/R1 |
| T2 | Бинарники собираются под новыми именами | 0100-02 | `cargo build --bin taktc`, `--features lsp --bin takt-lsp`, `--bin takt-sim` — успешно; `lamc`/`lam-lsp` не существуют | A2/R2 |
| T3 | Крейты `takt-lang`/`takt-sim`, тесты зелёные | 0100-01 | `cargo test` зелёный; пути импорта `takt_lang::`/`takt_sim::` | A3/R4 |
| T4 | Принимается только `.takt` | 0100-04 | `examples/*.takt` (244), `.lam` в дереве = 0; `taktc fmt` каталога находит `.takt` | A4/R3 |
| T5 | `precheck.sh` зелёный на каждом слое | 0100-01…06 | `EXIT=0` (fmt/clippy/test/детерминизм/размер/версия/ссылки) | A6/R6 |
| T6 | Версия языка 0.5.0 + тег | 0100-06 | `check-language-version.sh` = 0.5.0; `git tag -l v0.5.0` | A7/R5 |
| T7 | Плагин IntelliJ собирается под именем Takt | 0100-05 | `./gradlew --offline test` — BUILD SUCCESSFUL, распознаёт `.takt` | A8/R7 |
| T8 | Кодоген неизменен по существу | 0100-04/06 | дифф `examples/generated/` при пересборке — **только** `.lam→.takt`/`Lam→Takt`/`lamc→taktc` в комментариях | R2 |

### Разбивка проверок по функциональности

Обратная функциональность — весь корпус тестов `takt-lang`/`takt-sim`,
вывод генераторов, публичный API, редакторные расширения. Единые условия
(T3/T5/T8) прогоняются против каждого затронутого слоя; статус — в отчёте.

<!-- Легенда: ✅ пройдено · ❌ провалено · ⬜ не проверялось · — не применимо -->

### Тестовые данные и окружение

- Тулчейн: `cargo` (nightly), проверки `precheck.sh` (`cc`/`cmake`/`ninja`/`iec2c`/
  `verilator`/`yosys`), `./gradlew` (JDK 21 foojay) для плагина IntelliJ.
- Специальных фикстур фича не вводит; корпус — переименованный `examples/` +
  `tests/data/`.

## Отчёт о тестировании

### Резюме

**Вердикт: ГОТОВО.** Сквозной ренейм Lam → Takt выполнен по [ADR](0100-language-rename-takt.md#архитектура-adr)
(Option A, жёсткий разрыв) за **6 слоёв** 0100-01…06, каждый — отдельный коммит с
зелёным `precheck.sh`. Все проверки тест-плана T1–T8 пройдены (✅). Поведение
языка/компилятора/симулятора/LSP **неизменно** (ренейм не трогает логику). Версия
языка поднята `0.4.0`→`0.5.0`, коммит помечен тегом `v0.5.0`. Исправлений
(`docs/fixes/`) не потребовалось.

### Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Полнота ренейма (греп A1) | ✅ | 0 остаточного `Lam`/`lamc`/`.lam`/`` `grammar` `` вне документированных исключений |
| T2 | Бинарники `taktc`/`takt-lsp`/`takt-sim` | ✅ | собираются; старых имён нет. `CARGO_BIN_EXE_takt-sim` — дефис дословно |
| T3 | Крейты + пути импорта | ✅ | `takt-lang`/`takt-sim`, `takt_lang::`/`takt_sim::`; `cargo test` зелёный |
| T4 | Только `.takt` | ✅ | 244 `.takt`, 0 `.lam`; extension-логика (`taktc.rs:476` + 8 тест-обходов) на `"takt"` |
| T5 | `precheck.sh` на каждом слое | ✅ | EXIT=0 на 0100-01…06 |
| T6 | Версия 0.5.0 + тег | ✅ | `check-language-version` = 0.5.0; тег `v0.5.0` |
| T7 | Плагин IntelliJ | ✅ | `./gradlew --offline test` BUILD SUCCESSFUL; `.takt` |
| T8 | Кодоген неизменен | ✅ | дифф снапшотов — только токены имён в комментариях |

### Результаты по функциональности

Слои и приёмы:

- **0100-01** крейты+пути: `git mv` каталогов, `grammar::`→`takt_lang::` (защищён
  LALRPOP `grammar::SourceUnitParser`).
- **0100-02** бинарники: `lamc`→`taktc`/`lam-lsp`→`takt-lsp`/`simulation`→`takt-sim`;
  ловушка `env!("CARGO_BIN_EXE_takt-sim")` (дефис дословно, не `_`).
- **0100-03** текст `Lam`→`Takt` в коде (комментарии/доки).
- **0100-04** расширение `.lam`→`.takt` **атомарно** (244 файла + 9 обходов + 375
  путей фикстур + генератор + скрипты + снапшоты).
- **0100-05** редакторные расширения: IntelliJ (`org/lam`→`org/takt`, 45 классов) +
  **Zed** (обнаружен при проработке, включён — свод).
- **0100-06** документация + `Takt.ebnf` + версия 0.5.0 + грамматика A1.

Примеры/контрпримеры языка — **неприменимо**: синтаксис/семантика не
менялись; корпус доказывает неизменность поведения.

### Найденные дефекты

Дефектов кода не найдено. **Функциональные пропуски, пойманные проверкой внутри
разработки** (исправлены в тех же слоях, отдельных исправлений не заводилось):

- `CARGO_BIN_EXE_takt-sim` с подчёркиванием (E-101) → дефис (0100-02).
- `TaktFileType.getDefaultExtension()` вернул `"lam"` → `"takt"` (0100-05).
- Тесты плагина ссылались на каталог `grammar/` (переименован 0100-01) →
  `takt-lang/` (0100-05).
- Ссылки markdown на `grammar/src/…` и `examples/*.lam` → починены.

### Выводы и дальнейшие шаги

Фича закрывается (`ГОТОВО`). **Осознанные остатки** (документированы, не дефекты):

- **Тест-фикстуры `tests/data/**`** — комментарии со старыми именами оставлены
  (часть LSP/goto-фикстур позиционно-связана с офсет-ассертами; смена длины
  сломала бы тесты). Кандидат на аккуратный проход.
- **Тег `v0.4.0`** в истории отсутствовал (пробел до фичи) — кандидат.
- **Переезд репозитория** `github.com/Pastor/BuT` — административный, вне кода.

Язык поднят до 0.5.0 (тег `v0.5.0`). Дефектов-исправлений нет.

## Итог (что сделано)

**Закрыта 2026-07-24** (`ГОТОВО`, вердикт [отчёта](0100-language-rename-takt.md#отчёт-о-тестировании)
— ГОТОВО). Принята **Option A** ([ADR](0100-language-rename-takt.md#архитектура-adr)):
сквозной жёсткий ренейм идентичности **Lam → Takt** (Typed, Automata, Known
Timing). Синтаксис и семантика языка **не менялись** — только имена, расширение и
тексты.

Сделано за **6 слоёв** (каждый — коммит с зелёным `precheck.sh`; детали —
[отчёт](0100-language-rename-takt.md#отчёт-о-тестировании) и dev-доки):

- **[01](0100-language-rename-takt.md#разработка)** крейты + пути импорта:
  `grammar`→`takt-lang`, `simulation`→`takt-sim`, `grammar::`→`takt_lang::`
  (защищён LALRPOP `mod grammar`).
- **[02](0100-language-rename-takt.md#разработка)** бинарники: `lamc`→`taktc`,
  `lam-lsp`→`takt-lsp`, `simulation`→`takt-sim` (ловушка `CARGO_BIN_EXE_takt-sim` —
  дефис дословно).
- **[03](0100-language-rename-takt.md#разработка)** текст `Lam`→`Takt` в коде.
- **[04](0100-language-rename-takt.md#разработка)** расширение `.lam`→`.takt`
  **атомарно** (244 файла + обходы каталогов + пути фикстур + генератор + скрипты +
  снапшоты).
- **[05](0100-language-rename-takt.md#разработка)** редакторные расширения:
  IntelliJ (`org/lam`→`org/takt`, 45 классов) + **Zed** (обнаружен при проработке,
  включён — свод); `./gradlew` BUILD SUCCESSFUL.
- **[06](0100-language-rename-takt.md#разработка)** документация + `Takt.ebnf`
  + **версия языка 0.4.0→0.5.0** (тег **`v0.5.0`**) + грамматика A1.

**Осознанные остатки** (документированы, не дефекты): тест-фикстуры `tests/data/**`
(комментарии со старыми именами — позиционно-связаны с офсет-ассертами тестов);
историческая хроника `CHANGES.md`/карточки закрытых фич/`.claude` (датированные
записи под тогдашними именами); `takt-lang/src/lib.rs:117` `grammar::SourceUnitParser`
(локальный LALRPOP-модуль, инвариант). Переезд репозитория — административный, вне
кода. Отсутствие тега `v0.4.0` — пре-существующий пробел (кандидат).

Версия языка **0.5.0** (тег `v0.5.0`). Исправлений (`docs/fixes/`) не потребовалось —
функциональные пропуски ловились и правились внутри слоёв.
