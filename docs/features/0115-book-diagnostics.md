# Фича: Раздел документа «Диагностики»

- **Номер:** 0115
- **Статус:** ГОТОВО
- **Зависит от:** нет (инфраструктура `book/` заведена фичей [документ описания языка Takt (book/, mdBook)](0101-language-book.md))
- **Приоритет / Tier:** **Tier 3** (документация; раздел описывает уже реализованные возможности языка — сам синтаксис/семантику не меняет)
- **Связанные issue (анализ):** новая фича (правило 24: каждый раздел документа — отдельная фича; запрос заказчика 2026-07-24)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | не заведён (стадия 2) |
| Анализ | не заведён (стадия 3) |
| Разработка | [`docs/development/`](README.md) (задачи `0115-YY-*`) |
| Тест-план | [`0115-book-diagnostics.md#тест-план`](README.md) |
| Отчёт о тестировании | [`0115-book-diagnostics.md#отчёт-о-тестировании`](README.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0115-YY-*`) |

## Краткое описание

Описывает **диагностики** компилятора: категории кодов (лексические, синтаксические, семантические, по целям) и формат сообщений об ошибках и предупреждениях.

> Фича зарегистрирована **2026-07-24** по запросу заказчика (правило 24 — каждый раздел документа заводится отдельной фичей); далее по правилу 17.

## Документирование (правило 24)

Наполняет раздел `book/src/15-diagnostics/index.typ` описанием языка (см. «Краткое описание»); примеры на Takt — в подкаталоге `15-diagnostics/examples/`, изображения — в `15-diagnostics/images/`. Раздел **описывает** уже реализованные возможности языка, синтаксис/семантику **не меняет**. В документ не попадают детали реализации, номера фич и процесс разработки.

## Итог (что сделано)

Наполнен раздел `book/src/15-diagnostics/index.typ` описанием языка (правило 24 —
язык не меняется, Tier 3; ADR/анализ не требовались). Раздел концептуальный, не
дублирует каталог (правило 25 — полный перечень в приложении «Ошибки»):

- **Формат сообщения** — `путь:строка:колонка: <вид> [<код>]: <текст>` (пример).
- **Ошибки vs предупреждения** — ошибка прекращает работу, предупреждение
  продолжает.
- **Префиксы (слой)** — `LE`/`SY`/`SE` (лексика/синтаксис/семантика),
  `CC`/`RS`/`ST`/`SV`/`PU` (по целям), `AM`/`DF` (карта/`--define`), `SIM`
  (симулятор); по коду понятно, где искать.
- **Справочник** — ссылка на приложение «Ошибки и предупреждения»; блок «Частые
  ошибки» разделов ссылается туда же.
- **Частые ошибки** (правило 26): `SE-003` (переменная не найдена), `SE-002`
  (неизвестное состояние), `SE-011` (нет начального состояния).

Раздел — мета (о диагностиках), поэтому модели-примера не содержит; иллюстрация —
формат сообщения.

**Живая проверка:** `./scripts/precheck.sh` зелёный; сборка PDF (`make -C book
build`) чистая. Язык не менялся → версия языка та же.

