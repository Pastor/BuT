# Фича: Модуль semantic/statement.rs — 999 строк при пределе 1000

- **Номер:** 0225
- **Статус:** ГОТОВО
- **Зависит от:** нет (проверено на стадии анализа)
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу-03 (запрос заказчика)

## Стадии жизненного цикла

Все стадии — **разделы этой карточки**: «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог».
Отдельным артефактом остаются только исправления —
[`docs/fixes/`](../fixes/README.md) (при необходимости `0225-YY-*`).

## Краткое описание

`takt-lang/src/semantic/statement.rs` — 999 строк при пределе ~1000. Подробности находки, замеры и оговорки — в разделе «Что известно на
входе»; форма решения определяется стадиями архитектуры и анализа.

> Фича зарегистрирована **из бэклога кандидатов** (запрос заказчика 2026-08-03:
> перевести кандидатов в фичи без проработки); далее проходит жизненный цикл по
> своду.

## Что известно на входе

> Фича заведена **из кандидата** блока 2 `FEATURES.md` (запрос заказчика
> 2026-08-03: перевести кандидатов в фичи без проработки). Проработка —
> стадии 2–3 жизненного цикла; ниже — запись кандидата **как есть**, вместе с
> замерами и оговорками, сделанными при его заведении.
>
> Замеры кандидата **не перепроверялись** при переводе в фичу: их
> актуальность подтверждает пробой стадия архитектуры (урок — записи
> кандидатов устаревают, и две из них оказались неверны).

**Класс:** инструменты и процесс (Tier 4 — приоритет по решению заказчика
2026-08-03: синтаксис → семантика → генерация → остальное).

**`takt-lang/src/semantic/statement.rs` — 999 строк при пределе ~1000.**
Замер при закрытии 0155 (`scripts/check-module-size.sh` зелёный, но запаса
нет). Следующая правка модуля обязана начинаться с разделения по образцу
`semantic/validate/` («директория-подмодуль + `use super::*`», приёмы фичи), иначе проверка размера её отвергнет. В реестр долга
`scripts/module-size-baseline.txt` файл **не вносить**: реестр замораживает
нарушителей, а этот в лимит укладывается.

Прежде блок был пуст: все кандидаты переведены в фичи–0177 (2026-07-28,
решение заказчика) — каждая получила карточку стадии 1 без проработки, и отбор
«что брать, что не брать» идёт по таблице блока 1, а не по этому списку. Две
записи, помеченные «устарело», сняты как опровергнутые (сверка цели `st` есть —
`takt-sim/tests/conformance/conformance_st_tests.rs`; проверка компиляции `c-hal` есть —
`scripts/precheck.sh`). Заметка о расхождении `>>` между целями работой не
является и перенесена в живой контекст [CLAUDE.md](../../CLAUDE.md).

Новые находки записывать сюда: подтверждать пробой или чтением кода и указывать,
при какой работе вскрыта.

## Документирование

**Не требуется.** Язык не меняется: перемещается блок тестов внутри крейта
`takt-lang`. Документ `book/` описывает язык, а не раскладку исходников
компилятора. Версия языка не поднимается.

## Архитектура (ADR)

- **Status:** Accepted
- **Date:** 2026-08-17
- **Authors:** Архитектор + аналитик
- **Related issues:** [Фича](0225-statement-module-size.md);
  правило размера — [ADR](0027-module-size-split.md#архитектура-adr), приём деления —
  [ADR](0088-module-size-remaining.md#архитектура-adr), образец выноса тестов —
  [ADR](0129-semantic-deep-nesting.md#архитектура-adr)

### Context

Правило проекта: модуль — не больше ~1000 строк вместе с тестами
(`docs/CODE.md`), контроль — `scripts/check-module-size.sh` (храповик: новый
нарушитель, рост записи долга, незакрытая запись и запись на несуществующий файл
дают отказ). Кандидат, из которого заведена фича, записал: «`statement.rs` — 999
строк при пределе ~1000… следующая правка модуля **обязана** начинаться с
разделения, иначе проверка её отвергнет».

#### Что показала проба (замер кандидата перепроверен)

**1. Числа устарели.** Сегодня файл — **976** строк, не 999. История размера
(`git show <коммит>:./takt-lang/src/semantic/statement.rs | wc -l`):

| Коммит | Фича | Строк |
|---|---|---|
| `3dbfcc3d` | 0129 | 977 |
| `a37a4275` | 0155 | **999** ← замер кандидата |
| `cbb0388c` | 0185 | 971 |
| `1a45cc54` | 0189 | 976 |
| `47e5a5fb` | 0243 | 976 |

**2. Утверждение «следующая правка будет отвергнута» опровергнуто историей.**
После замера модуль правили **трижды** (0185, 0189, 0243), разделения не
делали, и проверка ни разу не отказал: запас в 24 строки есть, а две из трёх правок
файл даже сократили. То есть проблема не «проверка блокирует работу», а «запаса
мало» — риск, а не отказ.

**3. Файл не самый близкий к пределу.** Замер по всем модулям и тестам
(`wc -l`, порог 950):

| Строк | Файл |
|---|---|
| 998 | `takt-lang/src/generator/sv/sv_fsm.rs` |
| 994 | `takt-sim/tests/conformance/conformance_st_tests.rs` |
| 994 | `takt-lang/src/generator/sv/sv_expr.rs` |
| 994 | `takt-lang/src/generator/c/c_header.rs` |
| 981 | `takt-sim/tests/conformance/conformance_c_tests.rs` |
| 976 | `takt-lang/tests/semantic/semantic_tests.rs` |
| **976** | **`takt-lang/src/semantic/statement.rs`** |
| 974 | `takt-lang/src/generator/rust/rust_model.rs` |
| 973 | `takt-sim/tests/conformance/conformance_sv_tests.rs` |

`statement.rs` — седьмой, а не первый: класс «модуль вплотную к пределу» шире
одного файла (девять записей ≥ 970). Реестр долга при этом содержит **три**
файла, и `statement.rs` в нём справедливо нет.

**4. Естественный шов виден сразу.** Состав файла:

| Строки | Что |
|---|---|
| 1–58 | док-шапка модуля и `use` |
| 59–90 | `resolve_statement` |
| 91–316 | `resolve_ast_statement` |
| 317–377 | область видимости локальных (`register_local_var`, `unregister_local_vars`) |
| **378–976** | **`#[cfg(test)] mod tests`** — 599 строк, **61 %** файла |

То есть шесть из десяти строк модуля — тесты. Приём выноса в проекте уже есть и
проверен: фича так поступила с `semantic/expression.rs` (директория
`expression/` = `mod.rs` + `tests.rs`), приём деления описан ADR
(«директория-подмодуль + `use super::*`»).

### Decision Drivers

1. **Работа делается по замеру, а не по записи кандидата.** Запись отстала:
   и в числах, и в выводе о блокировке.
2. **Запас важнее разовой правки.** Модуль ядра семантики правят регулярно
   (три фичи за три недели); 24 строки запаса означают, что четвёртая правка
   начнётся с рефакторинга под давлением, а это худшее время для него.
3. **Приём деления уже выбран проектом**  — заводить второй
   способ для той же задачи не нужно.
4. **Поведение обязано остаться прежним.** Это рефакторинг: ни одной
   диагностики, ни одного узла, ни одной ветви не меняется.

### Considered Options

#### Option A. Вынести тесты в `semantic/statement/tests.rs`

**Pros:**
- Снимает 599 строк из 976 → модуль ≈ 378 строк, запас более чем двукратный.
- Ровно тот приём, которым проект уже решал ту же задачу (0129 для
  `expression.rs`), — читателю не нужно узнавать новую раскладку.
- Риск минимален: `mod tests` переезжает целиком, `use super::*` сохраняет
  видимость приватных функций.

**Cons:**
- Логика самого модуля не становится меньше: `resolve_ast_statement` (226
  строк) остаётся как есть.

#### Option B. Разделить логику по видам операторов (`statement/{control,decl,…}.rs`)

**Pros:**
- Уменьшает и код, не только тесты.

**Cons:**
- `resolve_ast_statement` — **один** `match` по `ast::Statement`; разрезание
  его по файлам разносит ветви одного разбора, а исчерпывающий `match` — это
  ровно то, чем проект защищает язык от пропущенных узлов (правило
  `wildcard_enum_match_arm`). Читаемость упадёт, выгода — 226 строк.
- Больше правки → больше риск регресса в ядре семантики ради задачи «дать
  запас», которую полностью решает A.

#### Option C. Ничего не делать (закрыть фичу отменой)

**Pros:**
- Ноль работы; запас в 24 строки формально есть, проверка зелёный.

**Cons:**
- Запас исчезает от одной средней правки: 0155 добавила к файлу 22 строки.
- Рефакторинг под давлением («правка не проходит проверка — срочно делим») — тот
  самый случай, ради которого храповик и заведён.

#### Option D. A + предупреждение проверки о приближении к лимиту («жёлтая зона»)

**Pros:**
- Класс виден заранее у всех девяти файлов, а не в момент отказа.

**Cons:**
- Это **другая** задача (правка контроля и его политики), у неё свои решения:
  где порог, ронять ли предкоммит, как не превратить предупреждение в шум.
  Смешивать её с рефакторингом одного модуля — расширять объём фичи.

### Decision

Принимается **Option A**: `takt-lang/src/semantic/statement.rs` превращается в
директорию `takt-lang/src/semantic/statement/` с `mod.rs` (логика) и `tests.rs`
(вынесенный `mod tests`), по образцу `semantic/expression/`.

Логика (`resolve_ast_statement` и соседи) **не переписывается** — Option B
отвергнут: он разносит ветви одного исчерпывающего `match` по файлам.

Предложение D (предупреждение о приближении к лимиту) выносится **кандидатом** в
`FEATURES.md`: замер этой фичи показал девять файлов в зоне ≥ 970 строк, то есть
предмет реальный, но самостоятельный.

### Consequences

#### Положительные

- Модуль ядра семантики получает двукратный запас; следующая правка не начнётся
  с вынужденного рефакторинга.
- Раскладка совпадает с `semantic/expression/` — один приём на две одинаковые
  задачи.
- Замер кандидата заменён верным: и число, и вывод «проверка отвергнет правку».

#### Отрицательные / Action items

- Пути `include_str!`/относительные ссылки внутри перенесённых тестов, если бы
  они были, сместились бы на уровень (в этом файле их нет — проверено).
- Остальные восемь файлов зоны ≥ 970 строк фича не трогает; класс вынесен
  кандидатом.

#### Acceptance criteria

1. `takt-lang/src/semantic/statement.rs` больше нет; есть
   `semantic/statement/mod.rs` и `semantic/statement/tests.rs`.
2. Оба файла — заметно ниже лимита (`wc -l`), в реестр долга ничего не
   добавлено.
3. `scripts/check-module-size.sh` зелёный.
4. Состав и число тестов не изменились: те же имена тестов проходят.
5. `./scripts/precheck.sh` — код 0 (в том числе `clippy -D warnings` и все
   3000+ тестов).
6. Поведение компилятора неизменно: `examples/generated/` после прогона чист.

## Анализ

### Цель и контекст

Дать модулю `takt-lang/src/semantic/statement.rs` запас по правилу размера,
вынеся его тесты (599 строк из 976, 61 % файла) в отдельный файл — приём
ADR, образец `semantic/expression/`.

Правка **чисто структурная**: ни одной строки логики не меняется, поэтому язык,
диагностики, генерация и симуляция не затрагиваются.

### Зависимости фичи

- **Зависит от:** нет. Правило и контроль (0027), приём деления (0088) и образец
  выноса тестов (0129) закрыты. Фича (вынос оставшихся нарушителей ядра —
  `tree`/`mod`/`type_inference`/`lib`) **заморожена** и с этой не пересекается:
  там файлы из реестра долга, здесь — файл, в лимит укладывающийся.
- **Влияние на порядок разработки:** никого не разблокирует. Косвенно облегчает
  любую следующую правку `statement.rs`.

### Требования и проверяемые условия

- **R1. Файл разделён по образцу.** `semantic/statement/mod.rs` (логика) +
  `semantic/statement/tests.rs` (`#[cfg(test)] mod tests`), подключение —
  `#[cfg(test)] mod tests;`, внутри `use super::*` (как у `expression/`).
- **R2. Запас появился.** `mod.rs` — заметно ниже лимита 1000 (ожидание ≈ 380
  строк), `tests.rs` — тоже (ожидание ≈ 600).
- **R3. Реестр долга не пополнен.** `scripts/module-size-baseline.txt` остаётся
  из трёх записей: файл в лимит укладывается, а реестр замораживает
  нарушителей.
- **R4. Набор тестов не изменился.** Те же имена тестов, то же их число;
  ни один не потерян и не отключён.
- **R5. Поведение неизменно.** Компилятор, симулятор и генераторы ведут себя
  так же: `examples/generated/` после прогона предкоммита чист.
- **R6. Сборка строгая.** `clippy --all-targets --all-features -- -D warnings`
  зелёный: вынос тестов часто оставляет неиспользованные импорты.

### Критерии приёмки и способ проверки

| # | Критерий | Способ проверки |
|---|---|---|
| A1 | R1 | `ls takt-lang/src/semantic/statement/` — `mod.rs`, `tests.rs`; файла `statement.rs` нет |
| A2 | R2 | `wc -l` обоих файлов |
| A3 | R3 | `git diff scripts/module-size-baseline.txt` — пусто; `scripts/check-module-size.sh` — код 0 |
| A4 | R4 | сверка списка имён тестов до и после (`cargo test -- --list`, фильтр по модулю) |
| A5 | R5 | `git status --porcelain examples/generated/` после `precheck.sh` — пусто |
| A6 | R6 + общая исправность | `./scripts/precheck.sh` — код 0 |

### Особенности по обратной функциональности

Обратная совместимость **не затрагивается**: перемещение приватного модуля
внутри крейта. Публичный API (`takt_lang::…`) неизменен — `statement` не
экспортируется наружу (`pub(crate)`-уровень), путь `crate::semantic::statement`
сохраняется: директория с `mod.rs` даёт то же имя модуля, что и файл. Версия
языка не поднимается, версия крейта — тоже (публичного изменения
нет).

### Редакторский слой

**Не требуется.** Лексика, синтаксис и семантика языка не меняются; новых
токенов, узлов АСД, форм объявления и диагностик нет. LSP (`takt-lang/src/lsp/`)
и плагины (`extensions/`) правок не требуют — они зовут семантику через
публичные входы, а те не двигаются.

### Документирование

**Не требуется.** Язык не меняется; документ `book/` описывает язык, а не
раскладку исходников компилятора.

### Риски и зависимости

- **Риск: молчаливая потеря теста.** Перенос `mod tests` целиком безопасен, но
  ошибка в подключении (`mod tests;` без `#[cfg(test)]` или, наоборот,
  забытый `mod`) даёт «зелёную» сборку при непрогоняемых тестах. Снижение —
  A4: сверка **списка имён** тестов до и после, а не только числа зелёных.
- **Риск: неиспользуемые импорты после разреза** — ловится `clippy -D warnings`
  (A6).
- **Риск: живой контекст отстанет.** `CLAUDE.md` ссылается на модуль
  (`semantic/statement.rs` в разделе о разрешении операторов); путь надо
  поправить — проверка `check-claude-md.py` сверяет пути с деревом и такую ссылку
  поймает.
- Зависимостей от незакрытых фич нет.

## Разработка

### Задача

#### Что было

`takt-lang/src/semantic/statement.rs` — **976** строк при пределе 1000 (замер
кандидата «999» относился к состоянию на закрытии 0155; с тех пор файл правили
трижды и он даже сократился). Из 976 строк **599** — блок `#[cfg(test)] mod
tests`, то есть 61 % файла.

#### Что сделано

Файл превращён в директорию-подмодуль по образцу `semantic/expression/`
(приём ADR, применённый фичей):

| Файл | Строк | Содержимое |
|---|---|---|
| `takt-lang/src/semantic/statement/mod.rs` | **381** | док-шапка, `resolve_statement`, `resolve_ast_statement`, `register_local_var`/`unregister_local_vars`; в конце — `#[cfg(test)] mod tests;` |
| `takt-lang/src/semantic/statement/tests.rs` | **602** | перенесённый блок тестов; шапка объясняет причину выноса, дальше `use super::*` |

Логика **не переписывалась**: ни одна строка `resolve_ast_statement` не
тронута, ветви исчерпывающего `match` остались в одном файле (Option B ADR
отвергнут именно за их разнос).

Путь модуля в дереве не изменился (`crate::semantic::statement`), поэтому
`named_block.rs` и `function.rs`, зовущие `resolve_statement`, правок не
потребовали.

Живой контекст `CLAUDE.md` поправлен: запись об инварианте указывала на
`semantic/statement.rs`, теперь — на `semantic/statement/mod.rs`. Исторические
ADR (0035, 0044, 0155) не трогались — это артефакты стадий, то есть история.

Обратная функциональность: публичный API крейта не менялся, язык —
тоже; версия языка не поднимается.

#### Проверки

```sh
cargo test --lib -- --list | grep semantic::statement   # список тестов до и после — совпал
./scripts/check-module-size.sh                          # код 0, реестр долга не пополнен
./scripts/precheck.sh                                   # код 0
git status --porcelain examples/generated/              # пусто — вывод целей не изменился
```

Результаты — в отчёте [0225-statement-module-size.md#отчёт-о-тестировании](0225-statement-module-size.md#отчёт-о-тестировании).

## Тест-план

### Область и цель

Фича — **чистый рефакторинг раскладки**: блок тестов переезжает из
`semantic/statement.rs` в `semantic/statement/tests.rs`, логика не меняется.
Отсюда две группы проверок:

1. **Цель достигнута** — файл разделён, запас появился, реестр долга не
   пополнен (R1–R3).
2. **Ничего не потеряно** — тот же набор тестов, то же поведение компилятора,
   строгая сборка (R4–R6).

Главный риск здесь — **молчаливая потеря тестов**: ошибка в подключении даёт
зелёную сборку при непрогоняемом файле. Поэтому проверяется не число зелёных
тестов, а **список их имён** до и после.

### Проверки (условие → ожидаемый результат)

| # | Проверка | Предусловие | Ожидаемый результат | Ссылка на R/A |
|---|---|---|---|---|
| T1 | `ls takt-lang/src/semantic/statement/` | правка внесена | `mod.rs` и `tests.rs`; файла `statement.rs` нет | R1 / A1 |
| T2 | `wc -l takt-lang/src/semantic/statement/*.rs` | то же | оба файла заметно ниже 1000 | R2 / A2 |
| T3 | `git diff scripts/module-size-baseline.txt` | то же | пусто — реестр долга не пополнен | R3 / A3 |
| T4 | `./scripts/check-module-size.sh` | то же | код 0 | R3 / A3 |
| T5 | `cargo test --lib -- --list \| grep semantic::statement`, сверка **до** и **после** | снят список до правки | списки совпадают побайтово (26 имён) | R4 / A4 |
| T6 | `./scripts/precheck.sh` | то же | код 0, включая `clippy --all-targets --all-features -- -D warnings` и полный прогон тестов | R5, R6 / A6 |
| T7 | `git status --porcelain examples/generated/` после T6 | precheck прогнан | пусто — порождённый код не изменился | R5 / A5 |
| T8 | Проверка ссылок на модуль | правка внесена | живой контекст `CLAUDE.md` указывает на существующий путь; проверка `check-claude-md.py` (в составе T6) зелёный | риск анализа |

### Разбивка проверок по функциональности

Единые условия и ожидаемые результаты прогоняются против каждой задеваемой
обратной функциональности; фиксируется статус.

| Функциональность | Статус | Замечание |
|---|---|---|
| Семантика (разрешение операторов) | ✅ | T5, T6 — логика не менялась, тесты те же |
| Потребители модуля (`named_block.rs`, `function.rs`) | ✅ | путь `crate::semantic::statement` сохранён; сборка в T6 |
| Генерация целей `c`/`st`/`rust`/`sv`/`plantuml` | ✅ | T7 — вывод байт-в-байт прежний |
| Симулятор | ✅ | T6 — потактовые сверки в составе прогона |
| Публичный API крейта | — | не менялся: модуль внутренний |
| LSP и плагины | — | язык не менялся, правок не требуется |
| Документ `book/` | — | не требуется: раскладка исходников в документ не входит |

<!-- Легенда: ✅ пройдено · ❌ провалено · ⬜ не проверялось · — не применимо -->

### Тестовые данные и окружение

- Модуль: `takt-lang/src/semantic/statement/` (после правки), образец раскладки
  — `takt-lang/src/semantic/expression/`.
- Списки имён тестов до/после — во временном каталоге сессии
  (`tests-before.txt`, `tests-after.txt`).
- Окружение: толчейн Rust `1.97.1` (пин `rust-toolchain.toml`), обычный набор
  инструментов `precheck.sh` (`cc`, `iec2c`, `verilator`, `yosys`, `typst`).

## Отчёт о тестировании

### Резюме

**Пройдено, фича готова к закрытию.** Все восемь проверок тест-плана зелёные.
Модуль разделён: `statement/mod.rs` — **381** строка, `statement/tests.rs` —
**602**; список имён тестов совпал побайтово (26 имён), порождённый код не
изменился, `./scripts/precheck.sh` — код 0 (3110 тестов, 6 игнорируемых).

Дефектов не найдено, исправления (`docs/fixes/0225-YY-*`) не заводятся.

### Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Состав каталога | ✅ | `takt-lang/src/semantic/statement/{mod.rs,tests.rs}`; прежнего `statement.rs` нет |
| T2 | `wc -l` | ✅ | 381 и 602 при лимите 1000 (было 976 в одном файле) |
| T3 | Реестр долга | ✅ | `scripts/module-size-baseline.txt` не тронут — те же три записи |
| T4 | `./scripts/check-module-size.sh` | ✅ | код 0: «проверено файлов, записей долга 3, превышение 1098 строк» |
| T5 | Списки имён тестов до/после | ✅ | `diff` пуст: 26 имён `semantic::statement::tests::*` в обоих |
| T6 | `./scripts/precheck.sh` | ✅ | код 0; `cargo test: 3110 passed, 6 ignored (14 suites, 29.52s)` |
| T7 | `git status --porcelain examples/generated/` | ✅ | пусто — вывод всех целей байт-в-байт прежний |
| T8 | Ссылки на модуль | ✅ | `CLAUDE.md` переведён на `semantic/statement/mod.rs`; проверка `check-claude-md.py` в составе T6 зелёный |

По ходу прогона T6 нашлась **битая ссылка в самом ADR** этой фичи
(`0088-module-size-split-round2.md` вместо `0088-module-size-remaining.md`) —
поймал проверка проверки ссылок. Исправлено, повторный прогон зелёный.

### Результаты по функциональности

| Функциональность | Статус | Замечание |
|---|---|---|
| Семантика (разрешение операторов) | ✅ | логика не менялась; те же 26 тестов модуля |
| Потребители модуля (`named_block.rs`, `function.rs`) | ✅ | путь `crate::semantic::statement` сохранён — директория с `mod.rs` даёт то же имя |
| Генерация целей | ✅ | T7 |
| Симулятор | ✅ | потактовые сверки в составе T6 |
| Публичный API крейта | — | не менялся: модуль внутренний |
| LSP и плагины | — | **правок не требуется**: лексика, синтаксис и семантика не менялись |
| Документ `book/` | — | не требуется: раскладка исходников в документ не входит |

### Выводы и дальнейшие шаги

1. **Замер кандидата устарел вдвойне.** Число: 999 → 976. Вывод «следующая
   правка модуля обязана начинаться с разделения, иначе проверка её отвергнет» —
   опровергнут историей: после того замера модуль правили трижды (0185, 0189,
   0243), разделения не делали, и проверка не отказывал ни разу. Настоящий предмет
   фичи — не блокировка, а **малый запас**.
2. **`statement.rs` не был самым близким к пределу** — седьмой из девяти файлов
   зоны ≥ 970 строк (ближе всех `sv_fsm.rs` — 998). Класс шире одного файла и
   вынесен **кандидатом** в `FEATURES.md`: предупреждение проверки о приближении к
   лимиту («жёлтая зона»), Option D ADR.
3. **Приём деления не изобретался:** тот же, что у `semantic/expression/`  — директория-подмодуль + `use super::*`.
4. Исправлений не требуется; фича переводится в `ГОТОВО`.

## Итог (что сделано)

`takt-lang/src/semantic/statement.rs` разделён на `semantic/statement/mod.rs`
(**381** строка — логика) и `semantic/statement/tests.rs` (**602** — тесты) по
образцу `semantic/expression/` (приём ADR, применённый фичей). Логика
не переписывалась: ветви исчерпывающего `match` в `resolve_ast_statement`
остались в одном файле.

**Замер кандидата устарел вдвойне.** Число: не 999, а **976** (после того
замера файл правили трижды, и две правки его сократили). Вывод «следующая
правка обязана начинаться с разделения, иначе проверка её отвергнет» — опровергнут
историей: проверка не отказывал ни разу. Настоящий предмет — **малый запас** (24
строки), а не блокировка. Вдобавок файл был лишь **седьмым** из девяти в зоне
≥ 970 строк; класс вынесен кандидатом («жёлтая зона» проверки размера).

Список имён тестов совпал побайтово (26), порождённый код не изменился,
`./scripts/precheck.sh` — код 0 (3110 тестов).

Детали — в [отчёте](0225-statement-module-size.md#отчёт-о-тестировании),
[ADR](0225-statement-module-size.md#архитектура-adr) и
[анализе](0225-statement-module-size.md#анализ).
