# Фича: Проверки без контрольных тестов получают контрольных тестов

- **Номер:** 0315
- **Статус:** ГОТОВО
- **Зависит от:** 0255 (закрыта; проставлено 2026-08-20)
- **Связанные issue (анализ):** процессный бэклог `FEATURES.md` — «Пять проверок
  предкоммита не контролируются ничем»

## Стадии жизненного цикла (правило 17)

Все стадии — **разделы этой карточки** (правило 32): «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог».
Отдельным артефактом остаются только исправления —
[`docs/fixes/`](../fixes/README.md) (при необходимости `0315-YY-*`).

## Краткое описание

Четыре проверки предкоммита не проверялись ничем — ни внешним контролем, ни
самопроверкой. Зелёный прогон такого проверки неотличим от проверки, который не
смотрит; ровно так дыра в `check-diagnostic-codes.sh` прожила от фичи до
0255.

> Фича зарегистрирована из процессного бэклога `FEATURES.md`.

## Документирование (правило 24)

**Не требуется:** правится процесс проверки, а не язык.

## Архитектура (ADR)

- **Статус:** Accepted
- **Дата:** 2026-08-20
- **Фича:** [../features/0315-gate-guards.md](0315-gate-guards.md)

### Контекст

Повод не теоретический: предметом фичи была **дыра в проверке**, прожившая от
фичи именно потому, что сломать его никто не пробовал. Тогда же родилось
правило: у проверки обязан быть либо внешний контроль `scripts/test-*.sh`, либо
внутренняя самопроверка (`--self-test`), и она идёт **первой**.

#### Замер (2026-08-20)

Пересчёт всех 25 проверок `scripts/check-*`:

| Состояние | Сколько |
|---|---|
| внешний контроль `test-*.sh` | 8 |
| внутренняя самопроверка | 12 |
| **ни того, ни другого** | **4** |
| подсказка, на код возврата не влияет | 1 (`check-stale-candidates.sh`) |

Четвёрка без прикрытия: `check-exhaustive-nodes.sh`,
`check-language-version.sh`, `check-repo-url.sh`, `check-links.py`.

Запись бэклога называла **пять** и другой состав: `check-module-size.sh` с
тех пор получил контроля, а `check-positional-scenarios.py` — самопроверку.
Замер снят заново, как требует правило 30.

### Решение

- `check-links.py` — **внутренняя самопроверка**: он единственный из четвёрки
  работает по дереву целиком, и синтетический вход строится на временном
  каталоге, а не на копии репозитория.
- Три остальных — **внешние контроля** `test-*.sh`, гоняющие проверка на копии
  дерева. Каждому проверке добавлена переменная корня (`EN_ROOT`, `LV_ROOT`,
  `RU_ROOT`) — образец `DD_ROOT`/`BD_ROOT`/`FG_ROOT`.

Каждый контроль проверяет не только «плохое ловится», но и «хорошее проходит», и
**вырожденный вход** (пропавший файл, отсутствующий якорь) — проверка, которому
нечего проверять, обязан падать, а не рапортовать об успехе (класс исправления).

### Отвергнутые варианты

1. **Самопроверка во всех четырёх.** Двум из них (адрес репозитория, версия
   языка) нужен целый набор файлов; синтетическое дерево естественнее строить
   снаружи, а внутри пришлось бы городить временные каталоги внутри проверки.
2. **Один общий контроль на все проверки.** Он знал бы устройство каждого — второе
   знание, расходящееся с проверкой.
3. **Ничего не делать, раз проверки зелёные.** Ровно это и позволило дыре 0132
   прожить до 0255.

### Последствия

- Из 25 проверок без прикрытия не осталось ни одного (кроме подсказки).
- Проверки, ловящие **предмет** каждого проверки, записаны словами: контроль
  `test-repo-url.sh` проверяет ту самую строку `cd <имя>`, ради которой проверка и
  заводился.

## Анализ

### Цель и контекст

Четыре проверки предкоммита не проверялись ничем. Зелёный прогон такого проверки
читается как «всё согласовано», хотя может означать «проверка не смотрит».

### Зависимости фичи (правило 17/19)

- **Зависит от:** 0255 (правило «у проверки обязан быть контроль»). Закрыта.
- **Влияние на порядок разработки:** закрывает запись процессного бэклога
  «Пять проверок предкоммита не контролируются ничем».

### Требования и проверяемые условия

- **R1.** `check-links.py` имеет самопроверку, и она стоит первой.
- **R2.** У `check-exhaustive-nodes.sh` есть контроль.
- **R3.** У `check-language-version.sh` есть контроль.
- **R4.** У `check-repo-url.sh` есть контроль.
- **R5.** Каждый контроль проверяет и «хорошее проходит», и «вырожденный вход
  отвергается».
- **R6.** Предкоммит зелёный.

### Разбор

#### Замер разошёлся с записью бэклога

Запись называла пять проверок и другой состав: `check-module-size.sh` с тех пор
получил контроля, `check-positional-scenarios.py` — самопроверку. Это ровно тот
случай, о котором предупреждала сама запись: «начинать с замера заново, список
меняется с каждым новым проверкой».

#### Почему у одного самопроверка, а у трёх контроля

`check-links.py` работает по дереву целиком, и синтетический вход для него —
пара файлов во временном каталоге: строить его внутри дешевле. Остальным нужен
целый набор файлов в определённых местах — снаружи это естественнее.

#### Что именно проверяют контроля

Не «проверка запускается», а **предмет** каждого:

- исчерпаемость — снятый атрибут и атрибут **в комментарии** (ровно та дыра,
  которой проверка болел до 0163);
- версия языка — отставший README, отставший живой контекст, отсутствующий и
  **двойной** якорь;
- адрес — разошедшийся slug, doc-ссылка в исходниках и строка `cd <имя>` после
  `git clone`, ради которой проверка заводился.

### Риски

- **Рост времени предкоммита** — четыре быстрых прогона на временных деревьях;
  в нормативе.

## Разработка

### Задача

#### Состав правки

| Файл | Что сделано |
|---|---|
| `scripts/check-links.py` | самопроверка `--self-test` (битая ссылка ловится; целая, внешняя, в код-спане и в блоке — нет) |
| `scripts/check-exhaustive-nodes.sh` | корень переопределяется `EN_ROOT` |
| `scripts/check-language-version.sh` | корень переопределяется `LV_ROOT` |
| `scripts/check-repo-url.sh` | корень переопределяется `RU_ROOT` |
| `scripts/test-exhaustive-nodes.sh` | 4 условия |
| `scripts/test-language-version.sh` | 5 условий |
| `scripts/test-repo-url.sh` | 5 условий |
| `scripts/precheck.sh` | контроля и самопроверка идут перед своими проверками |

#### Проверено

- Каждый контроль прогнан отдельно: все условия проходят.
- `./scripts/precheck.sh` — код 0; в логе видны все четыре прикрытия.

## Тест-план

### Условия

| # | Условие | Проверка |
|---|---|---|
| П1 | Битая ссылка ловится; целая, внешняя, в коде и в блоке — нет | `check-links.py --self-test` |
| П2 | Снятый атрибут `deny` ловится | `test-exhaustive-nodes.sh` (2) |
| П3 | Атрибут **в комментарии** не засчитывается | там же (3) |
| П4 | Пропавший файл — отказ | там же (4) |
| П5 | Отставшие README / живой контекст ловятся | `test-language-version.sh` (2, 3) |
| П6 | Отсутствующий и двойной якорь ловятся | там же (4, 5) |
| П7 | Разошедшийся адрес и doc-ссылка ловятся | `test-repo-url.sh` (2, 4) |
| П8 | Отставший `cd <имя>` ловится | там же (3) |
| П9 | Каждый контроль принимает здоровый вход | все (1) |
| П10 | Предкоммит зелёный | `./scripts/precheck.sh` |

### Почему тесты именно такие

- **«Хорошее проходит» (П9) обязательно:** контроль, ловящий всё подряд,
  доказывает лишь, что проверка умеет падать.
- **Вырожденный вход (П4, П6)** — проверка, которому нечего проверять, обязан
  падать; иначе зелёный прогон означает пустоту с обеих сторон (класс исправления).
- **Проверяется предмет проверки, а не его запуск:** атрибут в комментарии,
  двойной якорь, `cd <имя>` — это те самые дефекты, ради которых проверки
  заводились.

## Отчёт о тестировании

### Окружение

- macOS (darwin 25.5.0), POSIX sh, Python 3.14. Дата: 2026-08-20.

### Замер (правило 30)

| Состояние проверки | До | После |
|---|---|---|
| внешний контроль | 8 | 11 |
| внутренняя самопроверка | 12 | 13 |
| **ни того, ни другого** | **4** | **0** |
| подсказка (код возврата не меняет) | 1 | 1 |

Четвёрка: `check-exhaustive-nodes.sh`, `check-language-version.sh`,
`check-repo-url.sh`, `check-links.py`.

### Сводка прогонов

| Прогон | Результат |
|---|---|
| `check-links.py --self-test` | ловушка взведена |
| `sh scripts/test-exhaustive-nodes.sh` | 4/4 |
| `sh scripts/test-language-version.sh` | 5/5 |
| `sh scripts/test-repo-url.sh` | 5/5 |
| `./scripts/precheck.sh` | код 0 |

### Сверка с тест-планом

| # | Условие | Итог |
|---|---|---|
| П1…П4 | Ссылки и исчерпаемость | ✅ |
| П5…П8 | Версия языка и адрес | ✅ |
| П9…П10 | Здоровый вход, предкоммит | ✅ |

### Что показал прогон

1. **Список бэклога устарел.** Он называл пять проверок; двое из них прикрытие
   уже получили. Замер снят заново — правило 30 сработало на процессной записи
   так же, как на дефектной.
2. **Якорь живого контекста строже, чем казалось.** Первая редакция контроля
   версии писала в `CLAUDE.md` произвольную строку с номером — проверка отверг её:
   он требует форму «**сейчас `X.Y.Z`**» рядом с упоминанием константы. То есть
   контроль заодно проверил, что проверка **не** довольствуется любым числом.
3. **Проверка «атрибут в комментарии»** воспроизводит настоящую дыру фичи
   — проверка искал подстроку где угодно в строке и находил её в док-комментарии.

### Дефекты

Дефектов проверок не найдено: все четыре ведут себя как обещано, и теперь это
доказано, а не предполагается.

### Редакторский слой (правило 29)

**Не требуется** — подтверждено: код инструментов не менялся.

### Вердикт

**Готово.** Каждая проверка предкоммита либо имеет контроля, либо проверяет себя сам.

## Итог (что сделано)

**Из 25 проверок без прикрытия не осталось ни одного** (кроме подсказки
`check-stale-candidates.sh`, не влияющей на код возврата).

- **`check-links.py`** получил самопроверку: битая ссылка ловится, а целая,
  внешняя, в код-спане и внутри блока кода — нет.
- **Три проверки получили внешних контрольных тестов** (`test-exhaustive-nodes.sh`,
  `test-language-version.sh`, `test-repo-url.sh`), работающих на **копии**
  дерева: проверкам добавлены переменные корня `EN_ROOT`, `LV_ROOT`, `RU_ROOT`
  (образец `DD_ROOT`, `BD_ROOT`, `FG_ROOT`).
- **Замер разошёлся с записью бэклога:** она называла **пять** проверок и
  другой состав — `check-module-size.sh` с тех пор получил контроля,
  `check-positional-scenarios.py` — самопроверку. Запись сама предупреждала:
  «начинать с замера заново».
- **Контроль проверяет предмет проверки, а не его запуск:** атрибут `deny` **в
  комментарии** (дыра, которой проверка болел до 0163), **двойной** якорь версии,
  строка `cd <имя>` после `git clone` (ради неё проверка и заводился).
- **«Хорошее проходит» — обязательное условие каждого контроля:** ловящий всё
  подряд доказывает лишь, что проверка умеет падать.
- **Вырожденный вход обязан отвергаться:** пропавший файл и отсутствующий
  якорь дают отказ, а не рапорт об успехе (класс исправления).

Детали — в [отчёте](0315-gate-guards.md#отчёт-о-тестировании); отвергнутые варианты — в
[ADR](0315-gate-guards.md#архитектура-adr).
