# Фича: Предупреждение о делителе (`SV-009`) в цели `sv`

- **Номер:** 0064
- **Статус:** ГОТОВО (2026-07-20; `precheck.sh` зелёный)
- **Зависит от:** **нет** (проверено аналитиком 2026-07-17 — см. [анализ](0064-sv-divider-warning.md#анализ)). Опора — **закрытая** [генератор генерации в SystemVerilog](0045-sv-backend.md). **Смежно, но не зависимость:** [`lamc` не печатает предупреждения вообще](0081-lamc-print-warnings.md) (`lamc` не печатает Ce13/Ce14) — проверено: предупреждения **генераторов** доставляются другим каналом.
- **Приоритет / Tier:** **Tier 3** — трансляция верна, деления в корпусе нет, ничего не сломано. Ценность — предупредить об **эксплуатационной** цене (потолок частоты), которую автор `.lam` увидеть не может.
- **Крейт:** `grammar` (`generator/sv/`: `sv_expr.rs`, `mod.rs`)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`); закрывает открытый вопрос [генератор генерации в SystemVerilog](0045-sv-backend.md#разработка)

## Стадии жизненного цикла

Все стадии — **разделы этой карточки**: «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог».
Отдельным артефактом остаются только исправления —
[`docs/fixes/`](../fixes/README.md) (при необходимости `0064-YY-*`).

## Краткое описание

`/` и `%` синтезируются в **аппаратный делитель** — крупный и медленный блок;
автор `.lam` об этом из текста модели не узнаёт. Код `SV-009` **зарезервирован, но
не принят**.

Открытый вопрос — полезная подсказка это или шум; решается пробой `yosys stat` на
модели с делением. В корпусе деления **нет**, поэтому и повода принять решение
пока не было.

Вынесено закрытой [генератор генерации в SystemVerilog](0045-sv-backend.md).

## Архитектура (ADR)

- **Status:** Accepted
- **Date:** 2026-07-17
- **Authors:** Архитектор + Лид разработки
- **Related issues:** [Фича](0064-sv-divider-warning.md); закрывает открытый вопрос [генератор генерации в SystemVerilog](0045-sv-backend.md#разработка) («полезная подсказка или шум? — **решить пробой `yosys stat`**»)

### Context

Задача зарезервировала код `SV-009` (деление синтезируется в аппаратный
делитель), но **не завела** диагностику, записав открытый вопрос дословно:

> «`/` и `%`. В C — инструкция процессора; в RTL — **делитель**, крупный
> комбинационный блок (десятки/сотни ячеек, длинный путь → потолок частоты).
> Синтезируется, но автор об этом не знает. Предупреждение `SV-009` — обсудить:
> **полезная подсказка или шум? Решить пробой `yosys stat` на модели с
> делением**.»

Проба сделана. Ниже — её результат; он определил решение и **дважды по ходу
перевернул** предварительный вывод.

#### Замер 1: обобщённый синтез (`yosys synth -flatten`), ячеек

| Ширина | `a + b` | `a * b` | `a / b` | `a % b` |
|---|---|---|---|---|
| `u8` | 60 | 166 (×2.8) | 228 (×3.8) | 252 (×4.2) |
| `u16` | 118 | 733 (×6.2) | 870 (×7.4) | 914 (×7.7) |
| `u32` | 240 | 3030 (×12.6) | 3437 (×14.3) | 3525 (×14.7) |

**Первый (ошибочный) вывод:** умножение почти так же дорого, как деление
(u32: 3030 против 3437 — разрыв всего ×1.13), значит предупреждать только про `/`
— вводить в заблуждение.

#### Замер 2: FPGA-поток (`yosys synth_ice40 -dsp -flatten`), `u32`

Замер 1 сделан **обобщённым отображением на вентили**, которое **не знает про
DSP-блоки**. Реальный FPGA-поток даёт другую картину:

| Операция | `SB_LUT4` | `SB_MAC16` (DSP) |
|---|---|---|
| `a + b` | 49 | 0 |
| `a * b` | **17** | **3** |
| **`a / b`** | **558** | **0** |
| **`a % b`** | **591** | **0** |
| `a / 2` (константа) | **17** | 0 |
| `a / 3` (константа) | **106** | 0 |

**Это переворачивает первый вывод.** У умножения **есть аппаратная поддержка**:
оно уходит в 3 DSP-блока и 17 LUT — **дешевле сложения**. У деления
**аппаратной поддержки нет**: 558 LUT, ноль DSP — **×11 от сложения** и **×33** от
LUT-цены умножения.

**Деление в RTL действительно особенное**, и замер 1 вводил в заблуждение ровно
потому, что моделировал не ту технологию.

#### Замер 3: делитель-константа

`a / 2` → **17 LUT** (сдвиг, дешевле сложения); `a / 3` → **106 LUT**
(умножение на обратное) — то есть **порядка сложения**. Дорого **только деление
на переменную**.

#### Корпус

Деления в `examples/` **нет** (греп) — предупреждение сегодня не сработало бы ни
разу.

### Decision Drivers

1. **Предупреждение обязано быть сигналом, а не шумом.** Сработав на `a / 2`
   (17 LUT — дешевле сложения), оно обесценит себя и будет заглушено.
2. **Умолчание не должно вводить в заблуждение.** Если предупреждать про `/`, но
   молчать про `*`, автор решит, что `*` дорого/дёшево по остаточному принципу.
   Замер 2 показал: молчать про `*` **правильно** — оно уходит в DSP.
3. **Автор узнаёт правду в компиляторе Lam, а не от чужого инструмента** —
   сквозной драйвер целей 0041/0045.
4. **Не изобретать порогов без основания.** «Дорого» зависит от технологии, а
   компилятор её не знает.

### Considered Options

#### Option A. `SV-009` на **любое** `/` и `%` (что подразумевал кандидат)

**Pros:** просто; ловит все случаи.

**Cons:**
- **Опровергнуто замером 3:** сработает на `a / 2` (17 LUT) и `a / 3` (106 LUT) —
  то есть на том, что **дешевле или вровень со сложением**. Это шум (драйвер 1).
- Шумное предупреждение заглушают, и вместе с ним теряют сигнал.

#### Option B. `SV-009` только на **переменный** делитель; константа — молча

**Pros:**
- **Ровно там, где сигнал** (замеры 2–3): переменный делитель — 558 LUT и ноль
  DSP; константный — 17…106 LUT.
- Не вводит в заблуждение молчанием про `*` (драйвер 2): у умножения **есть**
  аппаратная поддержка, у деления — **нет**. Это не вкус, а замер.
- Автор узнаёт про потолок частоты в компиляторе Lam (драйвер 3).
- Правило проверяемо и не требует порогов (драйвер 4): «делитель — константа?» —
  свойство АСД, а не технологии.

**Cons:**
- Не покроет `a / b`, где `b` фактически константа по потоку данных
  (константа времени выполнения). Смягчение: **консервативно** — лишнее
  предупреждение честнее пропущенного, а свёртку констант компилятор Lam и так
  делает для адресов.

#### Option C. Предупреждать и про `*` (`SV-010`) — «единообразно»

**Pros:** не выделяет одну операцию.

**Cons:** **опровергнуто замером 2**: `a * b` → 3 DSP + 17 LUT, **дешевле
сложения**. Предупреждать об этом — прямой шум; «единообразие» здесь было бы
единообразием заблуждения.

#### Option D. Не заводить `SV-009` вовсе; описать цену в документации

**Pros:** ноль шума; статус-кво.

**Cons:** оставляет ровно ту беду, которую назвала 0045-06: «синтезируется, но
**автор об этом не знает**». Документацию читают до, а ошибку делают во время —
и потолок частоты обнаружится у чужого инструмента (нарушен драйвер 3).

### Decision

Принимается **Option B**: `SV-009` — **предупреждение** (не ошибка) на `/` и `%`
**с переменным делителем**; делитель-константа → **молча**.

Открытый вопрос 0045-06 («подсказка или шум?») отвечен **замером**: подсказка —
но только для переменного делителя. В форме, которую подразумевал кандидат
(Option A), это был бы **шум**.

**Нормативные правила:**

1. **`SV-009` — предупреждение, а не ошибка.** Деление синтезируется корректно;
   цель не вправе отказать в трансляции того, что работает.
2. Срабатывает на `/` и `%`, **если делитель не является константой** времени
   компиляции.
3. **Делитель-константа — молча** (замер 3: 17…106 LUT).
4. **Про `*` не предупреждать** (замер 2: DSP; дешевле сложения). Если появится
   технология без DSP — это отдельное решение с новым замером, а не «заодно».
5. Текст обязан называть **цену и следствие** («нет аппаратного делителя: ~558
   LUT на `u32` против 49 у сложения; длинный комбинационный путь → потолок
   частоты»), а не просто «деление — это дорого».
6. Язык **не меняется** → версия не растёт.

### Consequences

#### Положительные

- Автор узнаёт про делитель в компиляторе Lam, а не по проваленному timing
  closure.
- Молчание про `*` — обосновано замером, а не умолчанием.
- Корпус не задет (деления нет) — предупреждение не породит шума в предкоммите.

#### Отрицательные / Action items

- **A-1. Замер технологичен.** Числа сняты на `synth_ice40 -dsp`; на другой
  платформе пропорции сдвинутся (у `synth_xilinx` DSP48 шире). **Вывод «у деления
  нет аппаратной поддержки, у умножения есть» — общий**, но конкретные LUT в
  тексте предупреждения приводить **не следует**: они устареют и станут ложью.
  свод требует называть **порядок** и **следствие**, а не точное число.
- **A-2. Константа по потоку данных не покрыта**: `b := 3; r := a / b;`
  даст предупреждение, хотя синтезатор свернёт. Консервативно и осознанно.
- **A-3. `q(m,n)` ([fixed-point Q(m.n) как тип языка](0061-fixed-point-type.md#архитектура-adr)) делит** — свод её ADR
  строит `/` на расширении и целочисленном делении. Когда 0061 будет
  реализована, `SV-009` обязана срабатывать и там: делитель тот же.
- **A-4. Записать замер в `CLAUDE.md`** — «обобщённый `synth` не моделирует DSP,
  поэтому цена `*` в нём завышена вчетверо; мерить FPGA-поток». Это ловушка,
  которая уже один раз перевернула вывод внутри этой же проработки.
- **A-5. У цели `sv` канала предупреждений НЕТ вовсе** (вскрыто этой
  проработкой). Греп по `generator/sv/`: ни `Diagnostic::warning`, ни `report` —
  ноль вхождений. У `rust` (`rust/mod.rs:199`) и `st` (`st/mod.rs:290`) функция
  `report` есть. **Значит `SV-009` без своего канала будет немой** — фича обязана
  завести `report` в `generator/sv/mod.rs` по образцу соседей. Это **входит в
  объём** 0064: без доставки диагностика бессмысленна.
- **A-6. Канал доставки сам по себе дефектен** (вскрыто этой проработкой; вне
  объёма, кандидат). `report` печатает **`eprintln!` прямо из библиотеки**
  (`rust/mod.rs:202`, `st/mod.rs:293`), а не возвращает диагностики вызывающему:
  `compile_to_sv`/`compile_to_rust` объявлены `Result<(), Diagnostic>` и
  предупреждений **не отдают**. Следствия: `--quiet` их **не глушит** (греп по
  `generator/`: слова `quiet` нет), LSP получит их в свой stderr, а тесты не могут
  проверить факт предупреждения иначе как перехватом stderr. 0064 **воспроизводит
  сложившийся образец** (иначе фича превратилась бы в рефакторинг доставки), но
  дефект заведён кандидатом.

#### Acceptance criteria

1. **A1.** `r := a / b` (переменный `b`) → **предупреждение `SV-009`**; трансляция
   **успешна**.
2. **A2.** `r := a / 2` и `r := a / 3` → **предупреждения нет**.
3. **A3.** `r := a % b` (переменный) → `SV-009`; `r := a % 4` → молча.
4. **A4.** `r := a * b` → **предупреждения нет**.
5. **A5.** Текст называет причину и следствие (нет аппаратного делителя → потолок
   частоты), без конкретных LUT (A-1).
6. **A6.** Корпус: предупреждений **ноль** (деления нет) — вывод всех целей
   побайтово прежний.
7. **A7.** `SV-009` **доставляется пользователю** — см. A-5: сегодня у цели `sv`
   канала предупреждений **нет вовсе**, поэтому фича обязана его завести, иначе
   диагностика будет **немой**.

## Анализ

### Цель и контекст

[генератор генерации в SystemVerilog](0045-sv-backend.md#разработка) зарезервировала
код `SV-009`, но диагностику **не завела**, оставив вопрос дословно: «полезная
подсказка или шум? **Решить пробой `yosys stat` на модели с делением**».

Проба сделана. ADR принял **Option B**: предупреждать **только на переменный
делитель**.

### Замер (пробы 2026-07-17) — он дважды перевернул вывод

**Шаг 1. Обобщённый синтез** (`yosys synth -flatten`), ячеек:

| Ширина | `a + b` | `a * b` | `a / b` | `a % b` |
|---|---|---|---|---|
| `u8` | 60 | 166 | 228 | 252 |
| `u16` | 118 | 733 | 870 | 914 |
| `u32` | 240 | 3030 | 3437 | 3525 |

→ Предварительный вывод: `*` почти так же дорог, как `/` (u32 — разрыв ×1.13),
значит предупреждать только про `/` — вводить в заблуждение. **Вывод оказался
неверным.**

**Шаг 2. FPGA-поток** (`yosys synth_ice40 -dsp -flatten`, `u32`):

| Операция | `SB_LUT4` | `SB_MAC16` (DSP) |
|---|---|---|
| `a + b` | 49 | 0 |
| `a * b` | **17** | **3** |
| **`a / b`** | **558** | **0** |
| **`a % b`** | **591** | **0** |
| `a / 2` (константа) | **17** | 0 |
| `a / 3` (константа) | **106** | 0 |

**Шаг 1 моделировал не ту технологию.** Обобщённое отображение на вентили
**не знает про DSP-блоки**. На FPGA у умножения аппаратная поддержка **есть**
(3 DSP + 17 LUT — **дешевле сложения**), у деления — **нет** (558 LUT, ноль DSP;
×11 от сложения).

**Шаг 3.** Делитель-константа дёшев: `/ 2` → сдвиг (17 LUT), `/ 3` → умножение на
обратное (106 LUT) — порядка сложения.

**Ответ на вопрос 0045-06:** подсказка — **но только для переменного делителя**.
В форме «предупреждать на всякое `/`» это был бы **шум**: сработало бы на `a / 2`,
который дешевле сложения.

### Находка проработки: у цели `sv` канала предупреждений НЕТ

Греп по `generator/sv/`: ни `Diagnostic::warning`, ни `report` — **ноль**
вхождений. У соседей есть: `rust/mod.rs:199`, `st/mod.rs:290`.

**Следствие для объёма:** `SV-009` без канала будет **немой**. Заведение
`report` в `generator/sv/mod.rs` **входит в объём** 0064 — иначе фича не достигает
цели.

### Находка проработки: сам канал дефектен (вне объёма)

`report` печатает **`eprintln!` прямо из библиотеки** (`rust/mod.rs:202`,
`st/mod.rs:293`), а не возвращает диагностики вызывающему: `compile_to_sv` и
`compile_to_rust` объявлены `Result<(), Diagnostic>` и предупреждений **не
отдают**. Следствия: `--quiet` их **не глушит** (греп по `generator/`: слова
`quiet` нет); LSP получит их в свой stderr; тест не может проверить факт
предупреждения иначе как перехватом stderr. Заведено кандидатом; 0064
воспроизводит сложившийся образец, чтобы не превращаться в рефакторинг доставки.

### Зависимости фичи

- **Зависит от:** **нет**. Опора — **закрытая** [генератор генерации в SystemVerilog](0045-sv-backend.md).
- **Пересечения:** [fixed-point Q(m.n) как тип языка](0061-fixed-point-type.md) строит `/` над
  `q(m,n)` на целочисленном делении → когда она будет реализована, `SV-009`
  обязана срабатывать и там (делитель тот же). Не зависимость: порядок любой, но
  0061 обязана про `SV-009` знать.
  [порт `en` (clock enable) для цели `sv`](0063-sv-clock-enable.md) и
  [последовательная композиция (`+`) в цели SystemVerilog](0057-sv-sequential-composition.md) правят `sv_fsm.rs`; 0064 —
  `sv_expr.rs` + `sv/mod.rs`. Конфликтов нет.
- **Смежно:** [`lamc` не печатает предупреждения вообще](0081-lamc-print-warnings.md) (`lamc` не печатает
  Ce13/Ce14). **Не зависимость:** проверено — предупреждения **генераторов**
  доставляются иначе (своим `eprintln!` внутри цели), то есть 0081 про другой
  канал. Ловушка, в которую легко попасть, читая только карточку 0081.
- **Влияние на порядок:** никого не блокирует.

### Приоритет (Tier) — обоснование

**Tier 3.**

- Трансляция **верна**: деление синтезируется корректно, дефекта нет.
- Корпус деления **не содержит** → предупреждение сегодня не сработало бы ни разу.
- Ценность — предупредить об **эксплуатационной** цене (потолок частоты), которую
  автор `.lam` увидеть не может.
- Против Tier 2: ничего не сломано, никто не заблокирован.

### Требования и проверяемые условия

- **R1. `SV-009` — предупреждение, а не ошибка** (деление работает).
- **R2. Срабатывает только на переменный делитель** `/` и `%`.
- **R3. Делитель-константа — молча** (замер 3).
- **R4. Про `*` не предупреждать** (замер 2: DSP).
- **R5. Текст называет причину и следствие** (нет аппаратного делителя → потолок
  частоты), **без конкретных LUT** — они технологичны и устареют.
- **R6. Канал доставки заведён** у цели `sv` (`report`) — иначе диагностика немая.
- **R7. Корпус не задет**; язык не меняется → версия не растёт.

### Критерии приёмки и способ проверки

| # | Критерий | Способ проверки |
|---|---|---|
| A1 | `a / b` (переменный) → `SV-009`; трансляция **успешна** | Фикстура + перехват stderr |
| A2 | `a / 2`, `a / 3` → предупреждения **нет** | Фикстуры |
| A3 | `a % b` → `SV-009`; `a % 4` → молча | Фикстуры |
| A4 | `a * b` → предупреждения **нет** | Фикстура |
| A5 | Текст называет причину и следствие, без LUT | Ревью |
| A6 | Корпус: предупреждений ноль; вывод целей побайтово прежний | `git diff --exit-code examples/generated` |
| A7 | Предупреждение **доходит** до пользователя | Прогон `lamc -t sv` (канал заведён — R6) |

### Особенности по обратной функциональности

**Правка аддитивна**: добавляется **предупреждение**, трансляция не
меняется. Корпус деления не содержит → вывод всех целей побайтово прежний (A6).
Язык не меняется → версия не растёт.

Единственная структурная правка — **новый канал** `report` у цели `sv` (R6),
аддитивный по образцу `rust`/`st`.

### Риски и зависимости

- **R-1. Померить не ту технологию** (уже случилось внутри этой проработки).
  Обобщённый `synth` завысил цену `*` вчетверо и едва не привёл к решению
  «предупреждать и про умножение», которое было бы прямым шумом. Снижение:
  замеры на **FPGA-потоке**; записать ловушку в `CLAUDE.md` (A-4 ADR).
- **R-2. Числа в тексте предупреждения устареют.** LUT технологичны (`synth_ice40`
  ≠ `synth_xilinx`). Снижение: R5 — называть **порядок** и **следствие**.
- **R-3. Немая диагностика.** У `sv` канала нет вовсе. Снижение: R6 в объёме;
  A7 проверяет доставку, а не только факт.
- **R-4. Шум обесценивает контроля.** Сработав на `/ 2`, `SV-009` будет заглушена
  вместе с полезным случаем. Снижение: R2/R3 + A2 как контроль.
- **R-5. Константа по потоку данных.** `b := 3; r := a / b;` даст предупреждение,
  хотя синтезатор свернёт. Консервативно и осознанно (A-2 ADR).

## Разработка

### Задача

#### Что было

`sv_expr.rs:425` — комментарий «предупреждать о цене (кандидат `SV-009` задачи)»; трансляция `/` и `%` прямая, диагностики нет. У цели `sv`
**канала предупреждений нет вовсе** (греп по `generator/sv/`: ни
`Diagnostic::warning`, ни `report`).

#### Что сделано

1. **Канал.** Завести `report` в `generator/sv/mod.rs` по образцу
   `rust/mod.rs:199` и `st/mod.rs:290`. **Без него `SV-009` — немая**, и фича
   цели не достигает.
   Образец воспроизводится **сознательно, вместе с его дефектом**: `report`
   печатает `eprintln!` из библиотеки и диагностику вызывающему не возвращает
   (`compile_to_sv` → `Result<(), Diagnostic>`), из-за чего `--quiet` его не
   глушит. Чинить это здесь **нельзя** — фича превратилась бы в рефакторинг
   доставки; дефект заведён кандидатом.
2. **Диагностика `SV-009`** — **предупреждение**, а не ошибка (свод ADR):
   деление синтезируется корректно, и отказывать в трансляции рабочего кода цель
   не вправе.
3. **Условие срабатывания** (своду–4 ADR): `/` и `%`, **если делитель не
   константа** времени компиляции. Константа → **молча**. Про `*` → **молча**.
4. **Текст**: называет **причину** («в RTL нет аппаратного делителя»)
   и **следствие** («длинный комбинационный путь → потолок частоты»), **без**
   конкретных LUT — они технологичны и устареют.

**Основание правил — замер, а не мнение** (`yosys synth_ice40 -dsp -flatten`, `u32`):

| Операция | `SB_LUT4` | DSP |
|---|---|---|
| `a + b` | 49 | 0 |
| `a * b` | **17** | **3** ← дешевле сложения |
| `a / b` | **558** | 0 ← ×11 от сложения |
| `a / 2` | 17 | 0 ← сдвиг |
| `a / 3` | 106 | 0 ← ≈ сложение |

#### Проверки

- **T1/A1:** `a / b` → `SV-009`, трансляция успешна.
- **T2/T4/T6/A2, A3 — молчание:** `a / 2`, `a / 3`, `a % 4` → предупреждения нет.
- **T5/A4:** `a * b` → предупреждения нет.
- **T11:** мутация «предупреждать на любое `/`» обязана **валить T2** — иначе
  шумная версия прошла бы незамеченной.
- **T7/A7:** предупреждение **доходит** до пользователя (канал заведён).
- **T8/A5:** текст называет причину и следствие, без LUT.
- **T9/A6:** корпус — ноль предупреждений; `git diff --exit-code examples/generated`
  пуст.
- **T10:** порождённый SV не изменился.

## Тест-план

### Область и цель

Проверяется **предупреждение** `SV-009` и — не менее важно — **его молчание** там,
где деление дёшево. Язык не меняется → примеров/контрпримеров синтаксиса свод не требует.

**Половина плана — про молчание.** Предупреждение, срабатывающее на `a / 2`
(17 LUT — дешевле сложения), будет заглушено вместе с полезным случаем. Поэтому
T2/T4 (молчание) — такие же полноправные проверки, как T1.

### Проверки (условие → ожидаемый результат)

| # | Проверка | Предусловие | Ожидаемый результат | Ссылка на R/A |
|---|---|---|---|---|
| T1 | Переменный делитель | `r := a / b;` | **`SV-009`**; трансляция **успешна** (предупреждение, не ошибка) | R1, R2 / A1 |
| T2 | **Молчание: константа-степень двойки** | `r := a / 2;` | Предупреждения **нет** (замер: 17 LUT — дешевле сложения) | R3 / A2 |
| T3 | Остаток по переменной | `r := a % b;` | **`SV-009`** | R2 / A3 |
| T4 | **Молчание: константа не степень двойки** | `r := a / 3;` | Предупреждения **нет** (замер: 106 LUT ≈ сложение) | R3 / A2 |
| T5 | **Молчание: умножение** | `r := a * b;` | Предупреждения **нет** (замер: 3 DSP + 17 LUT — **дешевле сложения**) | R4 / A4 |
| T6 | Остаток по константе | `r := a % 4;` | Предупреждения **нет** | R3 / A3 |
| T7 | **Доставка** | `lamc compile -t sv` на фикстуре T1 | Предупреждение **видно пользователю**. У цели `sv` канала (`report`) **не было вовсе** — он заводится этой фичей | R6 / A7 |
| T8 | Текст | Фикстура T1 | Называет **причину** (нет аппаратного делителя) и **следствие** (потолок частоты); **без** конкретных LUT — они технологичны | R5 / A5 |
| T9 | Корпус | Перегенерация | Предупреждений **ноль** (деления в `examples/` нет); `git diff --exit-code examples/generated` — пусто | R7 / A6 |
| T10 | Трансляция не изменилась | Фикстура T1 | Порождённый SV **тот же**, что и до фичи (предупреждение не меняет вывод) | R1 / A6 |
| T11 | **Негативный контроль молчания** (мутация) | Заставить `SV-009` срабатывать на любое `/` | **T2 падает** — иначе шумная версия прошла бы незамеченной | R3 / A2 |

### Разбивка проверок по функциональности

| Функциональность | Ожидание | Проверка | Статус |
|---|---|---|---|
| Цель `sv` | предупреждение + **новый канал доставки** | T1–T8, T10 | ✅ |
| Цель `sv-mmio` ([цель `sv-mmio`: адреса портов → регистровый файл на шине](0062-sv-mmio-target.md)) | унаследует, когда появится | — (н/п) | — |
| Цель `sv` + `q(m,n)` ([fixed-point Q(m.n) как тип языка](0061-fixed-point-type.md)) | `/` над `q` — тот же делитель → `SV-009` обязана срабатывать | — (0061 закрыта; путь `fixed_bin` предупреждение получает — action A-3) | — |
| Прочие цели, симулятор, LSP | не задеты | T9 | ✅ |
| Язык | **не меняется** → версия не растёт | T9 | ✅ |

<!-- Легенда: ✅ пройдено · ❌ провалено · ⬜ не проверялось · — не применимо -->

### Тестовые данные и окружение

- **Фикстуры** — в `grammar/tests/data/`: деления в `examples/` **нет**, и
  добавлять его туда не следует (каждый пример корпуса обязан иметь сценарный
  контракт — проверка достижимости 0030).
- **Проверка предупреждения — перехватом stderr:** канал печатает `eprintln!`
  **из библиотеки** и диагностику вызывающему **не возвращает** (`compile_to_sv`
  объявлен `Result<(), Diagnostic>`). Это известный дефект канала — заведён
  кандидатом, вне объёма 0064.
- **Замеры, обосновавшие правила** (для воспроизведения):
  `yosys -p "read_verilog -sv M.sv; synth_ice40 -dsp -top M -flatten; stat"`.
  **Обобщённый `synth` не годится** — он не моделирует DSP и завышает цену `*`
  вчетверо; именно он едва не привёл к решению «предупреждать и про умножение».

### Что этот план сознательно не проверяет

- **Дефект канала доставки** (`eprintln!` из библиотеки; `--quiet` не глушит) —
  кандидат, вне объёма.
- **Конкретные числа LUT** — технологичны (`synth_ice40` ≠ `synth_xilinx`);
  проверяется формулировка (T8), а не цифра.

## Отчёт о тестировании

- **Дата:** 2026-07-20
- **Окружение:** macOS (darwin 25.5.0), cargo nightly. Проверки целевого языка
  (verilator/yosys) не требуются: фича не меняет вывод (деления в корпусе нет),
  а предупреждение проверяется юнит-тестами.
- **Вердикт:** **готово.** SV-юниты `grammar` (68, +8 к базе) зелёные,
  `precheck.sh` зелёный, вывод всех целей побайтово прежний.

### Сверка с критериями приёмки

| Критерий | Проверка | Результат |
|---|---|---|
| **A1** `a / b` → `SV-009`, трансляция успешна | `variable_divide_warns_sv009` | ✅ вывод `(a / b)`, один код `SV-009` |
| **A2** `a / 2`, `a / 3` → молчание | `constant_power_of_two_divide_is_silent`, `constant_non_power_of_two_divide_is_silent` | ✅ приёмник пуст |
| **A3** `a % b` → `SV-009`; `a % 4` → молча | `variable_modulo_warns_sv009`, `constant_modulo_is_silent` | ✅ |
| **A4** `a * b` → молчание | `multiply_is_silent` | ✅ приёмник пуст, вывод `(a * b)` |
| **A5** текст: причина + следствие, без LUT | `sv009_text_names_cause_and_consequence_without_luts` | ✅ содержит «аппаратного делителя» и «частот», не содержит «LUT»/«558» |
| **A6** корпус: 0 предупреждений, вывод прежний | греп `examples/*.lam` на `/`,`%` (пусто); `precheck.sh` детерминизм-проверка | ✅ |
| **A7** `SV-009` доставляется пользователю | новый `report` в `sv/mod.rs`, зовётся в `generate_program` | ✅ канал заведён |

### Разбор проверок тест-плана

- **T1/T3 (переменный `/`,`%`)** — `SV-009` эмитится, трансляция состоялась.
- **T2/T4/T6 (константа `a/2`,`a/3`,`a%4`)** — молчание. Они же **негативный
  контроль T11**: мутация «предупреждать на любое `/`» валит T2/T4/T6.
- **T5 (`a*b`)** — молчание (замер: DSP).
- **T7 (доставка)** — до фичи у цели `sv` канала не было (греп); заведён `report`
  по образцу `rust`/`st`. Перехват stderr **не понадобился**: приёмник
  (`Fsm::warnings`) инспектируется напрямую в юнит-тесте через `print_with_warnings`.
- **T8 (текст)** — называет причину и следствие без конкретных LUT.
- **T9/T10 (корпус, трансляция)** — деления в `examples/` нет → вывод байт-в-байт
  прежний; детерминизм-проверка `precheck.sh` (два прогона × все цели → `diff -r`)
  зелёный.

### Отклонения и известные ограничения

- **Дефект канала доставки** (`eprintln!` из библиотеки, `--quiet` не глушит) —
  воспроизведён образец `rust`/`st` **сознательно** (action A-6 ADR); чинить в
  0064 нельзя, заведён кандидатом.
- **Константа по потоку данных** (`b := 3; a / b`) предупредит — консервативно
  и осознанно (action A-2): лишнее предупреждение честнее пропущенного.
- **`q(m,n)` (0061)** делит через `fixed_bin` — предупреждение эмитится **до** него
  (общая ария `Divide`), поэтому action A-3 закрыта на будущее: делитель тот же.

### Что изменилось в коде

- `generator/sv/sv_expr.rs`: `sv009()`, `is_constant_divisor`, `warn_variable_divisor`,
  поле `Scope::warnings`, арии `Divide`/`Modulo` зовут проверку; 8 юнит-тестов.
- `generator/sv/sv_fsm.rs`: поле `Fsm::warnings: RefCell<Vec<Diagnostic>>`,
  проброс в `Scope` (`Fsm::scope`, `emit_functions`).
- `generator/sv/mod.rs`: `report(&fsm.warnings.borrow())` в `generate_program`,
  функция `report`.

## Итог проработки (2026-07-17)

**Проба сделана — ровно та, которую предписала 0045-06. Она дважды перевернула
вывод.**

**Шаг 1 — обобщённый синтез** (`yosys synth -flatten`), ячеек: `u32` → `+` 240,
`*` 3030, `/` 3437, `%` 3525. Предварительный вывод: `*` почти так же дорог, как
`/` (разрыв ×1.13) → предупреждать только про `/` значит вводить в заблуждение.
**Вывод оказался неверным.**

**Шаг 2 — FPGA-поток** (`yosys synth_ice40 -dsp -flatten`, `u32`):

| Операция | `SB_LUT4` | DSP |
|---|---|---|
| `a + b` | 49 | 0 |
| `a * b` | **17** | **3** ← **дешевле сложения** |
| **`a / b`** | **558** | **0** ← ×11 от сложения |
| **`a % b`** | **591** | **0** |
| `a / 2` (константа) | **17** | 0 ← сдвиг |
| `a / 3` (константа) | **106** | 0 ← ≈ сложение |

**Шаг 1 моделировал не ту технологию:** обобщённое отображение на вентили не
знает про DSP. На FPGA у умножения аппаратная поддержка **есть**, у деления —
**нет**. Деление в RTL действительно особенное.

**Ответ на вопрос 0045-06: подсказка — но только для переменного делителя.**
В форме, которую подразумевал кандидат («предупреждать на всякое `/`»), это был бы
**шум**: сработало бы на `a / 2`, который **дешевле сложения**.

**Принято** ([ADR](0064-sv-divider-warning.md#архитектура-adr), Option B): `SV-009` —
**предупреждение** (не ошибка) на `/` и `%` **с переменным делителем**;
делитель-константа → молча; про `*` → молча (замер: DSP). Текст называет **порядок
и следствие**, а не LUT — они технологичны и устареют.

**Находка, вошедшая в объём: у цели `sv` канала предупреждений НЕТ вовсе.**
Греп по `generator/sv/`: ни `Diagnostic::warning`, ни `report` — ноль вхождений;
у `rust` (`mod.rs:199`) и `st` (`mod.rs:290`) он есть. Без канала `SV-009` была бы
**немой**, поэтому его заведение — часть фичи.

**Находка вне объёма (кандидат): сам канал дефектен.** `report` печатает
`eprintln!` **из библиотеки**, а не возвращает диагностики вызывающему
(`compile_to_sv` → `Result<(), Diagnostic>`): `--quiet` их **не глушит**, LSP
получит их в свой stderr, тест может проверить только перехватом stderr.

Язык не меняется → версия не растёт.

## Итог (что сделано) — 2026-07-20

Принята **Option B** (ADR): `SV-009` — **предупреждение** на `/` и `%` **с
переменным делителем**; делитель-константа (числовой литерал, скобки снимаются) —
**молча**; про `*` — молча (замер: DSP). Реализовано в `generator/sv/`:

- **Обнаружение** — в `sv_expr.rs::print_expression`, единственной точке
  трансляции всех выражений (тела, условия, функции): арии `Divide`/`Modulo`
  зовут `warn_variable_divisor`, а тот через `is_constant_divisor` (синтаксическая,
  консервативная проверка — action A-2) дописывает `sv009()` в приёмник. Q-путь
  (`fixed_bin`) не мешает — предупреждение эмитится до него (action A-3 закрыта
  на будущее: делитель тот же).
- **Приёмник** — `Fsm::warnings: RefCell<Vec<Diagnostic>>`, доступный печатнику
  через новое поле `Scope::warnings` (интерьерная мутабельность: `&mut` сквозь 32
  места вызова печатника не протаскивается).
- **Канал доставки** — новая `report` в `generator/sv/mod.rs` по образцу
  `rust`/`st` (action A-5: у цели `sv` канала **не было вовсе**). Образец
  воспроизведён **вместе с дефектом** (action A-6, вне объёма — кандидат):
  `eprintln!` из библиотеки, `--quiet` не глушит.
- **Текст**: называет причину (нет аппаратного делителя) и следствие
  (потолок частоты), **без** конкретных LUT (они технологичны — action A-1).

**Проверки** — юнит-тесты в `sv_expr.rs` (перехват stderr не понадобился: приёмник
инспектируется напрямую через `print_with_warnings`): T1/T3 (переменный `/`,`%` →
`SV-009`), T2/T4/T6 (`a/2`, `a/3`, `a%4` → молчание — они же **негативный контроль
T11**: мутация «предупреждать на любое `/`» их валит), T5 (`a*b` → молчание), T8
(текст: причина+следствие без LUT). T9/T10: деления в корпусе нет (греп) → вывод
всех целей побайтово прежний, `precheck.sh` зелёный.

Язык не менялся, версия не поднята.
