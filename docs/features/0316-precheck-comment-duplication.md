# Фича: Комментарий шага не пересказывает заголовок проверки

- **Номер:** 0316
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено на стадии анализа 2026-08-20)
- **Связанные issue (анализ):** процессный бэклог `FEATURES.md` — «Комментарий
  шага `precheck.sh` пересказывает правило своего проверки»

## Стадии жизненного цикла

Все стадии — **разделы этой карточки**: «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог».
Отдельным артефактом остаются только исправления —
[`docs/fixes/`](../fixes/README.md) (при необходимости `0316-YY-*`).

## Краткое описание

Комментарий шага `precheck.sh` и заголовок его проверки повторяли друг друга
дословно — два носителя одного знания, расходящиеся молча. Четыре найденных
пересказа переписаны, новый ловит проверка.

> Фича зарегистрирована из процессного бэклога `FEATURES.md`.

## Документирование

**Не требуется:** правится процесс, а не язык.

## Архитектура (ADR)

- **Статус:** Accepted
- **Дата:** 2026-08-20
- **Фича:** [../features/0316-precheck-comment-duplication.md](0316-precheck-comment-duplication.md)

### Контекст

У шага `precheck.sh` и у скрипта проверки разные роли:

- заголовок скрипта объясняет **правило**: что проверяется, зачем, каким
  замером повод измерен;
- комментарий шага объясняет **шаг**: почему он здесь и почему в этом месте
  (например «после сборки — проверке нужны бинарники»).

Когда комментарий пересказывает заголовок, знание раздваивается. Замер фичи: сняв исключение в `check-legacy-names.sh`, пришлось править **два**
комментария `precheck.sh`, повторявших это исключение своими словами, — а
разошлись бы они молча.

#### Замер (2026-08-20)

Все шаги `precheck.sh`, у которых есть комментарий перед вызовом скрипта:

| Величина | Значение |
|---|---|
| шагов с комментарием | 34 |
| из них с **дословным** совпадением ≥60 символов | **4** |
| длина совпадений | 64…85 символов |

Пересказывались: урок (у шага версии языка), повод фичи (у шага
пробы), находка трёх отставших снимков (у шага `book/`), замер команд README.

Запись бэклога называла замер узким («посчитано на одном проверке, у скольких
шагов та же связка — не мерялось») — теперь посчитано.

### Решение

1. Четыре комментария переписаны: остаётся объяснение **шага** и ссылка на
   заголовок проверки.
2. Проверка `scripts/check-precheck-comments.py` (шаг предкоммита, с
   самопроверкой): ищет общий **дословный** кусок длиной от 60 символов между
   комментарием шага и заголовком его скрипта.

### Почему порог, а не запрет совпадений

Короткие совпадения законны и полезны: комментарий обязан называть предмет
(«фича», «проверка снимков»). Порог 60 символов отделяет **пересказ абзаца**
от совпадения термина; проверен на всех 34 шагах — после правки находок ноль,
и ни одно законное упоминание находкой не стало.

### Отвергнутые варианты

1. **Изымать комментарии шагов.** Они объясняют порядок и место шага — знание,
   которого в проверке нет по существу.
2. **Сверять номера фич.** Ловит не пересказ, а его отсутствие: комментарий
   может дословно повторять правило, не называя фичи.
3. **Требовать ссылки формально** («в комментарии обязана быть строка
   `scripts/…`»). Ссылку можно поставить и оставить пересказ рядом.

### Последствия

- Правило проверки живёт в одном месте; шаг объясняет своё.
- Новый пересказ невозможен: проверка падает списком мест.

## Анализ

### Цель и контекст

Комментарий шага и заголовок проверки повторяли друг друга дословно — два носителя
одного знания, расходящиеся молча.

### Зависимости фичи

- **Зависит от:** нет.
- **Влияние на порядок разработки:** закрывает последнюю содержательную запись
  процессного бэклога о `precheck.sh`.

### Требования и проверяемые условия

- **R1.** Четыре найденных пересказа переписаны.
- **R2.** Проверка ловит дословное совпадение ≥60 символов.
- **R3.** Объяснение шага находкой не становится.
- **R4.** Короткое совпадение (имя фичи, термин) законно.
- **R5.** Вырожденный вход (нет шагов с комментарием) — отказ.
- **R6.** Самопроверка идёт первой; предкоммит зелёный.

### Разбор

#### Почему порог именно дословный

Смысловое сходство машине недоступно, а дословный кусок — признак
копирования. Проверено: после правки на 34 шагах находок ноль, при том что
термины и номера фич повторяются повсеместно.

#### Что осталось в комментариях

Место шага в порядке (`после сборки — нужны бинарники`), связь с соседним шагом
(«контроль идёт первым») и предмет одним словом. Всё, что объясняет **правило**,
живёт в заголовке скрипта.

### Риски

- **Ложная тревога** на длинной цитате в комментарии. Ответ: цитата и есть
  пересказ; если она нужна, её место — в заголовке проверки.

## Разработка

### Задача

#### Состав правки

| Файл | Что сделано |
|---|---|
| `scripts/check-precheck-comments.py` | проверка с самопроверкой: общий дословный кусок ≥60 символов между комментарием шага и заголовком его скрипта |
| `scripts/precheck.sh` | четыре комментария переписаны; добавлен шаг проверки |

#### Переписанные шаги

| Шаг | Что пересказывалось |
|---|---|
| `test-language-version.sh` | урок («проверка, который никогда не падал…») |
| `test-probe.sh` | повод фичи и обещание контроля |
| `check-book-generated.sh` | находка трёх отставших снимков |
| `check-readme-commands.sh` | замер команд README |

#### Проверено

- `python3 scripts/check-precheck-comments.py --self-test` — ловушка взведена.
- `python3 scripts/check-precheck-comments.py` — 35 шагов, пересказов нет.
- `./scripts/precheck.sh` — код 0.

## Тест-план

### Условия

| # | Условие | Проверка |
|---|---|---|
| П1 | Дословный пересказ ловится | `--self-test` (1) |
| П2 | Объяснение шага находкой не становится | там же (2) |
| П3 | Короткое совпадение законно | там же (3) |
| П4 | Пустой комментарий находки не даёт | там же (4) |
| П5 | Отсутствие шагов — вырожденная проверка | код проверки (`sys.exit`) |
| П6 | Рабочий `precheck.sh` зелёный | прогон проверки |
| П7 | Предкоммит | `./scripts/precheck.sh` |

### Почему тесты именно такие

- **П2 и П3 — половина смысла:** проверка, ловящий любое совпадение, запретил бы
  называть предмет шага и был бы снят при первом же неудобстве.
- **П5:** проверка, которому нечего проверять, обязан падать (класс исправления).

## Отчёт о тестировании

### Окружение

- macOS (darwin 25.5.0), Python 3.14. Дата: 2026-08-20.

### Замер

| Величина | До | После |
|---|---|---|
| шагов с комментарием | 34 | 35 (добавлен шаг проверки) |
| дословных совпадений ≥60 символов | **4** | **0** |
| длина совпадений | 64…85 символов | — |

### Сводка прогонов

| Прогон | Результат |
|---|---|
| `check-precheck-comments.py --self-test` | ловушка взведена |
| `check-precheck-comments.py` | 35 шагов, пересказов нет |
| `./scripts/precheck.sh` | код 0 |

### Сверка с тест-планом

| # | Условие | Итог |
|---|---|---|
| П1…П4 | Пересказ, объяснение шага, короткое совпадение, пустой комментарий | ✅ |
| П5…П7 | Вырожденный вход, рабочий файл, предкоммит | ✅ |

### Что показал прогон

1. **Замер оказался мельче ожидаемого:** запись бэклога подозревала связку у
   многих шагов, а нашлись четыре. Это довод **за** проверка: класс редкий и
   именно поэтому не замечается глазами.
2. **Порог пришлось проверять на живом файле.** Термины и номера фич
   повторяются повсеместно; 60 символов — та граница, при которой законные
   упоминания не дают находок, а пересказ абзаца даёт.
3. **Самопроверка ловит обе стороны:** не только «пересказ пойман», но и
   «объяснение шага не пойман» — проверка, ловящий любое совпадение, был бы снят
   при первом неудобстве.

### Дефекты

Дефектов инструментов не найдено; предмет — комментарии.

### Редакторский слой

**Не требуется** — подтверждено: код не менялся.

### Вердикт

**Готово.** Пересказов нет, и новый ловится машиной.

## Итог (что сделано)

**Правило живёт в заголовке проверки, комментарий шага объясняет шаг.**

- **Замер-20** (запись бэклога называла его узким — теперь посчитано):
  34 шага с комментарием, у **четырёх** дословное совпадение с заголовком
  проверки длиной 64…85 символов. Пересказывались урок, повод фичи,
  находка трёх отставших снимков и замер команд README.
- **Проверка `check-precheck-comments.py`** (шаг предкоммита, с самопроверкой):
  общий **дословный** кусок ≥60 символов — находка, проверка падает списком мест.
- **Порог, а не запрет совпадений:** комментарий обязан называть предмет
  («фича», «проверка снимков»). 60 символов отделяют пересказ абзаца от
  совпадения термина; проверено на всех 34 шагах — законные упоминания
  находкой не стали.
- **Что осталось в комментариях:** место шага в порядке («после сборки —
  нужны бинарники»), связь с соседним шагом, предмет одним словом. Всё, что
  объясняет **правило**, — в заголовке скрипта.
- **Сверка номеров фич отвергнута:** она ловит не пересказ, а его
  отсутствие — комментарий может повторять правило, не называя фичи.

Детали — в [отчёте](0316-precheck-comment-duplication.md#отчёт-о-тестировании);
отвергнутые варианты — в [ADR](0316-precheck-comment-duplication.md#архитектура-adr).
