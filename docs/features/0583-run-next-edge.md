# Фича: прогон подсвечивает стрелку, которая сработает

- **Номер:** 0583
- **Статус:** ГОТОВО
- **Зависит от:** [графическое представление модели](0539-model-layout-ui.md) (закрыта)
- **Tier:** 2
- **Класс:** прочее
- **Связанные issue (анализ):** по требованию заказчика, 2026-09-10

## Стадии жизненного цикла

Все стадии — **разделы этой карточки**: «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог». Раздел
заводится в момент прохождения стадии, а не заранее. Отдельным артефактом
остаются только исправления — [`docs/fixes/`](../fixes/README.md) (при
необходимости `0583-YY-*`).

## Краткое описание

На прогоне подсвечено ребро, чьё условие выполнится на следующем такте: под ним
жёлтый ореол, линия и наконечник — чернилами той же пары. Прежде горели только
текущее состояние и состояние, куда уйдёт автомат. Вариант В выбран заказчиком
по макету из пяти.

> Фича зарегистрирована по требованию заказчика 2026-09-10; далее проходит
> жизненный цикл по своду.

## Документирование

Документ [`book/`](../../book/) — не требуется: язык не меняется. Книга
оформления — «Ребро».

## Архитектура (ADR)

- **Status:** Accepted
- **Date:** 2026-09-10
- **Authors:** заказчик (требование, выбор варианта), разработка

### Context

Модуль отдаёт на каждом такте ожидаемые переходы парами «из, в» — по ним уже
помечалось состояние-цель. Ребро листа несёт те же имена концов.

### Decision

- **Ребро узнаётся по паре «из, в»**, если его начало сейчас активно; правило —
  чистая функция `nextEdgeKeys`.
- **Вид переключается без перестроения листа** — классом `next` у группы ребра
  и наконечником своего набора (`arrow-*-next`), тем же путём, каким
  переключается выбор. Выбор сильнее прогона.

### Consequences

Два ребра с одной парой концов (разные условия между теми же состояниями)
подсветятся оба: пара модуля не называет условия.

## Разработка

`scheme.js` (`nextEdgeKeys`, `markerOf`, `setRunning`), `app.css`, книга
оформления.

## Тест-план

1. `nextEdgeKeys`: из нескольких выходящих — нужное, неактивное начало, нет
   перехода.
2. Прогон страницы: на такте подсвечено ровно ребро ожидаемого перехода.

## Отчёт о тестировании

Тест «прогон: подсвечена стрелка, которая сработает на следующем такте». Прогон
страницы — в разделе «Итог».

## Итог

ГОТОВО.
