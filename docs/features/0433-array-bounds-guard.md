# Фича: Проверка границ массива: статическая диагностика и guard в порождённом коде по флагу

- **Номер:** 0433
- **Статус:** ГОТОВО
- **Зависит от:** 0028 (`SE-028`), 0358 (носитель типа базы), 0434 (константный индекс), 0187 (порты)
- **Tier:** 1 — инструменты противоречат друг другу молча: один вход даёт **пять** разных поведений, и ни один инструмент не жалуется
- **Класс:** генерация
- **Связанные issue (анализ):** запрос заказчика 2026-08-23

## Стадии жизненного цикла (правило 17)

Все стадии — **разделы этой карточки** (правило 32).

## Краткое описание

**Запрос заказчика 2026-08-23:** «Мы в процессе компиляции (трансляции) с языка
Takt знаем размер массивов и поэтому статически можем проверить выход за
границу массивов, выставив guard в транслируемый код — это всё по флагу».

Размер массива известен компилятору всегда (он часть типа), а индекс бывает
переменным — и тогда каждый потребитель ведёт себя по-своему, вплоть до
неопределённого поведения в прошивке.

## Замер (правило 30, 2026-08-23)

Вход: `var data: [u8; 3] := {1, 2, 3};` и `probe := data[i];`, где `i` растёт
на единицу каждый такт (то есть с третьего такта индекс выходит за границу).

| Потребитель | Трасса `probe` по тактам | Что происходит |
|---|---|---|
| эталон | `2 3` → **`SIM-010`** | останов с диагностикой |
| **`c`, `c-hal`** | `2 3 3 1 0` | **чтение за границей массива** — неопределённое поведение; `cc -Werror` молчит |
| **`rust`** | `2 3` → **паника** | `index out of bounds: the len is 3 but the index is 3` |
| **`st`, `st-at`** | `2 3 0 4 0` | чтение по правилам MatIEC |
| **`sv`, `sv-mmio`** | `2 3 0 1 2` | индекс **усечён** по ширине (0365) — циклический доступ |

**Литеральный** индекс вне границ уже отвергает семантика (`SE-028`):
`data[7]` при `[u8; 3]` — ошибка компиляции.

### Второй предмет: Константа в позиции индекса и разряда (уточнение заказчика 2026-08-23)

> «То же самое можно сделать и к обращению к конкретному биту. Если есть
> константа (не переменная) и можно в семантическом анализе проверить
> статически выход за границу массива (границу области памяти) — выдавать
> ошибку».

Замер-23 показал, что проверка обрывается на **литерале**: значение
константы компилятору известно (её инициализатор свёрнут на стадии 2), а проверка на него не смотрит.

| Запись | Ожидание | Что сейчас |
|---|---|---|
| `d[7]` при `[u8; 3]` | ошибка | **`SE-028`** ✅ |
| `d[K]` при `const K: u8 := 5` | ошибка | **принимается молча**; эталон отвечает `SIM-010` в такте, а прошивка `c` читает за границей ❌ |
| `x.70` при `x: u8` | ошибка | **`SE-125`** ✅ |
| `s.f.70` при поле `u8` | ошибка | **`SE-125`** ✅ |
| `#0x100.70` (ячейка) | ошибка | **`SE-098`** ✅ |
| `x.B` при `const B: u8 := 70` | ошибка | **принимается молча** ❌ |
| `x.b` при `var b: u8 := 70` | время выполнения | `SIM-011` в такте — переменная, статически недоказуемо |

То есть **две** проверки (`SE-028` для индекса, `SE-125`/`SE-098` для разряда)
обрывают разбор на литерале, хотя значение константы известно тем же
вычислителем, что сворачивает инициализаторы. Уточнение заказчика сводится к
одному правилу: **проверка смотрит на значение, а не на форму записи**.

Ни один инструмент цели не возражает: `cc -Wall -Wextra -Werror`,
`iec2c`, `rustc`, `verilator`, `yosys` вывод принимают, `taktc` возвращает
ноль.

## Архитектура (ADR)

- **Status:** Accepted
- **Date:** 2026-08-23
- **Authors:** архитектор, разработчик, **заказчик** (оба решения)

### Decision (решения заказчика 2026-08-23)

1. **Поведение guard:** «сообщить наружу через HAL/порт». Отсюда форма:
   доступ за границей **не выполняется**, а признак уходит в синтетический
   выходной порт `bounds_fault` типа `bit`.
2. **Умолчание флага:** guard **выключен**. Вывод корпуса не меняется, цена
   прошивки не растёт; включает сборка (`taktc --bounds-check`, у эталона
   `takt-sim --bounds-check`).

### Форма

Проход `semantic::bounds_guard` оборачивает **оператор**, содержащий
индексацию переменным индексом:

```text
p := d[i];
⇓
if i < 3 { p := d[i]; } else { bounds_fault := 1; }
```

**Порт — не выдумка ради фичи, а существующий механизм:** у цели `c` он
становится колбэком записи (`write_bit`), у `rust` — методом HAL, у `st` —
выходом FB, у `sv` — выходом модуля. Печатники целей **не трогаются вовсе** —
приём тот же, что у разворота среза (0400) и подъёма результата вызова
(0431/0432).

**Оборачивается оператор, а не выражение:** условие строится из размера
массива, известного у базы, и одного `if` хватает на все доступы оператора.

**Нижняя граница проверяется только у знакового индекса:** у беззнакового
`i >= 0` истинно всегда, и линт цели ответил бы предупреждением, а проверки
считают его ошибкой.

**Эталон получает тот же проход** (`takt-sim --bounds-check`): иначе
включённый guard развёл бы эталон с целями — он останавливал бы прогон
(`SIM-010`), а прошивка продолжала.

### Consequences

- При включённом флаге все пятеро дают одну трассу.
- Без флага вывод и поведение прежние — на это стоят две проверки.

#### Acceptance criteria

Трассы эталона, `c`, `rust`, `st` и `sv` совпадают при включённом guard;
без флага порт не появляется, а эталон отвечает `SIM-010`.

## Что решено заказчиком (было: предстоит решить)

1. **Поведение guard при нарушении.** Варианты: пропустить операцию (чтение
   даёт нуль, запись не выполняется), прижать индекс к границе, остановить
   автомат (перевести в терминальное состояние), сообщить наружу через HAL.
   Выбор — решение заказчика: это наблюдаемое поведение прошивки, а не
   деталь печати.
2. **Флаг и его умолчание.** Guard стоит тактов и вентилей, поэтому включение
   по умолчанию изменило бы вывод всего корпуса и цену прошивки; выключение по
   умолчанию оставляет опасное поведение штатным. Имя флага и умолчание —
   часть решения.
3. **Область.** Все ли цели (`c`, `c-hal`, `rust`, `st`, `st-at`, `sv`,
   `sv-mmio`), и что делает эталон: у него guard **всегда включён**
   (`SIM-010`), и это уже расхождение с флагом «выключено».
4. **Статическая часть.** Индекс, чей диапазон доказуем (переменная цикла
   `for` с литеральными границами, 0321; индекс, ограниченный сравнением на
   ребре), guard не требует — вопрос, доказывать ли это в первой редакции или
   оставить «guard на каждый переменный индекс».
5. **Константа против литерала.** Правило «значение известно ⇒ проверяем»
   должно действовать в обеих проверках (`SE-028` и `SE-125`/`SE-098`), а
   вычислитель для этого уже есть (`const_eval`, 0192/0208). Вопрос — какой
   диагностикой отвечать: прежними кодами (текст назовёт значение) или новыми.
6. **Запись против чтения.** Запись за границей опаснее: у цели `c` это порча
   соседнего поля структуры. Возможно, для записи guard обязателен даже при
   выключенном флаге.

## Документирование (правило 24)

**Требует решения на стадии анализа.** Поведение при выходе за границу —
наблюдаемое свойство языка: если guard меняет исполнение (пропуск, прижатие,
останов), это описывается в [`book/`](../../book/) наравне с семантикой
переполнения (0127). Флаг компилятора в документ языка не попадает (правило
24), но его следствие — попадает.

## Анализ

### Цель и контекст

Свести пятерых к одному поведению на доступе за границей — по флагу.

### Замер (правило 30, 2026-08-23)

Таблицы — выше. Поведение снято прогоном прошивок.

### Зависимости фичи — закрыты.

### Требования и проверяемые условия

- При включённом guard пятеро дают одну трассу.
- Без флага вывод корпуса и поведение прежние.
- Вывод с guard принимают инструменты целей.

### Критерии приёмки и способ проверки

Потактовая сверка эталона с четырьмя прошивками плюс две проверки умолчания.

### Редакторский слой (правило 29)

**Не затронут:** язык не менялся — добавился флаг сборки и синтетический порт,
которого в исходнике нет.

### Особенности по обратной функциональности

Вывод корпуса не изменился: флаг выключен, проход не зовётся.

### Риски и зависимости

Синтетический порт **без адреса**: в режимах `c-hal`/`st-at`/`sv-mmio`, где
адрес обязателен, включённый guard даст диагностику слоя адресов — названная
граница, проверять её отдельной пробой при взятии этих режимов в работу.

## Разработка

### Задача

#### Что было

Переменный индекс не проверялся никем: пять потребителей — пять поведений.

#### Что сделано

- `semantic/bounds_guard.rs` — проход (обход тел, условие, синтетический порт).
- `generator/mod.rs` — поле `GenerateOptions::bounds_check`.
- `compile_cli/mod.rs`, `bin/taktc.rs` — флаг `--bounds-check` и справка.
- `pipeline.rs` — вызов прохода по флагу (конвейер принимает опции целиком).
- `takt-sim/src/bin/takt_sim.rs` — флаг `--bounds-check` у эталона.
- `book/src/05-expressions` — правило описано словами (правило 24).

#### Проверки

Пробы всеми целями и их инструментами, семь тестов набора, предкоммит.

## Тест-план

### Область и цель

Доказать, что guard согласует пятерых и что умолчание ничего не меняет.

### Проверки (условие → ожидаемый результат)

| № | Условие | Ожидание |
|---|---|---|
| T1 | Трасса эталона с guard | `(2,0) (4,0) (6,0) (6,1) (6,1)` |
| T2 | Прошивка `c` | совпадает |
| T3 | Прошивка `rust` | совпадает |
| T4 | ПЛК `st` | совпадает |
| T5 | RTL `sv` | совпадает |
| T6 | Эталон без флага | `SIM-010`, прогон обрывается |
| T7 | Вывод `c` без флага | порта `bounds_fault` нет |
| T8 | `./scripts/precheck.sh` | код 0 |

### Разбивка проверок по функциональности

- Поведение с guard: T1–T5.
- Умолчание: T6, T7.
- Обратная совместимость: T8.

### Тестовые данные и окружение

`takt-sim/tests/conformance/conformance_bounds_guard_tests.rs`, фикстура
`takt-sim/tests/data/eval/conformance_bounds_guard.takt`.

## Отчёт о тестировании (правило 20)

### Резюме

Все проверки пройдены. Вердикт: **ГОДНО**.

### Фактические результаты по проверкам

| № | Результат |
|---|---|
| T1–T5 | `2/0 3/0 3/1 3/1 3/1` на пробе и `(2,0) (4,0) (6,0) (6,1) (6,1)` на фикстуре — у всех пятерых |
| T6, T7 | зелёные |
| T8 | код 0 |

### Результаты по функциональности

Инструменты целей вывод с guard приняли: `cc -Werror`, `rustc`, `iec2c` + `cc`,
`verilator --lint-only -Wall`, `yosys synth`.

### Найденные дефекты

**Порт-признак меняет интерфейс:** у цели `rust` в трейте `Hal` появляется
`write_bit`, и драйвер, его не реализующий, не собирается (`E0046`). Это
следствие выбранной формы, а не дефект — но оно означает, что включение флага
ломает существующие реализации HAL. Названо в документе.

**Предел размера модуля** заставил переписать вызов прохода: шесть вставок в
`lib.rs` заменены одним параметром конвейера (`parse_and_construct` принимает
опции целиком).

### Выводы и дальнейшие шаги

Статическая часть запроса заказчика (константа в позиции индекса) закрыта
отдельной фичей [константа в позиции индекса и разряда проверяется статически](0434-const-index-bounds.md).

## Итог (что сделано)

Доступ за границей массива по переменному индексу давал **пять** разных
поведений, и ни один инструмент не возражал. Теперь по флагу `--bounds-check`
все пятеро ведут себя одинаково: доступ не выполняется, признак уходит в
выходной порт `bounds_fault`.

**Форма и умолчание — решения заказчика** (2026-08-23): «сообщить наружу
через HAL/порт», флаг выключен по умолчанию.

**Печатники целей не трогались вовсе:** guard живёт в семантике, а признак —
обычный выходной порт, который каждая цель печатает своим штатным способом.

**Эталон получает тот же проход** своим флагом — иначе включённый guard
развёл бы его с целями.
