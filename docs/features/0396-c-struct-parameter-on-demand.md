# Фича: Структурный параметр цели c эмитится всегда, а не по нужде

- **Номер:** 0396
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено на стадии анализа 2026-08-22)
- **Tier:** 4 — расставлен по признаку вреда (шкала — в блоке 1 `FEATURES.md`)
- **Класс:** генерация
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-22

## Стадии жизненного цикла (правило 17)

Все стадии — **разделы этой карточки** (правило 32): «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог».
Отдельным артефактом остаются только исправления —
[`docs/fixes/`](../fixes/README.md) (при необходимости `0396-YY-*`).

## Краткое описание

- **Структурный параметр цели `c` эмитится всегда, а не по нужде.** Замер
  [цель c эмитит структурный параметр даже там, где он не нужен](0260-c-unused-struct-parameter.md) (2026-08-19): фича
  сняла предупреждения заглушкой `(void)параметр;`, но параметр в сигнатуре
  остался. Точный интерфейс дал бы предикат «нужен ли параметр» по телу —
  **транзитивный вверх** (родитель обязан иметь `main`, если он нужен потомку),
  как `needs_hal` у цели `rust`. Цена: двойной проход генерации либо знание обо
  всех каналах (переменные корня, порты, состояние соседа, время, `userdata`),
  согласование пяти модулей эмиссии и сигнатура, зависящая от тела (правка одной
  строки модели меняет сигнатуры всей цепочки предков). Отвергнуто **по
  цене, а не по существу**: пока заглушка держит оба проверки зелёными, класс
  косметический.

> Фича зарегистрирована из блока кандидатов `FEATURES.md` **без проработки**
> (решение заказчика 2026-08-22: «завести фичи из кандидатов без проработки»).
> Текст кандидата перенесён **дословно**, вместе с замером и его датой: замер
> есть свидетельство, а пересказ его теряет. Стадии 2 и 3 не пройдены —
> зависимости и объём **не оценивались**; далее фича проходит жизненный цикл по
> правилу 17.

## Документирование (правило 24)

**Не требуется.** Предмет — форма вывода цели `c` (внутренние сигнатуры
порождённых `static`-функций); язык, диагностики и наблюдаемое поведение не
меняются.

<!-- При закрытии фичи (стадия 8, статус ГОТОВО) сюда добавляется раздел
     «## Итог (что сделано)» —
     ссылки на отчёт/исправления (правило 21). Незакрытые фичи «Итога» не имеют. -->

## Архитектура (ADR)

- **Status:** Proposed — исполнителю; **брать по решению заказчика** (класс косметический)
- **Date:** 2026-08-22
- **Authors:** Архитектор + Аналитик
- **Related issues:** [Фича](0396-c-struct-parameter-on-demand.md);
  замер и заглушка — [цель c эмитит структурный параметр даже там, где он не нужен](0260-c-unused-struct-parameter.md#архитектура-adr); образец предиката —
  `needs_hal` цели `rust` ([генератор генерации в Rust](0050-rust-backend.md#архитектура-adr))

### Context

Протокол вызова цели `c` единообразен: под-модель получает `main`,
пользовательская функция — `model`, помощник HAL — `m`. Тело пользуется ими не
всегда; фича погасила предупреждения заглушкой `(void)параметр;`, но
параметр в сигнатуре остался.

Замер **переснят 2026-08-22** по корпусу: в `examples/generated/c/` **53**
заглушки — то есть 53 места, где параметр передаётся и не используется.

Оба проверки цели (`cmake` корпуса и `cc -c` для `c-hal`) зелены: заглушка
делает вывод валидным. Класс **косметический** — цена в интерфейсе и в
читаемости порождённого кода, а не в валидности.

### Decision Drivers

1. **Точный интерфейс лучше, но не бесплатен:** предикат «нужен ли параметр»
   **транзитивен вверх** — родитель обязан иметь `main`, если он нужен потомку.
2. **Сигнатура начинает зависеть от тела:** правка одной строки модели меняет
   сигнатуры всей цепочки предков — диффы порождённого кода станут шире.
3. **Знание о каналах:** «нужен ли `main`» складывается из переменных корня,
   портов, состояния соседа (0267), времени и `userdata` — пять источников,
   каждый со своим обходом.
4. **Образец есть:** `needs_hal` цели `rust` решает ровно такую задачу и
   работает; риск известен и ограничен.

### Considered Options

#### Option A. Оставить заглушки (статус-кво)

**Pros:** ноль работы; проверки зелены; вывод стабилен.
**Cons:** 53 лишних параметра и заглушки в корпусе; читателю прошивки видно
«параметр, который не нужен».

#### Option B. Предикат «нужен ли параметр», транзитивный вверх

По образцу `needs_hal`: обход тела + сумма по вызываемым.

**Pros:** точный интерфейс; заглушки исчезают вместе с параметрами.
**Cons:** двойной проход генерации (сигнатуру надо знать до печати тела, а
предикат считается по телу); согласование пяти модулей эмиссии; вывод корпуса
поедет целиком — снапшоты обновляются разом.

#### Option C. Печатать параметр, но помечать `__attribute__((unused))`

**Cons:** расширение GNU — цель `c` держится C11 (0284 отвергла расширение по
той же причине).

### Decision

**Принят Option B** (решение заказчика 2026-08-23 — брать фичу): класс
косметический, а цена (двойной проход, пять модулей, полный сдвиг снапшотов)
сопоставима с ценой языковых фич.

Если Option B принимается, предикат обязан быть **одним** носителем и
считаться так же, как `needs_hal` — иначе два правила «нужен ли параметр»
разъедутся.

### Consequences

#### Положительные (B)

- Сигнатуры порождённых функций отражают их зависимости; 53 заглушки исчезают.

#### Отрицательные / Action items

- Снапшоты `examples/generated/c` обновляются целиком — один большой дифф.
- Сигнатура зависит от тела: правка модели меняет интерфейс цепочки предков.
- Проверка детерминированности (0048) обязан остаться зелёным: предикат считается
  по `BTreeMap`-обходам, иначе порядок поедет.

#### Acceptance criteria

1. Параметр печатается только там, где тело (или вызываемые им) им пользуется.
2. Заглушек `(void)параметр;` в выводе не остаётся (кроме случаев, где параметр
   всё-таки нужен по одному из каналов, но не назван в тексте).
3. Оба проверки цели `c` зелены без исключений по флагам.
4. Потактовые сверки не изменились — поведение прежнее.
5. Снапшоты корпуса обновлены одним диффом; `precheck.sh` зелёный.

## Анализ

### Цель и контекст

Цель `c` передаёт структурный параметр (`main`, `model`, `m`) всегда, а гасит
неиспользование заглушкой (0260). Фича делает интерфейс точным.

### Замер (правило 30, переснят 2026-08-22)

- **53 заглушки** `(void)параметр;` в `examples/generated/c/` — столько мест
  принимают параметр и не пользуются им;
- оба проверки цели зелены: класс **косметический**, вывод валиден;
- образец решения — `needs_hal` цели `rust`, работающий с 0050.

### Зависимости фичи (правило 17/19)

- **Зависит от:** нет; опорная [цель c эмитит структурный параметр даже там, где он не нужен](0260-c-unused-struct-parameter.md)
  закрыта.
- **Влияние на порядок:** брать по решению заказчика — цена сопоставима с
  языковой фичей, польза косметическая.

### Требования и проверяемые условия

- **R1.** Параметр печатается только по нужде; предикат транзитивен вверх.
- **R2.** Предикат — **один** носитель (как `needs_hal`), а не копия в каждом
  модуле эмиссии.
- **R3.** Поведение не изменилось: все потактовые сверки зелены.
- **R4.** Оба проверки цели `c` зелены **без** исключений по флагам.
- **R5.** Детерминированность вывода сохранена.
- **R6.** Снапшоты корпуса обновлены осознанно, одним диффом.

### Критерии приёмки и способ проверки

| # | Критерий | Способ проверки |
|---|---|---|
| A1 | R1 | греп по выводу: параметр отсутствует там, где не используется |
| A2 | R2 | чтение + мутация (вторая копия предиката ломает вывод) |
| A3 | R3 | `takt-sim/tests/conformance/` целиком |
| A4 | R4 | проверки предкоммита |
| A5 | R5 | двойной прогон генерации + `diff -r`  |
| A6 | R6 | ревью диффа снапшотов |

### Объём работ

| Место | Что делается | Риск |
|---|---|---|
| предикат «нужен ли параметр» | новый носитель, транзитивный по вызовам | средний |
| пять модулей эмиссии цели `c` | сигнатура и вызов согласуются | **высокий**: разъезд даёт невалидный C |
| снапшоты | обновление корпуса целиком | низкий, но дифф широкий |

**Двойной проход:** сигнатуру надо знать до печати тела, а предикат
считается по телу. У цели `rust` эта задача решена (`function_needs`) — образец
есть.

### Редакторский слой (правило 29)

**Не применимо:** предмет — форма вывода одной цели.

### Особенности по обратной функциональности

Сигнатуры порождённых функций — **внутренний** интерфейс модуля цели `c`
(функции `static`), поэтому пользовательский код не задет. Исключение —
помощники HAL (`<Root>_bind_default_hal` и подобные): они видны пользователю,
и их сигнатуру трогать нельзя.

### Риски и зависимости

- **Разъезд сигнатуры и вызова** — даёт невалидный C; контроль обязан собирать
  вывод, а не только сравнивать текст.
- **Ширина диффа снапшотов** маскирует посторонние изменения: обновлять
  корпус надо отдельным коммитом.

## Разработка

### Задача: признак — общий носитель

`generator/c/c_needs.rs::needs_state` спрашивает **тот же**
`rust_needs::function_needs`, которым живёт цель `rust` (0050): у неё нужда
раскладывается на «нужен ли HAL» и «какие переменные передать», у `c` обе
сводятся к одному указателю. Своё знание здесь разошлось бы с тем, о котором предупреждает ADR.

Модуль `generator/rust/rust_needs` открыт до `pub(crate)`; вопрос
семантический, и второго обхода для него быть не должно.

### Задача: указатель по нужде

| Место | Правка |
|---|---|
| `c_decl.rs` | сигнатура и прототип пользовательской функции |
| `c_expr/call.rs` | первый аргумент вызова |
| `c_model.rs` | прототип, определение и `assert` функций модели; вызовы дочерних |
| `c_model_init.rs` | вызовы `_init` дочерних |
| `c_map.rs` | `root_arg(child, caller_is_main)` — один ответ на все места вызова |

**Пустой список параметров печатается `void`**: `f()` в C означает «список
неизвестен» (K&R), и `cc -Wstrict-prototypes` отвечает «a function declaration
without a prototype is deprecated». До фичи случай не возникал — указатель
стоял всегда.

**Порт требует `main` всегда**, даже объявленный в самой под-модели:
колбэки HAL (`write_numeric`, `userdata`) живут в структуре корня. Без этой
ветви порождённый C не компилировался — поймал контроль, а не свои тесты.

### Задача: контроля

`takt-lang/tests/targets/c_param_on_demand_tests.rs` — функция без обращения к
состоянию, контроль (функция с обращением), `void` в пустом списке с прогоном
`cc -Wstrict-prototypes`, под-модель без обращения к корню.

## Замер результата

| Что | До | После |
|---|---|---|
| заглушек `(void)model;` | 8 | **0** |
| заглушек `(void)main;` | 45 | **30** |
| файлов корпуса изменилось | — | 2 |

**Вывод корпуса не поехал целиком**, вопреки оценке ADR: параметр снят
только там, где он действительно не нужен, а таких мест в `examples/`
оказалось немного.

## Названная граница

**Признак считается на модель, а не на функцию.** Оставшиеся 30 заглушек —
это модели, которым `main` нужен (есть порт либо общая переменная), но
конкретная функция (`_init`, `_is_done`) им не пользуется. Точнее было бы
считать нужду отдельно для каждой из четырёх функций модели — сигнатуры тогда
разойдутся между ними, что законно. Вынесено кандидатом.

## Тест-план

| № | Условие | Ожидаемый результат |
|---|---|---|
| T1 | Функция без обращения к состоянию | параметра нет |
| T2 | Контроль: функция с обращением | параметр есть |
| T3 | Пустой список | печатается `void`, `-Wstrict-prototypes` молчит |
| T4 | Под-модель без обращения к корню | `main` нет, вызовы согласованы |
| T5 | Под-модель с портом | `main` есть (колбэки живут в корне) |
| T6 | Проверки цели (корпус, `c-hal`) | зелены |
| T7 | Предкоммит | зелёный |

## Отчёт о тестировании

**Окружение:** macOS (darwin 25.5.0), toolchain `1.97.1`, полный
`./scripts/precheck.sh`.

| № | Результат |
|---|---|
| T1 | `function_without_state_has_no_pointer` |
| T2 | `function_with_state_keeps_the_pointer` |
| T3 | `empty_parameter_list_is_void` (прогон `cc`) |
| T4 | `submodel_without_root_access_has_no_main` |
| T5 | контроль (`whole_file_import_compiles_in_c`) — он же и нашёл пропуск |
| T6 | |
| T7 | |

### Примеры и контрпримеры (правило 16)

- **Пример:** `fn constant() -> u8 { return 7; }` → `static uint8_t X_constant(void)`.
- **Контрпример 1:** функция, читающая переменную модели, — параметр на месте.
- **Контрпример 2:** под-модель с портом — `main` на месте, потому что колбэк
  живёт у корня.

### Дефекты

Один найден при разработке и исправлен: признак не учитывал порт, объявленный
в самой под-модели, — порождённый C не компилировался. Поймал **чужой** контроль
(0184).

### Вердикт

**Готово** — с названной границей (признак на модель, не на функцию).

## Итог (что сделано)

Указатель на состояние печатается по нужде: 23 заглушки из 53 исчезли вместе
с параметрами (все восемь `(void)model;` и пятнадцать `(void)main;`).
Признак — **общий носитель** с целью `rust`, как и требовала ADR.

**Оценка ADR о «полном сдвиге снапшотов» не подтвердилась**: изменились
два файла корпуса. Параметр снимается только там, где он не нужен, а таких
мест в `examples/` немного.

Попутно снят дефект, которого фича не касалась: пустой список параметров
печатался `()` — «список неизвестен» по стандарту C, и `-Wstrict-prototypes`
это отвергает. До фичи случай не возникал.

**Граница названа:** признак на модель, а не на функцию; оставшиеся 30
заглушек — там, где модели `main` нужен, а конкретной функции нет. Вынесено
кандидатом.

Детали — в разделах «Разработка», «Замер результата» и «Отчёт о
тестировании».

