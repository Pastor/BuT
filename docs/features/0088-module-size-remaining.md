# Фича: Остальные нарушители лимита размера модуля (17 файлов)

- **Номер:** 0088
- **Статус:** ГОТОВО (см. «Итог (что сделано)» ниже)
- **Зависит от:** не проставлено — проставит аналитик на стадии анализа (правило 17)
- **Приоритет / Tier:** **Tier 3** (качество кода/процесс; не багфикс) — проставлен ADR
- **Крейт:** `grammar`, `simulation`
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`)

## Стадии жизненного цикла (правило 17)

Все стадии — **разделы этой карточки** (правило 32): «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог».
Отдельным артефактом остаются только исправления —
[`docs/fixes/`](../fixes/README.md) (при необходимости `0088-YY-*`).

## Краткое описание

[разделение переросших модулей (validate.rs, lsp.rs, c_expr.rs)](0027-module-size-split.md) взяла в объём **три** худших модуля и
**заморозила** остальные храповиком `scripts/check-module-size.sh` (реестр
`scripts/module-size-baseline.txt` — **узаконенный долг**, а не разрешение).

Остаток:

- **крупнейший файл проекта — `grammar/tests/semantic_tests.rs`, 4766 строк**, на
  31% больше `validate.rs`, а правило распространяется на тесты **явным текстом**
  «вместе с тестами»;
- следом — `semantic/mod.rs` (1708);
- суммарное превышение по 15 модулям `src` — **6255 строк**.

Смежно с фичей [вынос вычислителя адреса из `address_map.rs` (лимит размера)](0069-address-map-eval-split.md) (новый нарушитель
`address_map.rs`, вне реестра).

Вынесено из [разделение переросших модулей (validate.rs, lsp.rs, c_expr.rs)](0027-module-size-split.md).

> Фича зарегистрирована **2026-07-17** переводом кандидата из `FEATURES.md`
> (решение заказчика: «завести фичи по кандидатам, пока без проработки»).
> **Проработка не проводилась:** ADR, анализ, зависимости, Tier и объём — за
> стадиями 2–3 (правило 17). Текст ниже — **перенос находки кандидата** вместе с
> подтверждающими её пробами; это описание проблемы, а **не** принятое решение.

## Архитектура (ADR)

- **Status:** Accepted
- **Date:** 2026-07-23
- **Authors:** Архитектор + Системный аналитик
- **Related issues:** [Фича](0088-module-size-remaining.md);
  продолжение [ADR](0027-module-size-split.md#архитектура-adr) (правило размера + храповик
  `check-module-size.sh`); образцы разделения — `semantic/validate/`, `lsp/`,
  `generator/c/c_expr/`, `semantic/index/`, `address_map/`.

### Context

[разделение переросших модулей (validate.rs, lsp.rs, c_expr.rs)](0027-module-size-split.md#архитектура-adr) взяла в объём **три** худших модуля и
**заморозила** остальные храповиком `scripts/check-module-size.sh` (реестр
`scripts/module-size-baseline.txt` — узаконенный долг). На сегодня в реестре
**18 файлов** (суммарное превышение — тысячи строк):

| Класс | Файлы (строк) |
|---|---|
| Тесты | `semantic_tests` 5015, `parser_tests` 1892, `codegen_tests` 1393, `lsp_tests` 1266, `lexer_tests` 1081 |
| Генераторы | `rust_model` 1345, `c_source` 1169, `rust_expr` 1112, `c_model` 1073 |
| Парсер | `ast` 1204, `lexer` 1154 |
| Симуляция | `unit/viewport` 1455 |
| **Ядро семантики** | `semantic/tree` 1842, `semantic/mod` 1747, `semantic/expression` 1360, `type_inference` 1191 |
| **Пришпиленные к baseline** | `bin/lamc` 1993, `lib` 1402 |

Классы **сильно различаются риском**. Тесты и генераторы делятся механически и
проверяются быстро (`cargo test` таргета, codegen-снапшоты, детерминизм-проверка).
**Ядро семантики** — сердце компилятора: `mod.rs`/`tree.rs`/`expression.rs`
несут перекрёстные зависимости AST-узлов и проходов; их дробление — крупная
перетряска с реальным риском тонкого регресса. **`lamc.rs`/`lib.rs`** прошлые
фичи (0043, 0075, 0085, 0062) **намеренно** держали большими как «тонкий
диспетчер / публичный API, пришпиленный к baseline» — их дробление противоречит
принятым решениям и требует их пересмотра.

Правило (свод `docs/CODE.md`, 0027): «дели по логике; реестр — долг, а не
разрешение». Оно не запрещает разбить всё, но объём и риск полного захода
несоразмерны Tier-3 характеру работы (качество, не багфикс).

### Decision Drivers

1. **Разный риск по классам.** Механический вынос тестов/генераторов ≠ перетряска
   ядра компилятора. Один объём для всех — либо слишком рискованно, либо слишком
   мало.
2. **Отсутствие регресса — обязательно.** Вынос — **чистое перемещение** кода;
   вывод генераторов байт-в-байт неизменен (детерминизм-проверка), поведение —
   тоже. `mod.rs`-реэкспорт держит пути импорта (правило 11).
3. **Соразмерность.** Быстрее всего сокращает реестр устранение записей у самой
   границы (файл −N строк уходит из реестра целиком).
4. **Пересмотр прежних решений — отдельно.** `lamc.rs`/`lib.rs` пришпилены
   осознанно; трогать их — значит перерешать 0043/0085 — это не механика.

### Considered Options

#### Option A. Вынести все 18 файлов за одну фичу

**Pros:** реестр обнуляется (или почти); правило соблюдено полностью.

**Cons:** максимальный риск (ядро) + пересмотр пришпиленных решений в одной
корзине с механикой; много прогонов `precheck.sh`; несоразмерно Tier 3; высокая
вероятность тонкого регресса в `semantic/`.

#### Option B. Безопасная часть сейчас, ядро и пришпиленные — отдельно (принята)

Объём 0088 — **12 чисто-делимых файлов**: тесты (5) + генераторы (4) + парсер
(2: `ast`, `lexer`) + симуляция (1: `viewport`). **Ядро семантики** (`tree`,
`mod`, `expression`, `type_inference`) и **пришпиленные** (`lamc`, `lib`) —
**вынесены из объёма** в отдельную фичу (высокий риск / пересмотр прежних ADR).

**Pros:** сокращает реестр **18 → 6** при низком риске; вся работа —
механическое перемещение под зелёным `precheck.sh`; ядро дробится осознанно и
отдельно, а не заодно.

**Cons:** реестр не обнуляется — остаётся 6 записей (заведены новой фичей).

#### Option C. Только быстрые победы (файлы у границы)

Вынести лишь `c_model`/`lexer_tests`/`rust_expr`/`ast`… минимальной правкой.

**Pros:** максимально дёшево и безопасно; быстро уменьшает **число** нарушителей.

**Cons:** оставляет крупные безопасно-делимые файлы (`semantic_tests` 5015 —
худший в проекте) нетронутыми; половинчато.

### Decision

Принимается **Option B** (решение заказчика): объём фичи — **безопасная
часть, 12 файлов**; ядро семантики и пришпиленные `lamc`/`lib` — **новая фича**
(заводится при закрытии 0088; см. Consequences).

Стратегия дробления по классам:

- **Тесты** (`tests/X.rs` — отдельный бинарник): переводятся в
  **директория-подмодуль** — `tests/X.rs` становится тонким файлом с `mod`-
  объявлениями, тела едут в `tests/X/<тема>.rs`. Один тест-бинарник сохраняется
  (быстрее N бинарников), каждый файл ≤ 1000. Границы — по теме проверок (Ce/SE,
  enum, address, struct, verify, …), а не по строкам.
- **Генераторы** (`rust_model`/`rust_expr`/`c_model`/`c_source`): по печатникам,
  образец — уже существующие `{c,rust,sv}_blocks.rs`; новые подмодули
  реэкспортируются, вывод байт-в-байт неизменен (детерминизм-проверка).
- **Парсер** (`ast`, `lexer`): по категориям узлов/токенов.
- **Симуляция** (`viewport`): по ответственности (геометрия/рендер/раскладка).

**Порядок разработки** — по возрастанию превышения (быстрее уходят записи из
реестра), но крупный `semantic_tests` (5015) берётся отдельной подзадачей ввиду
объёма. Каждый вынос: переместить → обновить `module-size-baseline.txt` (удалить
уложившиеся, уменьшить оставшиеся) → зелёный целевой тест → зелёный `precheck.sh`
→ коммит.

**Гарантия отсутствия регресса:** вынос — чистое перемещение; публичные пути
держит `mod.rs`-реэкспорт (правило 11); вывод генераторов сверяется детерминизм-
проверкой и codegen-снапшотами; поведение — существующими тестами. Ни один тест не
меняет **утверждений** — только адрес.

### Consequences

#### Положительные

- Реестр `module-size-baseline.txt` сокращается **18 → 6** без риска для ядра.
- Худший файл проекта (`semantic_tests` 5015) перестаёт быть монолитом.
- Устанавливается **воспроизводимый приём** дробления тестов (директория-
  подмодуль) для будущих фич.

#### Отрицательные / Action items

- **Реестр не обнуляется:** 6 записей (`semantic/{tree,mod,expression,type_inference}`,
  `lamc`, `lib`) остаются. При закрытии 0088 — **завести новую фичу** «Дробление
  ядра семантики и пересмотр пришпиленных `lamc`/`lib`» (внесёт их в свой объём;
  пересмотр 0043/0085 — её ADR).
- **Тест-бинарники:** директория-подмодуль сохраняет один бинарник на группу —
  время компиляции тестов не растёт кратно.
- **`viewport`** формально в `simulation`, не в ядре семантики — включён как
  безопасный (рендер, не язык); если дробление окажется нетривиальным, переносится
  в отложенную фичу (решение в анализе).

#### Acceptance criteria

1. Из реестра `module-size-baseline.txt` **удалены** все 12 файлов безопасной
   части (каждый ≤ 1000 либо разбит на подмодули ≤ 1000); остаются ровно 6
   отложенных записей.
2. `./scripts/precheck.sh` **зелёный** (в т.ч. `check-module-size.sh`,
   детерминизм-проверка, все тесты) на каждом коммите-выносе.
3. Вывод генераторов (`c`/`rust`/`st`/`sv`/`plantuml`) **байт-в-байт неизменен**;
   ни одно тест-утверждение не изменено (только расположение).
4. Пути импорта потребителей не изменились (`mod.rs`-реэкспорт).
5. При закрытии заведена фича-преемник на 6 отложенных файлов.

## Анализ

### Цель и контекст

Вынести из реестра `scripts/module-size-baseline.txt` **безопасную часть**
нарушителей лимита размера модуля (решение заказчика, [ADR](0088-module-size-remaining.md#архитектура-adr),
Option B): 12 чисто-делимых файлов. Ядро семантики и пришпиленные `lamc`/`lib` —
отдельная фича (заводится при закрытии). Вынос — **чистое перемещение**: вывод
генераторов байт-в-байт неизменен, поведение неизменно, пути импорта держит
`mod.rs`-реэкспорт.

### Зависимости фичи (правило 17/19)

- **Зависит от:** нет (продолжение 0027, но 0027 закрыта). Иных нет.
- **Влияние на порядок:** не разблокирует другие фичи. Порождает **фичу-преемник**
  (ядро семантики + пересмотр пришпиленных) — заводится при закрытии 0088.

### Декомпозиция на подзадачи (по классам, порядок — по риску/дешевизне)

| № | Подзадача | Файлы (строк) | Приём | Проверка |
|---|---|---|---|---|
| 0088-01 | Генератор C — init | `c_model` 1073 → 782 (+ `c_model_init` 309) | вынос init-группы `_init` | `codegen_tests` + детерминизм |
| 0088-02 | Генераторы (остальные) | `rust_model` 1345, `c_source` 1169, `rust_expr` 1112 | по печатникам (образец `*_blocks`) | codegen + детерминизм |
| 0088-03 | Парсер | `ast` 1204, `lexer` 1154 | по категориям узлов/токенов | `parser_tests`/`lexer_tests` |
| 0088-04 | Симуляция | `unit/viewport` 1455 | геометрия/рендер/раскладка | тесты simulation |
| 0088-05 | Тесты (мелкие) | `lexer_tests` 1081, `lsp_tests` 1266, `codegen_tests` 1393, `parser_tests` 1892 | директория-подмодуль по темам | `cargo test --test X` |
| 0088-06 | Тесты (крупнейший) | `semantic_tests` 5015 | директория-подмодуль по темам (Ce/SE/enum/address/struct/verify/…) | `cargo test --test semantic_tests` |

Каждая подзадача: переместить → обновить реестр → зелёный целевой тест → зелёный
`precheck.sh` → коммит. Ни одно тест-утверждение не меняется (только адрес).

### Требования и проверяемые условия

- **R1.** Каждый вынесенный файл ≤ 1000 строк (либо разбит на подмодули ≤ 1000);
  запись удалена из реестра.
- **R2.** Вывод генераторов (`c`/`rust`/`st`/`sv`/`plantuml`) **байт-в-байт**
  неизменен (детерминизм-проверка + codegen-снапшоты).
- **R3.** Все существующие тесты зелёные **без изменения утверждений**.
- **R4.** Пути импорта потребителей неизменны (`mod.rs`/файловый реэкспорт).
- **R5.** `precheck.sh` зелёный на каждом коммите-выносе (в т.ч.
  `check-module-size.sh`).

### Критерии приёмки и способ проверки

| # | Критерий | Способ |
|---|---|---|
| A1 | реестр 18 → 6 (удалены 12 безопасных) | `grep -c '^[0-9]' module-size-baseline.txt` |
| A2 | вывод генераторов неизменен | `codegen_tests` + детерминизм-проверка precheck |
| A3 | поведение неизменно | полный `cargo test` |
| A4 | precheck зелёный | `./scripts/precheck.sh` |
| A5 | заведена фича-преемник (6 файлов) | карточка новой фичи |

### Особенности по обратной функциональности

Полностью аддитивно (правило 11): перемещение кода, реэкспорт держит публичные
пути. Язык не меняется, версии крейтов — при необходимости патч (перемещение
приватных модулей публичный API не трогает).

### Риски и зависимости

- **Скрытая приватная зависимость** выносимой функции от остающейся — ловится
  компилятором (unused import / not found) сразу.
- **Изменение вывода** от случайной правки — ловит детерминизм-проверка + codegen.
- **Тест-бинарники:** директория-подмодуль сохраняет один бинарник на группу.
- **`viewport`:** если дробление окажется нетривиальным — переносится в
  отложенную фичу (решение по факту).

## Разработка

### Задача

#### Что было

`grammar/src/generator/c/c_model.rs` — **1073 строки** (нарушитель лимита, запись
в реестре). Содержит и `_init`-генерацию, и `_tick`/`is_done`/переходы —
разнородные ответственности в одном файле.

#### Что сделано

Вынесена **init-группа** (инициализация стартового состояния и вложенных
элементов в `_init`, фича R6) в новый модуль
`grammar/src/generator/c/c_model_init.rs` (309 строк):

- Функции: `generate_model_init`, `generate_start_state_init`, `is_real_array`,
  `generate_array_init`, `generate_parallel_items_init`, `generate_concat_item_init`.
- Наружу (в остающуюся `_tick`-логику `c_model.rs`) видны `pub(super)`
  `generate_model_init` и `generate_concat_item_init`; прочие — приватные модуля.
- `c_model.rs` импортирует их через `use super::c_model_init::{…}`; лишние
  импорты (`generate_expr`, `TypeNode`, `ExpressionNode`, `VariableNode`,
  `generate_scalar_init`), уехавшие с функциями, удалены.
- `generator/c/mod.rs` — добавлено `mod c_model_init;`.

**Чистое перемещение:** тело функций скопировано дословно, вывод C неизменен.
`c_model.rs`: **1073 → 782** строки (уложился в лимит) — запись **удалена** из
`scripts/module-size-baseline.txt` (реестр 18 → 17). `c_model_init.rs` (309) — не
нарушитель.

Стеки: только `grammar`. `simulation` — н/п.

#### Проверки

- `cargo build --bin lamc` — без предупреждений.
- `cargo test --test codegen_tests` — **39 passed** (вывод C байт-в-байт неизменен).
- `./scripts/precheck.sh` — зелёный (детерминизм-проверка, `check-module-size.sh`,
  все тесты, сборка примеров).

### Задача

#### Что было

`grammar/src/generator/rust/rust_expr.rs` — **1112 строк** (нарушитель лимита).
Содержал и печатник **выражений** (`print_expression`), и печатник **условий**
(`print_condition` и спутники) — две разные ветви (ADR: у `=` разная
семантика).

#### Что сделано

Печатник **условий** вынесен в новый модуль
`grammar/src/generator/rust/rust_cond.rs` (281 строка):

- Функции: `print_condition`, `state_comparison`, `model_of`, `cond_binary`,
  `cond_bool_binary`, `condition_type`, `condition_as_bool`.
- Наружу нужен только `condition_as_bool` (потребитель — `rust_model.rs`);
  путь сохранён **реэкспортом** `pub(crate) use …rust_cond::condition_as_bool;`
  в `rust_expr.rs` — импорт в `rust_model.rs` **не менялся** (правило 11).
- Приватные функции `rust_expr`, понадобившиеся вынесенному блоку, повышены до
  `pub(crate)`: `variable`, `rational`, `bit_mask`, `member_index`,
  `call_arguments`, метод `Scope::hal_receiver`. Осевшие импорты (`ConditionNode`,
  `function_return`) удалены.
- `generator/rust/mod.rs` — добавлено `mod rust_cond;`.

**Чистое перемещение:** тело функций скопировано дословно, вывод Rust неизменен.
`rust_expr.rs`: **1112 → 847** (уложился) — запись **удалена** из реестра
(**17 → 16**). `rust_cond.rs` (281) — не нарушитель.

Стеки: только `grammar`. `simulation` — н/п.

#### Проверки

- `cargo build --bin lamc` — без предупреждений.
- `./scripts/precheck.sh` — зелёный (детерминизм-проверка + `conformance_rust_tests`
  подтверждают вывод Rust байт-в-байт неизменным; `check-module-size.sh` учитывает
  −1 запись; все тесты, сборка примеров).

### Задача

#### Что было

`grammar/src/generator/rust/rust_model.rs` — **1345 строк** (нарушитель). Содержал
и эмиссию структуры/enum/`new`/`init`, и **такт** (`emit_tick`) с guard-формулами и
всеми видами **переходов** (простые, `= Модель`/`|`/`+`).

#### Что сделано

Эмиссия **такта и переходов** вынесена в новый модуль
`grammar/src/generator/rust/rust_tick.rs` (548 строк):

- Функции: `emit_tick`, `emit_guard`, `emit_transitions`, `emit_enter_of`,
  `emit_extend`, `emit_extend_transition`, `call_args`.
- Наружу нужен только `emit_tick` (`pub(crate)`, вызывается `emit_model` в
  остатке `rust_model`) — импортируется `use …rust_tick::emit_tick;`.
- Приватные помощники остатка, понадобившиеся группе, повышены до `pub(crate)`:
  `seq_enum_name`, `seq_field_name`, `needs_hal`, `submodel_name` и **поле**
  `StateTable::emit_end`. Осевшие импорты (`emit_named_blocks`/
  `emit_model_named_blocks`, `condition_as_bool`, `unwrap_outer`, `StmtOutput`,
  `TypeNode`, `Formula`, `StateNode`) удалены из `rust_model`.
- `generator/rust/mod.rs` — добавлено `mod rust_tick;`.

**Чистое перемещение:** тело функций скопировано дословно, вывод Rust неизменен.
`rust_model.rs`: **1345 → 819** (уложился) — запись удалена из реестра
(**16 → 15**). `rust_tick.rs` (548) — не нарушитель.

Стеки: только `grammar`. `simulation` — н/п.

#### Проверки

- `cargo build --bin lamc` — без предупреждений.
- `./scripts/precheck.sh` — зелёный (детерминизм-проверка + `conformance_rust_tests`
  подтверждают вывод Rust байт-в-байт неизменным; `check-module-size.sh` учитывает
  −1 запись; все тесты, сборка примеров).

### Задача

#### Что было

`grammar/src/parser/lexer.rs` — **1154 строки** (нарушитель). Содержал и лексер
(`Lexer`, `LexicalError`, сканеры), и **перечисление токенов** `Token` (все
терминалы грамматики) вместе с его `impl fmt::Display` (~300 строк).

#### Что сделано

Перечисление токенов вынесено в новый модуль `grammar/src/parser/token.rs`
(311 строк):

- `pub enum Token<'input>` (все терминалы) + `impl fmt::Display for Token`.
  `Token` самодостаточен — варианты хранят только `&'input str`/`bool`/`i64`,
  внешних типов нет; `impl Display` зависит лишь от `std::fmt`.
- **Путь `parser::lexer::Token` сохранён реэкспортом** `pub use
  crate::parser::token::Token;` в `lexer.rs` — от него зависит lalrpop
  (`use super::parser::lexer::{Token, LexicalError};` + `extern { enum Token }`),
  а также doc-тесты и потребители (правило 11). `use std::fmt` в `lexer.rs`
  удалён (осел вместе с `Display`).
- `parser/mod.rs` — добавлено `pub mod token;`.

**Чистое перемещение:** `Token` и `Display` скопированы дословно, разбор/вывод
неизменны. `lexer.rs`: **1154 → 851** (уложился) — запись удалена из реестра
(**15 → 14**). `token.rs` (311) — не нарушитель.

Стеки: только `grammar`. `simulation` — н/п.

#### Проверки

- `cargo build --bin lamc` — без предупреждений.
- `./scripts/precheck.sh` — зелёный (сборка lalrpop-грамматики против
  реэкспортированного `Token`, `lexer_tests`/`parser_tests`, детерминизм-проверка,
  `check-module-size.sh` −1 запись).

### Задача

#### Что было

`grammar/src/parser/ast.rs` — **1204 строки** (нарушитель). Содержал все узлы АСД;
крупнейший — узел **`Expression`** (`pub enum Expression` + `impl Expression`,
~300 строк).

#### Что сделано

Узел выражения вынесен в новый модуль `grammar/src/parser/ast_expr.rs`
(315 строк):

- `pub enum Expression` (все виды выражений) + `impl Expression` (аксессоры
  `loc`/…). Импортирует из `parser::ast` типы, встречающиеся в вариантах:
  `Identifier`, `Member`, `NamedArgument`, `ParameterList`, `Statement`,
  `StringLiteral`, `Type` (взаимная ссылка `ast` ↔ `ast_expr` — Rust допускает).
- **Путь `parser::ast::Expression` сохранён реэкспортом** `pub use
  crate::parser::ast_expr::Expression;` в `ast.rs` — от него зависят
  lalrpop-грамматика, семантика и генераторы (правило 11).
- `parser/mod.rs` — добавлено `pub mod ast_expr;`.

**Чистое перемещение:** `Expression` и его `impl` скопированы дословно, разбор/
вывод неизменны. `ast.rs`: **1204 → 902** (уложился) — запись удалена из реестра
(**14 → 13**). `ast_expr.rs` (315) — не нарушитель.

Стеки: только `grammar`. `simulation` — н/п.

#### Проверки

- `cargo build --bin lamc` — без предупреждений (каскад «`Expression` не найден»
  снят: `ast_expr` компилируется — не хватало импорта `ParameterList`).
- `./scripts/precheck.sh` — зелёный (lalrpop против реэкспорта, все тесты,
  детерминизм-проверка, `check-module-size.sh` −1 запись).

### Задача

#### Что было

`grammar/tests/lexer_tests.rs` — **1081 строка** (нарушитель): helpers + 62
плоских `#[test]` без модульной структуры.

#### Что сделано

**Первый вынос тестового файла** — приём «директория-подмодуль»:

- Вторая половина тестов (секция «Тесты Display токенов» и далее) вынесена в
  `grammar/tests/lexer_tests/part2.rs`. Helpers и импорты берутся из родителя
  через `use super::*` (glob — без пер-элементных `unused_imports`).
- `lexer_tests.rs` объявляет подмодуль **`#[path = "lexer_tests/part2.rs"] mod
  part2;`** — `#[path]` обязателен: корень тест-бинарника ищет `mod` в `tests/`,
  а **не** в подкаталоге по имени файла (иначе `E0583 file not found`).
- **Один тест-бинарник** сохранён (подмодуль, а не второй файл `tests/*.rs`) —
  время компиляции тестов не растёт кратно.

**Чистое перемещение:** утверждения тестов не менялись, только адрес.
`lexer_tests.rs`: **1081 → 529**; `part2.rs` — 561; оба ≤ 1000 → запись удалена
из реестра (**13 → 12**).

Стеки: только `grammar` (тесты). `simulation` — н/п.

#### Проверки

- `cargo test --test lexer_tests` — **62 passed, 0 failed** (36 в корне + 26 в
  `part2`, один бинарник).
- `./scripts/precheck.sh` — зелёный (`check-module-size.sh` −1 запись, все тесты).

### Задача

#### Что было

`grammar/tests/lsp_tests.rs` — **1266 строк** (нарушитель). Структура — **четыре**
отдельных `#[cfg(feature = "lsp")] mod`-блока: `lsp_integration` (10–803),
`diagnostic_location_tests`, `formatting_tests`, `lsp_multifile`.

#### Что сделано

Три меньших mod-блока (`diagnostic_location_tests`, `formatting_tests`,
`lsp_multifile`, строки 805–1266) вынесены **целиком** в подмодуль
`grammar/tests/lsp_tests/more.rs`:

- Блоки самодостаточны (свои `use` внутри каждого), поэтому `use super::*` не
  нужен — перемещены как есть.
- В `lsp_tests.rs` (crate root) добавлено `#[cfg(feature = "lsp")]
  #[path = "lsp_tests/more.rs"] mod more;`. **`#[path]` относителен `tests/`**
  (объявление на **корне** тест-бинарника, как в 0088-06), поэтому путь —
  `lsp_tests/more.rs`.

**Урок:** первая попытка резала по строке **внутри** первого mod — граница
пересекла закрытие `mod lsp_integration` (`unexpected closing delimiter`). Резать
тестовые файлы нужно **по границам mod-блоков**, а не по номеру строки: `mod`
вложен → `#[path]` от каталога-имени-mod; `mod` на корне → `#[path]` от `tests/`.

**Чистое перемещение:** утверждения не менялись. `lsp_tests.rs`: **1266 → 809**;
`more.rs` — 467; оба ≤ 1000 → запись удалена из реестра (**12 → 11**).

Стеки: только `grammar` (тесты). `simulation` — н/п.

#### Проверки

- `cargo test --features lsp --test lsp_tests` — **69 passed, 0 failed** (один
  бинарник: `lsp_integration` в корне + `more` с тремя mod).
- `./scripts/precheck.sh` — зелёный (`--all-features`, `check-module-size.sh` −1).

### Задача

#### Что было

`grammar/tests/codegen_tests.rs` — **1393 строки** (нарушитель): helpers
(`tmp_but_file`, `generate_c_content`, `generate_h_content`) + 39 плоских
`#[test]` на корне тест-бинарника (без mod-структуры).

#### Что сделано

Вторая половина тестов (с `test_concat_non_last_has_break_inside_if`) вынесена в
подмодуль `grammar/tests/codegen_tests/part2.rs`:

- Helpers и импорты — из родителя через `use super::*` (glob). Резал по границе
  **полного `#[test]`**, а не по номеру строки.
- `codegen_tests.rs` (crate root) объявляет `#[path = "codegen_tests/part2.rs"]
  mod part2;` — `#[path]` от `tests/` (объявление на корне бинарника).

**Чистое перемещение:** утверждения не менялись. `codegen_tests.rs`: **1393 →
715**; `part2.rs` — 690; оба ≤ 1000 → запись удалена из реестра (**11 → 10**).

Стеки: только `grammar` (тесты). `simulation` — н/п.

#### Проверки

- `cargo test --test codegen_tests` — **39 passed, 0 failed** (один бинарник).
- `./scripts/precheck.sh` — зелёный (`check-module-size.sh` −1, все тесты,
  детерминизм-проверка).

### Задача

#### Что было

`grammar/src/generator/c/c_source.rs` — **1169 строк** (нарушитель). Из них
собственно код — **30 строк** (`generate_source`), остальное — inline-модуль
`#[cfg(test)] mod tests { … }` (~1136 строк, helpers `make_map_and_owner`/
`expr_to_str` + 67 плоских `#[test]`).

#### Что сделано

Вторая половина тестов (с `test_generate_source_functions`) вынесена в
**вложенный** подмодуль `mod part2` внутри `mod tests` (приём/08,
адаптированный для inline-тестов в `src`):

- Файл — `grammar/src/generator/c/c_source/tests/part2.rs` (**естественный**
  путь модуля: `c_source.rs` → `c_source/` → `tests/` → `part2.rs`; **без**
  `#[path]`, объявление — голое `mod part2;` внутри `mod tests`).
- Helpers и импорты — из родителя через `use super::*` (glob): `super` для
  вложенного `part2` — это модуль `tests`, где живут helpers и все `use`.
- Резал по границе **полного `#[test]`**, а не по строке.

**Чистое перемещение:** ни одно утверждение не менялось, только адрес. Код
`generate_source` не тронут → вывод генератора `c` байт-в-байт неизменен.

- `c_source.rs`: **1169 → 604** (30 строк кода + первая половина тестов);
- `c_source/tests/part2.rs` — **573**;

оба ≤ 1000 → запись удалена из реестра (**10 → 9**).

Стеки: только `grammar`. `simulation` — н/п.

#### Проверки

- `cargo test -p grammar --lib generator::c::c_source` — **67 passed, 0 failed**
  (оба подмодуля `tests` + `tests::part2`).
- `./scripts/precheck.sh` — зелёный (`check-module-size.sh` −1, все тесты,
  детерминизм-проверка → вывод генераторов неизменен).

### Задача

#### Что было

`grammar/tests/parser_tests.rs` — **1892 строки** (нарушитель): helpers
(`must_parse`, `first_named_model`) + 105 плоских `#[test]` на корне
тест-бинарника (без mod-структуры).

#### Что сделано

Вторая половина тестов (с `location_methods`) вынесена в подмодуль
`grammar/tests/parser_tests/part2.rs` (приём/08):

- Helpers и импорты — из родителя через `use super::*` (glob). Резал по границе
  **полного `#[test]`**, захватывая ведущий doc-комментарий.
- `parser_tests.rs` (crate root) объявляет `#[path = "parser_tests/part2.rs"]
  mod part2;` — `#[path]` от `tests/`.

**Чистое перемещение:** утверждения не менялись. `parser_tests.rs`: **1892 →
937**; `part2.rs` — 963; оба ≤ 1000 → запись удалена из реестра (**9 → 8**).

Стеки: только `grammar` (тесты). `simulation` — н/п.

#### Проверки

- `cargo test --test parser_tests` — **105 passed, 0 failed** (один бинарник).
- `./scripts/precheck.sh` — зелёный (`check-module-size.sh` −1, все тесты,
  детерминизм-проверка).

### Задача

#### Что было

`grammar/tests/semantic_tests.rs` — **5015 строк**, **крупнейший файл проекта**
(нарушитель №1): helpers (`build`, `build_err`) + **294** плоских `#[test]` на
корне тест-бинарника (без mod-структуры). Одним подмодулем  не
уложиться — нужно **6 файлов** ≤ 1000.

#### Что сделано

Тесты разбиты на **5** подмодулей `grammar/tests/semantic_tests/part{2..6}.rs`
(родитель держит helpers + первую группу):

- Резал по границе **полного элемента** — от начала ведущего doc-комментария до
  конца `#[test] fn` (урок/08; иначе висячий `///` на границе, как исправление
  для 0088-08). Точки раскола — ближайший `#[test]` к 950/1880/2810/3740/4670.
- Helpers и импорты — из родителя через `use super::*` (glob).
- `semantic_tests.rs` (crate root) объявляет `#[path = "semantic_tests/partN.rs"]
  mod partN;` для каждой части.

**Ловушка:** два top-level `use` (`StatementNode`, `type_node::TypeNode`)
жили **в середине** оригинального файла (не в шапке). При расколе они попали в
`part2`, и `part5` перестала видеть `TypeNode` (`use super::*` реэкспортит
только items **родителя**, не сиблингов). Исправлено **подъёмом обоих `use` в
шапку родителя** — теперь `super::*` раздаёт их всем частям.

**Чистое перемещение:** ни одно утверждение не менялось. `semantic_tests.rs`:
**5015 → 948**; части — 940/934/933/941/355; все ≤ 1000 → запись удалена из
реестра (**8 → 7**).

Стеки: только `grammar` (тесты). `simulation` — н/п.

#### Проверки

- `cargo test --test semantic_tests` — **294 passed, 0 failed** (один бинарник).
- `./scripts/precheck.sh` — зелёный (`check-module-size.sh` −1, все тесты,
  детерминизм-проверка).

### Задача

#### Что было

`simulation/src/unit/viewport.rs` — **1455 строк** (нарушитель, последний из
безопасной части). Врезка `FEATURES.md` пессимистично оценивала его как «одна
функция `create_svg` ~1128 строк, требует разбиения тела на под-функции (риск
изменить SVG)». **Факт при осмотре иной:** `create_svg` — ~550 строк, а внутри
файла есть самодостаточный **inline-подмодуль** `pub(super) mod graph` (~567
строк вместе со своими тестами), зависящий только от `super::Positions` и типов
крейта.

#### Что сделано

Подмодуль `graph` вынесен в **файл-модуль** `simulation/src/unit/viewport/graph.rs`
(строки 735–1299 → отдельный файл, дедент 4 пробела; `viewport.rs` объявляет
`pub(super) mod graph;`, doc-комментарий сохранён над объявлением):

- **`create_svg` не тронут вовсе** → SVG-вывод байт-в-байт неизменен, риск из
  врезки **не возникает** (нужды дробить тело функции нет — естественная граница
  модуля решает задачу).
- Публичные пути неизменны: `graph::unit_to_graph`/`graph::calculate_graph`
  зовутся из `compute_layout` как прежде (`super` внутри `graph` по-прежнему =
  `viewport`, `use super::Positions` цел).
- Тесты подмодуля (`test_dist_*`, `test_segments_*`, `test_unit_to_graph_*`,
  `test_calculate_graph_*`) переехали вместе с ним.

**Чистое перемещение:** `viewport.rs` **1455 → 889**; `viewport/graph.rs` — 567;
оба ≤ 1000 → запись удалена из реестра (**7 → 6**). `cargo fmt -p simulation`
применён к вынесенному файлу.

Стеки: только `simulation`. `grammar` — н/п.

#### Проверки

- `cargo test -p simulation --lib unit::viewport` — **31 passed, 0 failed**
  (тесты `viewport` + подмодуля `graph`).
- `./scripts/precheck.sh` — зелёный (`check-module-size.sh` −1, все тесты,
  `run_simulations.sh` не в precheck, но GIF-путь кода не менялся).

#### Итог по безопасной части фичи

Реестр **18 → 6**. Оставшиеся 6 — **ядро семантики** (`tree`, `mod`,
`expression`, `type_inference`) и **пришпиленные** (`lamc`, `lib`): это scope
**фичи-преемника** (ADR, Option B), заводится при закрытии 0088.

## Тест-план

### Область и цель

Фича — **чистое перемещение** кода (Tier 3, качество/процесс): разбиение
файлов-нарушителей размера на подмодули ≤ 1000 строк без изменения поведения и
байт-в-байт вывода генераторов. Соответственно проверки — не «новая
функциональность», а **отсутствие регресса** и выполнение критериев приёмки
A1–A5 анализа (реестр 18 → 6, вывод неизменен, все тесты зелёные, precheck
зелёный, заведена фича-преемник).

Синтаксис/семантика языка **не затронуты** → правило 16 (примеры/контрпримеры
языка) неприменимо: фича не меняет язык.

### Проверки (условие → ожидаемый результат)

| # | Проверка | Предусловие | Ожидаемый результат | Ссылка на R/A |
|---|---|---|---|---|
| T1 | Реестр размеров уменьшается ровно на вынесенные файлы | после каждой подзадачи-YY | `grep -c '^[0-9]' module-size-baseline.txt`: 10 → 6 за сессию (18 → 6 за фичу) | A1 / R1 |
| T2 | Каждый вынесенный/родительский файл ≤ 1000 строк | после выноса | `check-module-size.sh` зелёный; `wc -l` каждой части < 1000 | R1 / A1 |
| T3 | Целевой тест-бинарник зелёный без изменения утверждений | c_source/parser_tests/semantic_tests/viewport | `cargo test --test X` (или `--lib module::path`): все passed, число тестов = прежнему | R3 / A3 |
| T4 | Вывод генераторов `c`/`rust`/`st`/`sv`/`plantuml` байт-в-байт неизменен | вынос c_source (генератор) | детерминизм-проверка `precheck.sh` (два прогона × все цели → `diff -r`) зелёный | R2 / A2 |
| T5 | Публичные пути импорта неизменны | вынос модулей (`mod`/`#[path]`/реэкспорт) | компиляция без правок у потребителей; `super::*`/`mod X;` держат пути | R4 / A4 |
| T6 | `precheck.sh` зелёный на каждом коммите-выносе | каждая подзадача | `EXIT=0` (fmt/check/clippy/test/сборка примеров/детерминизм/ST-проверка) | R5 / A4 |
| T7 | Заведена фича-преемник по 6 оставшимся файлам | закрытие 0088 | карточка [вынос оставшихся нарушителей размера модуля (ядро семантики + пришпиленные)](0099-module-size-core.md), статус `СОЗДАНА` | A5 |

### Разбивка проверок по функциональности

Обратная функциональность (правило 11) — весь корпус тестов `grammar` и
`simulation`, вывод генераторов, публичный API. Единые условия (T3–T6)
прогоняются против каждой задеваемой подзадачи; статус фиксируется в отчёте.

<!-- Легенда: ✅ пройдено · ❌ провалено · ⬜ не проверялось · — не применимо -->

### Тестовые данные и окружение

- Тулчейн: `cargo` (nightly, edition проекта); проверки `precheck.sh` — `cc`/`cmake`/
  `ninja`, `iec2c` (MatIEC), `verilator`, `yosys`.
- Специальных фикстур фича не вводит — использует существующий корпус тестов и
  `examples/` (перемещение не добавляет тестовых данных).

## Отчёт о тестировании

### Резюме

**Вердикт: ГОТОВО.** Безопасная часть (12 файлов, ADR Option B) вынесена
полностью: реестр `scripts/module-size-baseline.txt` **18 → 6**. Все проверки
тест-плана T1–T7 пройдены (✅), исправлений (`docs/fixes/`) не потребовалось.
Каждая из 12 подзадач…12 закоммичена **с зелёным `precheck.sh`**. Вывод
генераторов байт-в-байт неизменен (детерминизм-проверка), поведение неизменно (полный
корпус тестов), публичные пути целы.

### Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Реестр уменьшается на вынесенные файлы | ✅ | 18 → 6; за последнюю сессию 10 → 6 (09–12) |
| T2 | Каждый файл ≤ 1000 строк | ✅ | все части < 1000; `check-module-size.sh` зелёный |
| T3 | Целевой тест зелёный без правки утверждений | ✅ | c_source 67, parser_tests 105, semantic_tests 294, viewport 31 — passed, число тестов = прежнему |
| T4 | Вывод генераторов байт-в-байт неизменен | ✅ | детерминизм-проверка `precheck.sh` зелёный; `create_svg`/`generate_source` не тронуты |
| T5 | Публичные пути импорта неизменны | ✅ | `mod X;`/`#[path]`/`use super::*` держат пути; правок у потребителей нет |
| T6 | `precheck.sh` зелёный на каждом коммите | ✅ | `EXIT=0` на всех 12 коммитах |
| T7 | Заведена фича-преемник | ✅ | [вынос оставшихся нарушителей размера модуля (ядро семантики + пришпиленные)](0099-module-size-core.md), `СОЗДАНА` (6 файлов) |

### Результаты по функциональности

Приёмы выноса (по классам файлов):

- **Генераторы** (0088-01/02/03/09): init-группа C → `c_model_init.rs`; печатники
  Rust → `rust_cond.rs`/`rust_tick.rs`; inline-тесты C → `c_source/tests/part2.rs`.
  Вывод целей `c`/`rust` байт-в-байт неизменен.
- **Парсер/лексер** (0088-04/05): `enum Token` → `parser/token.rs`; узел
  `Expression` → `parser/ast_expr.rs`.
- **Тесты** (0088-06/07/08/10/11): директория-подмодуль (`part2`/`more`) с
  `use super::*`; крупнейший файл проекта `semantic_tests.rs` (5015) → 5
  подмодулей. Ловушка 0088-11: два top-level `use` из середины файла подняты в
  шапку родителя (иначе `use super::*` не раздаёт их сиблингам).
- **Симуляция** (0088-12): `unit/viewport.rs` (1455) → вынос самодостаточного
  подмодуля `graph` в файл; `create_svg` **не тронут** → SVG-вывод неизменен.
  Врезка «viewport — одна функция ~1128 строк» оказалась неверной
  (`create_svg` ~550 строк); дробление тела и связанный с ним риск для SVG **не
  потребовались**.

Примеры/контрпримеры языка (правило 16) — **неприменимо**: фича язык не меняет.

### Выводы и дальнейшие шаги

Фича закрывается (`ГОТОВО`). Остаток — **6 файлов** (ядро семантики `tree`/`mod`/
`expression`/`type_inference` + пришпиленные `lamc`/`lib`): их вынос требует
декомпозиции логики и пересмотра пришпиливания (не чистое перемещение), поэтому
вынесен в **фичу-преемник [вынос оставшихся нарушителей размера модуля (ядро семантики + пришпиленные)](0099-module-size-core.md)** (критерий
A5). Язык не менялся → версии не поднимаются (правило 22). Дефектов не найдено —
исправления не заводились.

## Итог (что сделано)

**Закрыта 2026-07-24** (`ГОТОВО`, вердикт [отчёта](0088-module-size-remaining.md#отчёт-о-тестировании)
— ГОТОВО). Принята **Option B** ([ADR](0088-module-size-remaining.md#архитектура-adr)):
в объём взята **безопасная часть** (чистое перемещение, вывод байт-в-байт
неизменен); ядро семантики и пришпиленные — фича-преемник.

Сделано (12 подзадач, каждая — отдельный коммит с зелёным `precheck.sh`; реестр
`scripts/module-size-baseline.txt` **18 → 6**):

- **Генераторы** (`grammar`): 0088-01 init-группа C → `c_model_init.rs`; 0088-02
  печатник условий Rust → `rust_cond.rs`; 0088-03 такт+переходы Rust →
  `rust_tick.rs`; 0088-09 inline-тесты `c_source` → `c_source/tests/part2.rs`.
- **Парсер** (`grammar`): 0088-04 `enum Token` → `parser/token.rs`; 0088-05 узел
  `Expression` → `parser/ast_expr.rs`.
- **Тесты** (`grammar`): 0088-06 `lexer_tests`→`part2`, 0088-07 `lsp_tests`→`more`,
  0088-08 `codegen_tests`→`part2`, 0088-10 `parser_tests`→`part2`, 0088-11
  `semantic_tests` (крупнейший файл проекта, 5015) → 5 подмодулей `part2..6`.
- **Симуляция** (`simulation`): 0088-12 `unit/viewport.rs` → вынос подмодуля
  `graph` в файл; `create_svg` не тронут → SVG-вывод неизменен.

Ключевые находки (детали — [отчёт](0088-module-size-remaining.md#отчёт-о-тестировании)):
приём «директория-подмодуль + `use super::*`» переносится и на inline-тесты в
`src` (вложенный `mod part2`); при расколе `semantic_tests` два top-level `use`
из середины файла пришлось поднять в шапку родителя; врезка о `viewport` как
«одной функции ~1128 строк» оказалась неверной — естественная граница модуля
`graph` решила задачу перемещением, без дробления тела и риска для SVG.

**Остаток → фича-преемник** [вынос оставшихся нарушителей размера модуля (ядро семантики + пришпиленные)](0099-module-size-core.md) (критерий A5
анализа): 6 файлов — ядро семантики (`tree`, `mod`, `expression`,
`type_inference`) и пришпиленные (`lamc`, `lib`), где вынос не сводится к чистому
перемещению. Язык не менялся → версии не поднимаются (правило 22); исправлений
(`docs/fixes/`) не потребовалось.
