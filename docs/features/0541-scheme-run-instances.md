# Фича: подсветка прогона у нескольких экземпляров одной модели на листе композиции

- **Номер:** 0541
- **Статус:** СОЗДАНА
- **Зависит от:** нет (наследует [графическое представление модели — раскладка состояний и рёбер в отдельном файле](0539-model-layout-ui.md), закрыта)
- **Tier:** — (кандидат из итога закрытой фичи, не дефект: шкала признака вреда
  неприменима)
- **Класс:** прочее
- **Связанные issue (анализ):** кандидат из итога [закрытой фичи о схеме](0539-model-layout-ui.md), 2026-09-08
- **Tier:** <признак вреда: 1 | 2 | 3 | 4 | proc — шкала в блоке 1 `FEATURES.md`>
- **Класс:** <синтаксис | семантика | генерация | прочее>
- **Связанные issue (анализ):** <T-… или «новая фича»>

## Стадии жизненного цикла

Все стадии — **разделы этой карточки**: «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог». Раздел
заводится в момент прохождения стадии, а не заранее. Отдельным артефактом
остаются только исправления — [`docs/fixes/`](../fixes/README.md) (при
необходимости `0541-YY-*`).

## Краткое описание

Подсветка прогона на схеме сопоставляет активные состояния с узлами по имени, и
на листе композиции с несколькими экземплярами одной модели (`Middle = Engine +
(Engine | Engine) + …`) узел подсвечивается, если активен **любой** экземпляр:
какой именно шаг цепочки идёт сейчас, схема не показывает. Фича даёт эталону и
модулю адрес экземпляра рядом с именем состояния (путь по дереву юнитов) и учит
лист композиции подсвечивать только свой шаг и свою ветвь, а лист модели — все
активные экземпляры разом с числом на узле.

> Фича зарегистрирована из кандидатов итога закрытой фичи о схеме состояний (2026-09-08, [карточка](0539-model-layout-ui.md));
> далее проходит жизненный цикл по своду.


## Документирование

Язык не меняется: синтаксис, семантика и поведение целей прежние. Меняется
инструмент — описание подсветки прогона в подразделе «Схема состояний» главы
«Инструментарий» [`book/`](../../book/): уточнить, что на листе композиции
подсвечивается идущий шаг и ветвь, а на листе модели — все активные экземпляры.
