# Фича: Раздел документа «Управляющие конструкции»

- **Номер:** 0106
- **Статус:** ГОТОВО
- **Зависит от:** нет (инфраструктура `book/` заведена фичей [документ описания языка Takt (book/, mdBook)](0101-language-book.md))
- **Приоритет / Tier:** **Tier 3** (документация; раздел описывает уже реализованные возможности языка — сам синтаксис/семантику не меняет)
- **Связанные issue (анализ):** новая фича (каждый раздел документа — отдельная фича; запрос заказчика 2026-07-24)

## Ссылки на артефакты

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | не требуется (Tier 3; язык не меняется) |
| Анализ | не требуется (Tier 3; описание уже реализованных конструкций) |
| Разработка | раздел `book/src/06-control-flow/index.typ` + пример `examples/control.takt` |
| Тест-план | компиляция примера `taktc` (цель `c`) + `precheck.sh` |
| Отчёт о тестировании | см. «Итог (что сделано)» ниже |
| Исправления | не потребовались |

## Краткое описание

Описывает **управляющие конструкции** в телах: `if`, `while`/`loop`, `for`, `match`/`switch`; именованные блоки `enter`/`exit`/`always` состояния и уровня модели.

> Фича зарегистрирована **2026-07-24** по запросу заказчика (каждый раздел документа заводится отдельной фичей); далее по своду.

## Документирование

Наполняет раздел `book/src/06-control-flow/index.typ` описанием языка (см. «Краткое описание»); примеры на Takt — в подкаталоге `06-control-flow/examples/`, изображения — в `06-control-flow/images/`. Раздел **описывает** уже реализованные возможности языка, синтаксис/семантику **не меняет**. В документ не попадают детали реализации, номера фич и процесс разработки.

## Итог (что сделано)

Наполнен раздел `book/src/06-control-flow/index.typ` (язык не меняется,
Tier 3; ADR/анализ не требовались):

- **`if`/`else`** (условие — в смысле раздела «Выражения и условия»).
- **`match`** с ветвями `образец => { … }` и `_` (по перечислению).
- **Циклы**: `while`, `loop` (синоним `while`), C-style `for` (инициализатор;
  условие; шаг).
- **`break`/`continue`** внутри циклов; **`return`** в функции.
- Разграничено: управляющие конструкции — часть **тел** блоков/функций, они **не**
  задают переходы автомата (переходы — `ref`/`next`, отсылка к разделу «Модели и
  состояния»); блоки `enter`/`exit`/`always` подробно — в разделе «Именованные
  условия и блоки».
- Пример `book/src/06-control-flow/examples/control.takt` — **компилируется**
  `taktc` (цель `c`, EXIT 0): `match`, `while`+`break`, `for`, `if`/`else`,
  `return`; `continue`/`loop` проверены отдельной компиляцией.

**Живая проверка:** пример компилируется; `./scripts/precheck.sh` зелёный; сборка
PDF (`xelatex`, TeX Live 2026) чистая (`Missing character`/`undefined`/`Overfull`
= 0).

