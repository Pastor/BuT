# Фича: `semantic/unused.rs` не обходит `formulas`

- **Номер:** 0082
- **Статус:** ГОТОВО (2026-07-20; `precheck.sh` зелёный)
- **Зависит от:** нет (опора — `semantic/unused.rs`, типы `Formula`/`Ltl`); обострено [`lamc` не печатает предупреждения вообще](0081-lamc-print-warnings.md)
- **Приоритет / Tier:** **Tier 2** — ложное предупреждение (вводит в заблуждение, кода не портит)
- **Крейт:** `grammar` (`semantic/unused.rs`)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`)

## Стадии жизненного цикла (правило 17)

Все стадии — **разделы этой карточки** (правило 32): «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог».
Отдельным артефактом остаются только исправления —
[`docs/fixes/`](../fixes/README.md) (при необходимости `0082-YY-*`).

## Краткое описание

Переменная, используемая **только в формуле** (LTL/Guard), считается
неиспользуемой — то есть Ce13 даёт **ложное** предупреждение.

Выявлено при [lTL-формулы в блоках кода: разбор вместо тихой потери](0035-ltl-in-blocks.md).

## Архитектура (ADR)

- **Status:** Accepted
- **Date:** 2026-07-20
- **Authors:** Архитектор + Лид разработки
- **Related issues:** [Фича](0082-unused-formulas.md); выявлено при [lTL-формулы в блоках кода: разбор вместо тихой потери](0035-ltl-in-blocks.md); обострено [`lamc` не печатает предупреждения вообще](0081-lamc-print-warnings.md) (Ce13 теперь печатается пользователю)

### Context

`semantic::unused::check_unused_variables` (Ce13, `SE-036`) собирает использования
переменных обходом дерева модели: переменные, функции, условия, именованные блоки,
состояния, вложенные модели. Но **не** обходит **формулы** (`ModelNode::formulas`,
`StateNode::formulas`). Поэтому переменная, встречающаяся **только** в LTL/Guard-
формуле (`: [LTL] G flag;`, `invariant Inv = g = 0;`), считается неиспользуемой —
Ce13 даёт **ложное** предупреждение.

Дефект был латентным, пока `lamc` не печатал предупреждения. **Фича**
подключила Ce13 к CLI — теперь ложное срабатывание видно пользователю.

### Decision Drivers

1. **Переменная в свойстве верификации — использование.** LTL/Guard-свойство на
   неё опирается; объявлять её мёртвой неверно.
2. **Не глушить настоящие находки.** Реально неиспользуемая переменная обязана
   по-прежнему давать `SE-036`.
3. **Обход — тем же механизмом**, что и прочие узлы (единообразие `collect_from_*`).

### Considered Options

#### Option A (принято). Обходить формулы наравне с прочими узлами

Добавить `collect_from_formula` (Guard → `collect_from_condition`; LTL →
`collect_from_ltl`; `Formulas` → рекурсия) и вызвать его для `ModelNode::formulas`
(в `collect_from_model_tree`) и `StateNode::formulas` (в `collect_from_state`).

**Pros:**
- Устраняет ложный Ce13 (драйвер 1) в один обход.
- Настоящие находки сохранены (драйвер 2): формула добавляет использования, но не
  снимает проверку.
- Единообразно с существующими `collect_from_*` (драйвер 3).

**Cons:**
- Имя LTL-атома может быть именем **состояния**, а не переменной. Добавление его в
  `used` безвредно: проверяются только имена переменных, а лишнее имя лишь
  подавляет несуществующее предупреждение. Консервативно (драйвер 2 не нарушен).

#### Option B. Исключить из проверки переменные, встречающиеся в формулах

**Cons:** требует отдельного прохода-исключения; дублирует логику сбора и
расходится с ней при добавлении нового вида формулы.

### Decision

Принимается **Option A**.

**Нормативные правила:**

1. **`collect_from_formula`** покрывает все варианты `Formula` (`None`,
   `Formulas`, `LTL`, `Guard`); `collect_from_ltl` — все варианты `Ltl`.
2. Вызывается для формул **модели** и **состояния**.
3. **Настоящие находки не глушатся**: реально мёртвая переменная даёт `SE-036`
   (негативный контроль).
4. Язык **не меняется** → версия не растёт (правило 22).

### Consequences

#### Положительные

- Ce13 перестаёт ложно срабатывать на переменных свойств — особенно важно после
  0081 (предупреждения видны пользователю).

#### Отрицательные / Action items

- **A-1. Имя состояния в `used`** через LTL-атом — безвредная сверх-аппроксимация
  (см. Cons Option A).

#### Acceptance criteria

1. **A1.** Переменная только в LTL-формуле состояния (`: [LTL] G flag;`) → нет
   `SE-036`.
2. **A2.** Переменная только в `invariant` (Guard уровня модели) → нет `SE-036`.
3. **A3.** Реально неиспользуемая переменная → `SE-036` (не заглушено).
4. **A4.** `precheck.sh` зелёный; язык и версия не изменились.

## Анализ

### Зависимости

- **Нет.** Опора — `semantic/unused.rs` (Ce13) и типы `Formula`/`Ltl`.
- **Обострено [`lamc` не печатает предупреждения вообще](0081-lamc-print-warnings.md):** Ce13 теперь
  печатается пользователю, ложное срабатывание стало видимым.
- **Приоритет / Tier:** **Tier 2** — ложное предупреждение (не порча кода, но
  вводит в заблуждение).

### Точки интеграции (замер по коду)

| Что | Где | Как |
|---|---|---|
| Обход использований | `unused.rs::collect_from_model_tree` | vars/funcs/conditions/blocks/states/nested — **без** formulas |
| Формулы модели | `ModelNode::formulas: Vec<Formula>` | не обходятся |
| Формулы состояния | `StateNode::{Simple,Implement}::formulas` | не обходятся (в `..`) |
| `Formula::Guard(ConditionNode, _)` | — | переиспользует `collect_from_condition` |
| `Formula::LTL(Ltl)` | `verification::ltl::Ltl` | нужен обход атомов |

### Ключевые решения анализа

1. **Guard → `collect_from_condition`** (уже есть); **LTL → новый
   `collect_from_ltl`** (обход атомов, `Atom(name) → used`).
2. **Формулы состояния** требуют добавить `formulas` в деструктуризацию обоих
   вариантов `StateNode`.
3. **Атом LTL — имя переменной или состояния** (0049/0068); имя состояния в `used`
   безвредно.

### Риски

- **Заглушить настоящую находку** — снят негативным контролем
  (`test_truly_unused_var_still_warns_after_formula_traversal`).

## Разработка

### Задача

#### Что было

`collect_from_model_tree` (`semantic/unused.rs`) обходил vars/functions/conditions/
named_blocks/states/nested, но **не** `ModelNode::formulas` и `StateNode::formulas`.
Переменная только в LTL/Guard-формуле → ложный `SE-036`.

#### Что сделано

1. **`collect_from_formula(formula, used)`**: `Guard(cond, _)` →
   `collect_from_condition`; `LTL(ltl)` → `collect_from_ltl`; `Formulas` →
   рекурсия; `None` → ничего.
2. **`collect_from_ltl(ltl, used)`**: `Atom(name) → used.insert`; рекурсия по всем
   темпоральным/логическим операторам (match исчерпывающий).
3. **Вызов** для `borrowed.formulas` в `collect_from_model_tree` и для `formulas`
   в `collect_from_state` (добавлено в деструктуризацию обоих вариантов `StateNode`).

#### Проверки

- **T1/A1:** `var_used_only_in_ltl_formula_no_unused_warning`.
- **T2/A2:** `var_used_only_in_invariant_no_unused_warning`.
- **T3/A3:** `truly_unused_var_still_warns_after_formula_traversal` (негативный контроль).
- **T4/A4:** `precheck.sh` зелёный.

## Тест-план

### Область и цель

Проверяется, что переменная в LTL/Guard-формуле считается **использованной** (нет
ложного `SE-036`), и — не менее важно — что реально мёртвая переменная **по-
прежнему** предупреждается.

### Проверки

| # | Проверка | Предусловие | Ожидаемый результат | R/A |
|---|---|---|---|---|
| T1 | Переменная в LTL-формуле состояния | `: [LTL] G flag;`, `flag` больше нигде | нет `SE-036` на `flag` | R1 / A1 |
| T2 | Переменная в `invariant` (Guard модели) | `invariant Inv = g = 0;`, `g` больше нигде | нет `SE-036` на `g` | R1 / A2 |
| T3 | **Негативный контроль** | реально мёртвая `dead` | `SE-036` на `dead` (не заглушено) | R3 / A3 |
| T4 | Прочее не задето | `precheck.sh`, корпус | зелёный; язык/версия не изменились | R4 / A4 |

### Разбивка по функциональности

| Функциональность | Ожидание | Проверка | Статус |
|---|---|---|---|
| Обход формул (Ce13) | переменная свойства — использование | T1, T2 | ✅ |
| Настоящие находки | мёртвая переменная предупреждается | T3 | ✅ |
| Прочие проходы, цели | не задеты | T4 | ✅ |
| Язык | не меняется → версия не растёт | — | ✅ |

<!-- Легенда: ✅ пройдено · ❌ провалено · ⬜ не проверялось · — не применимо -->

### Тестовые данные и окружение

- **Тесты:** `grammar/tests/unused_formula_tests.rs`
  (`var_used_only_in_ltl_formula_no_unused_warning`,
  `var_used_only_in_invariant_no_unused_warning`,
  `truly_unused_var_still_warns_after_formula_traversal`) — через
  `unused_variable_warnings`.

### Что план сознательно не проверяет

- **Полноту вариантов `Ltl`** — match исчерпывающий (компилятор гарантирует);
  T1 репрезентативен для механизма обхода.

## Отчёт о тестировании

- **Дата:** 2026-07-20
- **Окружение:** macOS (darwin 25.5.0), cargo nightly.
- **Вердикт:** **готово.** Три теста `unused_formula_tests` зелёные, `precheck.sh`
  зелёный. Язык и версия не изменились.

### Сверка с критериями приёмки

| Критерий | Проверка | Результат |
|---|---|---|
| **A1** var в LTL-формуле → нет `SE-036` | `var_used_only_in_ltl_formula_no_unused_warning` | ✅ |
| **A2** var в `invariant` → нет `SE-036` | `var_used_only_in_invariant_no_unused_warning` | ✅ |
| **A3** мёртвая var → `SE-036` | `truly_unused_var_still_warns_after_formula_traversal` | ✅ (не заглушено) |
| **A4** прочее не задето | `precheck.sh` | ✅ зелёный |

### Наблюдения

- **Дефект обострён 0081:** до подключения предупреждений к CLI ложный Ce13 был
  латентным; после — виден пользователю. 0082 закрывает его вовремя.
- **Синтаксис LTL-формулы:** `: [LTL] G flag;` (маркер `[LTL]`, оператор внутри);
  форма `: [G] …` парсером **не** принимается — уточнено пробой.
- **Guard переиспользует `collect_from_condition`** — новый код только у LTL
  (`collect_from_ltl`).

### Отклонения

- **Имя состояния в `used`** через LTL-атом — безвредная сверх-аппроксимация
  (проверяются только имена переменных; A-1 ADR).

## Итог (что сделано) — 2026-07-20

Принята **Option A**: `unused.rs` обходит формулы наравне с прочими
узлами. Добавлены `collect_from_formula` (Guard → `collect_from_condition`, LTL →
`collect_from_ltl`, `Formulas` → рекурсия) и `collect_from_ltl` (обход атомов);
вызов — для `ModelNode::formulas` (в `collect_from_model_tree`) и
`StateNode::formulas` (в `collect_from_state`, добавлено в деструктуризацию обоих
вариантов). Переменная только в `: [LTL] G flag;` или `invariant Inv = g` больше
**не** даёт ложный `SE-036`; реально мёртвая — по-прежнему даёт (негативный
контроль). Дефект обострён 0081 (Ce13 теперь печатается). Язык не менялся, версия
не поднята.

## История

> Фича зарегистрирована **2026-07-17** переводом кандидата из `FEATURES.md`.
> Проработана и закрыта 2026-07-20. Текст ниже — исходная находка кандидата.
