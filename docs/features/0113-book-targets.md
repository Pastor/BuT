# Фича: Раздел документа «Цели генерации»

- **Номер:** 0113
- **Статус:** ГОТОВО
- **Зависит от:** нет (инфраструктура `book/` заведена фичей [документ описания языка Takt (book/, mdBook)](0101-language-book.md))
- **Приоритет / Tier:** **Tier 3** (документация; раздел описывает уже реализованные возможности языка — сам синтаксис/семантику не меняет)
- **Связанные issue (анализ):** новая фича (правило 24: каждый раздел документа — отдельная фича; запрос заказчика 2026-07-24)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | не заведён (стадия 2) |
| Анализ | не заведён (стадия 3) |
| Разработка | [`docs/development/`](README.md) (задачи `0113-YY-*`) |
| Тест-план | [`0113-book-targets.md#тест-план`](README.md) |
| Отчёт о тестировании | [`0113-book-targets.md#отчёт-о-тестировании`](README.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0113-YY-*`) |

## Краткое описание

Описывает **цели генерации**: обзор целей (C, C-HAL, ST, ST-AT, Rust, SystemVerilog, SV-MMIO, PlantUML), их назначение и принцип сохранения поведения модели между целями.

> Фича зарегистрирована **2026-07-24** по запросу заказчика (правило 24 — каждый раздел документа заводится отдельной фичей); далее по правилу 17.

## Документирование (правило 24)

Наполняет раздел `book/src/13-targets/index.typ` описанием языка (см. «Краткое описание»); примеры на Takt — в подкаталоге `13-targets/examples/`, изображения — в `13-targets/images/`. Раздел **описывает** уже реализованные возможности языка, синтаксис/семантику **не меняет**. В документ не попадают детали реализации, номера фич и процесс разработки.

## Итог (что сделано)

Наполнен раздел `book/src/13-targets/index.typ` описанием языка (правило 24 — язык
не меняется, Tier 3; ADR/анализ не требовались). Раздел **языковой** — описывает
цели и принцип, без команд/вывода инструмента и порождённого кода (те — в
прикладных 16/17, приложение):

- **Обзор целей** (таблица): `c`/`c-hal`/`rust`/`st`/`st-at`/`sv`/`sv-mmio`/
  `plantuml` — что порождает и назначение.
- **Порты и оборудование** — три способа связи порта с железом: колбэк HAL (`c`),
  прямой доступ по адресу (`c-hal`/`st-at`), регистровый файл на шине (`sv-mmio`);
  адресные цели требуют адрес у порта.
- **Сохранение поведения** — модель ведёт себя одинаково во всех целях и в
  симуляторе (детерминизм, семантика; со ссылками на разделы 08/11/12).
- **Частые ошибки** (правило 26): `SE-052` (порт без адреса для `c-hal`/`st-at`),
  `SV-005` (конструкция не переводится в `sv`).
- Пример `starter.takt` — магнитный пускатель с самоблокировкой; компилируется во
  **все 8 целей** (0 предупреждений) и **проверен симуляцией** (правило 27д):
  «Пуск» → `Running` (держит после отпускания), «Стоп» → `Off`. Полный код всех
  целей — ссылка на приложение showcase.

**Живая проверка:** пример компилируется (8 целей) и симулируется;
`./scripts/precheck.sh` зелёный; сборка PDF (`make -C book build`) чистая. Язык не
менялся → версия языка та же.

