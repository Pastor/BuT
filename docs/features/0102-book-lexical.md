# Фича: Раздел документа «Лексическая структура»

- **Номер:** 0102
- **Статус:** ГОТОВО
- **Зависит от:** нет (инфраструктура `book/` заведена фичей [документ описания языка Takt (book/, mdBook)](0101-language-book.md))
- **Приоритет / Tier:** **Tier 3** (документация; раздел описывает уже реализованные возможности языка — сам синтаксис/семантику не меняет)
- **Связанные issue (анализ):** новая фича (правило 24: каждый раздел документа — отдельная фича; запрос заказчика 2026-07-24)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | не требуется (Tier 3; язык не меняется) |
| Анализ | не требуется (Tier 3; описание уже реализованной лексики) |
| Разработка | раздел `book/src/02-lexical/index.typ` + пример `examples/tokens.takt` |
| Тест-план | компиляция примера `taktc` (цель `c`) + `precheck.sh` |
| Отчёт о тестировании | см. «Итог (что сделано)» ниже |
| Исправления | не потребовались |

## Краткое описание

Описывает **лексическую структуру** языка: кодировку исходного текста, идентификаторы, ключевые слова, числовые/битовые/строковые литералы, комментарии, операторы и пунктуацию. Задаёт правила разбора текста на токены до синтаксического анализа.

> Фича зарегистрирована **2026-07-24** по запросу заказчика (правило 24 — каждый раздел документа заводится отдельной фичей); далее по правилу 17.

## Документирование (правило 24)

Наполняет раздел `book/src/02-lexical/index.typ` описанием языка (см. «Краткое описание»); примеры на Takt — в подкаталоге `02-lexical/examples/`, изображения — в `02-lexical/images/`. Раздел **описывает** уже реализованные возможности языка, синтаксис/семантику **не меняет**. В документ не попадают детали реализации, номера фич и процесс разработки.

## Итог (что сделано)

Наполнен раздел `book/src/02-lexical/index.typ` описанием **лексической
структуры** языка Takt (правило 24 — язык не меняется, Tier 3; ADR/анализ не
требовались):

- **Кодировка** (UTF‑8, Unicode в идентификаторах), **пробелы** (не значимы),
  **комментарии** (`//`, `///`, `/* */`; `////` — обычный строчный).
- **Идентификаторы** (`_`/`$`/Unicode‑буква в начале; регистр значим; кириллица
  допустима), таблица **ключевых слов** сгруппирована по назначению (сверена с
  таблицей `KEYWORDS` лексера, включая LTL‑буквы `X`/`F`/`G`/`U`/`R`).
- **Литералы**: целые (dec/hex, разделитель `_`, отрицательные), дробные
  (`.5`, экспонента `1e10`), **адресные** (`0x…:бит`), логические, строковые
  (`"…"`/`'…'`, экранирование `\`, `unicode"…"`).
- **Операторы** (арифметика/побитовые/логические/сравнения/`:=`) и **пунктуация**;
  явно разведены `:=` (присваивание) и `=` (равенство/связка).
- Пример `book/src/02-lexical/examples/tokens.takt` — **компилируется** `taktc`
  (цель `c`, EXIT 0), демонстрирует комментарии всех видов, dec/hex‑литералы с
  `_`, операторы `:=`/`=`/`+`.

**Живая проверка:** пример компилируется; `./scripts/precheck.sh` зелёный. Сборка
PDF локально не верифицируется (нет `mdbook`/`mdbook-pandoc`/`xelatex` — вне
`precheck.sh`, как и для 0101).

**Побочная находка (вне объёма 0102):** seed‑пример `book/src/01-overview/
examples/blinker.takt` (фича 0101) **не компилируется** — форма `start state Off`
недопустима (нужно `start Off`), а `model Blinker { … }` требует верхнеуровневого
входа `start Main = Blinker;` (либо состояния на верхнем уровне без обёртки
`model`). Занесено в бэклог `FEATURES.md` для фичи‑преемника 0101.
