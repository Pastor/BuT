# Фича: `Location::filename()` возвращает `"0"`, а не путь

- **Номер:** 0073
- **Статус:** ГОТОВО (реализовано и проверено-20; `precheck.sh` зелёный)
- **Зависит от:** **нет** (строится на закрытой 0053 — не блокирует)
- **Приоритет / Tier:** **Tier 3** — чистота API (ложное имя метода)
- **Крейт:** `grammar` (`diagnostics.rs`, тесты `parser_tests`/`ast_tests`)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`); следствие [позиции в диагностиках (`файл:строка:колонка`) и настоящий `file_no`](0053-diagnostics-file-id.md)

## Стадии жизненного цикла

Все стадии — **разделы этой карточки**: «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог».
Отдельным артефактом остаются только исправления —
[`docs/fixes/`](../fixes/README.md) (при необходимости `0073-YY-*`).

## Краткое описание

Метод даёт `format!("{}", file_no)` — то есть **номер строкой**, тогда как имя
обещает **путь**; `parser_tests.rs:944` это закрепляет ассертом
`loc.filename() == "0"`.

После [позиции в диагностиках (`файл:строка:колонка`) и настоящий `file_no`](0053-diagnostics-file-id.md) путь доступен честно
(`Diagnostic::file`), поэтому метод — **ловушка для следующего читателя**.

Лечение: переименовать/удалить, поправив тесты.

Выявлено в [ADR](0053-diagnostics-file-id.md#архитектура-adr) (A1).

> Фича зарегистрирована **2026-07-17** переводом кандидата из `FEATURES.md`
> (решение заказчика: «завести фичи по кандидатам, пока без проработки»).
> **Проработка не проводилась:** ADR, анализ, зависимости, Tier и объём — за
> стадиями 2–3. Текст ниже — **перенос находки кандидата** вместе с
> подтверждающими её пробами; это описание проблемы, а **не** принятое решение.

## Архитектура (ADR)

- **Status:** Accepted
- **Date:** 2026-07-20
- **Authors:** Архитектор + Дизайнер диагностик
- **Related issues:** [Фича](0073-location-filename-path.md); следствие [ADR](0053-diagnostics-file-id.md#архитектура-adr) (путь стал доступен честно) и [кросс-файловый переход к декларации — точный путь вместо угадывания](0056-lsp-goto-exact-file.md)

### Context

`Location::filename()` (`diagnostics.rs:645`) возвращает
`format!("{}", file_no)` — **номер файла строкой**, тогда как имя обещает
**путь/имя файла**. Doc-комментарий честен («строковое представление номера
файла»), но **имя метода лжёт**. Рядом (`diagnostics.rs:655`) уже живёт честно
названный `try_file_no() -> Option<String>` с **тем же** значением (отличие лишь:
`filename()` паникует на не-`Source`, `try_file_no()` даёт `None`).

Метод **не используется в продуктовом коде** — только в двух тестах:

- `parser_tests.rs:944` — `assert_eq!(loc.filename(), "0");`, причём **на
  следующей строке** уже есть `assert_eq!(loc.try_file_no(), Some("0"…))` — то же
  свойство проверено дважды;
- `ast_tests.rs:76` — тест `location_filename`, стоящий вплотную к тесту
  `try_file_no()`, проверяющему то же.

**Почему «сделать так, чтобы `filename()` возвращал путь» нельзя.** `Location`
несёт только `file_no: u32`; путь живёт в **отдельном** реестре `FileTable`
(`file_no → путь`), к которому `Location` доступа не имеет. Честный путь
берётся через `Diagnostic::file` (0053) или `FileTable::path`, а **не** из
`Location`. Значит `filename()` в принципе не может выполнить обещание имени —
он **ловушка для следующего читателя** (`loc.filename()` читается как «дай путь»,
а вернёт `"0"`).

### Decision Drivers

1. **Честность API имён.** Метод, чьё имя обещает путь, а возвращает номер, —
   источник ошибок; хуже, что честный аналог (`try_file_no`) уже есть.
2. **Отсутствие потребителей.** Ни строки продуктового кода; только тесты,
   дублирующие проверку `try_file_no`.
3. **Минимальность и обратимость.** Правка не трогает язык, семантику, кодоген;
   вывод генераторов байт-в-байт неизменен.

### Considered Options

#### Option A. Удалить `filename()`, тесты перевести на `try_file_no`

`filename()` убирается; две тестовые проверки удаляются как **дубли** (свойство
уже покрыто `try_file_no` рядом).

**Pros:**
- Убирает ложь и дублирование одним ходом; `try_file_no` остаётся единственным,
  честно названным аксессором номера.
- Тесты не теряют покрытия: `try_file_no` проверяет то же, вплотную.

**Cons:**
- Формально сужает `pub`-API. Приемлемо: потребителей вне тестов
  нет, крейт внутренний, карта фичи прямо санкционирует «переименовать/удалить».

#### Option B. Переименовать `filename()` → `file_no_string()` (честное имя)

**Pros:**
- Сохраняет метод под честным именем.

**Cons:**
- Становится **точным дублем** `try_file_no` (оба отдают номер строкой) — лишь
  паника вместо `Option`. Два аксессора одного значения — тот же запах, что
  устраняем; выбор «паникующий или нет» уже выражает пара `start()`/`try_file_no`.

#### Option C. Сделать, чтобы `filename()` возвращал путь

**Pros:**
- Имя перестало бы лгать буквально.

**Cons:**
- **Неосуществимо дёшево:** `Location` не знает пути (только `file_no`); путь — в
  `FileTable`. Пришлось бы протаскивать реестр в `Location` (или в каждый вызов) —
  несоразмерно задаче и против границы 0053 («реестр — деталь компиляции, в
  публичный API `Location` не выходит»).

### Decision

Принимается **Option A** — удалить `filename()`, две тестовые проверки убрать как
дубли `try_file_no`. `try_file_no` остаётся единственным честным аксессором
номера файла; путь берётся через `Diagnostic::file` / `FileTable::path` (0053).

### Consequences

#### Положительные

- Ложное имя и дублирование устранены; читатель `Location` больше не примет номер
  за путь.

#### Отрицательные / Action items

- **0073-01:** удалить `Location::filename()` (`diagnostics.rs`); в
  `parser_tests.rs` убрать строку `assert_eq!(loc.filename(), "0");` (свойство
  покрыто соседним `try_file_no`); в `ast_tests.rs` удалить тест
  `location_filename` (дубль теста `try_file_no`). Проверить, что `not_a_file()`
  остаётся востребованным другими аксессорами (`start`/`end`/…).
- **Версия языка не поднимается** — правка внутренняя (метод диагностики),
  синтаксис/семантика языка не меняются; вывод генераторов байт-в-байт неизменен.

#### Acceptance criteria

1. `grep filename() grammar/ simulation/` не даёт совпадений (метод и его вызовы
   удалены).
2. `try_file_no()` и остальные аксессоры `Location` работают без изменений.
3. `./scripts/precheck.sh` зелёный; вывод генераторов на корпусе неизменен.

## Анализ

### Цель и контекст

Убрать ложно названный метод `Location::filename()` (возвращает `file_no`
строкой, не путь). Честный аналог `try_file_no()` уже существует; путь после
[позиции в диагностиках (`файл:строка:колонка`) и настоящий `file_no`](0053-diagnostics-file-id.md#архитектура-adr) берётся из `Diagnostic::file` /
`FileTable`. Решение — **Option A** ADR (удалить). Правка внутренняя,
язык/семантика/кодоген не затрагиваются.

### Зависимости фичи

- **Зависит от:** **нет.** Метод изолирован (2 тестовых вызова, 0 продуктовых).
  Строится на закрытой 0053 (честный путь уже доступен) — не блокирует.
- **Влияние на порядок разработки:** ни одна фича очереди от 0073 не зависит;
  завершение никого не разблокирует. Может браться в любой момент.

### Требования и проверяемые условия

- **R1. Ложное имя устранено.** `Location::filename()` в коде отсутствует.
- **R2. Покрытие не потеряно.** Проверка «Location несёт правильный `file_no`»
  остаётся — через `try_file_no()` (стоит вплотную к удаляемым вызовам).
- **R3. Прочие аксессоры целы.** `try_file_no`/`start`/`end`/`exclusive_end` и
  `not_a_file()` (панический помощник) продолжают работать (последний
  востребован `start`/`end`/…).
- **R4. Ничего, кроме диагностики, не задето.** Вывод генераторов на корпусе
  байт-в-байт неизменен; версия языка не поднимается.

### Критерии приёмки и способ проверки

| # | Критерий | Способ проверки |
|---|---|---|
| A1 | `filename()` и его вызовы удалены | `grep -rn '\.filename()\|fn filename' grammar/ simulation/` (исключая `import.rs::filename_path`/`filename_found…` — иное имя) — пусто |
| A2 | Покрытие `try_file_no` сохранено | `parser_tests::location_methods` и `ast_tests` с `try_file_no` — зелёные |
| A3 | Прочие аксессоры и `not_a_file` целы | сборка + тесты `diagnostics`/`parser`/`ast` зелёные |
| A4 | Кодоген неизменен | `./scripts/precheck.sh` зелёный; `git diff examples/generated/` пуст |

### Особенности по обратной функциональности

Публичный API `grammar` **сужается** (удаляется `pub fn filename`). свод:
приемлемо — потребителей вне тестов нет, крейт внутренний, ADR санкционирует.
Язык, семантика, генераторы, симуляция — **не задеты**.

### Риски и зависимости

- **Скрытый потребитель вне workspace** (напр. плагин IntelliJ) — маловероятно
  (метод возвращает номер строкой, бесполезен снаружи); снимается grep'ом по
  всему репозиторию перед удалением.
- **`try_file_no` мог бы тоже считаться лишним** — **нет:** он честно назван и
  покрывает `Option`-случай; в объём 0073 его судьба не входит.

## Разработка

### Задача

#### Что было

`Location::filename()` (`grammar/src/diagnostics.rs:645`) возвращает
`format!("{}", file_no)` — **номер файла строкой** при имени, обещающем путь.
Рядом (`:655`) — честный `try_file_no() -> Option<String>` с тем же значением.
Метод зовут **только** два теста, дублирующих проверку `try_file_no`:

- `grammar/tests/parser_tests.rs:944` — `assert_eq!(loc.filename(), "0");`
  (строка `:945` уже проверяет `try_file_no() == Some("0")`);
- `grammar/tests/ast_tests.rs:76` — тест `location_filename`, стоящий вплотную к
  тесту `try_file_no()`.

#### Что сделано

Реализовано по **Option A** ADR:

1. **Удалён** `pub fn filename(&self) -> String` (`diagnostics.rs:643–650`) вместе
   с doc-комментарием.
2. **`parser_tests.rs`** — удалена строка `assert_eq!(loc.filename(), "0");`
   (свойство покрыто соседним `assert_eq!(loc.try_file_no(), Some("0"…))`).
3. **`ast_tests.rs`** — удалён тест `location_filename` (дубль теста `try_file_no`
   ниже).

`not_a_file()` **оставлен** — востребован `start`/`end`/`exclusive_end`.

**Статус по функциональности:**

| Функциональность | Работа | Обоснование |
|---|---|---|
| Диагностика (`Location`) | **да** | Удалён один `pub`-метод; API сужен (потребителей вне тестов нет) |
| Тесты `parser`/`ast` | **да** | Две дублирующие проверки удалены; покрытие держит `try_file_no` |
| Язык / семантика / кодоген / симуляция | **н/п** | Не затрагиваются; версия языка **0.2.0** без изменений |

#### Проверки

- `grep -rn '\.filename()\|pub fn filename' grammar/ simulation/` → пусто (кроме
  `import.rs::filename_path`/`filename_found_returns_content_and_path` — иное имя).
- `cargo test -- --test-threads=1` и `cargo test --features lsp -- --test-threads=1`
  → зелёные; `location_methods`, `ast_tests::*try_file_no*` — проходят.
- `./scripts/precheck.sh` зелёный; `git diff examples/generated/` пуст.

## Тест-план

### Область и цель

Проверить, что ложно названный `Location::filename()` удалён, покрытие
«`Location` несёт верный `file_no`» сохранено через `try_file_no()`, а прочая
диагностика и вывод генераторов не задеты.

### Проверки (условие → ожидаемый результат)

| # | Проверка | Предусловие | Ожидаемый результат | Ссылка на R/A |
|---|---|---|---|---|
| T1 | `filename()` удалён | дифф применён | `grep '\.filename()\|pub fn filename' grammar/ simulation/` — пусто (кроме `import.rs::filename_path`/`filename_found…`, иное имя) | R1 / A1 |
| T2 | `try_file_no` покрывает свойство | тот же дифф | `parser_tests::location_methods` → зелёный (проверяет `try_file_no() == Some("0")`); `ast_tests` с `try_file_no` → зелёный | R2 / A2 |
| T3 | Прочие аксессоры целы | тот же дифф | `start`/`end`/`exclusive_end`/`begin_range` в `location_methods` — зелёные; `not_a_file()` компилируется (востребован ими) | R3 / A3 |
| T4 | Регресс отсутствует | тот же дифф | `cargo test -- --test-threads=1` (+ `--features lsp`) зелёный | R3 / A3 |
| T5 | Кодоген неизменен | весь `examples/` | `git diff examples/generated/` пуст; проверка детерминизма 0048 | R4 / A4 |
| T6 | `precheck.sh` зелёный | все инструменты | `EXIT=0` | R4 / A4 |

### Разбивка проверок по функциональности

<!-- Легенда: ✅ пройдено · ❌ провалено · ⬜ не проверялось · — не применимо -->

| Функциональность | Условие | Статус |
|---|---|---|
| Диагностика (`Location`) | `filename()` удалён, `try_file_no` покрывает | ⬜ |
| Тесты `parser`/`ast` | без регресса, без потери покрытия | ⬜ |
| Кодоген всех целей | вывод байт-в-байт неизменен | — (не задет) |
| Язык / семантика | не задеты | — |

### Тестовые данные и окружение

- **Данные:** `Location::Source(0, 10, 20)` (`parser_tests::location_methods`),
  `Location::Source(7, 0, 0)` (`ast_tests`).
- **Окружение:** `cargo test -- --test-threads=1`; `cargo test --features lsp`;
  полный `./scripts/precheck.sh` (проверка детерминизма 0048).

## Отчёт о тестировании

### Резюме

Реализована подзадача (Option A ADR): ложно названный
`Location::filename()` удалён, две дублирующие тестовые проверки убраны.
Покрытие «`Location` несёт верный `file_no`» держит `try_file_no()`. **Все
проверки тест-плана пройдены; блокеров нет.**

**Окружение:** macOS (darwin 25.5), rustc/clippy nightly, `cargo test`
однопоточно, полный `./scripts/precheck.sh`.

### Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | `filename()` удалён | ✅ | `grep '\.filename()\|pub fn filename' grammar/ simulation/` — пусто (совпадения `import.rs::filename_path`/`filename_found…` — иное имя, не `Location`) |
| T2 | `try_file_no` покрывает свойство | ✅ | `parser_tests::location_methods` (`try_file_no() == Some("0")`) и `ast_tests::location_try_file_no_source` (`Some("42")`) — зелёные |
| T3 | Прочие аксессоры целы | ✅ | `start`/`end`/`exclusive_end`/`begin_range` — зелёные; `not_a_file()` компилируется (востребован ими) |
| T4 | Регресс отсутствует | ✅ | `cargo test --features lsp -- --test-threads=1` — зелёный |
| T5 | Кодоген неизменен | ✅ | `git diff examples/generated/` пуст; проверка детерминизма 0048 зелёный |
| T6 | `precheck.sh` зелёный | ✅ | `EXIT=0` (после обновления baseline размера `parser_tests.rs` 1893→1892 — долг фиксируется по факту) |

### Замечания

- `parser_tests.rs` ужат на 1 строку → запись в `scripts/module-size-baseline.txt`
  уменьшена (1893 → 1892), как требует храповик размера («долг фиксируется по
  факту, допустима только правка-уменьшение»).
- `ast_tests.rs` (737) и `diagnostics.rs` (979) под лимитом 1000 — в реестре не
  числятся, их сжатие проверок не требует.
- API `grammar` формально сужен (удалён `pub fn filename`) — потребителей вне
  тестов нет (санкционировано ADR).

## Итог (что сделано)

Ложно названный `Location::filename()` (возвращал `file_no` строкой при имени,
обещающем путь) **удалён** — принят **Option A** ADR. Метод был избыточен:
рядом живёт честно названный `try_file_no()` с тем же значением, и «сделать, чтобы
`filename()` возвращал путь» **неосуществимо** — `Location` несёт только `file_no`,
путь — в отдельном реестре `FileTable` (0053), доступном через `Diagnostic::file`.

Метод использовался **только** в двух тестах, дублировавших проверку `try_file_no`
(в `parser_tests` оба ассерта стояли на соседних строках); обе проверки удалены,
покрытие держит `try_file_no`. Правка внутренняя: язык, семантика, кодоген и
симуляция не затронуты, вывод генераторов **байт-в-байт неизменен**, версия языка
не поднята. Публичный API `grammar` формально сужен (потребителей вне тестов нет —
свод, санкционировано ADR).

Реестр размера обновлён: `parser_tests.rs` ужат на строку (1893 → 1892) — долг
фиксируется по факту. `precheck.sh` зелёный; отчёт —
[`Location::filename()` возвращает `"0"`, а не путь](0073-location-filename-path.md#отчёт-о-тестировании).
