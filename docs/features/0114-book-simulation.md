# Фича: Раздел документа «Симуляция»

- **Номер:** 0114
- **Статус:** ГОТОВО
- **Зависит от:** нет (инфраструктура `book/` заведена фичей [документ описания языка Takt (book/, mdBook)](0101-language-book.md))
- **Приоритет / Tier:** **Tier 3** (документация; раздел описывает уже реализованные возможности языка — сам синтаксис/семантику не меняет)
- **Связанные issue (анализ):** новая фича (правило 24: каждый раздел документа — отдельная фича; запрос заказчика 2026-07-24)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | не заведён (стадия 2) |
| Анализ | не заведён (стадия 3) |
| Разработка | [`docs/development/`](README.md) (задачи `0114-YY-*`) |
| Тест-план | [`0114-book-simulation.md#тест-план`](README.md) |
| Отчёт о тестировании | [`0114-book-simulation.md#отчёт-о-тестировании`](README.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0114-YY-*`) |

## Краткое описание

Описывает **симуляцию** модели: семантику исполнения симулятором, наблюдаемость состояний и значений, сохранение и загрузку состояния.

> Фича зарегистрирована **2026-07-24** по запросу заказчика (правило 24 — каждый раздел документа заводится отдельной фичей); далее по правилу 17.

## Документирование (правило 24)

Наполняет раздел `book/src/14-simulation/index.typ` описанием языка (см. «Краткое описание»); примеры на Takt — в подкаталоге `14-simulation/examples/`, изображения — в `14-simulation/images/`. Раздел **описывает** уже реализованные возможности языка, синтаксис/семантику **не меняет**. В документ не попадают детали реализации, номера фич и процесс разработки.

## Итог (что сделано)

Наполнен раздел `book/src/14-simulation/index.typ` описанием языка (правило 24 —
язык не меняется, Tier 3; ADR/анализ не требовались). Раздел **языковой** —
концептуальный, без команд `takt-sim` (те в разделе 16); трасса-поведение
допустима (иллюстрирует семантику модели):

- **Модель — исполнимая спецификация**; симуляция — **эталон** (по модели
  исполнения 11; все цели соответствуют, 13).
- **Окружение и наблюдаемость** — сценарий подаёт входные порты; на каждом такте
  видны состояние и все значения (полная прозрачность против «чёрного ящика»).
- **Инварианты в прогоне** — проверяются каждый такт, нарушение останавливает.
- **Сохранение/загрузка состояния** — контрольные точки / продолжение прогона.
- **Частые ошибки** (правило 26): `SIM-019` (extern с возвратом не вычисляется),
  `SIM-025` (нарушение инварианта в прогоне).
- Пример `watchdog.takt` — контрольный таймер; наглядна **наблюдаемость** скрытого
  счётчика `idle`. Компилируется (0 предупреждений) и **проверен симуляцией**
  (правило 27д/е): `idle` растёт до `TIMEOUT`, авария, квитирование `kick`.

**Живая проверка:** пример компилируется и симулируется; `./scripts/precheck.sh`
зелёный; сборка PDF (`make -C book build`) чистая. Язык не менялся → версия языка
та же.

