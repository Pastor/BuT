# Фича: Живой контекст: раздел критических инвариантов дублирует подводные камни

- **Номер:** 0238
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено аналитиком 2026-08-16, стадия 3)
- **Связанные issue (анализ):** запрос заказчика 2026-08-16 — «`CLAUDE.md` стал большим, убери лишнее»

## Стадии жизненного цикла

Все стадии — **разделы этой карточки**: «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог».
Отдельным артефактом остаются только исправления —
[`docs/fixes/`](../fixes/README.md) (при необходимости `0238-YY-*`).

## Краткое описание

`CLAUDE.md` читается целиком в начале каждой сессии, поэтому его объём — плата
постоянная. Раздел «Критические инварианты (не нарушать)», написанный до [сокращение живого контекста CLAUDE.md](0180-claude-md-context-diet.md), **целиком** повторял шесть пунктов
«Технических подводных камней» — только в прежней, вдвое более длинной редакции.
Те же дубли нашлись у толчейна с CI («Команды» ↔ «Документ и процесс») и у
правила форматтера.

> Фича зарегистрирована по запросу заказчика (2026-08-16); далее проходит
> жизненный цикл по своду.

## Документирование

**Не требуется** — язык не затронут: правка целиком в контекстном документе для
AI-инструментов, к описанию языка (`book/`) отношения не имеющем.

## Архитектура (ADR)

- **Status:** Accepted (Option B)
- **Date:** 2026-08-16
- **Authors:** Архитектор
- **Related issues:** [Фича](0238-claude-md-duplicate-invariants.md); запрос заказчика 2026-08-16 («`CLAUDE.md` стал большим, убери лишнее»); продолжает [ADR](0180-claude-md-context-diet.md#архитектура-adr) (формат пункта живого контекста), смежно с [проверка согласованности живого контекста `CLAUDE.md`](0149-claude-md-consistency-gate.md) (проверка согласованности)

### Context

`CLAUDE.md` читается **целиком в начале каждой сессии** — это его цена, и она
постоянная: 1376 строк, 144 КБ.

[сокращение живого контекста CLAUDE.md](0180-claude-md-context-diet.md) задала формат пункта —
**инвариант → чем платим за нарушение → контроль → ссылка на разбор**, — и
переписала в нём раздел «Технические подводные камни». Раздел **«Критические
инварианты (не нарушать)»**, написанный до неё, остался в прежней редакции. Из
чего и вырос дубль: шесть пунктов «инвариантов» **все до одного** повторены
ниже, в компактном формате.

#### Замер (2026-08-16)

| Инвариант | «Критические инварианты» | Дубль в «подводных камнях» |
|---|---|---|
| Условия рёбер `ref` не разрешаются | 24 строки | 9 строк |
| Разделение `Condition`/`Expression` | 11 | 4 |
| Операторы `:=` / `=` | 7 | 3 |
| Адрес порта (+ бит, ключ карты, экспорт) | 67 | 22 |
| Вход в стартовое состояние не расходует такт | 10 | 5 |
| Model-level `always` каждый такт | 15 | 16 |
| **Итого** | **134** | **59** |

Дубль не ограничен этим разделом:

- **Толчейн и CI.** Абзацы «Толчейн зафиксирован» и «CI прогоняет весь
  `precheck.sh`» в разделе «Команды» (48 строк прозы) повторены пунктами
  раздела «Документ и процесс» (14 строк).
- **Форматтер.** Правило «добавил узел АСД — добавь его печать» стоит и врезкой
  в «Добавление конструкции языка», и первым абзацем раздела «Форматтер».

**Дубль в живом контексте дороже обычного.** Два описания одного предмета
расходятся неизбежно (свод заведено ровно по этому прецеденту), а здесь
расхождение читается как **два разных факта об одном инварианте** — и решать,
какой из них свежее, придётся в каждой сессии заново.

### Decision Drivers

1. **Стоимость постоянная.** Файл читается каждую сессию целиком; лишние 10 КБ
   платятся всегда, а полезны — никогда.
2. **Уникальное знание удалять нельзя.** Пункты «подводных камней» — цена
   дефектов, найденных дорого. Диета обязана быть **вычитанием дублей**, а не
   сокращением по объёму.
3. **Формат уже выбран** (0180): инвариант → цена → контроль → ссылка. У дубля
   одна редакция ему следует, другая нет — выбор очевиден.
4. **Список «не нарушать» полезен сам по себе** — как чек-лист перед правкой
   ядра, а не как второй текст тех же пунктов.
5. **Проверка живого контекста построчный** (`check_crate_versions` ищет пару
   «крейт … `сейчас X.Y.Z`» в **одной** строке, кандидат из `FEATURES.md`):
   перенос этой записи через строку молча выключает сверку версий. Диета обязана
   её не тронуть.

### Considered Options

#### Option A. Удалить раздел «Критические инварианты» целиком

**Pros:** максимальная экономия, ноль дублей.

**Cons:** теряется чек-лист «не нарушать» — единственное место, где шесть
самых дорогих инвариантов названы **вместе**; читателю пришлось бы собирать их
по четырём разделам.

#### Option B. Раздел становится указателем, детали — в «подводных камнях»

Шесть строк вида «инвариант — одной фразой → см. пункт ниже»; уникальные факты
длинной редакции переносятся в соответствующие компактные пункты. Тем же
приёмом снимаются дубли «Команды ↔ Документ и процесс» и «Форматтер ↔
Добавление конструкции языка».

**Pros:**
- чек-лист сохраняется, а текст перестаёт существовать в двух редакциях;
- каждый инвариант описан **один раз** и в формате 0180;
- ни один уникальный факт не теряется — переносится.

**Cons:**
- требует построчной сверки двух редакций (иначе «диета» унесёт факт);
- указатель тоже надо сопровождать: новый критический инвариант обязан попасть
  в обе точки.

#### Option C. Свести всё в один длинный раздел «Инварианты»

**Pros:** одно место.

**Cons:** «подводные камни» тематически разбиты (семантика, типы, цели,
симулятор, процесс) — слияние в плоский список ухудшает поиск и мешает
формату 0180.

### Decision

Принимается **Option B**.

1. Раздел «Критические инварианты (не нарушать)» — **указатель**: одна строка на
   инвариант со ссылкой на подробный пункт.
2. Уникальные факты прежней редакции переносятся в компактные пункты:
   `semantic/reference.rs` — структура данных, а **не** запрещённый проход;
   цель `c` адрес **не** эмитит, `address` — жёсткое ключевое слово.
3. «Команды» — только команды плюс внешние зависимости с их **уникальными**
   оговорками (`-I` у `iec2c`, переменные `ensure-iec2c.sh`, `Cargo.lock`
   отслеживается, MSRV); толчейн и CI остаются одним описанием в «Документ и
   процесс».
4. «Форматтер» — про поведение форматтера; правило «добавил узел — добавь
   печать» живёт во врезке «Добавление конструкции языка».

### Consequences

#### Положительные

- Файл, читаемый каждую сессию, худеет на **135 строк** (1376 → 1241, −9.8 %) и
  **15 КБ** (144 245 → 128 894 байта, −10.6 %) без потери единого факта — замер
  после правки.
- Каждый инвариант описан один раз и в формате 0180 — расходиться нечему.

#### Отрицательные / Action items

- **Указатель — вторая точка сопровождения.** Новый критический инвариант
  придётся вписать и в чек-лист, и в тематический раздел. Машина этого не
  проверяет (проверка сверяет статусы, пути и версии, а не полноту чек-листа) —
  цена названа осознанно.
- Кто помнил текст по прежнему разделу, найдёт его ниже — формулировки
  компактной редакции короче.

#### Acceptance criteria

1. Ни один инвариант не описан в файле дважды.
2. Уникальные факты длинной редакции присутствуют (сверка по ключевым словам).
3. `scripts/check-claude-md.py` зелёный, запись о версиях крейтов осталась
   **однострочной**.
4. `./scripts/precheck.sh` зелёный.

## Анализ

### Цель и контекст

`CLAUDE.md` читается целиком в начале каждой сессии, поэтому его объём — плата
постоянная. Запрос заказчика (2026-08-16): «стал большим, убери лишнее».
**Лишнее** определено объективно: то, что в файле сказано **дважды**. Уникальные
факты — цена дорого найденных дефектов (формат 0180) — не сокращаются.

### Зависимости фичи

- **Зависит от:** нет. Продолжает [сокращение живого контекста CLAUDE.md](0180-claude-md-context-diet.md)
  (формат пункта) и [проверка согласованности живого контекста `CLAUDE.md`](0149-claude-md-consistency-gate.md) (проверка
  согласованности) — обе закрыты.
- **Влияние на порядок разработки:** ничего не блокирует и не разблокирует.

### Замеры (2026-08-16)

**З1. Дубль разделов.** Все шесть пунктов раздела «Критические инварианты (не
нарушать)» (134 строки) повторены в «Технических подводных камнях» (59 строк) —
таблица в ADR. Длинная редакция написана **до** 0180 и его формату не следует.

**З2. Дубль толчейна и CI.** Абзацы раздела «Команды» (48 строк прозы) повторены
пунктами «Документ и процесс» (14 строк).

**З3. Дубль правила форматтера.** «Добавил узел АСД — добавь его печать» стоит и
врезкой в «Добавление конструкции языка», и абзацем раздела «Форматтер».

**З4. Что уникально в длинной редакции** (проверено сверкой code-спанов: 207
идентификаторов удаляемого текста против нового файла):

| Факт | Решение |
|---|---|
| `semantic/reference.rs` — структура данных, а не запрещённый проход | **перенесён** |
| цель `c` адрес не эмитит; `address` — жёсткое ключевое слово | **перенесён** |
| контроль `hal_bit_range_tests.rs` (UB доказан исполнением через `mmap`) | **перенесён** |
| `Cargo.lock` отслеживается, `resolver = "3"`, MSRV 1.97 | **оставлен** в «Командах» |
| перечни `SE-048`…`SE-054`, `AM-001…006` | опущены: реестр диагностик — `docs/diagnostics/README.md` |
| `Token::ColonAssign`, `Chain14<…>`, `word_bytes_for_bit`, `address_map/export.rs`, `--emit svd` | опущены: детали реализации, живут в коде и карточках фич |

### Требования и проверяемые условия

- **R1. Ни один инвариант не описан дважды.**
- **R2. Уникальный факт не теряется** — либо переносится, либо его отсутствие
  обосновано (З4).
- **R3. Чек-лист «не нарушать» сохраняется** — как указатель на подробные пункты.
- **R4. Запись о версиях крейтов остаётся однострочной.** Проверка
  `check_crate_versions` ищет пару «крейт … `сейчас X.Y.Z`» в пределах одной
  строки (кандидат `FEATURES.md`, замер исправления); перенос через строку
  молча выключает сверку.
- **R5. Проверки зелёные** — `check-claude-md.py` и предкоммит.

### Критерии приёмки и способ проверки

| # | Критерий | Способ проверки |
|---|---|---|
| A1 | Дублей нет | сверка списков пунктов по разделам |
| A2 | Уникальные факты на месте | grep по 22 ключевым словам + сверка 207 code-спанов удалённого текста |
| A3 | Чек-лист есть | раздел «Критические инварианты» — указатель из шести строк |
| A4 | Версии крейтов однострочно | grep `сейчас .*0\.30\.0` в одной строке |
| A5 | Проверки зелёные | `scripts/check-claude-md.py`, `./scripts/precheck.sh` |

### Особенности по обратной совместимости

Код не затронут. Меняется **контекстный документ**, а не поведение инструментов;
версии крейтов и языка прежние.

### Риски

- **Р1. Диета уносит факт.** Снижение — A2: сверка не «на глаз», а по списку
  идентификаторов удалённого текста.
- **Р2. Указатель разъедется с разделами.** Машина полноту чек-листа не
  проверяет; цена названа в ADR как принятая.

## Разработка

### Задача

#### Что было

`CLAUDE.md` — 1376 строк, 144 245 байт, читается целиком каждую сессию. Три
места говорили одно и то же дважды: раздел «Критические инварианты» (134 строки)
против шести пунктов «подводных камней» (59 строк), абзацы о толчейне и CI в
«Командах» против пунктов «Документ и процесс», правило форматтера — во врезке и
в разделе.

#### Что сделано

1. **«Критические инварианты» — указатель**: шесть строк «инвариант → раздел, где
   он разобран». Чек-лист «не нарушать» сохранён, второй редакции текста нет.
2. **Уникальные факты перенесены** в компактные пункты (формат 0180):
   - `semantic/reference.rs` — структура данных `ReferenceNode`, а **не**
     запрещённый проход (имя обманчиво);
   - адрес потребляют только `c-hal`/`st-at`, цель `c` его **не эмитит**;
     `address` — жёсткое ключевое слово;
   - контроль диапазона бита — **значенческий** `hal_bit_range_tests.rs` через
     `mmap(MAP_FIXED)`: UB доказан исполнением, а не чтением.
3. **«Команды»** — команды, зависимости и их уникальные оговорки (`-I` у
   `iec2c`, переменные `ensure-iec2c.sh`, `Cargo.lock`/MSRV). Толчейн и CI
   остались одним описанием — в «Документ и процесс».
4. **«Форматтер»** — про поведение форматтера; правило «добавил узел — добавь
   печать» живёт во врезке «Добавление конструкции языка».

**Обратная функциональность:** код не затронут — правка целиком в
контекстном документе; версии крейтов и языка прежние.

#### Проверки

- `scripts/check-claude-md.py` — зелёный (1241 строка, расхождений нет).
- **Сверка потерь:** 207 code-спанов удаляемого текста прогнаны против нового
  файла; каждый отсутствующий разобран в таблице анализа (три перенесены,
  остальные — детали реализации и перечни кодов, живущие в коде и реестре
  диагностик).
- Замер: 1376 → 1241 строка (−135, −9.8 %), 144 245 → 128 894 байта (−10.6 %).
- `./scripts/precheck.sh` — зелёный.

## Тест-план

### Область и цель

Правка документа, поэтому проверки — сверки текста, а не прогоны кода. Главная
опасность диеты одна: **унести факт**. Против неё стоит T2 — механическая сверка
идентификаторов удаляемого текста, а не чтение глазами.

### Проверки (условие → ожидаемый результат)

| # | Проверка | Предусловие | Ожидаемый результат | Ссылка на R/A |
|---|---|---|---|---|
| T1 | Дублей нет | новый `CLAUDE.md` | ни один инвариант не описан дважды | R1 / A1 |
| T2 | Факты не потеряны | 207 code-спанов удалённого текста | каждый отсутствующий — либо перенесён, либо назван деталью реализации | R2 / A2 |
| T3 | Чек-лист сохранён | раздел «Критические инварианты» | шесть строк-указателей | R3 / A3 |
| T4 | Версия крейта — одной строкой | `grep -cE "takt-lang.*сейчас .?0\.30\.0"` | ровно 1 (иначе проверка версий молча слепнет) | R4 / A4 |
| T5 | Проверка живого контекста | `scripts/check-claude-md.py` | код 0, «расхождений нет» | R5 / A5 |
| T6 | Предкоммит | `./scripts/precheck.sh` | код 0 | R5 / A5 |

### Разбивка проверок по функциональности

| Функциональность | Статус | Обоснование |
|---|---|---|
| Код `takt-lang` / `takt-sim` | — | не затронут |
| Проверки документов (0146, 0149, 0160, 0177) | ✅ | прогоняются предкоммитом |

<!-- Легенда: ✅ пройдено · ❌ провалено · ⬜ не проверялось · — не применимо -->

### Тестовые данные и окружение

`git show HEAD:CLAUDE.md` как база сравнения; сверка спанов — разовым скриптом
(регулярное выражение по ``` `…` ``` в удалённых областях), результат в анализе.

## Отчёт о тестировании

### Резюме

**Пройдено. Фича готова к закрытию (`ГОТОВО`).** Живой контекст сократился на
**135 строк** (1376 → 1241, −9.8 %) и **15 351 байт** (144 245 → 128 894,
−10.6 %); ни один уникальный факт не потерян — три перенесены, остальные
отсутствующие идентификаторы разобраны поимённо.

### Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Дублей нет | ✅ | шесть пунктов «инвариантов» сведены к указателю; толчейн/CI и правило форматтера описаны по одному разу |
| T2 | Факты не потеряны | ✅ | из 207 code-спанов удалённого текста отсутствуют 93; после разбора: 3 перенесены, ~30 — артефакты многострочных спанов, остальные — детали реализации и перечни кодов (реестр диагностик) |
| T3 | Чек-лист сохранён | ✅ | шесть строк-указателей с адресом подробного пункта |
| T4 | Версия крейта — одной строкой | ✅ | ровно одно совпадение; проверка версий не ослеп |
| T5 | Проверка живого контекста | ✅ | «проверено строк, расхождений нет» |
| T6 | Предкоммит | ✅ | код 0 |

#### Что именно перенесено (T2)

| Факт | Куда |
|---|---|
| `semantic/reference.rs` — структура данных, а **не** запрещённый проход | пункт «Условия рёбер `ref` не разрешаются» |
| адрес потребляют только `c-hal`/`st-at`; цель `c` не эмитит; `address` — жёсткое ключевое слово | пункт «Адрес порта: три источника» |
| контроль `hal_bit_range_tests.rs` — значенческий, через `mmap(MAP_FIXED)` | пункт «Бит адреса — диапазон [0, 63]» |

Третий перенос — не косметика: формат 0180 требует **называть контроля**, а
компактный пункт называл только проверка `cc -c`, то есть доказательство
компиляцией. Доказательство **исполнением** едва не пропало вместе с длинной
редакцией.

### Результаты по функциональности

| Функциональность | Статус | Чем проверено |
|---|---|---|
| Код `takt-lang` / `takt-sim` | — | не затронут: правка целиком в контекстном документе |
| Проверки документов (0146, 0149, 0160, 0177) | ✅ | предкоммит |

### Выводы и дальнейшие шаги

1. Критерий диеты — **дубль**, а не объём: сокращение по объёму унесло бы цену
   дефектов, ради которой пункты и написаны (формат 0180).
2. **Резерв на будущее назван, но не тронут:** самые крупные разделы —
   «Цели генерации» (301 строка) и «Семантика и диагностика» (229) — состоят из
   уникальных пунктов; сокращать их можно только выносом **замеров** в карточки
   фич, а это меняет договорённость 0180 («чем платим за нарушение» — часть
   пункта). Решение за заказчиком.
3. **Указатель — вторая точка сопровождения**, и машина её не контролирует: новый
   критический инвариант придётся вписать и в чек-лист, и в тематический раздел.
   Заведено кандидатом в `FEATURES.md`.
4. Исправлений не потребовалось.

## Итог (что сделано)

Живой контекст похудел на **135 строк** (1376 → 1241, −9.8 %) и **15 351 байт**
(−10.6 %) — **вычитанием дублей**, а не сокращением по объёму: уникальные факты
суть цена дорого найденных дефектов и формату 0180 принадлежат.

- Раздел «Критические инварианты» стал **указателем** из шести строк: чек-лист
  «не нарушать» сохранён, второй редакции текста нет.
- Три уникальных факта длинной редакции перенесены в компактные пункты, среди
  них — контроль `hal_bit_range_tests.rs`, доказывающий отсутствие UB
  **исполнением** (компактный пункт называл только проверка компиляции).
- «Команды» оставлены командами и внешними зависимостями; толчейн и CI описаны
  один раз — в «Документ и процесс».

Потери проверены не глазами: 207 code-спанов удаляемого текста прогнаны против
нового файла, каждый отсутствующий разобран (перенесён либо назван деталью
реализации, живущей в коде и реестре диагностик). Подробности — в
[отчёте](0238-claude-md-duplicate-invariants.md#отчёт-о-тестировании); исправлений не
потребовалось.

Названная граница: указатель — **вторая точка сопровождения**, и машина её
не контролирует (проверка сверяет статусы, пути и версии, а не полноту чек-листа).
Заведено кандидатом.
