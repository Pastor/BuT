# Фича: У фичи нет ADR

- **Номер:** 0092
- **Статус:** ГОТОВО
- **Зависит от:** не проставлено — проставит аналитик на стадии анализа
- **Приоритет / Tier:** **Tier 3** (процесс/документация)
- **Крейт:** документация (`docs/adr/`)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`)

## Стадии жизненного цикла

Все стадии — **разделы этой карточки**: «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог».
Отдельным артефактом остаются только исправления —
[`docs/fixes/`](../fixes/README.md) (при необходимости `0092-YY-*`).

## Краткое описание

`docs/adr/0018-*` **не существует** — стадия 2 тогда не оформлялась. При этом
ссылки «по образцу» кочуют по проработкам и **вводят в заблуждение**:
читатель идёт за несуществующим документом.

Решение (ретроспективный ADR либо снятие ссылок) — за стадией 2.

Выявлено при [разделение переросших модулей (validate.rs, lsp.rs, c_expr.rs)](0027-module-size-split.md).

> **Пункт «по мере проработки фич–0020 заводить ADR/анализ/тест-планы» из
> процессного бэклога устарел и в фичу не переводится:** 0018, 0019 и 0020 —
> `ГОТОВО`, их артефакты заведены. Живым остатком того пункта является **только**
> отсутствие ADR у 0018, то есть эта фича.

> Фича зарегистрирована **2026-07-17** переводом пункта **процессного бэклога**
> `FEATURES.md` (решение заказчика: «завести фичи по кандидатам и процессному
> бэклогу, пока без проработки»). **Проработка не проводилась:** ADR, анализ,
> зависимости, Tier и объём — за стадиями 2–3. Приоритет процессной
> фичи определяется по влиянию на разблокировку других (критерий 3).

## Архитектура (ADR)

- **Status:** Accepted
- **Date:** 2026-07-23
- **Authors:** Архитектор + Системный аналитик
- **Related issues:** [Фича](0092-adr-0018-retrofit.md); касается
  [приведение кода к требованиям docs/CODE.md](0018-code-guidelines.md) (нет ADR); упоминания —
  [ADR](0027-module-size-split.md#архитектура-adr), [анализ 0027](0027-module-size-split.md#анализ).

### Context

`docs/adr/0018-*` не существует: [приведение кода к требованиям docs/CODE.md](0018-code-guidelines.md)
(«Приведение кода к требованиям `docs/CODE.md`» — рефакторинг под чек-лист Rust
Design Patterns) заводилась до того, как стадия 2 (ADR) стала оформляться.
Кандидат отмечал, что ссылки «по образцу» **вводят в заблуждение** (читатель
идёт за несуществующим ADR).

**Проверка по коду (grep `ADR 0018`/`adr/0018`/`образцу 0018`):** живых вводящих
ссылок практически нет — оба реальных упоминания **уже уточнены**:

- `0027-module-size-split.md#архитектура-adr`: «ADR у 0018 отсутствует … образцом структуры
  служит [ADR]»;
- `0027-module-size-split.md#анализ`: «У фичи **нет ADR** … Образцом ADR
  взят 0024, образцом карточки/анализа — 0018».

**Ключевой факт:** реестр `docs/features/README.md` требует ADR лишь у фич, **меняющих
архитектуру/синтаксис/семантику языка**. Фича — причёсывание кода под
`docs/CODE.md`; язык/архитектуру она **не меняет**, поэтому под требование ADR
**не подпадает**. То есть отсутствие у неё ADR — **норма по действующему
правилу**, а не долг.

### Decision Drivers

1. **Не плодить археологию.** Ретроспективный ADR постфактум реконструирует
   «решение» 0018 («применить `docs/CODE.md`») — очевидное и уже описанное в её
   анализе; риск выдумать несуществовавшую развилку.
2. **Согласованность с собственным правилом.** ADR — для меняющих язык/архитектуру
   (adr/README). 0018 не такова — заводить ей ADR значило бы противоречить правилу.
3. **Устранить именно боль:** вводящую ссылку, а не «пустоту ADR». Боль —
   «читатель идёт за несуществующим документом»; она уже снята уточнениями, надо
   лишь закрепить норму.

### Considered Options

#### Option A. Завести ретроспективный ADR

**Pros:** формально у 0018 появляется ADR; ссылки «по образцу» перестают вести в
пустоту.

**Cons:** археология (реконструкция решения постфактум, риск выдумки);
противоречит правилу «ADR — для меняющих язык/архитектуру»; 0018 таковой не
является; создаёт прецедент ретро-ADR для десятков до-процессных фич (0001–0017).

#### Option B. Не заводить ADR; закрепить норму + выверить ссылки (принята)

Признать, что отсутствие ADR у 0018 законно (фича язык не меняет), закрепить это
в реестре ADR явной заметкой, и убедиться, что ни одна ссылка не ведёт к
несуществующему `adr/0018-*` как к образцу ADR (образец ADR — 0024).

**Pros:** честно (никакой выдумки); согласовано с правилом; дёшево; снимает
именно боль (вводящую ссылку); годится для всех до-процессных фич сразу.

**Cons:** у 0018 так и нет ADR — но это и есть корректное состояние.

### Decision

Принимается **Option B**: ретроспективный ADR **не заводится**. В реестр
`docs/features/README.md` добавляется заметка: **до-процессные фичи (0001–0018) и
любые фичи, не меняющие архитектуру/синтаксис/семантику языка, ADR не имеют — это
норма, а не долг; ссылаться на такую фичу как на «образец ADR» нельзя (образец
ADR — [канонический форматтер.lam (lamc fmt)](0024-lam-formatter.md#архитектура-adr); образцом карточки/анализа 0018 служить
может).** Оставшиеся упоминания 0018 выверяются (уже уточнены в `adr/0027`/
`analyze/0027`). Язык/код не меняются.

### Consequences

#### Положительные

- Норма зафиксирована один раз для **всех** до-процессных/не-архитектурных фич —
  вопрос «почему у XXXX нет ADR» закрыт системно.
- Никакой реконструкции-выдумки; согласовано с действующим правилом ADR.

#### Отрицательные / Action items

- Заметка в `docs/features/README.md` (разработка 0092-01).
- Финальный `grep` — ни одна ссылка не ведёт к `adr/0018-*` как к образцу ADR.

#### Acceptance criteria

1. В `docs/features/README.md` есть заметка-норма про фичи без ADR (0018 в т.ч.).
2. `grep -rn "adr/0018"` по `docs/` — нет ссылок на несуществующий файл как на
   образец ADR (упоминания «у 0018 нет ADR» — допустимы, они и фиксируют норму).
3. Ретроспективный ADR не создаётся (решение зафиксировано в этом ADR).

## Анализ

### Цель и контекст

Устранить путаницу «у 0018 нет ADR, а ссылки ведут к несуществующему документу».
Решение (ADR, Option B): ретроспективный ADR **не заводить** (0018 язык не
меняет → под требование ADR не подпадает), а закрепить норму в реестре ADR и
выверить ссылки.

### Зависимости фичи

- **Зависит от:** нет. Чисто документная фича.
- **Влияние на порядок:** ничего не разблокирует.

### Требования и проверяемые условия

- **R1.** В `docs/features/README.md` — заметка-норма: фичи, не меняющие
  архитектуру/язык (и до-процессные 0001–0018), ADR не имеют; образец ADR — 0024.
- **R2.** Ни одна ссылка в `docs/` не ведёт к `adr/0018-*` как к **образцу ADR**
  (мета-упоминания «adr/0018 не существует» допустимы — они фиксируют норму).
- **R3.** Ретроспективный ADR не создаётся.

### Критерии приёмки и способ проверки

| # | Критерий | Способ |
|---|---|---|
| A1 | заметка-норма в реестре ADR | `grep "Фичи без ADR" docs/adr/README.md` |
| A2 | нет ссылок на adr/0018 как образец | `grep -rn "adr/0018" docs/` (только мета-упоминания) |
| A3 | ретро-ADR не заведён | `ls docs/adr/0018*` → отсутствует |

### Особенности по обратной функциональности

Только документация. Код/язык/крейты не затрагиваются.

### Риски и зависимости

- **Соблазн археологии** (Option A) — отвергнут: реконструкция решения
  постфактум = выдумка; противоречит правилу «ADR — для меняющих язык/архитектуру».
- Находка: реальных вводящих ссылок почти не было — `adr/0027`/`analyze/0027`
  уже уточнены ранее; фича закрепляет норму, а не чинит массовую путаницу.

## Разработка

### Задача

#### Что было

`docs/adr/0018-*` не существует; ссылки «по образцу» могли вести к пустоте.
При проверке оказалось: `adr/0027`/`analyze/0027` уже уточнены («у 0018 нет ADR,
образец — 0024»), но **норма** «фичи без ADR — это законно» нигде не зафиксирована.

#### Что сделано

В `docs/features/README.md` (после вводного абзаца) добавлена **заметка-норма** (ADR, Option B): до-процессные фичи (0001–0018) и фичи, не меняющие
архитектуру/синтаксис/семантику языка, ADR **не имеют** — это корректно по
действующему правилу реестра; ссылаться на них как на **образец ADR** нельзя
(образец — 0024); ретроспективные ADR постфактум не заводятся.

Ретроспективный `docs/adr/0018-*` **не создаётся** (решение ADR). Только
документация — код/язык не затрагиваются.

#### Проверки

- `grep "Фичи без ADR" docs/adr/README.md` — заметка присутствует.
- `grep -rn "adr/0018" docs/` — только мета-упоминания «не существует» + уже
  уточнённый `analyze/0027`; ни одной ссылки на `adr/0018-*` как на образец.
- `ls docs/adr/0018*` — отсутствует (ретро-ADR не заведён).

## Тест-план

### Область и цель

Документная фича: проверки — grep-инспекции реестра ADR и ссылок (свод про
язык неприменимо — синтаксис/семантика не затрагиваются).

### Проверки (условие → ожидаемый результат)

| # | Проверка | Ожидаемый результат | R/A |
|---|---|---|---|
| T1 | `grep "Фичи без ADR" docs/adr/README.md` | заметка-норма присутствует | R1/A1 |
| T2 | `grep -rn "adr/0018" docs/` | только мета-упоминания «не существует» + уточнённый `analyze/0027`; нет ссылки на образец | R2/A2 |
| T3 | `ls docs/adr/0018*` | отсутствует (ретро-ADR не заведён) | R3/A3 |
| T4 | ссылки Markdown целостны | нет битых путей в правках | — |

### Разбивка проверок по функциональности

| Функциональность | Статус |
|---|---|
| Документация (реестр ADR) | ✅ (T1–T4) |
| Код / язык / крейты | — (не затрагивается) |

<!-- Легенда: ✅ пройдено · — не применимо -->

### Тестовые данные и окружение

grep/ls по `docs/`. Изменяемый файл — `docs/features/README.md` (заметка-норма).
Примеров/контрпримеров языка нет (фича не про язык).

## Отчёт о тестировании

### Резюме

**Готова к закрытию (ГОТОВО).** Ретроспективный ADR **не заводится** (ADR, Option B): 0018 язык/архитектуру не меняет → под требование ADR не
подпадает, отсутствие ADR у неё законно. Норма закреплена в реестре ADR; ссылки
выверены. Только документация — код/язык/крейты не затронуты.

### Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | заметка-норма в реестре ADR | ✅ | «Фичи без ADR — норма, а не долг» добавлена |
| T2 | нет ссылок на adr/0018 как образец | ✅ | только мета-упоминания + уточнённый `analyze/0027` |
| T3 | ретро-ADR не заведён | ✅ | `docs/adr/0018*` отсутствует |
| T4 | целостность Markdown-ссылок | ✅ | правки без битых путей |

### Результаты по функциональности

- **Документация (реестр ADR)** — ✅.
- **Код / язык** — не затрагивались.

### Выводы и дальнейшие шаги

Фича закрывается. Находка: реальных вводящих ссылок почти не было (`adr/0027`/
`analyze/0027` уже уточнены ранее) — фича закрепила **норму** для всех
до-процессных/не-архитектурных фич, а не чинила массовую путаницу. Исправлений
(`docs/fixes/`) не потребовалось.

## Итог (что сделано)

**Закрыта (`ГОТОВО`, 2026-07-23).** Развилка «ретроспективный ADR либо снятие
ссылок» разрешена в пользу **Option B** ([ADR](0092-adr-0018-retrofit.md#архитектура-adr)):
ретроспективный ADR **не заводится**.

- **Ключевой факт:** реестр ADR требует ADR лишь у фич, **меняющих**
  архитектуру/синтаксис/семантику языка. Фича — рефакторинг под `docs/CODE.md`,
  язык не меняет → под требование ADR **не подпадает**; отсутствие у неё ADR —
  **норма, а не долг**.
- **Сделано:** в `docs/features/README.md` добавлена **заметка-норма** «Фичи без ADR —
  норма, а не долг» (покрывает все до-процессные 0001–0018 и не-архитектурные
  фичи; образец ADR — 0024). Ретро-ADR не создан.
- **Находка:** реальных вводящих ссылок почти не было — `adr/0027`/`analyze/0027`
  уже уточнены ранее («у 0018 нет ADR, образец — 0024»). Фича закрепила **норму**
  системно, а не чинила массовую путаницу.

Только документация — код/язык/крейты не затронуты; версии не поднимаются. Ни
одна фича от 0092 не зависела.
