# Фича: Раздел документа «Практический пример: процессор» (шины, кеш, ядра)

- **Номер:** 0186
- **Статус:** ГОТОВО
- **Зависит от:** [параметризация моделей (ключевое слово `parameter`)](0185-model-parameters.md) — настройка ядра параметрами (`Core(id := 1)`); фича **закрыта** 2026-07-30
- **Tier:** предлагается 3 (документная; приоритет утверждает заказчик)
- **Связанные issue (анализ):** запрос заказчика 2026-07-30: «полное и детальное описание работы процессора при помощи языка: модель — память, модель — процессор, подмодель декодер, конвейер и т.д.»; **уточнён им же 2026-07-30**: добавить **шину данных**, **шину кода** и **кеш**; процессор сделать **ядром** («их можно сколько угодно»), в примере — **два ядра с общей шиной памяти**; название раздела — **«Практический пример: процессор»**

## Стадии жизненного цикла

Все стадии — **разделы этой карточки**: «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог».
Отдельным артефактом остаются только исправления —
[`docs/fixes/`](../fixes/README.md) (при необходимости `0186-YY-*`).

## Краткое описание

Новый раздел документа `book/src/20-processor/` — **«Практический пример:
процессор»** (название дано заказчиком; продолжает семейство раздела 18
«Практический пример»). Сквозной пример вычислительной машины, описанной
средствами Takt: учебный 8-битный процессор с памятью, **двумя шинами**,
**кешем**, конвейером и **несколькими ядрами**. Каждая часть машины — модель или
подмодель языка:

- **`Memory`** — модель памяти: хранение программы и данных (массив `[u8; N]`),
  протокол обращения — адрес/данные/строб (общие переменные);
- **шина кода** и **шина данных** — два **раздельных** протокола (гарвардская
  схема внутри ядра): выборка команды и обращение к данным идут своими наборами
  общих переменных, и в трассе видно, что в одном такте заняты обе;
- **`Cache`** — кеш команд ядра: теги, попадание/промах, заполнение строки при
  промахе; попадания и промахи наблюдаемы в трассе;
- **`Core`** — **ядро** (а не «процессор целиком»): регистры (аккумулятор `acc`,
  счётчик команд `pc`, флаг нуля `zf`), машинный цикл fetch → decode → execute,
  подмодель **`Decoder`** (`enum` опкодов + `match`) и свой кеш. Ядер может быть
  **сколько угодно** — они появляются композицией `Core | Core`, а различаются
  **параметрами** (`Core(id := 0)`);
- **общая шина памяти** нескольких ядер и **арбитр** доступа: два ядра, один порт
  памяти — кто-то ждёт, и это видно в трассе;
- **конвейер** — параллельная композиция `|` стадий внутри ядра с передачей через
  общие переменные: пока Execute исполняет команду N, Fetch выбирает команду N+1.

Раздел показывает язык «в сборе»: типы и перечисления, массивы, `match`,
композиция обоих видов, общие переменные, параметры моделей, функции — на
предмете, знакомом каждому инженеру. Полный перечень команд учебного процессора,
программа-пример (например, сумма массива), потактовые трассы (кеш, конвейер,
арбитраж двух ядер) — обязательные части раздела.

> Фича заведена **по запросу заказчика 2026-07-30**, **уточнена им же в тот же
> день** (шины, кеш, ядра, название раздела), проработана до стадии разработки
> (ADR + анализ) и **взята в разработку по его решению** («продолжай с 0186»,
> 2026-07-30) — с решениями ADR, вынесенными на согласование: кеш **команд**
> (когерентность двух кешей не нужна по построению) и ступень с двумя ядрами,
> **не транслируемая целью `sv`**.

**Граница, названная проработкой честно.** Ядро с вложенным кешем,
настраиваемое параметрами, живёт в режиме генерации **по умолчанию**
(`--parameters=assign`): режим `specialize` отвергает специализацию модели с
собственными вложенными моделями (`SE-087`), а цель `sv` в режиме `assign`
отвергает разные наборы аргументов у одной модели (`SV-016`). Значит ступень с
двумя ядрами транслируется целями `c`/`st`/`rust` и исполняется симулятором, но
**не** целью `sv`; раздел говорит об этом текстом. Снятие ограничения —
отдельная работа (кандидат: специализация модели с вложенными моделями).

## Документирование

Фича **и есть** документирование: новый раздел `book/src/20-processor/`
(каждый раздел — отдельная фича). Примеры раздела — свои
`.takt`-файлы в каталоге раздела, проверяемые проверкой книги (0133: компиляция
целью `c` + симуляция). В корпус `examples/` пример **не** входит (иной
предмет: корпус — регрессия компилятора, книга — обучение языку); перенос в
корпус возможен отдельной фичей, если пример окажется полезен как регрессия.

## Архитектура (ADR)

- **Status:** Proposed (на согласовании заказчика; редакция 2026-07-30 после уточнения)
- **Date:** 2026-07-30
- **Authors:** Архитектор + Аналитик
- **Related issues:** [Фича](0186-book-processor-example.md); опирается на [ADR](0182-pid-library-and-application.md#архитектура-adr) (протокол через общие переменные), [фичу](0185-model-parameters.md) (параметры моделей), [фичу](0133-book-examples-gate.md) (проверка примеров книги)

### Context

Заказчик просит раздел документа с **полным и детальным описанием работы
процессора** средствами языка: модель памяти, модель процессора, подмодель
декодера, конвейер. Это самый крупный сквозной пример книги: он обязан показать
язык «в сборе» и при этом остаться читаемым и проверяемым проверкой книги (0133 —
каждый пример компилируется целью `c` и симулируется).

#### Уточнение заказчика 2026-07-30 (после первой редакции ADR)

1. **Добавить шину данных, шину кода и кеш.** То есть машина перестаёт быть
   «процессор + массив»: обращение к памяти становится **протоколом на шине**, а
   между ядром и шиной появляется **кеш**.
2. **Процессор — это ядро**, и ядер «можно сколько угодно»; в примере — **два
   ядра с общей шиной памяти**. Значит нужна модель `Core`, множимая композицией,
   различение экземпляров (**параметры**) и **арбитраж** общей шины.
3. **Название раздела — «Практический пример: процессор»** (семейство раздела 18
   «Практический пример»).

Уточнение меняет не объём текста, а **состав моделей**: единиц становится больше,
и каждая обязана быть объяснима одной конструкцией языка, иначе раздел
превращается в курс архитектуры ЭВМ.

#### Что язык уже умеет (по закрытым фичам, без новых проб)

| Нужно примеру | Есть в языке | Фича |
|---|---|---|
| память как массив | `[u8; N]`, исполняется симулятором, инициализация в `_init` | 0076, 0029-05 |
| опкоды | `enum` с явными значениями | 0005 |
| декодер | `match` по опкоду | 0004 |
| стадии цикла | последовательная композиция `+` | 0057, 0181 |
| конвейер | параллельная композиция `\|`, общие переменные | 0003, 0059, 0079 |
| протокол память↔процессор | общие переменные корня  | 0182 |
| настройка машины | параметры моделей | 0185 |
| наблюдаемость | `--save-state`, потактовая трасса симулятора | 0032 |

#### Ограничения, которые определяют размер машины

- **Регистр языка:** без указателей, без косвенной адресации через выражения
  общего вида — адрес в память подаётся значением переменной;
- **элемент массива читается по индексу-выражению** (0076) — этого достаточно
  для `mem[pc]` и `mem[addr]`;
- **проверка книги** прогоняет пример без сценария входов (`-n 10`): программа
  обязана исполняться сама и завершаться (или устойчиво работать) — как
  `batch_cycle`;
- **свод**: книга описывает язык, не реализацию компилятора; раздел —
  про то, как язык выражает процессор, а не про архитектуру ЭВМ вообще.

### Considered Options

#### Option A. Однотактная машина: цикл fetch→decode→execute как автомат (без конвейера)

Одна модель `Processor` с состояниями `Fetch`/`Decode`/`Execute`; команда
исполняется за три такта.

**Pros:** проще всего читается; трасса очевидна.
**Cons:** не показывает конвейер — а он назван заказчиком прямо. Отвергается
как **единственная** форма, но остаётся в разделе как первая ступень изложения.

#### Option B. Конвейерная машина: стадии параллельной композицией, машинный цикл внутри стадий

`Processor = Fetch | Decoder | Execute`; между стадиями — регистры конвейера
(общие переменные: `if_ir`, `id_op`, `id_arg`); память — отдельная модель,
подключённая на общих переменных (`addr`/`rdata`/`wdata`/`wen`). Каждая стадия —
маленький автомат; хазарды сняты упрощением ISA (команды одинаковой длительности,
без переходов внутри конвейерного примера — переходы показываются на ступени A).

**Pros:** конвейер настоящий (стадии работают одновременно — семантика `|`);
регистры конвейера — честные общие переменные; каждая стадия остаётся простой.
**Cons:** сложнее объяснить порядок исполнения ветвей `\|` внутри такта —
разделу придётся проговорить модель памяти такта явно (это плюс для книги).

#### Option C. Диаграммный раздел без исполняемого примера

**Cons:** нарушает практику книги (примеры проверяются) и запрос
заказчика («описание работы процессора»). Отвергается.

#### Option D. Шины и кеш как **протоколы на общих переменных** (а не новые сущности языка)

Шина кода — набор `code_addr`/`code_data`/`code_req`/`code_ack`; шина данных —
`data_addr`/`data_rdata`/`data_wdata`/`data_wen`/`data_ack`; кеш — модель `Cache`
со своими массивами тегов и строк, говорящая с шиной кода тем же протоколом.
Арбитр — модель `Arbiter` (или состояние `Memory`), выбирающая, чей запрос
обслужить в этом такте.

**Pros:**
- ни одной новой конструкции языка не требуется — приём проверен фичей
  (протокол объявляется моделью, замыкается применением);
- «шина» становится **наблюдаемой**: занятость, ожидание, арбитраж видны в
  потактовой трассе — учебно ценнее любой картинки;
- гарвардская схема (две шины) объясняет, зачем композиция `|`: обе шины заняты в
  одном такте.

**Cons:** протокол на общих переменных требует дисциплины «кто пишет, кто читает»
— раздел обязан её проговорить (это и есть содержание ступени).

#### Option E. Кеш **команд** у каждого ядра; кеш данных и когерентность — вне раздела

**Pros:** программа в учебной машине неизменна (самомодифицирующегося кода нет),
поэтому двум ядрам **не нужна когерентность** — тема, которая утроила бы объём.
Попадание/промах при этом остаются наблюдаемыми, а рассказ о кеше — полным.
**Cons:** кеш данных — самая частая ожидаемая тема; раздел обязан честно сказать,
что он вне границ, и предложить упражнением.

#### Option F. Различение ядер: параметры (0185) против двух отдельных моделей

- **Параметры** (`Core(id := 0) | Core(id := 1)`) — одна модель, множится
  свободно, «сколько угодно ядер» выражено буквально. Цена **измерена пробой**
  2026-07-30: режим `--parameters=specialize` отвергает ядро с вложенным кешем
  (`SE-087`), а цель `sv` в режиме `assign` отвергает разные наборы аргументов у
  одной модели (`SV-016`). То есть ступень с двумя ядрами транслируется целями
  `c`/`st`/`rust` и исполняется симулятором, но не целью `sv`.
- **Две отдельные модели** `Core0`/`Core1` — работают везде, но это копипаста, и
  «сколько угодно ядер» превращается в «сколько скопируешь».

Выбор — параметры: раздел книги учит языку, а не обходу ограничений цели;
ограничение называется текстом.

### Decision (предлагается, редакция 2026-07-30)

**Раздел `book/src/20-processor/` под названием «Практический пример: процессор»,
изложение четырьмя ступенями, каждая — исполняемый файл** (Options A + B + D + E +
F):

1. **Ступень 1 — ядро, память и две шины** (Options A + D): `Memory` + `Core` с
   состояниями `Fetch → Decode → Execute`, подмодель `Decoder`; обращение к
   памяти — **протоколами двух шин**: шина кода (выборка команды) и шина данных
   (`LDA`/`STA`). ISA из 8 команд (`NOP`, `LDA addr`, `STA addr`, `ADD addr`,
   `SUB addr`, `JMP addr`, `JNZ addr`, `HLT`); регистры — `acc`, `pc`, `zf`.
   Программа-пример — сумма массива; раздел приводит потактовую трассу, в которой
   видно, что шины заняты **раздельно**.
2. **Ступень 2 — кеш команд** (Option E): модель `Cache` между ядром и шиной
   кода: массив тегов и строк, попадание/промах, заполнение строки при промахе.
   Трасса показывает первый проход цикла (промахи) и второй (попадания) — и тем
   объясняет, зачем кеш вообще нужен.
3. **Ступень 3 — конвейер внутри ядра** (Option B): стадии
   `Fetch | Decode | Execute` параллельной композицией, регистры конвейера —
   общие переменные; команды перехода вынесены (о хазардах — текстом, со ссылкой
   на ступень 1). Раздел показывает занятость стадий по тактам.
4. **Ступень 4 — два ядра с общей шиной памяти** (Options D + F):
   `Core(id := 0) | Core(id := 1)`, у каждого ядра **свой кеш**, общая шина
   памяти и модель `Arbiter` — в такте обслуживается один запрос, второе ядро
   ждёт. Трасса показывает такт ожидания и очередь обслуживания. Ядра различаются
   **параметрами**  — так «сколько угодно ядер» выражено языком, а не
   копированием модели.

Настройка ядра — параметрами (`id`, начальный `pc`); **размер памяти и кеша
остаётся константами**: параметризация размеров за границей 0185, и раздел
говорит это прямо.

Файлы примера: `machine.takt` (ступень 1), `cache.takt` (2), `pipeline.takt` (3),
`dual_core.takt` (4). Общий `isa.takt` не заводится, пока проба не покажет
необходимость (импорт между примерами книги уже есть у `pid_loop`).

#### Что остаётся за границей фичи

- **кеш данных и когерентность двух кешей** — упражнение; в разделе кеш
  **команд**, а программа неизменна, поэтому когерентность не нужна (Option E);
- прерывания, стек, подпрограммы — упомянуть как упражнение;
- хазарды и форвардинг конвейера — только текстом;
- перенос примера в корпус `examples/` — отдельная фича при желании заказчика;
- **цель `sv` для ступени 4**: два ядра с разными аргументами уплощение не
  выражает (`SV-016`), а `--parameters=specialize` отвергает ядро с вложенным
  кешем (`SE-087`) — проверено пробой 2026-07-30. Ступени 1–3 целью `sv`
  транслируются; ступень 4 — нет, и раздел это называет. Снятие ограничения —
  кандидат («специализация модели с вложенными моделями»).

### Consequences

**Положительные:** самый полный сквозной пример книги; показывает совместную
работу почти всех конструкций языка; шины, кеш, конвейер и арбитраж делают
семантику `|` и общих переменных **наблюдаемой**, а параметры моделей (0185)
получают предметную демонстрацию — «сколько угодно ядер» одной строкой.

**Отрицательные / риски:** объём раздела наибольший в книге, и уточнение его
увеличило — риск нечитаемости (смягчение: четыре самодостаточные ступени, каждая
со своим файлом и своей трассой); пример живёт только в книге и не защищает
компилятор регрессией (смягчение: проверка компилирует и симулирует каждый
файл); ISA придумана — раздел обязан явно сказать, что она учебная; ступень 4
целью `sv` не транслируется — ограничение названо текстом, а не скрыто.

#### Acceptance criteria (черновик, уточняет анализ)

1. **Все четыре** файла раздела компилируются целью `c` и симулируются проверкой
   книги (0133).
2. Программа-пример ступени 1 завершает вычисление (сумма массива) — значение
   аккумулятора в трассе совпадает с арифметикой вручную; в трассе видно
   раздельное обращение по шине кода и по шине данных.
3. Ступень 2: в трассе первого прохода цикла — промахи кеша, второго —
   попадания; счётчики попаданий/промахов наблюдаемы.
4. Ступень 3 демонстрирует одновременность стадий: в трассе виден такт, где
   заняты все три стадии разными командами.
5. Ступень 4: два ядра исполняют свои программы; в трассе виден такт, где арбитр
   обслужил одно ядро, а второе ждало; оба ядра доходят до `HLT`.
6. Раздел зарегистрирован в `SUMMARY.md` под названием «Практический пример:
   процессор», проверки глифов и ссылок зелёные.
7. PDF собирается (`make -C book build`).

## Анализ

### Цель и контекст

Реализовать решение ADR (редакция 2026-07-30 после уточнения заказчика):
раздел `book/src/20-processor/` под названием **«Практический пример: процессор»**
с **четырьмя** исполняемыми ступенями — ядро с двумя шинами (`machine.takt`), кеш
команд (`cache.takt`), конвейер стадий (`pipeline.takt`), два ядра с общей шиной
и арбитром (`dual_core.takt`). Язык **не меняется** — версия языка не растёт;
меняется только документ и его примеры.

### Требования и проверяемые условия

- **R1. Ступень 1 — ядро, память и две шины.** Модель `Memory` (массив
  `[u8; N]`) + модель `Core` (состояния `Fetch`/`Decode`/`Execute`, подмодель
  `Decoder`); обращение к памяти — **двумя протоколами**: шина кода
  (`code_addr`/`code_data`/`code_req`/`code_ack`) и шина данных
  (`data_addr`/`data_rdata`/`data_wdata`/`data_wen`/`data_ack`), оба на общих
  переменных. ISA из 8 команд; программа-пример «сумма массива»
  исполняется симулятором и завершается (`HLT`).
- **R2. Ступень 2 — кеш команд.** Модель `Cache` между ядром и шиной кода:
  массивы тегов и строк, попадание/промах, заполнение строки при промахе;
  счётчики попаданий и промахов — наблюдаемые переменные. В трассе первого
  прохода цикла — промахи, второго — попадания.
- **R3. Ступень 3 — конвейер.** `Fetch | Decode | Execute` параллельной
  композицией; регистры конвейера — общие переменные; в потактовой трассе есть
  такт, где все три стадии заняты разными командами.
- **R4. Ступень 4 — два ядра с общей шиной памяти.**
  `Core(id := 0) | Core(id := 1)` (различение — **параметрами**), у
  каждого ядра свой кеш; модель `Arbiter` обслуживает в такте **один** запрос. В
  трассе есть такт, где одно ядро обслужено, а второе ждёт; оба ядра доходят до
  `HLT`.
- **R5. Верность программы.** Значение аккумулятора после `HLT` равно сумме,
  посчитанной вручную; трассы в тексте раздела совпадают с трассами симулятора
  (копируются из прогона, не сочиняются — правило «зонд, потом проверка»).
- **R6. Проверка книги.** **Все четыре** файла компилируются целью `c` и симулируются
  (`taktc compile` + `takt-sim -n …`); специальных исключений в проверке
  не появляется.
- **R7. Текст раздела.** Объясняет: почему каждая часть машины — модель; почему
  шина — это **протокол**, а не сущность языка; семантику `+` и `|` на предмете;
  модель памяти такта (когда стадия видит запись соседа); зачем ядру кеш (на
  трассе, а не на словах); как параметры дают «сколько угодно ядер»; границы
  примера (учебная ISA; кеш данных и когерентность, хазарды, прерывания —
  текстом и упражнениями).
- **R8. Граница целей названа.** Раздел прямо говорит: ступени 1–3
  транслируются всеми целями, **ступень 4 — не целью `sv`** (уплощение не
  выражает два экземпляра с разными аргументами). Молчание здесь недопустимо:
  читатель, повторивший пример для FPGA, получил бы отказ без объяснения.
- **R9. Регистрация.** `SUMMARY.md` (название «Практический пример: процессор»),
  проверки глифов/ссылок, сборка PDF.

### Ограничения, которые определяют форму решения

- **О1. Проверка книги прогоняет примеры без сценария входов** (0133): программа
  обязана исполняться сама. Программа зашивается в ROM-массив инициализатором;
  входных портов пример не заводит.
- **О2. Инициализатор массива.** Содержимое ROM задаётся агрегатным
  инициализатором массива; symулятор исполняет массивы (0076), цель `c`
  инициализирует их в `_init` (0029-05). Записать программу в массив длиной
  N с хвостом нулей — проверить пробой на стадии разработки: если агрегат
  обязан перечислить все элементы, N выбирается равным длине программы + данных.
- **О3. Пределы `-n` проверки.** Сумма массива на учебной машине стоит
  ~5 тактов/команда × ~15 команд — прогону нужно больше 10 тактов; проверка
  читает число тактов из шапки примера (как у `fan.takt` с `--tick-hz`) —
  проверить и при необходимости расширить проверка **не** исключением, а числом
  тактов (исключения с причиной — только для некомпилируемых standalone).
- **О4. Имена.** Ловушки кодогена, найденные 0182: порт ≠ имя состояния;
  корневая модель берётся из имени файла (`machine.takt` → корень `Machine` —
  не совпадает с `Memory`/`Processor`); модель ≠ имя стандартной библиотеки IEC
  (`Memory` в IEC свободно — проверить пробой `iec2c`; книга целью `st` не
  проверяется, но пример не должен закладывать известную мину).
- **О5. Порядок ветвей `|` внутри такта.** Ветви исполняются последовательно в
  порядке объявления (эталон — цель `c`: под-модели тикают по очереди);
  конвейер ступени 2 обязан быть корректен при этом порядке — регистр конвейера
  читается стадией-потребителем **до** записи стадией-производителем в том же
  такте либо раздел явно проговаривает выбранную дисциплину (защёлкивание на
  следующем такте). Это учебно ценно: то же различие `always_ff`/`always_comb`
  раздел 14 показывает для `sv`.
- **О6. свод.** Никаких номеров фич и деталей реализации компилятора в
  тексте раздела.
- **О7. Ядро с вложенным кешем и параметрами живёт только в режиме умолчания.**
  Проба 2026-07-30 (`Core` с параметром `id` и вложенной моделью `Cache`,
  `Core(id := 0) | Core(id := 1)`): цель `c` в режиме `assign` — код 0; режим
  `--parameters=specialize` — отказ `SE-087` («специализация модели с вложенными
  моделями»); цель `sv` в `assign` — отказ `SV-016` («экземпляры одной модели
  делят регистры»). Значит ступень 4 собирается умолчанием и целью `sv` не
  транслируется (R8). Альтернатива «две отдельные модели `Core0`/`Core1`»
  ограничение снимает, но уничтожает смысл ступени («сколько угодно ядер»
  превращается в «сколько скопируешь») — отвергнута ADR.
- **О8. Когерентность кешей не нужна по построению.** Кеш — **команд**, а
  программа в памяти неизменна (самомодифицирующегося кода в ISA нет), поэтому
  два кеша не могут разойтись. Это не упрощение «чтобы не делать», а свойство
  выбранной ISA — раздел обязан сказать это прямо и вынести кеш данных в
  упражнение.
- **О9. Арбитраж обязан быть наблюдаемым, а не «правильным».** Учебная ценность —
  в том, что видно **ожидание**: счётчик отказов/ожиданий у каждого ядра и
  дисциплина (по кругу либо приоритет) названы текстом. Оптимальность арбитра —
  не предмет раздела о языке.
- **О10. Два одноимённых экземпляра трудно различать в наблюдении.**
  Квалифицированная адресация портов (`Модель::порт`) **не** различает два
  экземпляра одной модели — известное ограничение, с которым уже столкнулась
  задача (там сверка шла через снимок `--save-state`). Для ступени 4
  трассу двух ядер читать снимком состояния; проверить пробой в 0186-01, иначе
  критерий A6 нечем измерить.
- **О11. Протокол шины требует дисциплины такта.** «Кто пишет — кто читает» на
  общих переменных решается порядком ветвей `|` (О5). Для двух шин и арбитра
  дисциплина обязана быть **одной и той же** и описанной до кода: иначе ступени
  1–4 будут вести себя по-разному, и раздел научит неверному.

### Критерии приёмки и способ проверки

| # | Критерий | Способ проверки |
|---|---|---|
| A1 | Ступень 1 исполняется и завершается (R1) | `takt-sim book/src/20-processor/machine.takt -n <N>` → `HLT`, `acc` = сумма |
| A2 | Две шины работают раздельно (R1) | трасса: такт, в котором заняты и шина кода, и шина данных |
| A3 | Кеш наблюдаем (R2) | трасса: промахи на первом проходе цикла, попадания на втором; счётчики совпадают с числом обращений |
| A4 | Конвейер одновременен (R3) | трасса: такт с тремя занятыми стадиями (разные `pc` у стадий) |
| A5 | Два ядра и арбитраж (R4) | снимок состояния (`--save-state`, О10): такт, где одно ядро обслужено, второе ждёт; оба доходят до `HLT` |
| A6 | Трассы в тексте = трассы симулятора (R5) | копия из прогона; сверка при ревью |
| A7 | Проверка книги зелёный (R6) | `./scripts/precheck.sh` |
| A8 | Граница целей названа (R8) | текст раздела содержит оговорку о ступени 4 и цели `sv`; проба отказа приложена к отчёту |
| A9 | Раздел собран и зарегистрирован (R9) | `SUMMARY.md`, `make -C book build`, проверки глифов/ссылок |

### Предлагаемая декомпозиция разработки (задачи не заведены)

| Задача | Содержание |
|---|---|
| 0186-01 | пробы: агрегатный инициализатор ROM (О2), число тактов проверки (О3), имена (О4), наблюдаемость двух одноимённых ядер (О10), дисциплина такта на шине (О11); ISA, протоколы шин и программа-пример на бумаге |
| 0186-02 | `machine.takt` — ступень 1: память, ядро, декодер, **две шины**; прогон, трасса |
| 0186-03 | `cache.takt` — ступень 2: кеш команд (теги, промах/попадание, заполнение строки), счётчики, трасса двух проходов цикла |
| 0186-04 | `pipeline.takt` — ступень 3: конвейер стадий, дисциплина регистров конвейера (О5, О11), трасса занятости |
| 0186-05 | `dual_core.takt` — ступень 4: два ядра параметрами (0185), общая шина, `Arbiter`; трасса ожидания; проба отказа цели `sv` для оговорки R8 |
| 0186-06 | текст раздела `20-processor/index.md` (четыре ступени, границы, упражнения), SUMMARY, PDF, проверки |

### Особенности по обратной совместимости

Язык, компилятор и корпус не меняются; версия языка не растёт. Единственный
разделяемый артефакт — проверка книги: если понадобится задать примеру число тактов,
правка проверки обязана быть аддитивной (существующие примеры не перечитываются).

### Риски

- **Риск нечитаемого объёма.** Самый большой раздел книги, и уточнение его
  увеличило (шины, кеш, арбитр, второе ядро). Снижение: **четыре**
  самодостаточные ступени, каждая — свой файл и своя трасса; перечень команд и
  трассы — таблицами; каждая ступень добавляет **одну** идею.
- **Риск «курса архитектуры ЭВМ вместо языка».** Шины, кеш и арбитраж — темы, о
  которых легко писать без единой строки Takt. Снижение: О6 (каждый абзац привязан
  к конструкции) и правило «сначала код ступени, потом текст о нём».
- **Риск, что ступень 4 не соберётся ни в одном полезном режиме.** Проба
  2026-07-30 показала: `assign` + цель `c` — работает; `specialize` — `SE-087`;
  цель `sv` — `SV-016` (О7). Значит риск **снят замером**, а не надеждой, и
  остаточная часть — оговорка в тексте (R8). Если заказчик захочет ступень 4 и в
  `sv`, это отдельная фича (снятие `SE-087`), а не правка примера.
- **Риск неверной дисциплины такта на шине.** Протокол на общих переменных при
  последовательном порядке ветвей `|` легко сделать «работающим через такт» —
  трасса окажется вдвое длиннее ожидаемой и объяснение развалится. Снижение: О11
  — дисциплина одна на все ступени, описана до кода, проверена пробой 0186-01.
- **Риск неисполнимости конвейера при последовательном порядке ветвей (О5).**
  Конвейер может «съедать» команду за такт неверно. Снижение: дисциплина
  защёлкивания описана до кода; проба на стадии 0186-01; сверка трассы.
- **Риск упереться в отсутствие конструкции.** Например, агрегат ROM (О2) или
  ширина индекса. Снижение: пробы первой задачей; при находке — кандидат в
  фичи, пример адаптируется, а не молчит.
- **Риск смешения предметов.** Соблазн рассказывать архитектуру ЭВМ вместо
  языка. Снижение: каждый абзац привязан к конструкции Takt (О6).

## Разработка

### Задача

#### Что было

Анализ назвал пять вопросов, ответы на которые определяют форму примера, и
потребовал снять их **пробами до кода** (О2, О3, О4, О10, О11). Без ответов
раздел строился бы на догадках о том, что язык умеет.

#### Что сделано

##### Ответы проб (2026-07-30)

| # | Вопрос анализа | Ответ пробы | Следствие для примера |
|---|---|---|---|
| О2 | Как задаётся содержимое ПЗУ | Агрегат — **фигурные скобки**: `var rom: [u8;8] := {1, 2, …};`. Форма `[1, 2, …]` не разбирается (`SY-002`). Инициализатор обязан перечислить **все** элементы: `{1,2,3}` для `[u8;8]` → `CC-017` | размер массива равен длине программы; «хвост нулей» пишется явно |
| — | Индекс массива | **Только литерал или имя**: `mem[pc]` — да, `mem[pc + 1]` — `SY-002` («ожидалось `]`») | адрес обязан вычисляться в переменную заранее — а это ровно **адресный регистр шины**, то есть ограничение совпало с предметом |
| О3 | Хватит ли проверке книги `-n 10` | Проверка требует от симуляции **кода возврата 0**, а не завершения: прогон, упёршийся в лимит, — успех | правки проверки не нужны; машине можно быть длиннее десяти тактов |
| О4 | Имена | `Memory` в IEC 61131-3 свободно (`iec2c` принимает FB `ScalarMemory`). `step` — занято (`ST-014`), как и предупреждает живой контекст | имена ядра и памяти безопасны; «шаг» называть `cursor`/`phase` |
| О10 | Как различить два одноимённых ядра | Снимок `--save-state`: `{"kind":"Parallel","children":[{…"id":1},{…"id":2}]}` — экземпляры различаются структурно | трассы двух ядер в разделе читаются снимком, а не печатью шага |
| О11 | Дисциплина такта на шине | При `Core \| Memory` ветви идут в порядке объявления: ядро выставляет адрес, память отвечает **в том же такте**, но ядро прочитает ответ **в следующем** | получается честная синхронная память с задержкой в один такт — и трёхтактный машинный цикл вытекает из неё, а не назначается |

##### Находка вне объёма (записана кандидатом)

**Массив как общая переменная в цель `st` не транслируется**: `iec2c` отвергает
передачу массива в `FUNCTION_BLOCK` («Data type incompatibility … when invoking
FB»). Скалярная общая переменная работает. Для раздела значения не имеет — проверка
книги проверяет цель `c` и симуляцию, — но обещать «пример собирается всеми
целями» нельзя.

##### Система команд (учебная, 8-битная)

Команда — пара «опкод + аргумент», хранится **двумя массивами** (`prog_op`,
`prog_arg`), поэтому память отдаёт команду целиком за одно обращение. Это
упрощение названо в тексте раздела: в настоящей машине это две ячейки.

| Опкод | Мнемоника | Действие |
|---|---|---|
| 0 | `NOP` | ничего |
| 1 | `LDA a` | `acc := ram[a]` |
| 2 | `STA a` | `ram[a] := acc` |
| 3 | `ADD a` | `acc := acc + ram[a]` |
| 4 | `SUB a` | `acc := acc - ram[a]` |
| 5 | `JNZ a` | если `acc ≠ 0`, то `pc := a` |
| 6 | `HLT` | останов |

**Индексного регистра и косвенной адресации в ISA нет** — сознательно: адрес
операнда берётся из самой команды. Поэтому «сумма массива» из первой редакции ADR
невыразима без самомодификации кода, и программой-примером взято **умножение
сложением** — цикл, который складывает одно слагаемое ровно `n` раз. Косвенная
адресация — упражнение раздела.

##### Программа-пример

```text
0: LDA 0   ; acc := sum
1: ADD 1   ; acc += x
2: STA 0   ; sum := acc
3: LDA 2   ; acc := n
4: SUB 3   ; acc -= 1
5: STA 2   ; n := acc
6: JNZ 0   ; пока n ≠ 0 — на начало
7: HLT
```

Данные: `ram = {sum = 0, x = 7, n = 3, one = 1}`; ожидаемый результат —
`ram[0] = 21`.

#### Проверки

Все пробы воспроизводимы файлами в 5–20 строк; ни одна не опирается на чтение
кода компилятора.

- **Программа исполняется и даёт верный ответ** (проба ступени 1):
  `takt-sim -n 80` — машина доходит до `Halted`, снимок памяти
  `ram = [21, 7, 0, 1]`, то есть `7 × 3 = 21`, счётчик обнулён.
- **Цель `c`** принимает эту же модель (`taktc compile -t c`).
- Ограничение индекса и форма агрегата проверены **отказами** компилятора
  (`SY-002`, `CC-017`), а не догадкой.

Соответствие анализу: сняты О2, О3, О4, О10, О11; ISA и программа зафиксированы.
Следующая задача — `0186-02`: модель ступени 1 в каталоге раздела.

### Задача

> **Правка после замечания заказчика:** структура примеров переработана —
> модели вынесены в библиотеку `machine_lib.takt`, ступени стали сборками из
> импорта и параметров. См. [исправление](../fixes/0186-01-example-decomposition.md).

#### Что было

Пробы и система команд зафиксированы задачей
[раздел документа «Практический пример: процессор» (шины, кеш, ядра)](0186-book-processor-example.md#разработка), исполняемой модели у раздела ещё не
было.

#### Что сделано

`book/src/20-processor/examples/machine.takt` — первая ступень раздела: **одно
ядро, память и две шины**.

**Две шины — два набора общих переменных** (приём: протокол объявляет одна
сторона):

- шина кода — `code_addr` → `code_op`, `code_arg`;
- шина данных — `data_addr`, `data_wdata`, `data_wen` → `data_rdata`.

Память (`Memory`) обслуживает **обе** в одном такте: отдаёт команду по адресу
кода и одновременно читает (а при стробе — пишет) ячейку данных. Это и есть
предметный смысл двух шин: выборка команды не мешает обращению к данным.

**Ядро (`Core`) — трёхтактный машинный цикл** `Fetch → Decode → Execute`.
Три такта здесь **не назначены, а вытекают** из дисциплины шины, снятой пробой
О11: ядро выставляет адрес, память отвечает в том же такте, но ядро прочитает
ответ лишь в следующем. Поэтому `Fetch` только выставляет `code_addr`, `Decode`
забирает пришедшую команду и выставляет адрес операнда, а `Execute` работает уже
с пришедшими данными.

**Декодер** на этой ступени — состояние `Decode` плюс `match` по опкоду в
`Execute`; отдельной моделью-стадией он становится в конвейере (ступень 3). Образцы `match` записаны **мнемониками** (`Lda`, `Sta`, `Add`, `Sub`,
`Jnz`), а не числами: варианты перечисления работают в образцах, и форматтер их
сохраняет (проба).

**Программа** — «умножение сложением» (`sum := x * n`), её листинг приведён
комментарием рядом с массивами: читатель видит и мнемоники, и числа, которыми они
лежат в памяти.

**Останов ядра не завершает прогон**: `Halted` — терминальное состояние ядра,
но память продолжает обслуживать шины, поэтому композиция живёт. Для проверки книги
это законно (нужен код возврата 0, а не завершение), и предметно верно: память
не «заканчивается» оттого, что процессор встал.

#### Проверки

```sh
./target/debug/taktc compile -t c book/src/20-processor/examples/machine.takt -o out/
./target/debug/takt-sim -n 80 --save-state m.json book/src/20-processor/examples/machine.takt
./scripts/precheck.sh
```

- **Программа считает верно**: снимок памяти после останова —
  `ram = [21, 7, 0, 1]`, то есть `sum = 7 × 3 = 21`, счётчик `n` обнулён,
  слагаемое и единица не тронуты. Ядро в состоянии `Halted`, `pc = 8`.
- **Проверка книги**: файл компилируется целью `c` и симулируется (`-n 10`).
- **Форматтер**: файл в каноне (`fmt --check`).
- Раздела `index.md` у каталога пока нет — текст пишется задачей; на
  проверки это не влияет (проверка книги обходит `.takt`-файлы, `SUMMARY.md` — главы).

Соответствие анализу: закрыта первая половина **R1** (ядро, память, две шины,
ISA, программа); кеш — 0186-03, конвейер — 0186-04, два ядра — 0186-05.

### Задача

> **Правка после замечания заказчика:** структура примеров переработана —
> модели вынесены в библиотеку `machine_lib.takt`, ступени стали сборками из
> импорта и параметров. См. [исправление](../fixes/0186-01-example-decomposition.md).

#### Что было

Ступень 1 ([раздел документа «Практический пример: процессор» (шины, кеш, ядра)](0186-book-processor-example.md#разработка)) работала с памятью,
отвечающей за один такт. Кеша не было, и объяснять его было не на чем: при
мгновенной памяти он бесполезен.

#### Что сделано

`book/src/20-processor/examples/cache.takt` — вторая ступень: между ядром и шиной
кода встал **кеш команд**, а память команд стала **медленной**.

**Медленная память — условие задачи, а не украшение.** Память отвечает по шине
кода через `latency` тактов ожидания (`parameter latency: u8 := 3;` — настройка
сборки): шина получила квитирование `code_req` → `code_ready`. Без
этого кеш нечем оправдать: при памяти «ответ за такт» он только добавлял бы такт
(измерено — см. ниже).

**Кеш прямого отображения**, четыре массива на строку: `tags`, `valid`, `ops`,
`args`. Номер строки — `fetch_addr % 8`, тег — `fetch_addr / 8`.

**Попадание обслуживается прямо в состоянии `Lookup`, тем же тактом** — за
этим кеш и ставят. Промах уходит в состояние `Miss` (ждёт память) и `Fill`
(заполняет строку и отдаёт команду). Первая редакция отвечала на попадание
отдельным состоянием — и попадание стоило ровно столько же, сколько промах: кеш
«работал», счётчики росли, а трасса выигрыша не показывала. Дефект нашёлся
замером, а не чтением.

**Ядро ждёт кеш** — рукопожатие `fetch_req` → `fetch_ready`. Запрос снимается в
том же такте, в котором принят ответ (`if fetch_ready = 1 { fetch_req := 0; }` в
теле `Fetch`), иначе кеш успевает обслужить уже удовлетворённый запрос ещё раз и
счётчик попаданий врёт — тоже найдено замером.

**Строк столько же, сколько команд** (8): вытеснения в этой программе не
случается, все промахи — «холодные», первого прохода. Что бывает, когда программа
в кеш не помещается, — упражнение раздела; механизм тега при этом настоящий и
проверяется каждым обращением.

#### Проверки

```sh
./target/debug/takt-sim -n 400 --save-state c.json book/src/20-processor/examples/cache.takt
./scripts/precheck.sh
```

- **Счёт верен**: `ram = [35, 7, 0, 1]` — программа `sum := x * n` при `x = 7`,
  `n = 5` даёт 35; счётчик обнулён.
- **Кеш наблюдаем**: `hits = 28`, `misses = 8` — восемь холодных промахов
  (по числу команд), дальше только попадания.
- **Выигрыш измерен, а не заявлен** (та же программа, та же медленная память):

  | Вариант | Тактов до останова |
  |---|---|
  | ядро ↔ медленная память напрямую (проба) | **252** |
  | ядро ↔ кеш ↔ медленная память (`cache.takt`) | **192** |

  При `n = 3` разница была всего 18 тактов (154 против 136) — поэтому в примере
  оставлено `n = 5`: выигрыш кеша обязан быть виден, а не подразумеваться.
- **Ступень 1 сверена на той же программе**: `machine.takt` (быстрая память,
  кеша нет) — останов на 108-м такте, `ram = [35, 7, 0, 1]`. Обе ступени считают
  одно и то же и отличаются только устройством памяти.
- **Проверка книги**: файл компилируется целью `c` и симулируется; форматтер — канон.

Соответствие анализу: закрыт **R2** (кеш, попадание/промах, счётчики, трасса двух
проходов); текст ступени.

### Задача

> **Правка после замечания заказчика:** структура примеров переработана —
> модели вынесены в библиотеку `machine_lib.takt`, ступени стали сборками из
> импорта и параметров. См. [исправление](../fixes/0186-01-example-decomposition.md).

#### Что было

Ступени 1 и 2 показывали машину, у которой стадии машинного цикла — **состояния
одного** автомата: в каждом такте работает ровно одна. Конвейер — то, ради чего
в разделе вообще нужна параллельная композиция, — показан не был.

#### Что сделано

`book/src/20-processor/examples/pipeline.takt` — три стадии стали **тремя
моделями**, работающими одновременно; между ними — регистры конвейера (общие
переменные `if_*` и `id_*`).

**Дисциплина защёлкивания — несущее решение ступени.** Ветви параллельной
композиции исполняются в порядке объявления, поэтому стадии перечислены **от
конца к началу**:

```takt
start Machine = Execute | Decode | Fetch | Memory;
```

Так каждая стадия читает регистр, записанный соседом на **предыдущем** такте.
Обратный порядок (`Fetch | Decode | Execute`) дал бы «конвейер», в котором
команда пролетает все три стадии за один такт, — то есть не конвейер вовсе.
В аппаратуре тот же приём называется «читаем старое, пишем новое»; раздел 14
показывает его же для цели `sv` (`always_ff` против `always_comb`).

**Занятость стадий наблюдаема**: каждая стадия несёт с собой номер команды
(`if_pc`, `id_pc`, `ex_pc`) и признак `*_valid`. В трассе видно, как конвейер
заполняется, работает и опустошается.

**Переходов в программе нет намеренно** (решение ADR): конвейер с переходами
требует разговора о хазардах и очистке — он ведётся текстом раздела. Программа
прямолинейна: `sum := x + y + z`.

**Останов дренирует конвейер**: выборка, увидев `HLT`, уходит в `Drained` и
перестаёт подавать команды; исполнение доигрывает то, что уже в конвейере, и
встаёт в `Halted`.

#### Проверки

```sh
./target/debug/takt-sim -n 12 --save-state p.json book/src/20-processor/examples/pipeline.takt
./scripts/precheck.sh
```

- **Одновременность стадий (критерий A4)** — такты 4–6 трассы: в одном такте
  `if_pc = 2`, `id_pc = 1`, `ex_pc = 0` (и далее 3/2/1, 4/3/2), то есть **три
  разные команды** в трёх стадиях, все три `*_valid = 1`.
- **Счёт верен**: `acc` идёт 7 → 12 → 21, память после записи —
  `ram = [21, 7, 5, 9]`, то есть `7 + 5 + 9 = 21`.
- **Дренаж**: с 7-го такта выборка в `Drained`, с 8-го исполнение в `Halted`;
  трасса дальше неподвижна.
- **Проверка книги**: компилируется целью `c` и симулируется; форматтер — канон.

**Находка, записанная кандидатом**: `taktc fmt` **выносит комментарий из тела
блока наружу** — пояснение, стоявшее первой строкой внутри `always { … }`, после
форматирования оказывается после закрывающей скобки и читается как пояснение к
`ref`. Воспроизведено пробой в четыре строки; занесено в `FEATURES.md`. В
примерах раздела обойдено: комментарий ставится **перед** `always`.

Соответствие анализу: закрыт **R3** (конвейер, регистры конвейера, дисциплина
такта, трасса занятости); два ядра — 0186-05, текст — 0186-06.

### Задача

#### Что было

После [исправления](../fixes/0186-01-example-decomposition.md) модели жили в
библиотеке, но были рассчитаны на **одного** мастера шины: линии `fetch_*` и
`bus_*` — скаляры, и два экземпляра ядра писали бы в одни и те же имена.
«Ядер сколько угодно» оставалось словами.

#### Что сделано

**Линии стали массивами по номеру ядра.** У каждого мастера свои
`fetch_*[id]`, `arb_*[id]`, `data_*[id]`; общая на всю машину — только шина
памяти (`bus_*`), за неё ядра и спорят. Индекс — **имя параметра** ядра, поэтому
одна и та же модель множится сколько угодно раз и её экземпляры не дерутся за
общие имена.

**Число ядер в объявлениях фиксировано двумя** — это ограничение языка, а не
замысла: размер массива обязан быть константой, параметром его не задать (фича закрыта, но параметризация **размеров** за её границей). Больше ядер —
длиннее строки объявлений; модели не меняются.

**Заведён арбитр** (`Arbiter`): пускает на общую шину один запрос за раз и
**чередует** ядра (право первым получает тот, кого обслуживали не последним),
считая такты ожидания каждого в `waits`. Шина освобождается и по ответу памяти, и
по снятию запроса — без второго условия арбитр держал бы шину со старевшим
запросом (найдено прогоном: машина считала верно, но каждая команда платила
полную задержку памяти).

**Ядро получило два параметра**: `id` — номер (по нему выбираются линии) и
`base` — начало своей области данных. Программа у ядер **общая** (она в ПЗУ), а
данные — свои: ядро 0 считает `7 * 5`, ядро 1 — `7 * 3`. Так одна программа над
разными данными занимает два ядра.

**Память данных двухпортовая** — по порту на ядро; спорят ядра только за команды.
Это упрощение названо в тексте библиотеки.

**Сборки ступеней** (моделей ни одна не копирует):

| Ступень | Сборка |
|---|---|
| 1 | `Core(id := 0, base := 0) \| Cache(id := 0, enabled := 0) \| Arbiter \| Memory(latency := 0)` |
| 2 | то же, но `enabled := 1` и `latency := 3` |
| 3 | `StageExecute \| StageDecode \| StageFetch \| Memory(latency := 0, program := 1)` |
| 4 | два ядра, два кеша, арбитр, `Memory(latency := 3)` |

Конвейерная ступень подключена к шине **напрямую**: мастер один, делить
нечего. Попытка провести её через арбитр показала, зачем это важно: стадия
выборки рассчитана на ответ следующим тактом, а арбитр отвечает через два-три —
номера команд начали проскакивать (`if_pc`: 0 → 2 → 5). Ошибка найдена прогоном и
устранена не «подгонкой», а возвратом к честной схеме.

#### Проверки

```sh
./target/debug/takt-sim -n 900 --save-state d.json book/src/20-processor/examples/dual_core.takt
./scripts/precheck.sh
```

- **Оба ядра считают своё и верно**: `ram = [35, 7, 0, 1, 21, 7, 0, 1]` — окно
  ядра 0 даёт `7 * 5 = 35`, окно ядра 1 — `7 * 3 = 21`; оба в `Halted`.
- **Ядра идут в разном темпе**: ядро 1 (меньше итераций) встало на 158-м такте,
  ядро 0 — на 208-м.
- **Спор за шину наблюдаем**: `waits = [0, 5]` — ядро 1 суммарно простояло пять
  тактов, ожидая шину; первый такт ожидания виден уже на третьем шаге.
- **Кеши раздельны**: у ядра 0 — 28 попаданий и 8 промахов, у ядра 1 — 14 и 8
  (у него меньше итераций).
- **Ступени 1–3 после перевода на линии ядер**: 325 тактов (`ram[0] = 35`),
  209 тактов (`hits = 28`, `misses = 8`, `ram[0] = 35`), 8 тактов
  (`ram[0] = 13`) — все считают верно.
- **Проверка книги**: все четыре ступени компилируются целью `c` и симулируются;
  `machine_lib.takt` — библиотечный; форматтер — канон; `precheck.sh` — код 0.

Соответствие анализу: закрыт **R4** (два ядра, общая шина, арбитр, наблюдаемое
ожидание). Остался текст раздела.

### Задача

#### Что было

Четыре ступени работали (задачи…05 и [исправление](../fixes/0186-01-example-decomposition.md)),
но раздела как текста не существовало: каталог `book/src/20-processor/` содержал
только примеры и в `SUMMARY.md` не значился.

#### Что сделано

`book/src/20-processor/index.typ` — раздел **«Практический пример: процессор»**
(название дано заказчиком), зарегистрирован в `SUMMARY.md` после раздела о
библиотеке.

Порядок изложения — от сборки к деталям: из чего собрана машина (библиотека и
четыре сборки) → система команд и программа → четыре ступени → границы →
упражнения. Каждая ступень объясняется **конструкцией языка**, а не архитектурой
ЭВМ вообще: шина — протокол на общих переменных; трёхтактный цикл — следствие
договора о такте; конвейер — параллельная композиция с порядком ветвей «от конца
к началу»; «ядер сколько угодно» — параметры и линии, индексируемые номером ядра.

**Все числа в тексте сняты с прогонов**: 324 такта у ступени 1, 208 у ступени 2
(при `hits = 28`, `misses = 8`), такты 4–6 конвейера с тремя занятыми стадиями,
158-й и 208-й такты останова двух ядер и `waits = [0, 5]`. Таблица «что даёт кеш»
собрана из четырёх сборок одной машины (324 / 432 / 184 / 208 тактов) — измерена,
а не выведена из рассуждения.

**Границы названы прямо, а не умолчаны**: учебная система команд без индексного
регистра; кеш команд (согласование двух кешей не возникает по построению); число
ядер в объявлениях фиксировано двумя, потому что размер массива — константа;
индекс массива — литерал или имя; двухпортовая память данных; цель `sv` для
ступени с двумя ядрами не годится (со ссылкой на раздел о целях).

**Попутно снято предупреждение симулятора**: параметр кеша назывался `id`, как и
параметр ядра, и прогон печатал «имя 'id' объявлено несколькими моделями». Кеш
получил имя `serves` («какое ядро обслуживает») — читается лучше, и примеры
раздела идут без предупреждений.

#### Проверки

```sh
./scripts/precheck.sh
make -C book build
```

- **Проверка примеров книги**: `machine.takt`, `cache.takt`, `pipeline.takt`,
  `dual_core.takt` компилируются целью `c` и симулируются; `machine_lib.takt` —
  библиотечный, проверяется импортёрами.
- **Прогоны без предупреждений**: все четыре ступени печатают только шаги.
- **Проверки глифов и ссылок** — зелёные (58 файлов документа).
- **PDF собирается**: `book/book/pdf/takt-language.pdf`, 132 страницы (было 127).
- **Форматтер**: примеры раздела в каноне.

Соответствие анализу: закрыты **R5** (текст), **R6** (проверка), **R7** (объяснения
привязаны к конструкциям), **R8** (граница целей названа), **R9** (регистрация,
глифы, ссылки, PDF). Фича готова к закрытию — остаются тест-план и отчёт.

## Тест-план

### Область и цель

Фича документная: проверяется **раздел** — его примеры, числа и утверждения.
Особенность предмета в том, что примеры раздела **исполняются** проверкой книги,
поэтому «проверить текст» здесь означает: прогнать машину и сверить то, что
написано, с тем, что она делает.

Ключевое требование к проверкам — числа в тексте обязаны быть **снятыми**, а не
выведенными из рассуждения: раздел объясняет, зачем нужен кеш и что даёт
конвейер, и эти утверждения проверяются трассой.

### Проверки (условие → ожидаемый результат)

| # | Проверка | Предусловие | Ожидаемый результат | Ссылка на R/A |
|---|---|---|---|---|
| T1 | Ступень 1 исполняется и считает верно | прогон `machine.takt` | останов на 324-м такте, `ram[0] = 35` (`7 * 5`), счётчик обнулён | R1 / A1 |
| T2 | Две шины работают раздельно | трасса ступени 1 | в одном такте память отвечает и по коду, и по данным; машинный цикл трёхтактный | R1 / A2 |
| T3 | Кеш наблюдаем и полезен | прогон `cache.takt` + три другие сборки | `hits = 28`, `misses = 8`; 208 тактов против 432 без кеша на той же памяти | R2 / A3 |
| T4 | Конвейер одновременен | трасса `pipeline.takt` | такт, где заняты три стадии **разными** командами (`if_pc`/`id_pc`/`ex_pc`) | R3 / A4 |
| T5 | Два ядра и арбитраж | прогон `dual_core.takt` | оба ядра доходят до останова (158-й и 208-й такты), `waits = [0, 5]`, `ram = [35, 7, 0, 1, 21, 7, 0, 1]` | R4 / A5 |
| T6 | Числа текста = числа прогона | сверка текста раздела с трассами | каждое число раздела воспроизводится командой из раздела | R5 / A6 |
| T7 | Проверка книги зелёный | `./scripts/precheck.sh` | четыре ступени компилируются целью `c` и симулируются; библиотека — «библиотечный» | R6 / A7 |
| T8 | Прогоны без предупреждений | прогон каждой ступени | вывод содержит только шаги | R6 / — |
| T9 | Переиспользование, а не копии | чтение файлов | ни одна модель не описана дважды; ступени — импорт и сборка | R5 / — |
| T10 | Граница целей названа | текст раздела | сказано, что ступень с двумя ядрами целью `sv` не переносится | R8 / A8 |
| T11 | Раздел собран и зарегистрирован | `SUMMARY.md`, `make -C book build` | раздел в оглавлении, PDF собирается, проверки глифов и ссылок зелёные | R9 / A9 |
| T12 | Формат примеров | `taktc fmt --check` | примеры раздела в каноне | — |

### Разбивка проверок по функциональности

| Функциональность | Что проверяется | Статус |
|---|---|---|
| симулятор | все четыре ступени исполняются, значения сверены | ✅ |
| цель `c` | все четыре ступени компилируются (проверка книги) | ✅ |
| цели `st`/`rust`/`sv` | **не применимо**: проверка раздела — цель `c` и симуляция; перенос машины в прочие цели раздел не обещает (для `sv` прямо сказано, что ступень 4 не переносится) | — |
| форматтер | примеры в каноне | ✅ |
| документ | раздел, оглавление, глифы, ссылки, PDF | ✅ |

<!-- Легенда: ✅ пройдено · ❌ провалено · ⬜ не проверялось · — не применимо -->

### Тестовые данные и окружение

- Пять файлов раздела: библиотека `machine_lib.takt` и четыре сборки.
- Прогоны: `takt-sim -n …` (с `--save-state` там, где нужно различить экземпляры
  одноимённых моделей), `taktc compile -t c -I …`, `taktc fmt --check`.
- Сборка документа: `make -C book build` (mdBook + pandoc + xelatex).

### Что этот план сознательно не проверяет

- **Правильность архитектурных решений учебной машины** (глубина конвейера,
  политика кеша, дисциплина арбитра): предмет раздела — язык, а не оптимальность
  машины.
- **Перенос машины во все цели генерации**: раздел этого не обещает и прямо
  называет границу.
- **Скорость сборки документа**: PDF собирается, время не нормируется.

## Отчёт о тестировании

### Резюме

**✅ ПРОЙДЕН (12 из 12).** Раздел написан, зарегистрирован и собирается; все
четыре ступени машины исполняются и считают верно; каждое число текста снято с
прогона. Фича готова к закрытию.

Окружение: macOS (Darwin 25.5.0), толчейн `1.97.1`, mdBook + pandoc + xelatex.
Прогон: `./scripts/precheck.sh` — код 0; `make -C book build` — PDF на 132
страницы.

Объём: библиотека 465 строк, четыре сборки — 11, 8, 25 и 16 строк, текст раздела
232 строки.

### Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Ступень 1 считает верно | ✅ | останов на 324-м такте, `ram = [35, 7, 0, 1, …]` — `7 * 5`, счётчик обнулён |
| T2 | Две шины и трёхтактный цикл | ✅ | память обслуживает шину кода и шину данных в одном такте; цикл `Fetch → Decode → Execute` вытекает из договора о такте, а не назначен |
| T3 | Кеш наблюдаем и полезен | ✅ | `hits = 28`, `misses = 8`; четыре сборки одной машины: 324 / 432 / 184 / **208** тактов — медленная память без кеша вдвое дороже, чем с ним |
| T4 | Конвейер одновременен | ✅ | такты 4–6: `if_pc = 2`, `id_pc = 1`, `ex_pc = 0` (далее 3/2/1 и 4/3/2), все три стадии заняты **разными** командами |
| T5 | Два ядра и арбитраж | ✅ | `ram = [35, 7, 0, 1, 21, 7, 0, 1]`; ядро 1 встало на 158-м такте, ядро 0 — на 208-м; `waits = [0, 5]`, первый такт ожидания — на третьем шаге; кеши раздельны (28/8 и 14/8) |
| T6 | Числа текста = числа прогона | ✅ | все величины раздела воспроизводятся его же командами; ни одна не выведена рассуждением |
| T7 | Проверка книги | ✅ | `machine`, `cache`, `pipeline`, `dual_core` — «компилируется и симулируется»; `machine_lib` — «библиотечный, проверяется импортёром» |
| T8 | Прогоны без предупреждений | ✅ | по нулю предупреждений у всех четырёх ступеней (предупреждение о совпадении имён параметров снято переименованием) |
| T9 | Переиспользование вместо копий | ✅ | все модели живут в библиотеке; ступени — импорт и одна строка сборки; различия выражены параметрами (`enabled`, `latency`, `program`, `id`, `base`, `serves`) |
| T10 | Граница целей названа | ✅ | раздел прямо говорит: ступени 1–3 переносятся, ступень 4 целью `sv` — нет (уплощение не различает два экземпляра одной модели) |
| T11 | Раздел собран и зарегистрирован | ✅ | `SUMMARY.md` содержит раздел; проверки глифов (58 файлов) и ссылок зелёные; PDF — 132 страницы (было 127) |
| T12 | Формат примеров | ✅ | `fmt --check` — канон у всех пяти файлов |

### Результаты по функциональности

| Функциональность | Итог | Статус |
|---|---|---|
| симулятор | четыре ступени исполняются, значения сверены со счётом вручную | ✅ |
| цель `c` | четыре ступени компилируются (проверка книги) | ✅ |
| цели `st`/`rust`/`sv` | не применимо: проверка раздела — цель `c` и симуляция; для `sv` граница названа текстом | — |
| форматтер | примеры в каноне | ✅ |
| документ | раздел, оглавление, глифы, ссылки, PDF | ✅ |

### Дефекты и находки, вскрытые работой

Внутри фичи исправлены **четыре** дефекта её собственных моделей — все найдены
**прогоном**, ни один чтением:

1. запись в память шла по чужому адресу (адрес успевала переставить соседняя
   стадия) — заведена отдельная линия адреса записи;
2. попадание в кеш стоило столько же, сколько промах, — попадание обслуживается
   тем же тактом;
3. счётчик попаданий врал: запрос снимался позже, чем принимался ответ;
4. арбитр удерживал шину со старевшим запросом — освобождение и по снятию
   запроса.

Плюс переработка структуры по замечанию заказчика — [исправление](../fixes/0186-01-example-decomposition.md).

**Находки вне объёма** (записаны кандидатами в `FEATURES.md`, а не выданы за
сделанное):

- массив как общая переменная не переносится в цель `st` — `iec2c` отвергает
  вывод при рапорте компилятора об успехе; индекс массива не может быть
  выражением;
- форматтер выносит комментарий из тела блока наружу;
- вариант **импортированного** перечисления не разрешается в образце `match`,
  хотя в условии `ref` — разрешается;
- внешний интерфейс модели не выражается: `extern fn` не исполняется симулятором,
  агрегат не может быть аргументом параметра.

### Выводы и дальнейшие шаги

Исправлений (`docs/fixes/0186-YY-*`) сверх уже закрытого 0186-01 не требуется.
Ограничения, названные в разделе честно и работой не закрытые: учебная система
команд без индексного регистра; кеш команд (согласование двух кешей не возникает
по построению); число ядер в объявлениях линий фиксировано двумя; перенос ступени
с двумя ядрами в цель `sv`.

## Итог (что сделано)

Раздел [`book/src/20-processor/`](../../book/src/20-processor/index.typ) —
«Практический пример: процессор» — показывает язык «в сборе» на вычислительной
машине: память, две шины, кеш команд, конвейер и **два ядра** на общей шине с
арбитром. Все четыре ступени **исполняются**, и каждое число текста снято с
прогона, а не выведено рассуждением.

Главное решение фичи — **одна библиотека моделей и четыре сборки**: ступени
отличаются не копиями текста, а составом композиции и значениями параметров
(`enabled`, `latency`, `program`, `id`, `base`, `serves`). Первая редакция
примеров несла копии моделей; заказчик указал на это, и
[исправление](../fixes/0186-01-example-decomposition.md) переработал структуру:
библиотека 465 строк, сборки — 11, 8, 25 и 16 строк. «Ядер сколько угодно»
выражено буквально: линии объявлены массивами, индексом служит номер ядра —
значение параметра.

Два хода, предложенных заказчиком, **проверены пробами и отклонены** с
названными причинами: программа как параметр-массив не выражается (агрегат не
сворачивается константным вычислителем), а внешний интерфейс через `extern fn`
непригоден для документа — симулятор такую модель не исполняет, а примеры
раздела проверка **запускает**. Оба записаны кандидатами.

Работа вскрыла и устранила **четыре** дефекта собственных моделей (адрес записи,
цена попадания в кеш, счётчик попаданий, удержание шины арбитром) — все найдены
прогоном, ни один чтением, — и записала **четыре** находки о языке и
инструментах вне своего объёма.

Детали — [отчёт](0186-book-processor-example.md#отчёт-о-тестировании) (✅ 12 из 12).
