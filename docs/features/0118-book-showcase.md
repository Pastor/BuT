# Фича: Раздел документа «Развёрнутый пример»

- **Номер:** 0118
- **Статус:** ГОТОВО
- **Зависит от:** 0117 (исключение свод: раздел прикладной, показывает вывод инструментов)
- **Связанные issue (анализ):** новая фича (запрос заказчика 2026-07-24 — раздел, раскрывающий всю мощь языка)

## Стадии жизненного цикла

Все стадии — **разделы этой карточки**: «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог».
Отдельным артефактом остаются только исправления —
[`docs/fixes/`](../fixes/README.md) (при необходимости `0118-YY-*`).

## Краткое описание

Прикладной раздел документа `book/`, раскрывающий тракт Takt на одном примере
(дозатор): описание модели → генерация целей (`c`/`rust`/`sv`/`plantuml`) →
симуляция с визуализацией → верификация LTL → инвариант (guard). Показывает
реальные команды и вывод; опирается на исключение ([ADR](0117-book-tools.md#архитектура-adr)).

> Фича зарегистрирована **2026-07-24 по запросу заказчика**.

## Документирование

Заводит **новый прикладной раздел** документа (каждый раздел
отдельной фичей). Язык **не меняется**. Раздел показывает вывод инструментов —
допустимо по исключению своду. Номера фич/задач и сведения о
процессе в текст не попадают.

## Архитектура (ADR)

- **Status:** Accepted
- **Date:** 2026-07-24
- **Authors:** Архитектор, лид документации
- **Related issues:** [Фича](0118-book-showcase.md); опирается на исключение свод из [ADR](0117-book-tools.md#архитектура-adr)

### Context

Заказчик запросил сквозной раздел-пример, раскрывающий «всю мощь языка»:
генерация целей, симуляция с картинками, LTL-проверки, guard'ы. Раздел —
прикладной (показывает вывод инструментов), опирается на исключение своду. Два вопроса: (1) какая модель-носитель; (2) как встроить «картинку»
симуляции в PDF, если `takt-sim` рисует **анимированный GIF**, а `xelatex`
анимацию не встраивает.

### Decision Drivers

1. **Полнота охвата.** Модель должна затрагивать состояния/переходы, инвариант,
   верифицируемое свойство и быть симулируемой.
2. **Простота.** Пример понятен без предметной области.
3. **Картинка в PDF.** Нужен статический растр/вектор, а не анимация.

### Considered Options

#### Модель: (A) новая компактная «дозатор» / (B) переиспользовать `comprehensive`

- **A** — 3 состояния, `invariant`, `next`+`ref`, свойство `F Filling` держится.
  Компактна и самодостаточна. **Выбрана.**
- **B** — богаче, но длиннее и с деталями, уводящими от сути.

#### Картинка: (A) кадр SVG из симулятора → PDF через rsvg-convert / (B) GIF / (C) PlantUML→PNG

- **A** — `takt-sim --graphics-config default_svg.json` даёт покадровые SVG;
  `pandoc`+`rsvg-convert` встраивают SVG в PDF. **Выбрана** (проверено: PDF
  собирается, картинка внутри).
- **B** — GIF анимацию `xelatex` не встроит.
- **C** — нужен `plantuml.jar`/Java-рендер, в окружении отсутствует.

### Decision

Модель — новая **«дозатор»** (`examples/dispenser.takt`). Картинка — **статические
кадры SVG** из симулятора (наполнение и готовность), встроенные в PDF через
`rsvg-convert`. Раздел показывает генерацию (c/rust/sv/plantuml + фрагмент C),
симуляцию (трасса + кадры), верификацию (`F Filling` держится; `F Ready` —
нарушено под абстракцией, поучительно) и инвариант (`assert` в C).

### Consequences

#### Положительные

- Один пример демонстрирует весь тракт; выводы и код — реальные.
- Найден рабочий способ вставлять картинку симуляции в PDF (SVG-кадр).

#### Отрицательные / Action items

- Кадры SVG — снимок текущего рендера; при смене визуализации обновлять.

#### Acceptance criteria

1. `dispenser.takt` компилируется в c/rust/sv/plantuml, симулируется, `F Filling`
   держится, инвариант присутствует.
2. Раздел `book/src/17-showcase/index.typ` собран в PDF с кадрами SVG.
3. Сборка PDF (`xelatex`) чистая; `precheck.sh` зелёный.

## Анализ

### Цель

Прикладной раздел, сквозным примером раскрывающий тракт «описание → генерация →
симуляция → верификация → инвариант». Язык **не меняется** (Tier 3). Опирается на
исключение своду.

### Модель-носитель (проверено прогонами)

`examples/dispenser.takt` — дозатор, 3 состояния (`Idle`/`Filling`/`Ready`),
`invariant SafeLevel = level <= TARGET`, `next`+`ref`:

- **Генерация:** `c`, `rust`, `sv`, `plantuml` — все компилируются (EXIT 0).
- **Симуляция:** цикл `Filling(0→80) → Ready(100) → Idle(0) → …`; инвариант
  соблюдён (максимум `level` = 100 = TARGET).
- **Верификация:** `F Filling` — **держится** (из `Idle` безусловный `next` в
  `Filling`); `F Ready` — **нарушено** под управляющей абстракцией (контрпример
  «застрять в `Filling`» недостижим по данным) — поучительная иллюстрация границ
  абстракции.
- **Инвариант в C:** `assert(model->level <= CONST_DISPENSER_TARGET);` в
  `Dispenser_tick`.

### Картинка симуляции в PDF

`takt-sim -o` по умолчанию — анимированный GIF (в PDF не встраивается). Решение: `--graphics-config default_svg.json` даёт покадровые SVG; два кадра
(`dispenser-0003` наполнение, `dispenser-0006` готовность) скопированы в
`images/`; `pandoc`+`rsvg-convert` встраивают их в PDF (проверено: PDF собран,
картинки внутри).

### Ограничения

Раздел прикладной — показывать вывод инструментов можно. Номера
фич/задач и сведения о процессе в текст не попадают.

## Итог (что сделано)

- **Наполнен раздел `book/src/17-showcase/index.typ`** (часть «Применение»,
  `SUMMARY.md`): модель `dispenser.takt`, генерация целей (+ фрагмент C с
  `assert` от инварианта), симуляция (трасса + два кадра SVG), верификация
  (`F Filling` держится; `F Ready` — нарушено под управляющей абстракцией,
  с оговоркой), инварианты/guard'ы, итоговая связка.
- **Пример `examples/dispenser.takt`** — компилируется в `c`/`rust`/`sv`/
  `plantuml`, симулируется (цикл `Filling→Ready→Idle`), `F Filling` **держится**,
  инвариант `SafeLevel` соблюдён.
- **Картинки в PDF:** покадровый SVG из `takt-sim --graphics-config
  default_svg.json` (`images/dispenser-filling.svg`, `dispenser-ready.svg`),
  встроен через `pandoc`+`rsvg-convert` (анимированный GIF в PDF не встраивается).

**Живая проверка:** все прогоны реальны (генерация/симуляция/verify); сборка PDF
(`mdbook`+`xelatex`, TeX Live 2026) чистая (`Missing character`/`undefined`/
`Overfull` = 0, картинки встроены); `precheck.sh` зелёный.

> **[раздел «Практический пример» — полноценная модель группового управления лифтами](../fixes/0118-01-practical-example.md) (2026-07-25):** раздел
> переименован в **«Практический пример»** и переписан вокруг модели **группового
> управления лифтами** (примитивный дозатор → узнаваемое устройство).
> Добавлены: диаграммы верификации реальным выводом `taktc verify --emit-graph`
> ([экспорт графов верификации (Крипке/Бюхи/произведение) в Graphviz DOT](0124-verify-graph-export.md), Крипке/Бюхи/произведение → SVG),
> диспетчер секции (композиция функций), файл симуляции, приложение «Порождённый
> код примера» с **полным** кодом всех целей.
