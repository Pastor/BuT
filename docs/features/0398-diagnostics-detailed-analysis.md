# Фича: Разбор с примером есть у 83 кодов из 229

- **Номер:** 0398
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено на стадии анализа 2026-08-22)
- **Tier:** 3 — расставлен по признаку вреда (шкала — в блоке 1 `FEATURES.md`)
- **Класс:** прочее
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-22

## Стадии жизненного цикла (правило 17)

Все стадии — **разделы этой карточки** (правило 32): «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог».
Отдельным артефактом остаются только исправления —
[`docs/fixes/`](../fixes/README.md) (при необходимости `0398-YY-*`).

## Краткое описание

- **Разбор с примером есть у 83 кодов из 229.** Замер
  [запись реестра диагностик называет смысл](0311-diagnostic-descriptions.md) (2026-08-20): справочник
  теперь **называет смысл** каждого кода (проверка
  `scripts/check-diagnostic-descriptions.py`), но вторая часть приложения —
  «Подробный разбор основных ошибок» с примером и выводом инструмента —
  остаётся подмножеством. Какие коды заслуживают разбора, машине неизвестно:
  это решение о **пользе**, и список пришлось бы вести руками — он разошёлся бы
  с документом. Осмысленный предмет — не «разобрать
  все», а выбрать группу по признаку вреда и разобрать её целиком, начав с
  решения заказчика о признаке.

**Прежние 50 записей переведены в фичи** 2026-08-18 по решению заказчика
(«перевести всех кандидатов в фичи без проработки, решить, делать или нет,
позже»): 49 получили карточки `0252`…`0300` со статусом `СОЗДАНА` и стоят в
таблице выше, пятидесятая — «тега `v0.10.0` в репозитории нет» — снята как
**исполненная** фичей [пропущенные метки версий языка и контроль правила 22](0162-git-tag-v040.md) (правило 28).

Текст каждого кандидата перенесён в карточку **дословно**, вместе с замером
и датой: замер есть свидетельство, а пересказ его теряет. Стадии 2 и 3 не
пройдены ни у одной — зависимости и объём **не оценивались**. `Tier` расставлен
отдельным проходом 2026-08-18 по признаку вреда (шкала — в блоке 1): это
сортировка **уже снятого** замера, а не проработка.

Заводя сюда новую запись, помни правило 28: кандидат снимается **закрывающей
фичей**, и устаревшая запись здесь дороже отсутствующей.

> Фича зарегистрирована из блока кандидатов `FEATURES.md` **без проработки**
> (решение заказчика 2026-08-22: «завести фичи из кандидатов без проработки»).
> Текст кандидата перенесён **дословно**, вместе с замером и его датой: замер
> есть свидетельство, а пересказ его теряет. Стадии 2 и 3 не пройдены —
> зависимости и объём **не оценивались**; далее фича проходит жизненный цикл по
> правилу 17.

## Документирование (правило 24)

**Фича сама о документе.** Затронуто приложение «Ошибки и предупреждения»
(`book/src/appendix-errors/`), вторая часть — «Подробный разбор». Требуется:
назвать признак отбора в первом абзаце и довести разборы до него. Правило 24:
номеров фич, ссылок на ADR и деталей процесса в приложении быть не должно.

<!-- При закрытии фичи (стадия 8, статус ГОТОВО) сюда добавляется раздел
     «## Итог (что сделано)» —
     ссылки на отчёт/исправления (правило 21). Незакрытые фичи «Итога» не имеют. -->

## Архитектура (ADR)

- **Status:** Proposed — **признак отбора выбирает заказчик**
- **Date:** 2026-08-22
- **Authors:** Архитектор + Аналитик
- **Related issues:** [Фича](0398-diagnostics-detailed-analysis.md);
  описания — [запись реестра диагностик называет смысл](0311-diagnostic-descriptions.md#архитектура-adr); сверка кодов —
  [приложение «Ошибки» отстаёт от реестра диагностик на 19 кодов](0290-book-diagnostics-codes-gate.md#архитектура-adr)

### Context

Приложение `book/` «Ошибки и предупреждения» состоит из двух частей: **сводная
таблица** (называет смысл каждого кода — работа 0311, контролируется проверкой
`check-diagnostic-descriptions.py`) и **подробный разбор** (пример входа + вывод
инструмента).

Замер **переснят 2026-08-22**:

| Что | Прежняя запись (0311, 2026-08-20) | Пересъём |
|---|---|---|
| кодов в реестре | 229 | **234** |
| кодов в приложении | — | **234** (сверено проверкой) |
| разборов | 83 | **85** (заголовков `===` — 97, из них с кодом — 85) |

То есть разобрано **85 из 234**; за два дня реестр вырос на пять кодов, а
разборов прибавилось два. Пропорция сама по себе не дефект: разбор нужен там,
где текст диагностики без примера непонятен.

**Машина здесь бессильна по существу:** «заслуживает ли код разбора» —
вопрос о пользе, а не о согласованности. Список «кому нужен разбор» пришлось бы
вести руками, и он разошёлся бы с документом.

### Decision Drivers

1. **«Разобрать все» — не цель:** 234 разбора превратят приложение в свалку, а
   большинство кодов понятны из описания (0311 это и обеспечила).
2. **Признак отбора должен быть проверяемым**, иначе список станет вкусовым и
   расползётся.
3. **Проверка уже сверяет состав** (0290: множества кодов тождественны) — значит
   новый разбор не может «повиснуть» на несуществующем коде.
4. **Цена одного разбора невелика** (пример + вывод инструмента), но их
   десятки — работа объёмная и должна иметь границу.

### Considered Options

#### Option A. Отобрать по признаку вреда (Tier класса ошибки)

Разбирать коды, за которыми стоит **молчаливое расхождение** или **невалидный
вывод** — то есть те, что рождены фичами Tier 1–2.

**Pros:** признак объективен и уже применяется к фичам; список выводится из
карточек, а не ведётся руками.
**Cons:** привязывает документ к внутренней шкале процесса — читателю она
неизвестна (правило 24: номеров фич и процесса в документе быть не должно).
Значит признак используется **при отборе**, но в документ не попадает.

#### Option B. Отобрать по частоте: коды, которые автор встречает чаще

**Pros:** прямая польза читателю.
**Cons:** частоту нечем измерить — телеметрии нет; оценка была бы вкусовой.

#### Option C. Отобрать по «непонятности текста»: код, чьё сообщение не
объясняет причину без примера

**Pros:** ровно тот случай, ради которого разбор существует.
**Cons:** оценка субъективна; но её можно свести к проверяемому признаку —
**сообщение ссылается на устройство** (`представление`, `понижение`,
`раскладка`, `упакованный`) либо называет **чужой инструмент** (`iec2c`,
`verilator`, `clippy`).

#### Option D. Оставить как есть

**Cons:** приложение обещает «разбор основных ошибок», не называя, что такое
«основные»; читатель не знает, почему одного кода нет.

### Decision

**Решение заказчика 2026-08-23: Option C.** Аналитик предлагал его же (сообщение
ссылается на устройство или на чужой инструмент) как единственный
**проверяемый** признак: его можно применить грепом по реестру и получить
воспроизводимый список, а не вкусовой.

Что бы ни было выбрано, вторая часть приложения обязана **назвать свой
признак** — сегодня она называется «Подробный разбор основных ошибок», и слово
«основных» ничем не определено.

### Consequences

#### Положительные

- Читатель понимает, почему разобран один код и не разобран другой.
- Список воспроизводим: следующая фича, заводя код, сразу знает, нужен ли ему
  разбор.

#### Отрицательные / Action items

- Работа объёмная: даже узкий признак даст десятки разборов.
- Каждый разбор — пример **и** вывод инструмента; вывод обязан быть снят
  прогоном, а не написан по памяти (иначе класс «проза расходится с кодом»).
- Проверка придётся дополнить: сегодня он сверяет состав кодов, но не
  проверяет, что разобранные коды удовлетворяют признаку.

#### Acceptance criteria

1. Признак назван в самом приложении (в первом абзаце второй части).
2. Список разобранных кодов получается **применением признака**, а не
   перечислением: тот же признак воспроизводится скриптом.
3. Каждый новый разбор содержит вход и **снятый прогоном** вывод инструмента.
4. Проверка расширен проверкой признака (или заведён соседний).
5. `make -C book build` зелёный; `./scripts/precheck.sh` зелёный.

## Анализ

### Цель и контекст

Вторая часть приложения «Ошибки и предупреждения» — разбор с примером и выводом
инструмента — покрывает меньшинство кодов. Фича определяет **признак отбора** и
доводит разборы до него.

### Замер (правило 30, переснят 2026-08-22)

| Метрика | 0311 (2026-08-20) | Пересъём |
|---|---|---|
| кодов в реестре `docs/diagnostics/README.md` | 229 | **234** |
| кодов в приложении `book/` | — | **234** (тождество контролирует проверка) |
| заголовков разбора (`===`) | — | 97 |
| разборов **с кодом в заголовке** | 83 | **85** |

Вывод: за два дня реестр вырос на пять кодов, разборов прибавилось два —
пропорция сама по себе не дефект, дефект в том, что **признак отбора не
назван**.

### Зависимости фичи (правило 17/19)

- **Зависит от:** нет; опорные закрыты — [запись реестра диагностик называет смысл](0311-diagnostic-descriptions.md)
  (описания и проверка), [приложение «Ошибки» отстаёт от реестра диагностик на 19 кодов](0290-book-diagnostics-codes-gate.md)
  (тождество множеств кодов).
- **Влияние на порядок:** ждёт **решения заказчика** о признаке; после него
  работа исполнительская и делится на порции по группам кодов.

### Требования и проверяемые условия

- **R1.** Признак отбора назван в приложении (первый абзац второй части).
- **R2.** Список разобранных кодов **воспроизводим**: тот же признак даёт тот
  же список (проверяется скриптом).
- **R3.** Каждый разбор содержит вход и вывод инструмента, **снятый прогоном**.
- **R4.** Проверка сверки расширен: разобранный код удовлетворяет признаку, а
  удовлетворяющий — разобран (или внесён в узаконенный долг).
- **R5.** Правило 24 соблюдено: номеров фич и деталей процесса в документе нет.
- **R6.** `make -C book build` зелёный, символы в шрифте.

### Критерии приёмки и способ проверки

| # | Критерий | Способ проверки |
|---|---|---|
| A1 | R1 | чтение приложения |
| A2 | R2, R4 | новый/расширенный проверка (по образцу `check-book-diagnostics.py`) |
| A3 | R3 | прогон `taktc` на каждом примере разбора (проверка примеров документа, 0133) |
| A4 | R5 | греп: `фича \d{4}`, `ADR`, `docs/` в приложении отсутствуют |
| A5 | R6 | `precheck.sh` |

### Объём работ

| Порция | Что делается |
|---|---|
| 1 | признак + абзац в приложении + проверка признака |
| 2…N | разборы группами по 5–10 кодов; каждая порция — свой коммит |

Объём зависит от признака: предложенный аналитиком (сообщение ссылается на
устройство либо на чужой инструмент) даёт, по грубой оценке грепом, **несколько
десятков** кодов — то есть работа на несколько порций.

### Редакторский слой (правило 29)

**Не применимо:** предмет — документ.

### Особенности по обратной функциональности

Документ дополняется; коды, тексты и поведение инструментов не меняются.

### Риски и зависимости

- **Вывод инструмента в разборе обязан быть снят прогоном** — иначе документ
  начнёт расходиться с кодом (правленный трижды).
- **Проверка признака** нужен сразу: без него список расползётся при первом же
  новом коде.
- **Правило 24**: в приложение нельзя переносить объяснения «почему так
  сделано» из карточек фич — только поведение инструмента.

## Разработка

### Задача: признак назван в документе

Вторая часть приложения переименована: «Подробный разбор» вместо «Подробный
разбор **основных** ошибок», и признак назван прямо — разбор получают две
группы: частые ошибки письма и коды, чьё сообщение **ссылается на устройство**
инструмента («представление», «понижение», «раскладка», «упакованный») либо
называет **чужой инструмент** (`iec2c`, `verilator`, `clippy`).

Слово «основных» ничем не определялось, и читатель не знал, почему одного
кода нет, — это и был предмет фичи, а не пропорция 85 из 234.

### Задача: недостающие разборы

Замер-23 по признаку: подпадают **10** кодов, разбора не имели
**шесть** — `SE-124`, `SE-125`, `CC-023`, `RS-011`, `ST-021`, `ST-023`.
Написаны все шесть; вывод инструмента в каждом снят **прогоном**, а не
сочинён.

### Задача: проверка признака

`scripts/check-diagnostic-analysis.py` (шаг предкоммита) со контролем
`test-diagnostic-analysis.sh` из трёх условий: согласованное дерево
принимается, пропавший разбор ловится и **называется**, приложение без
разборов даёт **отказ** (проверка, рапортующий об успехе на пустоте, не проверка).

Признак применяется к **описанию реестра**, а не к тексту в исходнике:
реестр и есть каталог смыслов, его же читает проверка, и второе место
разошлось бы с первым.

### Побочная находка

Текст `ST-020` содержал **десятки пробелов подряд**: строка была склеена
продолжением литерала, сохраняющим отступ исходника. Тот же класс, что 0258
(«текст сообщения проверяется прогоном, а не чтением»); исправлено явной
склейкой.

## Тест-план

| № | Условие | Ожидаемый результат |
|---|---|---|
| T1 | Признак назван в приложении | заголовок и абзац перечисляют группы |
| T2 | Шесть кодов получили разбор | пример + вывод инструмента у каждого |
| T3 | Проверка на согласованном дереве | принимает |
| T4 | Пропавший разбор | ловится и назван |
| T5 | Вырожденный вход | отказ |
| T6 | Текст `ST-020` | без лишних пробелов |
| T7 | Предкоммит | зелёный |

## Отчёт о тестировании

**Окружение:** macOS (darwin 25.5.0), toolchain `1.97.1`, полный
`./scripts/precheck.sh`.

| № | Результат |
|---|---|
| T1 | `book/src/appendix-errors/` |
| T2 | шесть разборов, выводы сняты прогоном |
| T3 | «под признак подпадает 10 — все разобраны» |
| T4 | контроль, условие 2 |
| T5 | контроль, условие 3 |
| T6 | прогон `taktc compile -t st` |
| T7 | |

### Примеры и контрпримеры (правило 16)

- **Пример:** `SE-125` — сообщение называет `verilator`, значит разбор нужен.
- **Контрпример:** `SE-004` («переменная не найдена») — текст объясняет сам,
  разбора не требует; проверка его и не требует.

### Редакторский слой (правило 29)

**Не требуется:** язык не затронут.

### Дефекты

Найден и исправлен один: текст `ST-020` с лишними пробелами.

### Вердикт

**Готово.**

## Итог (что сделано)

Признак отбора **назван** и **проверяется машиной**: разбор получают коды,
чьё сообщение ссылается на устройство инструмента либо называет чужой
инструмент, — такое сообщение без примера понять нельзя, даже зная язык.

Шесть кодов, подпадавших под признак и лишённых разбора, разобраны; вывод
инструмента в каждом снят прогоном. Проверка `check-diagnostic-analysis.py` со
контролем из трёх условий вошёл в предкоммит.

**Предмет был не в пропорции** (85 из 234), а в том, что слово «основных»
ничем не определялось. Пропорция после фичи почти не изменилась — изменилось
то, что теперь известно, **почему** кода в разборе нет.

Побочно исправлен текст `ST-020`: десятки пробелов подряд от продолжения
литерала.

Детали — в разделах «Разработка» и «Отчёт о тестировании».

