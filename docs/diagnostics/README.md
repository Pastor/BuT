# Реестр кодов диагностик Takt

> **Единый источник истины** по кодам диагностик (`SE-036`, `CC-014`, `SIM-010`,
> …). Заведён фичей [реестр кодов диагностик (конфликт: `CC-014` занят дважды)](../features/0077-diagnostic-code-registry.md)
> ([ADR](../features/0077-diagnostic-code-registry.md#архитектура-adr)). Прежде коды жили только в
> исходниках (голыми литералами `Diagnostic::with_code("…")`) и в **вручную**
> пополняемых таблицах `README.md`, которые отставали (AM/PU не описаны, ST —
> частично) и не защищали от коллизий (`CC-014` был выбран двумя фичами).

## Как этим пользоваться

- **Выдаёшь новый код?** Найди ниже секцию нужного **префикса**, возьми
  **следующий свободный номер** (проверка печатает максимальный занятый по каждому
  префиксу), добавь строку сюда **и** используй тот же код в `.with_code("…")`.
  Реестр — источник истины: код без строки здесь **завалит** сборку.
- **Формат кода:** `XX-NNN` — 2–4 заглавные латинские буквы, дефис, ровно три
  цифры с ведущими нулями. Иных форм нет.
- **Пропуски номеров** (нет `CC-009`, `SE-024`…) — норма: код мог быть выведен или
  зарезервирован. Занятый номер **не переиспользуют** под другой смысл.
- **`RESERVED`/`RETIRED`** в столбце «Источник» помечают код, которого в исходнике
  **нет** (забронирован под будущее либо выведен): проверка не требует его эмиссии, но
  и не отдаёт номер повторно.

## Машинная проверка

`scripts/check-diagnostic-codes.sh` (в `precheck.sh` и CI) сверяет реестр с
исходниками: каждый эмитируемый код обязан быть здесь (иначе — ошибка
«незадокументированный код»), каждая строка реестра — встречаться в исходнике
(иначе — «протухшая запись»), формат `XX-NNN` соблюдён, дублей строк нет. Печатает
по префиксам максимальный занятый номер — это и есть «следующий свободный».

## Префиксы

| Префикс | Слой | Крейт |
|---|---|---|
| `LE` | лексические ошибки | `takt-lang` (`parser/lexer.rs`, диспетчер `code()`) |
| `SY` | синтаксические ошибки | `takt-lang` (парсер) |
| `SE` | семантические ошибки | `takt-lang` (`semantic/`, `address_map/`) |
| `CC` | цель C (кодоген) | `takt-lang` (`generator/c/`) |
| `RS` | цель Rust | `takt-lang` (`generator/rust/`) |
| `SV` | цель SystemVerilog | `takt-lang` (`generator/sv/`) |
| `ST` | цель Structured Text | `takt-lang` (`generator/st/`) |
| `AM` | парсинг внешней карты адресов | `takt-lang` (`address_map/parse.rs`) |
| `DF` | флаг `--define` | `takt-lang` (`address_map/`) |
| `PU` | цель диаграмм — **снята**, коды не порождаются | — |
| `SIM` | вычисление в симуляторе | `takt-sim` (`eval/`, диспетчер `EvalError::code()`) |
| `CS` | стиль кода (канон именования) | `takt-lang` (`style/`) |
| `FM` | форматирование (`taktc fmt`) | `takt-lang` (`format/`) |

> Столбец «Источник» — **первое** место эмиссии кода (файл:строка). Информативен;
> код может эмитироваться из нескольких мест с тем же смыслом (напр. `CC-015`).
> Тексты сообщений в столбце «Значение» — краткая суть, а не дословный текст
> диагностики (тот строится `format!` в исходнике).

## Реестр

### AM — карта адресов

| Код | Значение | Источник |
|---|---|---|
| `AM-001` | Внешняя карта адресов: ожидалось имя порта | `takt-lang/src/address_map/parse.rs:174` |
| `AM-002` | ожидался '=' после имени порта '…' | `takt-lang/src/address_map/parse.rs:202` |
| `AM-003` | ожидался адрес для порта '…' | `takt-lang/src/address_map/parse.rs:224` |
| `AM-004` | ожидался ';' после адреса порта '…' | `takt-lang/src/address_map/parse.rs:237` |
| `AM-005` | ожидался адрес для порта '…' | `takt-lang/src/address_map/parse.rs:227` |
| `AM-006` | повторная запись адреса для порта '…' во внешней карте | `takt-lang/src/address_map/parse.rs:137` |

### CC — цель C

| Код | Значение | Источник |
|---|---|---|
| `CC-001` | BitAccess на порт типа `float` не поддерживается | `takt-lang/src/generator/c/c_expr/condition.rs:341` |
| `CC-002` | Неразрешённая функция в условии перехода | `takt-lang/src/generator/c/c_expr/condition.rs:364` |
| `CC-003` | Ссылки на модели и состояния не поддерживаются в условиях переходов | `takt-lang/src/generator/c/c_expr/condition.rs:390` |
| `CC-004` | Модель с таким именем не найдена в карте (кодоген) | `takt-lang/src/generator/c/c_map.rs:30` |
| `CC-005` | Состояние с таким именем не найдено в карте (кодоген) | `takt-lang/src/generator/c/c_map.rs:42` |
| `CC-006` | Модель … не найдена | `takt-lang/src/generator/c/c_expr/condition.rs:100` |
| `CC-007` | Неподдерживаемый тип элемента конкатенации | `takt-lang/src/generator/c/c_model.rs:352` |
| `CC-008` | Начальное состояние модели не определено | `takt-lang/src/generator/c/c_model.rs:747` |
| `CC-010` | Ошибка записи выходного файла (`.h` или `.c`) | `takt-lang/src/generator/c/mod.rs:140` |
| `CC-011` | Состояние … не найдено в модели … | `takt-lang/src/generator/c/c_expr/condition.rs:111` |
| `CC-012` | Модель … не найдена | `takt-lang/src/generator/c/c_expr/condition.rs:92` |
| `CC-013` | Выражение … не разыменовано | `takt-lang/src/generator/c/c_expr/condition.rs:78` |
| `CC-014` | ~~Бит-вектор непредставимой ширины~~ — выведен фичей (`[bit;N]` теперь упаковывается: N≤64 скаляр, N>64 массив `uint64_t[⌈N/64⌉]`) | RETIRED |
| `CC-015` | Тип не представим в C (в т.ч. тип элемента массива) | `takt-lang/src/generator/c/c_hal.rs:141` |
| `CC-016` | Ширина доступа к регистру порта неизвестна для его типа C (`c-hal`) | `takt-lang/src/generator/c/c_hal.rs:152` |
| `CC-017` | Инициализатор массива не выразим в C (скалярный либо иной длины) | `takt-lang/src/generator/c/c_model.rs:230` |
| `CC-018` | Условие перехода не переводится в C (причина — заметкой) | `takt-lang/src/generator/c/c_model.rs:482` |
| `CC-019` | Состояние модели недостижимо из владельца условия: модель не встроена ни в одно состояние родителя | `takt-lang/src/generator/c/c_expr/condition.rs` |
| `CC-020` | **RETIRED**: тип `duration` цель `c` эмитит целым в миллисекундах — отказывать не за что. Номер не переиспользовать под другой смысл | — |
| `CC-021` | Обращение к ячейке по адресу целью `c` не транслируется: её порты идут через колбэки HAL, адресов она не знает. Доступ по адресу дают `c-hal`, `st-at`, `sv-mmio` (решение 4A) | `takt-lang/src/generator/c/c_anon.rs` |
| `CC-022` | Конструкция языка целью `c` не транслируется: ссылка на модель в выражении, срез бит-вектора (у настоящего массива срез переводится с фичи), блок кода/тип/список/адрес в позиции выражения, встроенная функция отладки. Вид назван **по-русски**, причина — тоже (образец `ST-011`/`RS-011`); позиция берётся у узла. Отличать от `CC-023`: здесь язык конструкцию **имеет**, а цель её не умеет — это ответ автору модели, а не сообщение о дефекте инструмента  | `takt-lang/src/generator/c/c_unsupported.rs` |
| `CC-023` | Узел не прошёл семантическое понижение и в C не переводится: неразрешённое условие, выражение или оператор, пустое выражение, неразрешённая переменная, невыведенный владелец порта, неразрешённое определение функции, невычисленное значение константы (вид назван в тексте, позиция — у узла). Защита в глубину: из корректной программы состояние недостижимо — его отсекает семантика (`SE-025`/`SE-003`), фичи и 0212 | `takt-lang/src/generator/c/c_unresolved.rs` |
| `CC-024` | **Предупреждение:** вызов встроенной функции (`debug`, `S`) в порождённый C не попадает — кода у неё нет. Прежде цель выбрасывала его **молча**, тогда как `st` и `rust` на том же входе отказывают (`ST-011`, `RS-011`): три цели отвечали на один вход тремя способами  | `takt-lang/src/generator/c/c_expr/stmt.rs` |
| `CC-025` | **RETIRED**: состояние-цепочка (`A + B`) **переводится** в табличную форму — страж строки есть условие «цепочка на последнем шаге, и он завершён», то есть ровно то, под которым переход печатает форма `switch`. Отказ, которым фича называла границу, потерял предмет: входа, не выразимого таблицей, не осталось. Номер не переиспользовать под другой смысл | — |

### CS — стиль кода

Предупреждения слоя стиля (`takt-lang/src/style/`). Проверка идёт по АСД и
семантику не запускает: благодаря этому её видит и `taktc fmt`, который
форматирует в том числе семантически некорректные файлы.

Уровень — **предупреждение**: стиль советует, а не запрещает. На код возврата
`fmt --check` эти диагностики **не влияют**.

| Код | Значение | Источник |
|---|---|---|
| `CS-001` | Имя объявления не соответствует канону именования (раздел «Стиль кода»); на код возврата `fmt --check` не влияет | `takt-lang/src/style/naming.rs` |

`CS-001` — имя не соответствует канону именования раздела «Стиль кода»
документа: `UpperCamelCase` для моделей, состояний, типов, перечислений,
структур и вариантов; `snake_case` для переменных, портов, функций и
параметров; `UPPER_SNAKE_CASE` для констант. Имя **не исправляется**
автоматически: механическая замена испортила бы замысел автора.

### DF — флаг --define

| Код | Значение | Источник |
|---|---|---|
| `DF-001` | Флаг `--define` задан не в форме `ИМЯ=ЗНАЧЕНИЕ` либо имя символа некорректно (ожидается `[A-Za-z_][A-Za-z0-9_]*`) | `takt-lang/src/address_map/env.rs:124` |
| `DF-002` | Значение `--define` не разбирается как адрес (десятичное, `0x…`, необязательный `:бит`) | `takt-lang/src/address_map/env.rs:147` |
| `DF-003` | Символ `--define` задан дважды: молчаливое затирание сделало бы адрес зависящим от порядка флагов (симметрия с `AM-006`) | `takt-lang/src/address_map/env.rs:160` |
| `DF-004` | Символ `--define` не использован: ни одно выражение адреса к нему не обращается (симметрия с `SE-051`) | `takt-lang/src/address_map/resolve.rs:156` |

### FM — форматирование

Отказы канонического форматтера (`takt-lang/src/format/`). Печать узла, которую
форматтер не умеет, — **явный отказ**: напечатать «что-нибудь» значило бы молча
испортить исходник автора.

Диагностика несёт **позицию** узла: без неё в большом файле
непокрытый узел приходилось искать грепом.

| Код | Значение | Источник |
|---|---|---|
| `FM-001` | Форматтер не умеет печатать узел АСД: молча потерять кусок исходника нельзя, поэтому `fmt` отказывает и называет вид узла | `takt-lang/src/format/mod.rs` |

`FM-001` — печать узла АСД пока не поддерживается форматтером. Сегодня это
`formula` и `assembly`; текст называет **вид** узла.

### LE — лексер

| Код | Значение | Источник |
|---|---|---|
| `LE-001` | Незакрытый блочный комментарий | `takt-lang/src/parser/lexer.rs:443` |
| `LE-002` | Незакрытая строка (EOF внутри строкового литерала) | `takt-lang/src/parser/lexer.rs:444` |
| `LE-003` | Незакрытый шестнадцатеричный литерал | `takt-lang/src/parser/lexer.rs:445` |
| `LE-004` | Отсутствует цифра после знака числа | `takt-lang/src/parser/lexer.rs:446` |
| `LE-005` | Недопустимый символ в шестнадцатеричном литерале | `takt-lang/src/parser/lexer.rs:447` |
| `LE-006` | Нераспознанный токен | `takt-lang/src/parser/lexer.rs:448` |
| `LE-007` | Отсутствует показатель степени в числе с плавающей точкой | `takt-lang/src/parser/lexer.rs:449` |
| `LE-008` | Ожидался токен `from` | `takt-lang/src/parser/lexer.rs:450` |
| `LE-009` | Числовой литерал вне диапазона `i64` | `takt-lang/src/parser/lexer.rs:162` |
| `LE-010` | Литерал времени вне представимого диапазона (нс/Гц) | `takt-lang/src/parser/lexer.rs:183` |
| `LE-011` | Единица времени у формы, которая её не допускает (`1.5s`, `1e3ms`, `0xFFms`) | `takt-lang/src/parser/lexer.rs:184` |

### PU — цель диаграмм (снята)

| Код | Значение | Источник |
|---|---|---|
| `PU-001` | **RETIRED**: цель диаграмм изъята — файлов диаграмм компилятор не пишет. Номер не переиспользовать | — |

### RS — цель Rust

| Код | Значение | Источник |
|---|---|---|
| `RS-001` | Ошибка записи файла `.rs` (ввод-вывод) | `takt-lang/src/generator/rust/mod.rs:99` |
| `RS-004` | имя даёт `Self`/`self` — непредставимо ни как идентификатор, ни как `r#Self` (отдельное правило языка). Прочие ключевые слова спасает регистр (`type` → `Type`) или `r#type` | `takt-lang/src/generator/rust/rust_name.rs:60` |
| `RS-005` | два имени слипаются после приведения регистра (`floor_sensor` и `FloorSensor`) | `takt-lang/src/generator/rust/rust_name.rs:73` |
| `RS-010` | LTL-формула в теле блока не транслируется в Rust (проверять через `taktc verify`) | `takt-lang/src/generator/rust/rust_stmt.rs:661` |
| `RS-011` | Конструкция не транслируется в Rust: срез **бит-вектора** (у него нет элементов — `[bit;N]` есть упакованный скаляр либо массив слов), строковый литерал вне `debug`, неразрешённый узел. Состав менялся: степень переводится с фичи (`wrapping_pow`), запись разряда — с 0250, срез настоящего массива — с 0355 | `takt-lang/src/generator/rust/rust_expr.rs` |
| `RS-012` | Корневой элемент карты не является моделью | `takt-lang/src/generator/rust/mod.rs:112` |
| `RS-013` | Состояние '…' не найдено | `takt-lang/src/generator/rust/rust_map.rs:125` |
| `RS-014` | тип не представим; `RS-015` — `--float-width=32` (цель всегда даёт `f64`) | `takt-lang/src/generator/rust/rust_type.rs` |
| `RS-015` | `--float-width=32` несовместим с целью rust (float → f64) | `takt-lang/src/generator/rust/rust_type.rs:148` |
| `RS-016` | тип порта не ложится на метод HAL (составной) | `takt-lang/src/generator/rust/rust_port.rs:116` |
| `RS-017` | Обращение к переменной модели из тела функции не транслируется в Rust — передать параметром | `takt-lang/src/generator/rust/rust_expr.rs:155` |
| `RS-018` | чтение выходного порта либо запись во входной | `takt-lang/src/generator/rust/rust_expr.rs:178` |
| `RS-019` | Присваивание в константу '…' недопустимо | `takt-lang/src/generator/rust/rust_expr.rs:542` |
| `RS-020` | условный переход в состояние '…' не переводится в Rust: … | `takt-lang/src/generator/rust/rust_model.rs:1132` |
| `RS-021` | последовательная композиция (`+`) вложена в шаг другой `+` | `takt-lang/src/generator/rust/rust_model.rs:263` |
| `RS-022` | нужен HAL, но он в этой области недоступен | `takt-lang/src/generator/rust/rust_expr.rs:125` |
| `RS-023` | **RETIRED**: тип `duration` цель `rust` эмитит `u32` в миллисекундах — отказывать не за что. Номер не переиспользовать под другой смысл | — |
| `RS-024` | Параметр целевой модели не найден при печати аргумента инстанцирования  | `takt-lang/src/generator/rust/rust_model.rs` |
| `RS-025` | Запись разряда порта не транслируется: у числового **выходного** порта установка одного разряда требует прочитать остальные, а HAL-трейт даёт выходу только запись; у однобитного порта разрядов, кроме нулевого, нет  | `takt-lang/src/generator/rust/rust_expr.rs` |
| `RS-026` | Имя объявления даёт поле структуры модели, которое цель `rust` печатает сама: состояние автомата (`state`), разделяемые переменные (`shared`), аппаратный слой (`hal`), механизм времени (`takt_dwell`/`takt_entry_ms`/`takt_prev_state`), аккумулятор `every` (`takt_every<N>`), шаг последовательной композиции (`<состояние>_seq`) либо экземпляр под-модели. Прежде такой вход давал `E0124: field … is already declared` при нулевом коде возврата `taktc`, а у пары с `state` операторы автора писали в поле состояния автомата. Набор занятых имён строится из носителей печати; слипание двух авторских имён после приведения регистра остаётся за `RS-005`  | `takt-lang/src/generator/rust/rust_fields.rs` |

### SE — семантика

| Код | Значение | Источник |
|---|---|---|
| `SE-001` | Модель не найдена | `takt-lang/src/diagnostics.rs:370` |
| `SE-002` | Ссылка на состояние не найдена | `takt-lang/src/semantic/tree.rs:1565` |
| `SE-003` | Переменная не найдена в области видимости | `takt-lang/src/semantic/expression.rs:98` |
| `SE-004` | Неизвестная встроенная функция | `takt-lang/src/semantic/builtin.rs:50` |
| `SE-005` | Переменная с таким именем уже объявлена | `takt-lang/src/semantic/tree.rs:414` |
| `SE-006` | Модель с таким именем уже объявлена | `takt-lang/src/semantic/tree.rs:251` |
| `SE-007` | Тип '…' уже объявлен | `takt-lang/src/semantic/tree.rs:405` |
| `SE-008` | Условие '…' уже объявлено | `takt-lang/src/semantic/tree.rs:423` |
| `SE-009` | Функция с таким именем уже определена | `takt-lang/src/semantic/tree.rs:658` |
| `SE-010` | Нет терминальных состояний | `takt-lang/src/semantic/validate/states.rs:150` |
| `SE-011` | Нет начального состояния в модели | `takt-lang/src/semantic/minimap.rs:211` |
| `SE-012` | Состояние '…' уже содержит оператор next | `takt-lang/src/semantic/tree.rs:1418` |
| `SE-013` | Файл импорта не найден | `takt-lang/src/semantic/import.rs:90` |
| `SE-014` | Недопустимое расширение файла импорта | `takt-lang/src/semantic/import.rs:129` |
| `SE-015` | Ошибка чтения файла импорта | `takt-lang/src/semantic/import.rs:134` |
| `SE-016` | Не удалось канонизировать путь «…»: … | `takt-lang/src/semantic/import.rs:104` |
| `SE-017` | Условие '…' уже объявлено | `takt-lang/src/semantic/tree.rs:431` |
| `SE-018` | Именованный блок кода при определении должен иметь имя | `takt-lang/src/semantic/tree.rs:606` |
| `SE-019` | Условие при определении должно иметь имя | `takt-lang/src/semantic/tree.rs:538` |
| `SE-020` | Имя состояния не задано | `takt-lang/src/semantic/tree.rs:1391` |
| `SE-021` | Идентификатор не задан | `takt-lang/src/semantic/tree.rs:50` |
| `SE-022` | При определении функция должна иметь имя | `takt-lang/src/semantic/function.rs:39` |
| `SE-023` | Порт должен иметь конкретный тип | `takt-lang/src/semantic/tree.rs:473` |
| `SE-025` | Неразрешённое условие перехода | `takt-lang/src/semantic/validate/common.rs:51` |
| `SE-026` | Запись во входной порт '…' запрещена | `takt-lang/src/semantic/validate/common.rs:207` |
| `SE-027` | Чтение из выходного порта '…' запрещено | `takt-lang/src/semantic/validate/common.rs:129` |
| `SE-028` | Индекс массива вне границ | `takt-lang/src/semantic/expression.rs:129` |
| `SE-029` | Начало среза … выходит за границы массива '…' (размер …) | `takt-lang/src/semantic/expression.rs:414` |
| `SE-030` | Переменная '…' не является массивом | `takt-lang/src/semantic/expression.rs:137` |
| `SE-033` | Состояние, названное справа в проверке `S(Модель) = Состояние`, у этой модели не объявлено (фича — прежде в ячейке стоял фрагмент текста сообщения, а не описание) | `takt-lang/src/semantic/validate/common.rs` |
| `SE-034` | Локальный тип '…' не найден | `takt-lang/src/semantic/type_node.rs:115` |
| `SE-035` | Переменная '…' имеет тип bit, но инициализирована значением … \ | `takt-lang/src/semantic/validate/enums.rs:36` |
| `SE-036` | переменная '…' объявлена, но нигде не используется | `takt-lang/src/semantic/unused.rs:418` |
| `SE-037` | Неявное приведение числа к булевому / недетерминированный переход | `takt-lang/src/semantic/validate/implicit_bool.rs:164` |
| `SE-038` | массив размером MAX_ARRAY_SIZE+1 должен давать ошибку Ce15 | `takt-lang/src/semantic/validate/tests_ce15_array_size.rs:39` |
| `SE-039` | псевдоним типа '…' образует циклическую зависимость | `takt-lang/src/semantic/validate/types.rs:149` |
| `SE-040` | структура '…' содержит дублирующееся поле '…' | `takt-lang/src/semantic/validate/structs.rs:43` |
| `SE-041` | поле '…' структуры '…' ссылается на неизвестный тип '…' | `takt-lang/src/semantic/validate/structs.rs:100` |
| `SE-042` | Перекрывающиеся условия переходов | `takt-lang/src/semantic/validate/nondeterminism.rs:218` |
| `SE-043` | Инициализатор переменной — не вариант её перечисления | `takt-lang/src/semantic/validate/enums.rs:171` |
| `SE-044` | лишняя точка с запятой | `takt-lang/src/lib.rs:1136` |
| `SE-045` | неизвестный именованный блок '…'; допустимые имена: enter, exit, always | `takt-lang/src/lib.rs:1125` |
| `SE-046` | состояние '…' недостижимо из начального состояния | `takt-lang/src/semantic/validate/states.rs:314` |
| `SE-047` | условие перехода всегда истинно — переход безусловный | `takt-lang/src/semantic/validate/constant_conditions.rs:61` |
| `SE-048` | оператор `address` ссылается на несуществующий порт '…' | `takt-lang/src/semantic/validate/ports.rs:117` |
| `SE-049` | адрес порта '…' задан оператором `address` более одного раза | `takt-lang/src/semantic/validate/ports.rs:128` |
| `SE-050` | внешняя карта переопределяет адрес порта '…', заданный в модели | `takt-lang/src/address_map/parse.rs:304` |
| `SE-051` | внешняя карта задаёт адрес для несуществующего порта '…' | `takt-lang/src/address_map/parse.rs:317` |
| `SE-052` | Порт используется в кодогенерации, но не имеет адреса ни из одного источника | `takt-lang/src/address_map/resolve.rs:322` |
| `SE-053` | Рекурсия функций запрещена (цикл в графе вызовов) | `takt-lang/src/address_map/resolve.rs:141` |
| `SE-054` | Имя инварианта конфликтует с существующим условием/переменной | `takt-lang/src/address_map/eval.rs:27` |
| `SE-055` | LTL-формула разобрана, но верификация LTL не реализована (предупр.) | `takt-lang/src/address_map/eval.rs:36` |
| `SE-056` | Неизвестный атом LTL-формулы (предупреждение) | `takt-lang/src/semantic/ltl_check.rs:80` |
| `SE-057` | Неизвестный конструктор параметрического типа (единственный — `q(m, n)`) | `takt-lang/src/semantic/type_node.rs:153` |
| `SE-058` | Литерал float вне диапазона точности `q(m, n)` — непредставим | `takt-lang/src/semantic/type_node.rs:303` |
| `SE-059` | неявное смешение типов '…' и '…' в арифметике fixed-point запрещено; \ | `takt-lang/src/semantic/validate/fixed.rs:254` |
| `SE-060` | Бит адреса порта вне диапазона [0, 63] | `takt-lang/src/address_map/resolve.rs:295` |
| `SE-061` | структура '…' не содержит поля '…' | `takt-lang/src/semantic/validate/member_access.rs:105` |
| `SE-062` | Превышен предел вложенности выражений/условий | `takt-lang/src/semantic/validate/depth.rs:77` |
| `SE-063` | Длительность непредставима в выбранном профиле времени | `takt-lang/src/semantic/duration.rs:150` |
| `SE-064` | Длительность не помещается в счётчик времени | `takt-lang/src/semantic/duration.rs:170` |
| `SE-065` | Смешение `duration` с числом в арифметике | `takt-lang/src/semantic/validate/fixed.rs:262` |
| `SE-067` | Частота тактирования объявлена дважды и по-разному | `takt-lang/src/semantic/time_ast.rs:96` |
| `SE-068` | Выдержка `after` вне условия перехода `ref` | `takt-lang/src/semantic/time_ast.rs:63` |
| `SE-069` | Модель объявила `clock`, но `--tick-hz` не передан (контракт частоты) | `takt-lang/src/semantic/duration.rs:251` |
| `SE-070` | `--tick-hz` не совпадает с объявленной `clock` частотой | `takt-lang/src/semantic/duration.rs:259` |
| `SE-071` | Во что пересчиталась длительность (информационное предупреждение) | `takt-lang/src/semantic/duration.rs:314` |
| `SE-072` | Выдержка `after` не сводится к константной длительности | `takt-lang/src/semantic/condition/after_const.rs` |
| `SE-073` | Вычисляемая выдержка требует частоты, кратной 1000 Гц (иначе множитель `hertz/1000` не цел) | `takt-lang/src/semantic/duration.rs` |
| `SE-074` | Импортированная модель использует объявления подключаемого файла, не импортированные вместе с ней | `takt-lang/src/semantic/import_adopt.rs` |
| `SE-075` | Параметр объявлен вне модели: на верхнем уровне файла либо внутри блока | `takt-lang/src/semantic/declaration.rs` |
| `SE-076` | Аргумент инстанцирования задан не формой `имя := значение` | `takt-lang/src/semantic/extend_args.rs` |
| `SE-077` | Инстанцируемая модель не объявляет параметров вовсе | `takt-lang/src/semantic/extend_args.rs` |
| `SE-078` | У модели нет параметра с таким именем (объявлены другие) | `takt-lang/src/semantic/extend_args.rs` |
| `SE-079` | Имя в модели объявлено, но не как `parameter` | `takt-lang/src/semantic/extend_args.rs` |
| `SE-080` | Параметр задан в одном вызове дважды | `takt-lang/src/semantic/extend_args.rs` |
| `SE-081` | Форма выражения не может быть реализацией модели | `takt-lang/src/semantic/extend.rs` |
| `SE-082` | **RETIRED** (0185-04): временный контроль «аргументы инстанцирования не применяются» (0185-02) снят — значения применяются всеми целями. Номер не переиспользовать | — |
| `SE-083` | Выражение не вычисляется при компиляции; причина названа (переменная, порт, форма, дробная арифметика) | `takt-lang/src/semantic/const_eval/mod.rs` |
| `SE-084` | Функция не вычисляется при компиляции: `extern`, встроенная, нет `return`, несовпадение числа аргументов. **Два места эмиссии**: вычислитель (`const_eval/call.rs`) и проверка инициализаторов объявлений (`validate/init_undefined_read.rs`, фича — вызов `extern` в инициализаторе `var`/`const`) | `takt-lang/src/semantic/const_eval/call.rs`, `takt-lang/src/semantic/validate/init_undefined_read.rs` |
| `SE-085` | Исчерпан предел константного вычисления: шагов либо глубины (цикл определений, незавершаемое тело) | `takt-lang/src/semantic/const_eval/mod.rs` |
| `SE-086` | Специализация модели: имя занято либо параметр/родитель не найден  | `takt-lang/src/semantic/specialize.rs` |
| `SE-087` | Специализация модели с собственными вложенными моделями не поддерживается  | `takt-lang/src/semantic/specialize.rs` |
| `SE-088` | Параметр там, где нужна величина, известная при генерации (`after PARAM`, адрес порта): константой он становится только при `--parameters=specialize` — сообщение называет режим  | `takt-lang/src/semantic/parameter_const.rs` |
| `SE-089` | Числовой литерал не помещается в тип приёмника (`var a: u8 := 300;`): лексер принимает объединение диапазонов всех целых типов, а влезает ли значение в конкретный — знает семантика  | `takt-lang/src/semantic/validate/literal_range.rs` |
| `SE-090` | Однобитный порт с адресом без позиции бита: принят бит 0 (предупреждение). Умолчание нормируется слоем адресов — до `c-hal`/`st-at`/`sv-mmio` и обеих выгрузок  | `takt-lang/src/address_map/resolve.rs` |
| `SE-091` | **RETIRED**: предупреждение о подозрительном адресе 0/1 потеряло предмет — размещение задаётся `at`, а `:=` означает начальное значение, то есть перепутать их больше нечем. Номер не переиспользовать под другой смысл | — |
| `SE-092` | Начальное значение у **входного** порта: значение входа приходит извне, задавать его нечем  | `takt-lang/src/semantic/validate/port_init.rs` |
| `SE-093` | **RETIRED**: временное предупреждение «начальное значение выставляют не все цели» снято — `sv`, `sv-mmio`, `st` и `st-at` научились выставлять его наравне с `c`/`c-hal`/`rust`, и молчаливой потери, о которой оно говорило, больше нет. Номер не переиспользовать под другой смысл | — |
| `SE-094` | Начальное значение порта не вычисляется при компиляции: значение выставляется до первого такта, поэтому переменная модели источником быть не может (задача 03) | `takt-lang/src/semantic/declaration.rs` |
| `SE-095` | Присваивание в позиции значения там, где его пропускает грамматика: **именованный аргумент вызова функции** (`twice(a := 3)`). Форма законна лишь при инстанцировании модели, а на уровне АСД это один узел `Function` — различает только семантика. Прочие формы отсекает `SY-006`  | `takt-lang/src/semantic/validate/assignment_position.rs` |
| `SE-096` | Запись по адресу в ячейку с необъявленным направлением (**предупреждение**): проверить законность записи компилятор не может. Текст называет способ снять — объявить именованный порт с тем же адресом (решение 3B) | `takt-lang/src/semantic/anon_collect.rs` |
| `SE-097` | У обращения `#АДРЕС` не задана разрядность доступа: по адресу может лежать байт, слово или бит, и выбирать за автора нельзя (класс — ширина доступа к MMIO угадыванию не подлежит) | `takt-lang/src/semantic/anon_port.rs` |
| `SE-098` | Позиция бита или разрядность поля выходит за слово доступа (`[0, 63]`, `бит + ширина ≤ 64`), либо позиция бита задана дважды, либо тип не задаёт разрядности. Пределы те же, что у именованного порта (`SE-060`) | `takt-lang/src/semantic/anon_port.rs` |
| `SE-099` | Обращение по адресу в инициализаторе объявления: содержимое памяти до первого такта неизвестно, и эталон дал бы ноль, а `c-hal` — чтение регистра  | `takt-lang/src/semantic/validate/init_undefined_read.rs` |
| `SE-100` | Состояние названо так же, как **дочерняя** модель того же владельца, которую оно реализует: имя состояния и имя модели попадают в одно пространство имён цели, и вход не переводит **ни одна** из целей. Тёзка **соседней** модели законна — это граница дефекта (форма К3) | `takt-lang/src/semantic/validate/name_collisions.rs` |
| `SE-101` | Модель объявлена с реализацией (`= …`) и одновременно содержит собственное состояние: она не может быть и композицией, и автоматом. Прежде такая запись **молча теряла** реализацию — исполнялось только собственное состояние  | `takt-lang/src/semantic/tree.rs` |
| `SE-102` | Файл в позиции **входа исполнения** не содержит ни одного состояния: это библиотека — набор типов, функций и переменных для подключения через `import`. Признак — отсутствие состояний **вовсе**; состояния без стартового — это `SE-011`, и подменять его нельзя. Проверку зовут точки, знающие позицию «вход» (конвейер целей и симулятор), но **не** языковой сервер: библиотеку открывают и правят в редакторе  | `takt-lang/src/semantic/validate/entry.rs` |
| `SE-104` | После формата fixed-point допустим только модификатор `sat` (`q(8, 8) sat` — насыщение вместо переноса). Прочее слово отвергается: опечатка иначе дала бы **молчаливый перенос** там, где автор просил насыщение  | `takt-lang/src/semantic/type_node.rs` |
| `SE-105` | Перечисление объявлено без вариантов: у перечисления обязан быть хотя бы один вариант. Прежде отказ давала **грамматика** (`SY-002` «нераспознанный токен `}`»), то есть правило языка было свойством квантификатора, а не решением; отказ стоит на **объявлении**, потому что при использовании сообщение вырождалось в `SE-043` с пустым списком допустимых вариантов  | `takt-lang/src/semantic/validate/enums.rs` |
| `SE-106` | В реализации состояния (`= M`, `= A \| B`, `= A + B`, форма `model M = A { … }`) стоит модель **без единого состояния**: исполнять в ней нечего. Позиция — **место использования**, сноска — объявление модели (и вложенная модель с состояниями, если это ловушка именования при `import`). Прежде класс не судил никто, и один вход давал **шесть** ответов: `CC-005`/`ST-013`/`RS-013`/`SV-011` без позиции и с пустым именем, а симулятор **рапортовал об успехе** (трасса `[—]`). Модель без состояний **вне** реализации законна — это контейнер объявлений  | `takt-lang/src/semantic/validate/implemented.rs` |
| `SE-107` | Объявление типа (`type`, `struct`, `enum`) занимает имя **встроенного типа языка** (`u8`…`u64`, `i8`…`i64`, `bit`, `bool`, `float`, `duration`, `unit`). Прежде запись принималась молча, и с этого места имя означало объявленное автором, а встроенный тип становился недоступен во всём файле: `type u8 = i64;` давал `int64_t` в цели `c`. Список имён берётся у единственного носителя `builtin_type_by_name`, поэтому `u128` под запрет **не попадает** — встроенным он не является | `takt-lang/src/semantic/type_registry.rs` |
| `SE-108` | Повторное объявление типа с тем же именем — своё либо пришедшее через `import`. Несёт **две** позиции: где объявлено повторно и где впервые (образец 0195). Прежде второе объявление молча затирало первое, и автор узнавал об этом из `SE-061` «структура не содержит поля» — диагностики о следствии. Если первое объявление пришло из подключённого файла, текст это называет: координата заметки опускается, потому что она принадлежит другому файлу | `takt-lang/src/semantic/type_registry.rs`, `takt-lang/src/semantic/import/adopt.rs` |
| `SE-109` | Инициализатор переменной ссылается на переменную, объявленную **ниже** по тексту. Имя в инициализаторе значит начальное значение и ссылается только назад (правило фичи); прежде запись принималась молча и давала разные значения у эталона (0) и в прошивке (1). Константы правилом не затронуты: у них ссылка вперёд разрешается проходами до неподвижной точки | `takt-lang/src/semantic/declaration.rs` |
| `SE-110` | Имя состояния или модели само по себе условием не является (нужен паттерн `S(Модель) = Состояние`) | `takt-lang/src/semantic/validate/common.rs` |
| `SE-111` | Левая часть присваивания не обозначает места записи (вызов функции, литерал, арифметическое выражение, приведение типа, …). Писать можно в переменную, поле структуры, элемент или срез массива, отдельный бит, порт либо ячейку `#АДРЕС`; прежде запись принималась молча, а невалидный код отвергали чужие инструменты (`cc`, `rustc`, `iec2c`) | `takt-lang/src/semantic/validate/assignment_place.rs` |
| `SE-112` | Целью записи назначена константа: она обозначает величину, а не хранилище. Прежде эталон запись **молча исполнял**, а цель `c` печатала присваивание макросу | `takt-lang/src/semantic/validate/assignment_place.rs` |
| `SE-113` | Инициализатор объявления читает **порт**: значение порта до первого такта не определено. Один вход давал **три** поведения и **два** отказа: эталон — ноль, `c`/`c-hal` — чтение (у `c-hal` настоящего регистра), `st`/`st-at` — молчаливую потерю инициализатора, `rust` — `RS-022`, `sv` — `SV-002`. Правило и обход общие с `SE-099`: ячейка и порт суть одна память  | `takt-lang/src/semantic/validate/init_undefined_read.rs` |
| `SE-114` | Инициализатор объявления — **дробная арифметика**, которую компилятор не может вычислить точно (деление и подобное): округление дробных задано эталоном. Точная арифметика (`+`, `-`, `*`, в том числе с целым) сюда не доходит — её сворачивает `const_eval::decimal`. Прежде один вход давал ноль у эталона, настоящее число у `c`/`rust`, молчаливую потерю у `st` и `0.1875` вместо `3.0` в поле `q(4, 4)`  | `takt-lang/src/semantic/declaration.rs` |
| `SE-115` | Структура объявлена **без полей**: запись невалидна по стандарту C (6.7.2.1 — расширение GNU, `cc -pedantic` предупреждает) и отвергается `iec2c` («no structure element declared»). Контроль: **непустая** структура проходит оба инструмента — ломается пустота. Симметрично `SE-105`, но причины разные  | `takt-lang/src/semantic/validate/structs.rs` |
| `SE-116` | Переход **недостижим**: он записан после безусловного, а тот завершает выбор. Ce14 (`SE-037`) этот класс не видит — она ищет **несколько** безусловных рёбер, здесь безусловное одно. Правило исполняли все цели и эталон (0213), но автору о нём не говорил никто; в корпусе такой записи нет ни одной  | `takt-lang/src/semantic/validate/unreachable_edge.rs` |
| `SE-117` | Индексируется не массив: `x[i]` при `x`, который массивом не объявлен. Прежде диагностика строилась конверсией `From<&str>` — код печатался `[?]`, а позиция вырождалась в «начало первого файла»  | `takt-lang/src/semantic/condition/mod.rs` |
| `SE-118` | Локальная функция объявлена **без тела**: тело обязательно, внешнюю функцию объявляют как `extern fn`. Прежде — `[?]` и ложная координата `1:1`  | `takt-lang/src/semantic/function.rs` |
| `SE-119` | **Внутренний инвариант семантики нарушен** — дефект компилятора, а не ошибка модели: до этого места вход обязана была отсечь более ранняя проверка. Общая воронка для мест, чью недостижимость держат другие проверки (`SE-034`, `SE-004`, `SE-081`); вид нарушения называется словом. Образец — `CC-023` цели `c`  | `takt-lang/src/semantic/internal.rs` |
| `SE-120` | **RETIRED**: специализация модели, подключённой из другого файла, **работает** — копия перепривязывает свои тела к себе (`import::adopt::adopt_specialized_copy`), и `Rc` с исходной моделью больше не делятся. Номер не переиспользовать под другой смысл | — |
| `SE-121` | Приведение `as` переполняет **знаковый** тип: значение в него не помещается, а знаковое переполнение есть ошибка программы (в C — неопределённое поведение, правило ADR). Прежде такой вход давал **четыре** ответа: эталон `0` молча, `c`/`rust` `44`, `st` теряла инициализатор  | `takt-lang/src/semantic/const_eval/mod.rs` |
| `SE-122` | Число аргументов вызова не совпало с числом параметров объявления (встроенная, локальная либо `extern`-функция). Прежде арность не проверял **никто**: цель `c` печатала `Two(model, 1)` при нулевом коде возврата (`cc`: «too few arguments»), а эталон останавливал прогон уже в такте  | `takt-lang/src/semantic/validate/arity.rs` |
| `SE-123` | Число элементов агрегата не отвечает объявлению: у массива — объявленному размеру, у структуры — числу полей. Прежде запись означала **разное**: эталон хранил лишние элементы, `rust` печатал невалидный код при нулевом коде возврата, `st` теряла инициализатор молча, `c` отвечала `CC-017`  | `takt-lang/src/semantic/validate/aggregate_length.rs` |
| `SE-124` | Структура содержит себя — прямо либо через цепочку полей (в том числе через элемент массива и через псевдоним типа): размер такого значения бесконечен, представления у него нет ни у одной цели. Прежде запись была недостижима иначе: одна из структур всегда ссылалась вперёд и получала неподдержанный тип; фича сняла зависимость от порядка объявления и потому ввела запрет | `takt-lang/src/semantic/validate/struct_cycle.rs` |
| `SE-125` | Разряд за **объявленной** шириной значения: у `[bit;96]` законны разряды 0..95, хотя хранится оно в двух словах. Прежде вход давал три исхода: эталон, `c` и `rust` писали молча, `verilator` вывод `sv` **отвергал** (`SELRANGE`) при нулевом коде возврата, `st` отказывала. Объявленная ширина — контракт (решение заказчика); переменный индекс остаётся за исполнением (`SIM-011`) | `takt-lang/src/semantic/validate/member_access.rs` |
| `SE-126` | Неизвестный атрибут объявления функции: допустимы `inline` (подставлять тело в место вызова) и `noinline` (не подставлять). Имя атрибута — обычный идентификатор, грамматика о наборе не знает (тот же приём, что у контекстных слов), поэтому набор судит семантика: молчаливый пропуск означал бы, что опечатка отменяет намерение автора  | `takt-lang/src/semantic/function.rs` |
| `SE-127` | Атрибут `inline` на **внешней** функции: тела у неё нет, подставлять нечего — её вызов остаётся вызовом в порождённом коде  | `takt-lang/src/semantic/function.rs` |
| `SE-128` | Функция помечена `inline`, но её тело подстановкой не выражается, и причина названа: `return` внутри цикла с заранее неизвестным числом итераций (тело гасится признаком выхода — цикл стал бы бесконечным), отсутствие `return` вовсе либо тип результата без начального значения. Ранний выход из ветвления и из `for` со счётчиком подстановке **поддаётся**; при эвристике (`--inline=auto`) непригодное тело просто остаётся вызовом, а при явном атрибуте молчание означало бы неисполненное указание автора | `takt-lang/src/semantic/inline/early.rs` |
| `SE-129` | Вставка `assembly` адресована неизвестной цели: имя — **язык вывода** (`c`, `st`, `rust`, `sv`), а не режим сборки (`c-hal`, `st-at`, `sv-mmio` печатают язык базовой цели). Метка, которой нет среди языков, не включает вставку нигде: молчаливо недействующая метка дала бы пустой вывод при нулевом коде возврата  | `takt-lang/src/semantic/target_block.rs` |
| `SE-130` | Узел-поддерево составного порта (`cfg.tail`, `bus[0]`, сам порт целиком) в позиции, где цель не может объявить временную: условие перехода, именованное условие, охранная формула, условие и шаг цикла. Порт развёрнут по листьям (0350/0390/0417), и узел собирается из них временной — а её объявляют только внутри тела блока. У цикла пролог встал бы перед ним, и значение перестало бы обновляться по итерациям: молчаливое расхождение вместо громкого отказа. Текст называет обход — перенести значение узла в переменную модели  | `takt-lang/src/semantic/condition/port_subtree.rs` |
| `SE-132` | `break` или `continue` **вне цикла**: прерывать нечего. Отказ, а не поведение: запись принималась молча, и потребители расходились — эталон прерывал тело и проверял переходы, а цель `c` печатала `break;` внутри ветви `case`, из-за чего управление выходило из `switch` мимо присваивания состояния, и автомат не уходил из состояния никогда (замер-03). Смысл «прервать тело блока» язык не обещает: в C для него нужен не `break`, а флаг или `goto`, то есть новая конструкция для восьми целей  | `takt-lang/src/semantic/loop_context.rs` |
| `SE-131` | Ветвь `match` **недостижима**: её образец уже встречался выше, а `match` берёт первое совпадение. То же у второй ветви `_`. Предупреждение, а не отказ: смысл записи определён, и её исполняют эталон и четыре цели (довод тот же, что у `SE-116`). Невалидный вывод чинится печатью — цели `c`, `rust` и `sv` недостижимую ветвь опускают: `cc` отвечал «duplicate case value», `clippy` — «same condition», `verilator` — «Multiple default statements»  | `takt-lang/src/semantic/validate/duplicate_match_arm.rs` |

### SIM — симулятор

| Код | Значение | Источник |
|---|---|---|
| `SIM-001` | Деление или остаток на ноль | `takt-sim/src/eval/error.rs:121` |
| `SIM-002` | Сдвиг на отрицательное число или ≥ 64 бит | `takt-sim/src/eval/error.rs:122` |
| `SIM-003` | Значение не помещается в знаковый тип назначения | `takt-sim/src/eval/error.rs:123` |
| `SIM-004` | Переполнение внутреннего 64-битного представления | `takt-sim/src/eval/error.rs:124` |
| `SIM-005` | Операция не определена для операндов таких типов | `takt-sim/src/eval/error.rs:125` |
| `SIM-006` | Значение нельзя привести к типу назначения | `takt-sim/src/eval/error.rs:126` |
| `SIM-007` | Тип не поддерживается симулятором (например, структуры) | `takt-sim/src/eval/error.rs:127` |
| `SIM-008` | Не удалось разобрать вещественный литерал | `takt-sim/src/expression.rs:251` |
| `SIM-009` | Переменная не найдена | `takt-sim/src/expression.rs:54` |
| `SIM-010` | Ошибка доступа к массиву (не массив, нецелый индекс, выход за границы) | `takt-sim/src/eval/error.rs:138` |
| `SIM-011` | Ошибка доступа к биту (не целое, номер бита вне разрядов значения) | `takt-sim/src/eval/error.rs` |
| `SIM-012` | Доступ к полю у значения, не являющегося структурой | `takt-sim/src/eval/access.rs:132` |
| `SIM-013` | Модель или состояние в роли значения (вне сравнения `S(Модель) = Состояние`) | `takt-sim/src/predicate.rs` |
| `SIM-014` | Конструкция не поддерживается симулятором (строки и др.) | `takt-sim/src/expression.rs:242` |
| `SIM-015` | Пустое выражение/условие не может быть вычислено | `takt-sim/src/expression.rs:232` |
| `SIM-016` | Неразрешённый узел не может быть вычислен | `takt-sim/src/expression.rs:237` |
| `SIM-017` | Оператор пока не поддерживается симулятором | `takt-sim/src/unit/statement.rs:362` |
| `SIM-018` | Превышен предел итераций цикла (100 000) | `takt-sim/src/unit/statement.rs:421` |
| `SIM-019` | Внешняя функция без тела: симуляция значения невозможна | `takt-sim/src/unit/statement.rs:530` |
| `SIM-020` | Встроенная функция значения в этой позиции не имеет (`S(Модель)` вне условия); вычислимые исполняются с фичи | `takt-sim/src/unit/statement.rs:538` |
| `SIM-021` | Неверное число аргументов при вызове функции | `takt-sim/src/unit/statement.rs:559` |
| `SIM-022` | Превышена глубина рекурсии (256) | `takt-sim/src/unit/statement.rs:590` |
| `SIM-023` | Функция не вернула значение | `takt-sim/src/unit/statement.rs:619` |
| `SIM-024` | `break`/`continue` вне цикла | `takt-sim/src/unit/statement.rs:627` |
| `SIM-025` | Нарушен инвариант / Guard-формула (assert языка Takt) | `takt-sim/src/unit/statement.rs:269` |
| `SIM-026` | Инициализатор структуры содержит не столько полей, сколько объявлено | `takt-sim/src/eval/error.rs:129` |
| `SIM-027` | Обращение к несуществующему полю структуры | `takt-sim/src/eval/access.rs:103` |
| `SIM-028` | Несоответствие типа структуры при присваивании | `takt-sim/src/eval/error.rs:131` |
| `SIM-029` | Обращение к структуре по номеру бита | `takt-sim/src/eval/access.rs:109` |
| `SIM-030` | Порт сценария не найден: имя не значится среди портов модели (квалифицированное `Модель::порт`) либо среди портов нужного направления  | `takt-sim/src/runner.rs` |
| `SIM-031` | Имя порта в сценарии неоднозначно: так названы порты нескольких моделей — нужна квалификация `Модель::порт`  | `takt-sim/src/runner.rs` |
| `SIM-032` | Длина позиционного массива сценария не равна числу портов направления: лишние значения игнорируются, недостающие не задаются  | `takt-sim/src/runner.rs` |
| `SIM-034` | Аргументы инстанцирования заданы композиции без собственных состояний — значения хранить негде  | `takt-sim/src/unit/builder.rs` |
| `SIM-035` | Значение аргумента инстанцирования не вычислено построителем юнита  | `takt-sim/src/unit/builder.rs` |
| `SIM-036` | Проверка состояния под-модели невозможна: модель не запущена в прогоне либо справа не имя состояния  | `takt-sim/src/predicate.rs` |
| `SIM-037` | Позиционная форма сценария устарела: индекс привязан к алфавитному месту имени порта  | `takt-sim/src/runner.rs` |

### ST — цель Structured Text

| Код | Значение | Источник |
|---|---|---|
| `ST-001` | Ошибка записи файла `.st` (ввод-вывод) | `takt-lang/src/generator/st/mod.rs:95` |
| `ST-002` | переменная обязана считаться используемой — иначе тест проверял бы фильтр | `takt-lang/src/generator/st/st_decl.rs:662` |
| `ST-004` | Размещение порта в цели `st-at`: адрес не выражается директивой `AT %…` (нет адреса, недопустимая ширина или бит) | `takt-lang/src/generator/st/st_at.rs:159` |
| `ST-005` | **RETIRED**: умолчание «бит 0» нормируется слоем адресов и объявляется `SE-090` — печатнику локации говорить об одном решении второй раз незачем. Номер не переиспользовать под другой смысл | — |
| `ST-006` | Порт '…' не булев, а в адресе задан бит: у локации %…… \ | `takt-lang/src/generator/st/st_at.rs:113` |
| `ST-007` | Массив нулевого размера ('…') невыразим в IEC 61131-3: \ | `takt-lang/src/generator/st/st_type.rs:177` |
| `ST-008` | … '…' не найдена в модели: объявление типа для ST построить нельзя | `takt-lang/src/generator/st/st_type.rs:233` |
| `ST-009` | Тело внешней функции неизвестно, а IEC 61131-3 требует тела | `takt-lang/src/generator/st/st_func.rs:443` |
| `ST-010` | LTL-формул (…) в блоке кода: в Structured Text они не \ | `takt-lang/src/generator/st/st_stmt.rs:211` |
| `ST-011` | Не транслируется в Structured Text: … | `takt-lang/src/generator/st/st_expr.rs:312` |
| `ST-012` | Корневой элемент карты не является моделью | `takt-lang/src/generator/st/mod.rs:113` |
| `ST-013` | q({m}, {n}): W = … > 32 — точное произведение шириной 2W не влезает в LINT | `takt-lang/src/generator/st/st_fixed.rs:242` |
| `ST-014` | Приведение float → q в цели st (LREAL_TO_INT округляет к ближайшему) | `takt-lang/src/generator/st/st_fixed.rs:177` |
| `ST-015` | Выдержка `after` попала в печатник условий вместо `st_model` (контроль пути: фича сняла прежний смысл «`duration` не поддерживается») | `takt-lang/src/generator/st/st_expr.rs` |
| `ST-016` | Вычисляемая выдержка `after` в профиле «часы» целью `st` не поддерживается (переменный `PT` таймера `TON`) | `takt-lang/src/generator/st/st_model.rs` |
| `ST-017` | Аргумент инстанцирования не печатается инициализатором экземпляра FB  | `takt-lang/src/generator/st/st_model.rs` |
| `ST-018` | Обращение к ячейке по адресу целью `st` не транслируется: она порождает библиотеку блоков, где порт есть вход/выход `FUNCTION_BLOCK`, а локации нет вовсе. Соберите целью `st-at`  | `takt-lang/src/generator/st/mod.rs` |
| `ST-019` | Поле **со смещением** (`#0xАДРЕС:БИТ as ТИП`) не выразимо локацией IEC 61131-3: она даёт либо бит (`%MX512.3`), либо целое слово (`%MB512`). Молча выронить смещение нельзя — вышло бы обращение к другому месту памяти  | `takt-lang/src/generator/st/mod.rs` |
| `ST-020` | Имя содержит символ вне алфавита идентификатора IEC 61131-3 (латиница, цифры, `_`). То же, что `SV-018`: ограничение цели, не языка. Прежде `iec2c` отвечал «unclosed output variable(s) declaration» — по этому сообщению причину не опознать  | `takt-lang/src/generator/st/st_reserved.rs` |
| `ST-021` | Перенос представления `q(m, n)` к `W = m + n` требует модуля `2^W`, а при `W ≥ 63` он в `LINT` (знаковое 64) непредставим. Молча считать по иному модулю нельзя: результат разошёлся бы с эталоном  | `takt-lang/src/generator/st/st_fixed.rs` |
| `ST-022` | **Предупреждение.** Охранная формула (`invariant`, `: [Guard] …;`) не переводится в Structured Text: конструкции `assert` в IEC 61131-3 не существует, а выразить проверку иначе значило бы добавить в программу ПЛК переменную, которой нет в модели, и семантику «нарушено, но работаем дальше» — расходящуюся с эталоном (`SIM-025` прогон останавливает). Предупреждение, а не отказ (решение заказчика 2026-08-15): трансляция продолжается, автор лишь узнаёт, что охрана в прошивку не попала. Выдаётся на каждую формулу — одно на модель означало бы молчаливую потерю второй  | `takt-lang/src/generator/st/mod.rs` |
| `ST-023` | Имя переменной совпадает (без учёта регистра) с именем объявленного типа: идентификаторы IEC 61131-3 регистронезависимы и делят одно пространство имён, поэтому `var pair: Pair` для MatIEC — два объявления одного имени. Ответ `iec2c` обманчив («invalid located variable declaration» с указанием на `AT %…`, которых в объявлении нет), а `taktc` возвращал ноль. Отказ принадлежит цели, а не языку: модель остаётся валидной для `c`, `c-hal`, `rust`, `sv` и `sv-mmio` (прецедент `ST-014`/`SV-012`) | `takt-lang/src/generator/st/st_reserved.rs` |
| `ST-024` | Имя размещённого порта совпадает с именем POU, которое цель печатает рядом в конфигурации: блока, программы (`<Root>Main`), ресурса (`Res0`) либо экземпляра (`Inst0`). Идентификаторы IEC регистронезависимы и делят одно пространство, поэтому порт `probe` в файле `probe.takt` для MatIEC — второе объявление того же имени: «invalid global variable(s) declaration» при нулевом коде возврата `taktc`. Список занятых имён — **из замера**: совпадение с именем конфигурации и задачи `iec2c` принимает, а локальную переменную правило не задевает  | `takt-lang/src/generator/st/st_reserved.rs` |
| `ST-025` | Имя объявления совпадает (без учёта регистра) с именем, которое цель печатает в том же POU: другой переменной, порта, константы либо служебного имени автомата (`state`, `is_done`). Пространство имён IEC плоское и регистронезависимо, поэтому `var level` рядом с `const LEVEL` для MatIEC — одно имя, объявленное дважды: «invalid located variable declaration» при нулевом коде возврата `taktc`, тогда как остальные потребители вход исполняют. Замер нашёл шесть видов таких пар; проверка стоит после фильтра использования — неиспользуемое объявление цель не печатает  | `takt-lang/src/generator/st/st_reserved.rs` |
| `ST-026` | Подчёркивание в недопустимом месте имени: в конце либо два подряд — идентификатор IEC 61131-3 такой формы не принимает. Правило снято прогоном `iec2c`, а не чтением стандарта: ведущее подчёркивание инструмент принимает, хотя стандарт запрещает и его. Прежде имя доезжало до вывода как есть, и `iec2c` отвечал «invalid located variable declaration» при нулевом коде возврата `taktc`. Отказ принадлежит цели: имя законно, и модель остаётся валидной для прочих  | `takt-lang/src/generator/st/st_reserved.rs` |

### SV — цель SystemVerilog

| Код | Значение | Источник |
|---|---|---|
| `SV-001` | Ошибка записи файла `.sv` (ввод-вывод) | `takt-lang/src/generator/sv/mod.rs:95` |
| `SV-002` | Конструкция не выразима в синтезируемом RTL: цикл `loop`/`while`, цикл `for` с **неизвестными при синтезе** границами, досрочный возврат из функции, срез массива, приведение к нескалярному типу. Состав менялся: `match` переводится с фичи (`case`), статический `for` — с 0321, приведение между целыми — с 0323, агрегат массива — с 0309 | `takt-lang/src/generator/sv/` |
| `SV-003` | `float` в типе **без** `--float-as-q` | `takt-lang/src/generator/sv/sv_fixed.rs:173` |
| `SV-004` | Вещественный тип (float) не существует в синтезируемом RTL | `takt-lang/src/generator/sv/sv_type.rs:88` |
| `SV-005` | `extern fn` в цели `sv`: внешнего кода в синтезируемом RTL нет — вызов выразить нечем | `takt-lang/src/generator/sv/sv_expr.rs:84` |
| `SV-006` | Порт `inout` **с адресом** в цели `sv-mmio`: направление принадлежит биту регистрового файла — бит либо пишется шиной, либо читается ею. У цели `sv` порт `inout` **переводится** с фичи (три сигнала `_i`/`_o`/`_we`); отказ остался только у регистрового интерфейса | `takt-lang/src/generator/sv/sv_mmio.rs` |
| `SV-007` | порт с именем `clk`, `rst_n`, `is_done`, `state`, `state_next` | `takt-lang/src/generator/sv/sv_module.rs:371` |
| `SV-008` | неконстантный `enter` стартового состояния | `takt-lang/src/generator/sv/sv_fsm.rs:81` |
| `SV-009` | Деление/остаток по переменному делителю — в RTL нет аппаратного делителя (предупреждение) | `takt-lang/src/generator/sv/sv_expr.rs:108` |
| `SV-010` | Корневой элемент карты не является моделью | `takt-lang/src/generator/sv/mod.rs:119` |
| `SV-011` | Состояние '…' не найдено | `takt-lang/src/generator/sv/sv_map.rs:151` |
| `SV-012` | имя = ключевое слово SystemVerilog (`fork`, `wire`, `time`, …) | `takt-lang/src/generator/sv/sv_module.rs:387` |
| `SV-013` | Порт занимает биты адреса шире регистра цели sv-mmio | `takt-lang/src/generator/sv/sv_mmio.rs:85` |
| `SV-014` | Имя зарезервировано регистровым интерфейсом цели sv-mmio | `takt-lang/src/generator/sv/sv_mmio.rs:101` |
| `SV-015` | Выдержка `after` попала в печатник условий вместо `sv_time` (контроль пути: фича сняла прежний смысл «`duration` не поддерживается») | `takt-lang/src/generator/sv/sv_expr.rs` |
| `SV-016` | Модель инстанцирована с разными наборами аргументов: уплощение цели `sv` даёт экземплярам одни регистры, разные настройки невыразимы  | `takt-lang/src/generator/sv/sv_fsm.rs` |
| `SV-017` | Обращение к ячейке по адресу целью `sv` не транслируется: у RTL адресного пространства нет — сигнал приходит на вывод кристалла. Соберите парной целью `sv-mmio`, где ячейка становится регистром файла  | `takt-lang/src/generator/sv/mod.rs` |
| `SV-018` | Имя содержит символ вне алфавита идентификатора SystemVerilog (латиница, цифры, `_`, `$`). Отказ принадлежит **цели**, а не языку: `c` и `rust` такие имена переводят, их проверки вывод принимают. Прежде отказ приходил от `verilator` на порождённом файле  | `takt-lang/src/generator/sv/sv_module.rs` |
| `SV-019` | Флаг `--bus` (адаптер шины) применён там, где регистрового файла нет: либо у модели ни одного порта с адресом, либо взята цель `sv` (у неё регистрового файла нет **по устройству** — MMIO-адрес для чистого RTL бессмыслен). Один код, два повода, и повод **назван словами** в тексте: сообщение «нет портов с адресом» на цели `sv` было бы ложью (образец `CC-023`) | `takt-lang/src/generator/sv/sv_apb.rs` |
| `SV-020` | Имя порта совпадает с именем модуля: `verilator` под `-Wall` отвечает `VARHIDDEN`, а проверка цели считает предупреждение ошибкой. Имя модуля строится из имени модели, а у корневой — из имени файла, поэтому совпадение возникает само собой (`out probe` в `probe.takt`). Проверяются порты модели, а не список портов модуля: в режиме `sv-mmio` адресованный порт объявляется сигналом, и `VARHIDDEN` приходит на него. Имя типа и имя функции конфликта не дают — замерено  | `takt-lang/src/generator/sv/sv_module.rs` |

### SY — парсер

| Код | Значение | Источник |
|---|---|---|
| `SY-001` | Недопустимый токен | `takt-lang/src/lib.rs:151` |
| `SY-002` | Нераспознанный токен | `takt-lang/src/lib.rs:163` |
| `SY-003` | Лишний токен после завершения конструкции | `takt-lang/src/lib.rs:171` |
| `SY-004` | Неожиданный конец файла | `takt-lang/src/lib.rs:176` |
| `SY-006` | Присваивание в позиции значения: `:=` разбирается только как оператор тела, шаг `for` и именованный аргумент вызова. Прежде форму принимала грамматика, а отвергала семантика (`SE-095`) — граница языка была описана в двух местах  | `takt-lang/src/parse_entry.rs` |
| `SY-005` | Превышен предел вложенности разбора (`MAX_PARSE_DEPTH`): дерево программы глубже, чем переживают рекурсивные потребители АСД — печать форматтера, `Clone`, `Drop`  | `takt-lang/src/lib.rs` |
| `SY-007` | Выражение в позиции оператора не имеет эффекта: оператором остаются присваивание и вызов функции. Прежде язык принимал любое выражение, а отказ давал **чужой** инструмент на порождённом файле (`cc -Wall -Werror`: `-Wunused-value`), решение 6A | `takt-lang/src/parser/lex_error.rs` |
| `SY-008` | Адресный литерал `0xАДРЕС:бит` стоит там, где вычисляется значение: адрес есть свойство размещения (`at`, оператор `address`, внешняя карта), а ячейка адресуется формой `#АДРЕС as ТИП`. Прежде форма принималась и давала молчаливое расхождение эталона и цели `c` (решение 7A) | `takt-lang/src/parse_entry.rs` |

