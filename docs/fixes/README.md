# Реестр исправлений

Стадия 7 жизненного цикла (свод в [../RULE.md](../RULE.md)). Исправления
`XXXX-YY-slug.md` заводятся при необходимости (по результатам тестирования) и
проходят весь путь фичи. `XXXX` — номер исходной фичи, `YY` — номер исправления.

Заготовка создаётся из шаблона [`../templates/fixes.md`](../templates/fixes.md).

| Исправление | Фича | Заголовок | Документ |
|------|------|-----------|----------|
| 0378-01 | 0378 | `ST-023` отвергала **тёзку перечисления**: список занятых имён брался у всех объявлений модели (`structs`, `enums`, `types`), тогда как цель печатает в разделе `TYPE` **только структуры** — перечисление идёт целым типом плюс `VAR CONSTANT` (откат Option C, 0041-02), псевдоним раскрывается. `var mode: Mode;` получал ложный отказ на входе, который `iec2c` **принимает** — **ИСПРАВЛЕН** (список строится из того, что цель печатает; два контрпримера — тёзка перечисления и тёзка псевдонима) | [0378-01-enum-namesake-false-positive.md](0378-01-enum-namesake-false-positive.md) |
| 0023-02 | 0023 | навигация к константам enum и к портам в выражениях `port.N` | [0023-02-enum-variants-and-port-members.md](0023-02-enum-variants-and-port-members.md) |
| 0272-02 | 0272 | Ловушка разброса в контроле обвязки замера падала под нагрузкой: пара `sleep 1`/`sleep 3` при секундной гранулярности `date +%s` измерялась как «3 с и 3 с» — разброс 0 %, и негодный замер объявлялся годным — **ИСПРАВЛЕН** (контраст увеличен до `sleep 1`/`sleep 6`; тот же класс, что 0272-01, но в соседнем условии — ловушка на времени обязана иметь запас на замедление) | [0272-02-measure-guard-spread-flaky.md](0272-02-measure-guard-spread-flaky.md) |
| 0182-04 | 0182 | Импорт не переносил **функции**: `import { f } from "lib"` отвечал `SE-017` «идентификатор не найден», хотя функция объявлена — категории `functions` в переносе не было; форма `import "lib";` целиком требовала импортировать `'p'` и `'r'` — параметр и локальную переменную функции — **ИСПРАВЛЕН** (ветка переноса с усыновлением владельца; `adopt_whole_file` переносит типы, их устройство и функции; библиотека операций стала выразимой — `pid_heater` зовёт `pid_compute` вместо копии закона, трасса не изменилась) | [0182-04-import-functions.md](0182-04-import-functions.md) |
| 0182-03 | 0182 | Импорт переносил **имя** типа, но не его устройство: `import { Pid }` давал цели `c` `Pid p;` без `typedef` (порождённый файл не компилировался: `unknown type name`), а симулятору — агрегат массивом (`SIM-012`), при этом `taktc` рапортовал об успехе (**Tier 1**) — **ИСПРАВЛЕН** (`carry_definition` переносит `structs`/`enums` вместе с именем, в том числе под псевдонимом; дожило потому, что ни один пример корпуса структуру не импортировал) | [0182-03-imported-type-definition.md](0182-03-imported-type-definition.md) |
| 0182-02 | 0182 | Файл без единого состояния — **библиотека**, а не ошибка: как вход отвергается новым `SE-102` с позицией и названной причиной, через `import` работает как прежде — **ИСПРАВЛЕН** (прежде `taktc` отвечал `SE-011` «должна содержать начальное состояние» из недр генератора и без позиции, а `takt-sim` рапортовал «Завершено: модель достигла терминального состояния за 1 шагов» — прогон автомата, которого в файле нет) | [0182-02-library-file-without-start.md](0182-02-library-file-without-start.md) |
| 0182-01 | 0182 | ПИД-регулятор переописан как **тип данных с операциями** (структура + `pid_init`/`pid_compute`/`pid_reset`) вместо модели-автомата — по образцу на C от заказчика; применение получает от библиотеки тип и протокол — **ИСПРАВЛЕН** (у автомата регулятор один на программу, у типа — столько, сколько объявлено переменных) | [0182-01-pid-law-as-type-and-functions.md](0182-01-pid-law-as-type-and-functions.md) |
| 0198-01 | 0198 (+0024) | Печать структуры теряет **место** комментария: ветка `ModelElement::Struct` печатала запись целиком через `out.line`, минуя привязку, — комментарий обо всей записи уезжал за `}` и становился ведущим у следующего элемента (та же участь у комментариев полей) — **ИСПРАВЛЕН** (`print_struct` по образцу `print_enum`; корпус класс не ловит: 21 запись `struct` на 308 файлов, комментариев в них ноль) | [0198-01-struct-comment-placement.md](0198-01-struct-comment-placement.md) |
| 0197-01 | 0197 (+0024/0226) | Канон стиля пересмотрен заказчиком: K&R `} else {` (и плоская цепочка `} else if c {`), имена `cond`/`invariant` — `UpperCamelCase` (прежде не проверялись вовсе, в том числе `invariant` в теле состояния), перечисление — по варианту на строке — **ИСПРАВЛЕН** (правило жило в трёх записях: печать форматтера, слой стиля, свод словами; машинной сверки «текст ↔ форматтер» нет и быть не может) | [0197-01-kr-else-cond-case-enum-lines.md](0197-01-kr-else-cond-case-enum-lines.md) |
| 0199-01 | 0199 | `taktc fmt` теряет реализацию модели (`model M = A \| B { … }`) и печатает невалидную короткую форму | [0199-01-fmt-drops-model-implement.md](0199-01-fmt-drops-model-implement.md) |
| 0010-01 | 0010 | Вырожденное условие принятия автомата Бюхи (GPVW) | [0010-01-buchi-acceptance.md](0010-01-buchi-acceptance.md) |
| 0098-01 | 0098 | Форматирование принятого по умолчанию HAL цели c-hal (одна строка → многострочно) | [0098-01-hal-formatting.md](0098-01-hal-formatting.md) |
| 0118-02 | 0118 (+04/05/10) | Такт — не единица физической величины: движение/уровень с датчиков и приводов, не счётом тактов (правило «Пример раздела: функциональный, из практики, с разбором-выносками»); лифт/бак/светофор/бойлер | [0118-02-tick-not-physical.md](0118-02-tick-not-physical.md) |
| 0124-01 | 0124 | Стиль графов верификации: равный радиус вершин + подписи наклонным ГОСТ тип А (контурами, самодостаточный SVG); поля документа 1.5 см; авто-установка шрифта в Makefile | [0124-01-graph-style-gost.md](0124-01-graph-style-gost.md) |
| 0118-01 | 0118 (+0124) | Раздел «Практический пример» — модель группового управления лифтами (верификация с DOT-графами, симуляция, генерация 6 целей в приложение) | [0118-01-practical-example.md](0118-01-practical-example.md) |
| 0022-01 | 0022 | Открытый верхний диапазон совместимости IDE (until-build) | [0022-01-untilbuild-open-range.md](0022-01-untilbuild-open-range.md) |
| 0003-01 | 0003 (+0050) | Тихая мистрансляция `+` внутри `\|` в целях C и Rust — заведён (Tier 1, молчаливо неверный вывод) | [0003-01-concat-in-parallel-silent.md](0003-01-concat-in-parallel-silent.md) |
| 0023-01 | 0023 | Проверка совместимости с новыми IDE (verifyPlugin) + валидность дескриптора | [0023-01-verifyplugin-descriptor.md](0023-01-verifyplugin-descriptor.md) |
| 0045-01 | 0045 | Устаревшие стабы SV в каталоге вывода (доделка проверки) — ИСПРАВЛЕН | [0045-01-stale-sv-artifacts.md](0045-01-stale-sv-artifacts.md) |
| 0053-01 | 0053 | `FileTable::default()` сталкивает первый импорт с корнем (`file_no` 0) — заведён, блокирует 0056-01 | [0053-01-file-table-default-collision.md](0053-01-file-table-default-collision.md) |
| 0005-01 | 0005 (+0029) | Цель `c` теряет знак перечисления — переход мёртв молча (Tier 1) — заведён, чинится в объёме 0060 | [0005-01-c-enum-signedness.md](0005-01-c-enum-signedness.md) |
| 0020-01 | 0020 | Бит адреса порта не проверяется — `c-hal` читает не тот бит (8…31) либо даёт UB (≥32); корпус содержит случай (Tier 1) — **ИСПРАВЛЕН** фичей [проверка диапазона бита адреса порта и безопасный принятый по умолчанию HAL](../features/0098-port-bit-range-safe-hal.md) (SE-060 + HAL по слову + проверка c-hal); разблокировал 0062 | [0020-01-port-bit-out-of-range.md](0020-01-port-bit-out-of-range.md) |
| 0041-01 | 0041 | Цель `st` молча склеивает одноимённые `fn` разных моделей — автомат стоит; `iec2c` принимает (Tier 1) — заведён, чинится в объёме 0065 | [0041-01-st-fn-dedup-silent.md](0041-01-st-fn-dedup-silent.md) |
| 0057-01 | 0057 | Симулятор не исполняет композицию с переходом `next` — берёт `next` сразу (Tier 2, расхождение эталонов); SV верен (сверен с C) — **ИСПРАВЛЕН** фичей [симулятор исполняет реализацию состояния с переходом next](../features/0181-sim-state-implementation-tick.md) (реализация состояния — дочерний юнит с общим контекстом; тройная сверка sim≡C≡SV); разблокировал 0166 | [0057-01-sim-composition-next.md](0057-01-sim-composition-next.md) |
| 0097-01 | 0097 (+0096) | Пример ПИД переведён с явного `q(8, 8)` на прозрачный `float`; q формируется флагами сборки (`--float-as-q` для sv, `--float-embedded` для c-hal/st-at) — ИСПРАВЛЕН | [0097-01-pid-native-float.md](0097-01-pid-native-float.md) |
| 0038-01 | 0038 | `semantic_tokens` не классифицирует члены под-моделей (`fn`/`state` в `model X` → `variable`) — молчаливо неверная подсветка на реальных файлах (Tier 2) — ИСПРАВЛЕН | [0038-01-subtree-token-classification.md](0038-01-subtree-token-classification.md) |
| 0120-01 | 0120 | Блоки «Частые ошибки» — код диагностики перед описанием (читаемость); свод дополнено, разделы 02–07 приведены к формату — ИСПРАВЛЕН | [0120-01-error-notes-code-first.md](0120-01-error-notes-code-first.md) |
| 0123-01 | 0123 | Подсветка в тексте не только ключевых слов, но и предопределённых типов (`bit`/`bool`/`float`/`u8`/`i8`…) — цветом без жирного (как DataTypeTok в коде) — ИСПРАВЛЕН | [0123-01-highlight-types.md](0123-01-highlight-types.md) |
| 0121-01 | 0121 | Разбор примера — стиль числовых выносок (`(1)`/`(2)` в комментариях кода + бесшовный рассказ-продолжение вместо заголовка «Разбор…»); свод переформулировано, примеры/разборы 02–07 переделаны — ИСПРАВЛЕН | [0121-01-example-callouts.md](0121-01-example-callouts.md) |
| 0121-02 | 0121 | Примеры разделов 02/03/05/06/07 заменены реальными устройствами (термореле/лифт/бойлер/конвейер/насосная станция) вместо искусственных; свод: функциональный пример + бриф + разбор всей модели с выносками — ИСПРАВЛЕН | [0121-02-functional-examples.md](0121-02-functional-examples.md) |
| 0121-03 | 0121 | Пример конвейера (0121-02) компилировался, но был бессвязен (kind не задавался → сортировки нет; counter перезатирался; самопетля) — перепроектирован в работающий сортировщик, проверен симуляцией — ИСПРАВЛЕН | [0121-03-conveyor-coherence.md](0121-03-conveyor-coherence.md) |
| 0121-04 | 0121 | свод дополнено проверкой примеров симуляцией (не только компиляцией); регресс всех примеров 02–07 симуляцией; насосная (07) была не симулируема (extern с возвратом → SIM-019) — заменена на входной порт pressure — ИСПРАВЛЕН | [0121-04-example-simulation-regress.md](0121-04-example-simulation-regress.md) |
| 0132-01 | 0132 | Врезка документа несла символ `⚠️`, которого нет в шрифте Fira Code: сборка PDF ругалась `Missing character`, символ молча выпадал из вывода. Переписано цитатным блоком в принятом для документа виде — ИСПРАВЛЕН | [0132-01-book-glyph-outside-font.md](0132-01-book-glyph-outside-font.md) |
| 0134-01 | 0134 | Приведение `x as u8` давало `TypeNode::Unsupported` (симулятор — `SIM-007`) на законном коде: знание о встроенных именах типов жило в двух списках и разъехалось — приведения знали только `bit`/`bool`/`float`/`unit`. Сведено в единый `builtin_type_by_name` — ИСПРАВЛЕН | [0134-01-cast-to-int-alias.md](0134-01-cast-to-int-alias.md) |
| 0134-02 | 0134 | Литерал длительности **в теле блока** давал `SIM-007` (`left := left - 1s;`), хотя документ обещает арифметику длительностей: узел `ExpressionNode::Duration` разбирают три места, и адаптер выражений остался с заглушкой «до 0134-03», пережившей саму 0134-03 — **ИСПРАВЛЕН** (существующий тест арифметики складывал две переменные и дефект не ловил) | [0134-02-duration-literal-in-body.md](0134-02-duration-literal-in-body.md) |
| 0181-01 | 0181 | Симулятор исполняет `always` под-модели композиции (и `\|`, и `+`) дважды за такт (Tier 2, эталон молча неверен); порождённый код верен. Невидим для всего покрытия: корпус пишет идемпотентные присваивания — **ИСПРАВЛЕН** (снят спуск в детей из `Unit::execution`; регресс на накапливающем теле) | [0181-01-sim-parallel-always-twice.md](0181-01-sim-parallel-always-twice.md) |
| [карточка](0186-01-example-decomposition.md) | 0186 | Примеры раздела дублировали модели: заведена библиотека `machine_lib.takt`, ступени стали сборками из импорта и параметров | 2026-07-30 |

| 0148-01 | 0148 | Сравнение `bit` с литералом давало некомпилируемый Rust (`bool == 1` → E0308) либо `bool == true` (валит `clippy::bool_comparison` под `-D warnings`) — **ИСПРАВЛЕН** (сведено к самому операнду; корпус этой формы не содержал) | [0148-01-rust-bit-compare.md](0148-01-rust-bit-compare.md) |
| 0148-02 | 0148 | Присваивание порта из порта (`led := btn;`) даёт `E0499` — двойное заимствование `hal`; порождённый Rust не компилируется — заведён (нужен вынос во временную, это ADR) | [0148-02-rust-port-to-port-borrow.md](0148-02-rust-port-to-port-borrow.md) |
| 0148-03 | 0148 | Тернарник в `bit`-порт (`late := (n > 5) ? 1 : 0;`) даёт некомпилируемый Rust: ветви печатаются числами при `write_bit(…, bool)` (`E0308` ×2) плюс лишние скобки вокруг условия `if` (`unused_parens` под `-D warnings`) — заведён (третий дефект того же класса; проверка гоняет только корпус, а в нём этой формы нет) | [0148-03-rust-ternary-to-bit-port.md](0148-03-rust-ternary-to-bit-port.md) |

| 0196-01 | 0196 | Имена типов выглядели как обычный текст: у ключа `LAM_TYPE` не было значения по умолчанию (плагин не поставлял ни одной схемы, а платформенный `CLASS_REFERENCE` цвета не имеет), и сам ключ не был выведен в панель настройки цветов — задать его руками тоже было нельзя — **ИСПРАВЛЕН** (схемы `TaktDefault.xml`/`TaktDarcula.xml` через `additionalTextAttributes`; четыре семантических ключа добавлены в `TaktColorSettingsPage`) | [0196-01-type-color-defaults.md](0196-01-type-color-defaults.md) |
| 0187-01 | 0187 | Присваивание синтаксически оставалось выражением: парсер принимал `(x := 7)` в инициализаторе, арифметике и условии, а отвергала форму лишь семантика (`SE-095`) — граница языка была описана в двух местах — **ИСПРАВЛЕН** (`:=` вынесен из цепочки приоритетов в три именованных правила: оператор тела, шаг `for`, именованный аргумент вызова; код `SY-006` с позицией токена; проба вскрыла третью позицию — `Pid(kp := 0.25)` из корпуса) | [0187-01-assignment-is-statement-in-grammar.md](0187-01-assignment-is-statement-in-grammar.md) |
| 0202-01 | 0202 | Живой контекст объявлял версию крейта `takt-lang` `0.27.0`, тогда как манифест — `0.28.0`; проверка `check-claude-md.py` заявляет сверку версий с манифестами и **прошёл зелёным** — он ищет пару «имя крейта … сейчас `X.Y.Z`» построчно, а единственное упоминание было перенесено через строку — **ИСПРАВЛЕН** (запись сведена в одну строку; мутация версии теперь валит проверка; сшивание логических строк в самом проверке — кандидат) | [0202-01-stale-crate-version-in-context.md](0202-01-stale-crate-version-in-context.md) |
| 0061-01 | 0061 | Цели `c`/`rust`/`st` переносили результат q-операции к ширине **хранения** (`int16_t`/`as i16`/`LINT_TO_INT`), а эталон и цель `sv` — к `W = m + n`: на `q(6, 6)` эталон давал −26.0, а три цели 38.0. Дожило из-за дыры в покрытии — весь корпус и все фикстуры сверки состоят из `q(8, 8)` и `q(4, 4)`, где `W` совпадает с шириной хранения — **ИСПРАВЛЕН** (перенос к `W` эмитится явно и только при `W ≠ S`: хелпер `lam_q_wrap` в C, пара сдвигов в Rust, `FUNCTION LAM_Q_WRAP` в ST, новая `ST-021` при `W ≥ 63`; четыре сверки значений на `W = 12`, ловушка проверена мутацией) | [0061-01-q-wrap-storage-width.md](0061-01-q-wrap-storage-width.md) |
| 0150-01 | 0150 | Проверка фичи падал на своей же фикстуре; `runner.rs` перерос лимит размера | [0150-01-gate-and-module-size.md](0150-01-gate-and-module-size.md) |
| 0150-02 | 0150 | Проверка сценариев видела чужие рабочие копии внутри дерева | [0150-02-gate-sees-worktrees.md](0150-02-gate-sees-worktrees.md) |
| 0301-01 | 0301 | Проверка имени файла в `probe.sh` кричала на идиому корпуса: `model Regulator` в `regulator.takt` объявлялся коллизией, хотя цель `c` такой вход принимает — контроль, кричащий всегда, замолкает навсегда — **ИСПРАВЛЕН** (список сужен до типов `struct`/`enum`/`type`; в контроль добавлен контрольный вход, которого у проверки не было — ровно тот пункт, что сама фича записала в свод как обязанность человека) | [0301-01-probe-model-name-false-alarm.md](0301-01-probe-model-name-false-alarm.md) |
| 0301-02 | 0301 | `probe.sh` завершался с кодом 1 на любой пробе с объявленным `struct`/`enum`/`type`, имя которого не совпадает с именем файла: под `set -eu` форма `[ … ] && printf` в теле цикла отдавала статус 1 подстановке — то есть падение наступало тогда, когда ловушки НЕТ, и замер не снимался вовсе — **ИСПРАВЛЕН** (тело цикла кончается успехом; в контроль добавлена отрицательная ветвь проверки, которой у неё не было) | [0301-02-probe-type-declaration.md](0301-02-probe-type-declaration.md) |
| 0272-01 | 0272 | Контроль обвязки замера проваливал первое условие на занятой машине: `sleep 1` длится ровно одну единицу измерения (`date +%s` даёт целые секунды), и округление «1 с против 2 с» — это 100 % разброса при пороге 20 %, то есть проверка мерила собственную гранулярность, а не предмет — **ИСПРАВЛЕН** (широкий порог задан обоим условиям с секундной командой — их оказалось два, и первая редакция исправления поправила лишь одно; условие 2 с трёхкратной разницей оставлено как было) | [0272-01-measure-guard-flaky.md](0272-01-measure-guard-flaky.md) |
| 0359-01 | 0359 | Равенство знакового с беззнаковым осталось непокрытым: правило маршрутизировалось по списку операторов из замера (`<`, `>`, `<=`, `>=`), и соседние узлы `=`/`!=` уехали прежним путём — `sv` считал `-1 == 255` истиной, `rust` и `st` не собирались — **ИСПРАВЛЕН** (равенство идёт тем же путём; правило исхода при отрицательном знаковом вынесено в носитель — у равенства оно другое, — а четыре копии сняты) | [0359-01-mixed-sign-equality.md](0359-01-mixed-sign-equality.md) |
| 0011-01 | 0011 | Сервер не умеет назвать свою версию | [0011-01-lsp-version-flag.md](0011-01-lsp-version-flag.md) |
| 0190-01 | 0190 | Временные каталоги тестов могут пересекаться | [0190-01-test-temp-dir-collision.md](0190-01-test-temp-dir-collision.md) |
| 0446-01 | 0446 | collapsible_if на обёртке выхода | [0446-01-inline-early-return.md](0446-01-inline-early-return.md) |
| 0429-01 | 0429 | Сборка verilator падала при параллельном прогоне | [0429-01-sv-conformance-flake.md](0429-01-sv-conformance-flake.md) |
| 0484-01 | 0484 | Документ называл уровень состояния местом блока `formula`, которого там нет (`SY-002`); у `assembly` мест ещё меньше — только тела — **ИСПРАВЛЕН** (обе оговорки внесены по замеру пяти позиций; асимметрия мест вынесена кандидатом) | [0484-01-target-block-sites.md](0484-01-target-block-sites.md) |
| 0488-01 | 0488 | Носитель ширины скаляра не знал упакованного бит-вектора: `st-at` отвечал `ST-004` на порту `[bit;12]`, который переводят семь прочих потребителей — **ИСПРАВЛЕН** (ветвь бит-вектора в носителе, контроль на `[bit;96]`) | [0488-01-scalar-port-bitvector.md](0488-01-scalar-port-bitvector.md) |
| 0511-01 | 0511 | Пополнение списка зарезервированных имён сделало ложным отказ на именах состояний (`state On`): в выводе `st` имя состояния — комментарий, а не идентификатор — **ИСПРАВЛЕН** (проверка алфавита отделена от занятости; примеры документа приведены к правилу, снят и segfault `iec2c` на `functions.takt`) | [0511-01-st-state-name-alphabet.md](0511-01-st-state-name-alphabet.md) |
| 0536-01 | 0536 | Комментарии новых файлов не прошли проверку: предкоммит не видел их до добавления в индекс | [0536-01-gate-empty-selection.md](0536-01-gate-empty-selection.md) |
| 0539-01 | 0539 | Открытая панель схемы показывала пустое поле при живой модели: вид переживает закрытие панели, и лист оставался за краем холста — **ИСПРАВЛЕН** (открытие проверяет видимость узлов и вписывает лист только когда не видно ни одного; заданный вручную масштаб сохраняется) | [0539-01-scheme-open-fit.md](0539-01-scheme-open-fit.md) |
| 0531-01 | 0531 | Страница на стенде отвечала «модуль не загружен: wasm[operation] is not a function»: адрес `wasm/<версия>/takt.wasm` помечен `immutable` на год, а версия крейта между выкладками не менялась — у читателя оставался вчерашний модуль под новой страницей — **ИСПРАВЛЕН** (адрес модуля несёт отпечаток содержимого запросом; путь прежний — его знает сервер; контроль E13 в проверке выкладки) | [0531-01-wasm-cache-fingerprint.md](0531-01-wasm-cache-fingerprint.md) |
| 0531-02 | 0531 | Прогон со сценарием обрывался на его последнем шаге: длину прогона страница эталону не передавала, и он брал её у сценария, как CLI без `-n` — **ИСПРАВЛЕН** (число шагов страницы доезжает до эталона ключом `-n`, сценарий задаёт только входы) | [0531-02-run-length-from-scenario.md](0531-02-run-length-from-scenario.md) |
| 0544-01 | 0544 | Имя состояния из модели печаталось под каждым узлом листа — третье место после легенды и подсказки, и на схеме с длинными именами читался столбик подписей, а не автомат — **ИСПРАВЛЕН** (под узлом печатается только подпись автора; имя остаётся в легенде и подсказке) | [0544-01-node-label.md](0544-01-node-label.md) |
| 0539-02 | 0539 | Масштаб схемы менялся колесом слишком быстро: колесо брало кнопочный шаг 1.1, а событий за одно движение приходит около десятка — лист прыгал от предела к пределу — **ИСПРАВЛЕН** (у колеса свой шаг 1.04; кнопки и клавиши прежние) | [0539-02-wheel-zoom-step.md](0539-02-wheel-zoom-step.md) |
| 0548-01 | 0548 | Расшифровка обозначений показывала пять одинаково серых кружков и стрелки без наконечников, образцы гаммы в настройках были бесцветны, а номер знака не менялся с кеглем — цвет принадлежит узлу, а не кружку; маркер объявлен в чужом SVG; кегль номера был отдельной ступенью — **ИСПРАВЛЕН** (образец рисуется в обёртке узла и своим наконечником, номер — доля кегля знака) | [0548-01-legend-samples-and-index.md](0548-01-legend-samples-and-index.md) |
| 0550-01 | 0550 | Звенья, положенные на линию, переживали уход фокуса: часть уходов шла мимо единой точки (`Escape`, смена уровня), а признак изгиба мерил расстояние до отрезка — точка на линии за спиной соседа считалась изгибом — **ИСПРАВЛЕН** (все уходы через одну точку и до смены листа; расстояние считается до прямой) | [0550-01-pin-rule-holes.md](0550-01-pin-rule-holes.md) |
| 0553-01 | 0553 | Панели одного места стоят в линию | [0553-01-panels-in-line.md](0553-01-panels-in-line.md) |
| 0094-01 | 0094 | Шаблон карточки нёс номер задачи в заголовке | [0094-01-template-task-number.md](0094-01-template-task-number.md) |
| 0557-01 | 0557 | Кнопка показа диагностик перестала прятать список: форма записи дала ему раскладку, а она сильнее атрибута `hidden` — **ИСПРАВЛЕН** (`.log[hidden]` рядом с правилом раскладки) | [0557-01-diagnostics-pane-hidden.md](0557-01-diagnostics-pane-hidden.md) |
| 0567-01 | 0567 | В пустом журнале диагностик мелькал ключ `diagnostics.none`: заглушку строит первая отрисовка, а та случается раньше словаря, и её переписывание стояло после раннего выхода перерисовки — **ИСПРАВЛЕН** (переписывание до выхода: языку мост не нужен) | [0567-01-diagnostics-none-key-flash.md](0567-01-diagnostics-none-key-flash.md) |
| 0555-01 | 0555 | Миникарта не убиралась ступенью «скрыть»: видимость ставилась свойством `hidden`, которого у `<svg>` нет, — атрибут не появлялся, и правило `[hidden]` не срабатывало — **ИСПРАВЛЕН** (видимость ставится атрибутом) | [0555-01-minimap-hidden-attribute.md](0555-01-minimap-hidden-attribute.md) |
| 0560-01 | 0560 | Схема не перестраивалась ни на правку модели, ни на открытие раскладки: признак «показана» брался у области вывода, где значения «схема» не бывает с самой фичи — **ИСПРАВЛЕН** (показ спрашивают у области кода) | [0560-01-scheme-never-redrawn.md](0560-01-scheme-never-redrawn.md) |
| 0564-01 | 0564 | Переименование и удаление трогали открытый файл вместо выбранного в структуре: сценарий и раскладку открывают, не меняя открытого — **ИСПРАВЛЕН** (действия обращены к выбранному, выбор отмечен в дереве) | [0564-01-actions-on-picked-file.md](0564-01-actions-on-picked-file.md) |
| 0539-03 | 0539 | Масштаб схемы не менялся щипком на планшете: слушали только колесо, а пара указателей уходила в пустоту — **ИСПРАВЛЕН** (щипок двумя пальцами, второй палец прекращает панораму) | [0539-03-pinch-zoom.md](0539-03-pinch-zoom.md) |
| 0539-04 | 0539 | Правка схемы пропадала после сохранения: раскладка писалась в существующий файл без ревизии, и сервер отвергал каждую запись, кроме первой, заводящей файл — **ИСПРАВЛЕН** (запись несёт ревизию проекта, как у модели и сценария) | [0539-04-layout-save-revision.md](0539-04-layout-save-revision.md) |
| 0539-05 | 0539 | Несохранённая расстановка схемы терялась при перезагрузке: черновик читался только при правленом тексте модели, а правка схемы текст не меняет — **ИСПРАВЛЕН** (раскладка черновика берётся, когда она по смыслу отличается от сохранённой; страница говорит об этом) | [0539-05-layout-draft-ignored.md](0539-05-layout-draft-ignored.md) |
| 0539-06 | 0539 | Край листа срезал излом, линию и знак условия у нулевой вертикали: начало листа отъезжало только за ноль, а излом с сеткой встаёт ровно на ноль — **ИСПРАВЛЕН** (предмет листа учитывается с запасом в полширины знака; свои места знаков входят в размер листа) | [0539-06-sheet-edge-clipping.md](0539-06-sheet-edge-clipping.md) |
| 0539-07 | 0539 | Не было видно, когда ребро можно выделить: нажатие принимал штрих в 1–2,5 px без курсора и отклика — **ИСПРАВЛЕН** (полоса нажатия 12 px, ореол при наведении, курсор «рука»; вариант Б по макету) | [0539-07-edge-hover.md](0539-07-edge-hover.md) |
| 0567-02 | 0567 | Структура проекта растягивалась и больше не сужалась: нижнюю границу мерили шириной узла, а она равна ширине панели — мерка росла вслед за растянутой панелью — **ИСПРАВЛЕН** (меряется текст и одна кнопка, мерка снимается после отрисовки дерева) | [0567-02-tree-width-measure.md](0567-02-tree-width-measure.md) |
| 0564-02 | 0564 | Полоса действий молчала при открытии сценария и раскладки: обновление стояло только в третьей ветви открытия файла, и проект с активным сценарием открывался без кнопок — **ИСПРАВЛЕН** (полосу обновляют все три ветви) | [0564-02-toolbar-silent-on-scenario.md](0564-02-toolbar-silent-on-scenario.md) |
| 0567-03 | 0567 | Журнал диагностик обрывался у структуры проекта: его вынесли из области кода только в рабочее поле, а поле само стоит рядом со структурой — **ИСПРАВЛЕН** (своё место сетки во всю ширину области при любой стороне структуры) | [0567-03-diagnostics-under-tree.md](0567-03-diagnostics-under-tree.md) |
| 0570-01 | 0570 | У проекта в списке не было видно описания: строка строилась из имени и уровня — **ИСПРАВЛЕН** (описание проекта — подсказкой при наведении; пустое подсказки не даёт) | [0570-01-project-description-tip.md](0570-01-project-description-tip.md) |
| 0573-01 | 0573 | Кнопка журнала диагностик не включала его: решала по видимости списка, а список скрыт и на вкладке журнала прогона — **ИСПРАВЛЕН** (кнопка спрашивает состояние области) | [0573-01-diag-toggle-asked-list.md](0573-01-diag-toggle-asked-list.md) |
| 0576-01 | 0576 | Конец ребра нельзя было передвинуть по окружности узла: место конца было следствием раздачи, а записи о нём в раскладке не было — **ИСПРАВЛЕН** (поле `ends` у ребра; ручки на концах выбранного ребра, как у изломов; раздача обходит закреплённые точки) | [0576-01-edge-ends-movable.md](0576-01-edge-ends-movable.md) |
| 0576-02 | 0576 | Рёбра, сходящиеся в одну точку, сливались в жирную линию: общий участок рисовался двумя штрихами, и сглаживание их складывало — **ИСПРАВЛЕН** (совпадающий участок у позже нарисованного ребра — пропуском штриха, совпадающий наконечник не рисуется) | [0576-02-shared-run-bold.md](0576-02-shared-run-bold.md) |
| 0560-02 | 0560 | Погашенный вывод цели возвращался при перезагрузке и открытии проекта: выбор писался пустой строкой, а носитель настроек читает пустоту как «выбора не было» и отвечает умолчанием — **ИСПРАВЛЕН** (погашенная панель пишется словом `none`) | [0560-02-output-panel-returns.md](0560-02-output-panel-returns.md) |
| 0564-03 | 0564 | Проект без активного файла открывался сценарием при пустом редакторе модели: брался первый файл по алфавиту, а `test-cold.json` стоит раньше `test.takt` — **ИСПРАВЛЕН** (открывается первая модель) | [0564-03-project-opens-model-first.md](0564-03-project-opens-model-first.md) |
| 0564-04 | 0564 | В окне выбора сценария не было видно выбранного файла: строка отмечалась `aria-pressed`, но оформления у нажатой строки не было — **ИСПРАВЛЕН** (пара «выбранное», как у файла в дереве) | [0564-04-scenario-pick-highlight.md](0564-04-scenario-pick-highlight.md) |
| 0564-05 | 0564 | К модели применялся сценарий другой модели того же проекта: сценарий выбирался из всех сценариев проекта и при открытии другой модели не переподбирался — **ИСПРАВЛЕН** (при открытии модели сценарий берётся только из её собственных; своих нет — прогон без сценария) | [0564-05-foreign-scenario.md](0564-05-foreign-scenario.md) |
