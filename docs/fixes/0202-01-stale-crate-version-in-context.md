# Исправление: версия крейта в живом контексте отстала, а проверка её не видел

> Фича: [../features/0202-fmt-diagnostic-formatting.md](../features/0202-fmt-diagnostic-formatting.md) · ADR: [../features/0202-fmt-diagnostic-formatting.md#архитектура-adr](../features/0202-fmt-diagnostic-formatting.md#архитектура-adr) · отчёт: [../features/0202-fmt-diagnostic-formatting.md#отчёт-о-тестировании](../features/0202-fmt-diagnostic-formatting.md#отчёт-о-тестировании)

- **Статус:** **ИСПРАВЛЕН** (2026-08-04). Найден на следующий день после
  закрытия 0202, при взятии в работу фичи.
- **Tier 3 — ложный факт в живом контексте.** Отказа нет; вред в том, что
  контекст читается в начале каждой сессии и становится предпосылкой работы.
- **Класс:** «контроль есть, но слеп к единственному месту, которое контролирует».

## Проблема

Фича подняла версию крейта `takt-lang` `0.27.0` → `0.28.0`, но запись в
живом контексте осталась прежней:

| Источник | Значение |
|---|---|
| `takt-lang/Cargo.toml` | `0.28.0` |
| `CLAUDE.md` | `0.27.0` |

Проверка `scripts/check-claude-md.py` заявляет сверку версий крейтов с манифестами
(класс 3, `check_crate_versions`) и **прошёл зелёным** на этом расхождении.

## Причина

Проверка ищет пару «имя крейта … `сейчас X.Y.Z`» построчно:

```python
for number, line in enumerate(lines, start=1):
    for crate, version in re.findall(
        r"`(takt-lang|takt-sim)`[^\n]{0,80}?сейчас `(\d+\.\d+\.\d+)`", line
    ):
```

В `CLAUDE.md` единственное упоминание было **перенесено через строку**:

```
… Это **не** версия крейта `takt-lang`
(`CARGO_PKG_VERSION`, сейчас `0.27.0`).
```

Имя крейта — в конце одной строки, «сейчас `0.27.0`» — в начале следующей.
Обход идёт по строкам, `[^\n]` перенос не пересекает, и совпадения не возникает
**никогда**. То есть проверка версий крейтов не проверяла ничего: в файле это
упоминание единственное.

Ирония в устройстве проверки: его комментарий предупреждает именно о ложной
уверенности — «первая редакция исключала [обратные кавычки], и мутация версии
проходила мимо проверки». Класс тот же, а лазейка другая: не символы, а **перенос
строки**.

## Лечение

Запись сведена в одну строку — проверка снова её видит. Мутационная проверка:
подмена `0.28.0` → `0.99.0` теперь валит проверка, откат делает его зелёным.

**Сама проверка не правится этим исправлением.** Ему следует сшивать логические
строки перед поиском (иначе тот же перенос вернётся при следующей правке
абзаца), но это отдельная работа со своим контролем — записана кандидатом в
`FEATURES.md`. Здесь снят **факт**, а не его причина.

## Замечание о порядке версий

Фича удалила публичные варианты `Token::{String, Template, Pragma}` —
ломающее изменение API, — и версию крейта **не подняла**; подъём сделала
следующая за ней 0202. Отдельного вреда нет: `0.28.0` покрывает оба изменения,
и ни одна промежуточная версия не выпускалась. Записано здесь, чтобы порядок не
выглядел ошибкой при чтении истории.
