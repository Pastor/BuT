// Библиотека: типы и функции для подключения через `import`.
//
// Состояний здесь нет намеренно - это не автомат, а набор объявлений.
const SCALE: u8 := 4;

fn doubled(value: u8) -> u8 {
    return value * 2;
}
