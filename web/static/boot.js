// Запуск страницы.
//
// Отдельным файлом, а не встроенным `<script>` в разметке: собранная страница лежит в
// каталоге бандла `b/<отпечаток>/`, и относительный `import` из встроенного скрипта
// считался бы от документа, то есть уводил бы в корень. Ссылку в атрибуте `src` сборка
// переписывает сама (нашлось прогоном страницы 2026-09-04: модули молча приезжали 404,
// а страница выглядела пустой).

import { main } from "./app.js";
import { t } from "./i18n.js";

main().catch((error) => {
  // Отказ загрузки модуля обязан быть виден: пустая страница неотличима от работающей,
  // пока автор не начнёт печатать.
  document.getElementById("version").textContent = t("bar.moduleFailed", {
    error: error?.message ?? error,
  });
});
