[package]
name = "but-visual"
version = "0.1.0"
authors = ["Pastor <viruszold@gmail.com>"]
edition = "2021"
license = "BSD"

[dependencies]
but-grammar = { path = "../grammar/but" }
but-simulators = { path = "../simulators" }
thiserror = "2"
