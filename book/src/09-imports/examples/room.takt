import {Thermostat} from "thermostat.takt"; // взять модель из файла

// Машина комнаты - это термостат, настроенный под неё: порог 20° вместо 22°
// по умолчанию. Библиотечный файл при этом не правится.
start Room = Thermostat(setpoint := 20);
