// Достижимость - F Done нарушено.
//
// Done объявлено, но ни один `ref` в него не ведёт: автомат вечно крутится
// Work -> Wait -> Work. Контрпример: [ Work -> Wait ]*
: [LTL] F Done;

start Work {
    ref Wait;
}

state Wait {
    ref Work;
}

state Done;
