// Пример для Се11: файл с числовым условием перехода.
// Семантически допустим (предупреждение - не ошибка).

var timer: [bit;8] := 0;

start S {
    always { timer := timer + 1; }
    ref T: timer;    // Предупреждение Се11: числовой тип [bit;8] вместо bool
}

state T {
    next S;
}
