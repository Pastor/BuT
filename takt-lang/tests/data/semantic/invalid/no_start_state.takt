// Контрпример: модель без начального состояния.
//
// Модель, содержащая состояния, обязана иметь ровно одно начальное
// состояние (start). Наличие только обычных state - ошибка.

model NoStart {
    state A;
    state B;
}
