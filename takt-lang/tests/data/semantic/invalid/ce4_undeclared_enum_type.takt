// Ce4: контр-пример - переменная с типом необъявленного перечисления.
//
// Ошибка: перечисление 'Status' нигде не объявлено,
// но используется как тип переменной current.
//
// Ожидаемое поведение: семантическая ошибка Ce4:
// "переменная 'current' объявлена с типом 'Status', но перечисление 'Status' не найдено"

var current: Status := 0;
start S;
