// Контрпример: индексирование переменной, которая не является массивом.
//
// Ожидается: семантическая ошибка - переменная 'flag' не является массивом.

var flag: bit := false;
var x: bit := flag[0];

start S;
