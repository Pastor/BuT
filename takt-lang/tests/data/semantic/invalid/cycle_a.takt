// Файл a.takt для проверки обнаружения циклических импортов.
//
// Образует цикл: a.takt -> b.takt -> a.takt
// При обработке этого файла семантический анализатор должен
// вернуть ошибку "Циклический импорт".

import "cycle_b.takt";

start Entry = CycleB { }
state Done;
