// контрпример: модель без состояний, не участвующая в реализации, законна -
// это контейнер объявлений.
model Lib {
    var z: u8 := 0;
}

var q: u8 := 0;

start S {
    always {
        q := 1;
    }
}
