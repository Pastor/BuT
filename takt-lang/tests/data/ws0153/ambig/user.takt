// Оба подключённых файла объявляют `level`: какое из объявлений имеется в
// виду, область не знает - переименование обязано отказать.
import "first.takt";
import "second.takt";

start Use {
    always {
        level := 3;
    }
    ref Use;
}
