// Библиотека для проверки границы: имя, введённое формой
// `import "файл" as Имя;`, остаётся именем модели и переименованию не подлежит.
var speed: u8 := 0;

start Idle {
    always {
        speed := 1;
    }
    ref Idle;
}
