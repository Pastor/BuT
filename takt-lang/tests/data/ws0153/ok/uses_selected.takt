// Потребитель формы `import { A } from "файл";`: исходное имя `speed` есть
// ссылка на объявление библиотеки, и переименование обязано её задеть.
import {speed} from "lib.takt";

start Tick {
    always {
        speed := 2;
    }
    ref Tick;
}
