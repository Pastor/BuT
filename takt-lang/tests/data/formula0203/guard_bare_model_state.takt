// Краткая форма паттерна ("Модель != Состояние" без `S(...)`)
// законна наравне с `S(Модель) != Состояние` - решение заказчика 2026-08-15.
model E {
    start Begin;
    state End;
}

start Run = E {
    : [Guard] E != End;
    ref Done: E != End;
    next Done;
}
state Done;
