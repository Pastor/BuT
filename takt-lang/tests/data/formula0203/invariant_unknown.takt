// `invariant` десахаризуется в `cond` + `Formula::Guard`
// Ожидание: ровно одно сообщение SE-025, а не два.
var level: u8 := 0;

invariant Safe = levl < 3;

start Run {
    always { level := level + 1; }
    ref Run;
}
