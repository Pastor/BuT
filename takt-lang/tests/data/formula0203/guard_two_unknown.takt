// Две ошибочные формулы - накопление: два
// сообщения, а не "первая на модель".
var level: u8 := 0;

start Run {
    : [Guard] levl < 3;
    : [Guard] leve > 0;
    always { level := level + 1; }
    ref Run;
}
