// LTL-формула в объём проверки не входит - у неё свой режим
// строгости (SE-056, предупреждение). Ожидание: ошибок нет.
var level: u8 := 0;

: [LTL] G neverdeclared;

start Run {
    always { level := level + 1; }
    ref Run;
}
