// Контрпример угадыванию: имя модели (`Motor`) не совпадает с именем файла
// (`engine.takt`). Прежний код строил кандидатов из имени модели - motor.takt -
// и не нашёл бы цель никогда. Путь обязан браться из реестра файлов.
import "engine.takt" as Motor;

start Main = Motor;
