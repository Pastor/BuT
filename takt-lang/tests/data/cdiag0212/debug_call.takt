// Встроенная функция отладки в позиции оператора: цель пропускает её молча
// (решение фичи 0189) - фича 0212 это поведение не меняет.
var v: u8 := 0;

start Main {
    always {
        debug(v);
    }
}
