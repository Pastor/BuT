// Встроенная функция отладки в позиции оператора: цель пропускает её молча
// (решение языка о позиции оператора) - на код отказа это не влияет.
var v: u8 := 0;

start Main {
    always {
        debug(v);
    }
}
