// Оператор задания адреса порта отдельно от объявления.
// Порт объявляется без инлайн-адреса; адрес задаётся оператором `address`.
model Sensors {
    in  BTN: u8;
    out LED: bit;

    address BTN = 0x00200000;
    address LED = 0x00200004:3;

    start Idle;
}
