// Импортёр: то же имя носит и импортированная переменная, и
// состояние - пространства разные, и компилятор такую запись принимает.
import { shared } from "lib.takt";

var o: u8 := 0;

start shared {
    always { o := shared; }
    ref shared;
}
