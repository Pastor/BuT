// Массив нулевого размера невыразим в IEC 61131-3 -
// диапазон `ARRAY [0..-1]` пуст. Ожидается ошибка ST-007; файл не создаётся.

var z: [u8; 0] := 0;

cond C = z[0] = 1;

start Idle {
    ref Done: C;
}

state Done {}
