//! Стиль оформления кода на Takt.
//!
//! Слой отделён от семантики намеренно: правила стиля выводятся из **текста** (АСД), а
//! не из смысла программы. Благодаря этому ими пользуется `taktc fmt`, который
//! семантику не запускает вовсе - и потому работает на файлах, которые семантика
//! отвергает. Именно такие файлы и правят, когда меняют имена.
//!
//! Коды диагностик слоя - `CS-...` (Code Style). Уровень - **предупреждение**: стиль
//! советует, а не запрещает, и на код возврата `fmt --check` не влияет.

pub mod naming;

pub use naming::naming_warnings;
