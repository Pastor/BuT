# Message catalogue: English.
#
# Format and substitutions - see `ru.txt`, the base catalogue. The set of keys
# and the set of `{placeholders}` in every key must match the base exactly;
# `scripts/check-messages.py` fails with a list otherwise.

# ─── Diagnostic printer captions (`diagnostics::batch`) ──────────────────────
diag.compile-error = Compilation error [{code}]: {message}
diag.note-label = note
diag.warning = Warning [{code}]: {message}

# ─── Message language selection failure (`diagnostics::lang`) ────────────────
lang.missing-value = the --lang key requires a language code
lang.unknown = unknown message language '{name}'; known: {known}

# ─── Trial codes ─────────────────────────────────────────────────────────────
se-034.local-type-not-found = local type '{name}' not found
se-034.parameter-type-unrecognised = parameter type '{name}' is not recognised: expected a type name, '[type; N]' or 'q(m, n)'

# ─── Semantics: model tree (`semantic/tree.rs`) ──────────────────────────────
se-021.identifier-missing = the identifier is missing
se-013.import-cycle = import cycle: {chain}
se-013.import-path-invalid = invalid import file path: “{file}”
se-006.model-already-declared = a model named '{name}' is already declared
se-019.condition-needs-name = a condition in a definition must have a name
se-019.invariant-needs-name = an invariant must have a name
se-054.invariant-name-taken = the invariant name '{name}' clashes with an existing condition or variable
se-018.block-needs-name = a named code block in a definition must have a name
se-022.function-needs-name = a function in a definition must have a name
se-009.function-already-defined = a function named '{name}' is already defined
se-020.state-name-missing = the state name is missing
se-012.state-has-next = state '{name}' already has a next statement
se-021.next-state-not-a-definition = a next-typed state is not supported as a definition
se-002.reference-not-found = reference '{name}' not found

# ─── Semantics: durations and time (`semantic/duration.rs`) ──────────────────
se-063.duration-negative = {what}: a negative duration is not representable
se-063.duration-not-whole-ms = {what}: the duration is not a whole number of milliseconds — a duration value is representable as whole milliseconds (rounding would silently change the delay)
se-064.duration-unrepresentable = {what}: the duration is not representable
se-064.duration-too-large = {what}: the duration exceeds {max} ms — it does not fit the {bits}-bit value representation (about 49.7 days)
se-073.tick-rate-not-multiple = a computed delay needs a rate that is a multiple of 1000 Hz: at {hertz} Hz a millisecond is not a whole number of ticks, and the comparison would round silently. Either use a multiple rate or keep the delay constant (the compiler converts that one exactly)
se-063.duration-profile-unrepresentable = {what}: the duration of {nanos} ns is not representable in the “{profile}” profile — {detail}
se-064.duration-counter-overflow = {what}: the duration of {nanos} ns does not fit the time counter (“{profile}” profile, 64 bits at most)
se-063.duration-negative-nanos = {what}: a negative duration ({nanos} ns) makes no sense
se-069.tick-hz-missing = the model requires a {declared} Hz clock; pass --tick-hz={declared}
se-070.tick-hz-mismatch = the model requires a {declared} Hz clock, the build sets {flag} Hz; bring --tick-hz to the declared rate
se-071.after-note = after delay → {name}: {nanos} ns = {units} {unit} (“{profile}” profile{suffix})

# ─── Semantics: common checks (`semantic/validate/common.rs`) ────────────────
se-033.state-not-in-model = state '{name}' not found in model '{model}': to the right of '=' in the check 'S(Model) = State' stands a state of the named model, not an arbitrary name
se-025.condition-unresolved = unresolved transition condition{quoted}: the name is not among variables, ports, `cond` conditions and states
se-027.output-port-read = reading from output port '{name}' is not allowed
se-026.input-port-write = writing to input port '{name}' is not allowed
se-110.model-name-not-a-condition = the model name '{name}' is not a condition by itself: write 'S({name}) = State' or the short form '{name} = State'
se-110.state-name-not-a-condition = the state name '{name}' is not a condition by itself: a state check is written as 'S(Model) = {name}' or in the short form 'Model = {name}'

# ─── Semantics: address cell access (`semantic/anon_port.rs`) ────────────────
se-098.address-too-wide = the address '{addr}' of an anonymous access does not fit a 64-bit signed integer: the cell address is wider than representable
se-098.bit-given-twice = the bit position is given twice: by an address literal (':{literal}') and by bit access ('.'). Keep one form: '#0x{addr}.N' — a single bit, '#0x{addr}:N as TYPE' — a field starting at bit N
se-098.bit-position-is-a-name = the bit position of an anonymous access is given by the name '{name}': a number was expected
se-098.type-without-width = type '{ty}' does not set the access width for a cell: the width of an address access is not guessed. Allowed are 'bit', 'bool', integer types and 'q(m, n)'
se-098.bit-out-of-range = bit position {bit} is out of the range [0, {max}]: the access word is no wider than 64 bits
se-098.field-out-of-word = a field of width {width} starting at bit {bit} runs past the access word: {bit} + {width} > {max}
se-097.access-without-width = the access '{written}' does not set the cell access width: add a cast ('{written} as u32') or take a single bit ('#0x{addr}.N')

# ─── Semantics: expressions (`semantic/expression/mod.rs`) ───────────────────
se-003.identifier-not-in-scope = identifier '{name}' not found in scope
se-028.index-out-of-bounds = index {index} is out of the array bounds (size {size})
se-030.not-an-array = {what} is not an array
se-029.slice-start-out-of-bounds = slice start {start} is out of the bounds of array '{name}' (size {size})
se-029.slice-end-out-of-bounds = slice end {end} is out of the bounds of array '{name}' (size {size})
se-029.slice-start-after-end = slice start {start} is greater than the end {end} for array '{name}'

# ─── Semantics: bits and enumerations (`semantic/validate/enums.rs`) ─────────
se-035.bit-value-out-of-set = variable '{name}' has type bit but is initialised with {value} (allowed numeric values: 0 or 1)
se-105.enum-without-variants = enumeration '{name}' is declared without variants: an enumeration must have at least one variant (add a variant or drop the declaration)
se-042.enum-type-not-found = variable '{name}' is declared with type '{enum}', but the enumeration '{enum}' is not found in scope
se-043.enum-init-not-a-variant = variable '{name}' has type '{enum}' but is initialised with {value} — not a variant of the enumeration (allowed variants: {variants})
se-043.enum-assign-not-a-variant = assigning {value} to variable '{name}' of type '{enum}' is not allowed — not a variant of the enumeration (allowed variants: {variants})

# ─── Semantics: name selection on import (`semantic/import/select.rs`) ───────
se-007.type-already-declared = type '{name}' is already declared
se-005.variable-already-declared = variable '{name}' is already declared
se-008.condition-already-declared = condition '{name}' is already declared
se-017.identifier-not-in-imported-file = identifier '{name}' not found in the imported file

# ─── Semantics: instantiation arguments (`semantic/extend_args.rs`) ──────────
se-080.argument-given-twice = parameter '{name}' is given twice in this call
se-076.argument-form = an instantiation argument is written as 'name := value'
se-076.argument-name-expected = a parameter name must stand to the left of ':=' in an instantiation argument
se-077.model-without-parameters = model '{model}' declares no parameters, there is nothing to set '{name}' to
se-079.not-a-parameter = '{name}' in model '{model}' is not declared as a parameter — only parameters are set on instantiation
se-078.parameter-not-found = model '{model}' has no parameter '{name}' (declared: {known})

# ─── Semantics: declarations (`semantic/declaration/mod.rs`) ─────────────────
se-023.port-needs-type = a port must have a concrete type
se-075.parameter-outside-model = parameter '{name}' is declared outside a model: the top level of a file cannot be instantiated — move the declaration into a model or make it a 'var'
se-075.parameter-inside-block = parameter '{name}' is declared inside a block: a parameter is set where the model is instantiated, so it is declared at model level only
se-094.port-initial-value-not-constant = the initial value of port '{name}' is set before the first tick, so it must be known at compile time: {cause}
se-084.initializer-calls-extern = the initialiser of '{name}' calls the external function '{func}': its value is unknown at compile time — the body lives outside the program, and the initial value is set before the first tick. Assign it in a state body: 'always {{ {name} := {func}(); }}'

# ─── Semantics: states (`semantic/validate/states.rs`) ───────────────────────
se-011.multiple-start-states = model '{name}' must have exactly one start state (found: {count})
se-010.no-terminal-states = {prefix}the model has no terminal states (states without transitions); the automaton cannot finish
se-010.no-path-to-terminal = {prefix}state '{name}' has no path to a terminal state
se-012.ref-with-next = {prefix}state '{name}' has ref transitions together with next: the ref transitions are unreachable after next runs

# ─── Semantics: ports and addresses (`semantic/validate/ports.rs`) ───────────
se-052.port-without-address = port '{name}' is used in code generation but has no address (neither inline, nor by the `address` statement, nor in an external map)
se-048.address-port-not-found = the `address` statement refers to a port '{name}' that does not exist
se-049.address-given-twice = the address of port '{name}' is given by the `address` statement more than once
se-049.address-inline-and-operator = the address of port '{name}' is given inline and by the `address` statement at once
port.nested-model-visible = port '{name}' is declared in the nested model '{model}' and will be visible to every model through the root model's port enumerations

# ─── Semantics: member access (`semantic/validate/member_access.rs`) ─────────
se-125.bit-beyond-width = bit {index} is beyond the declared value width ({width} bits): bits 0..{last} are addressable
se-061.struct-without-field = structure '{name}' has no field '{field}'
se-030.field-access-needs-struct = field access '{field}' is possible only on a structure

# ─── Semantics: file imports (`semantic/import.rs`) ──────────────────────────
se-013.import-file-not-found = import file not found: “{file}”. Search paths: {paths}
se-016.import-path-not-canonical = failed to canonicalise the path “{file}”: {cause}
se-016.import-path-outside = the import path “{file}” leaves the allowed search directories: {paths}
se-014.import-extension = invalid import file extension: “{file}”
se-015.import-read-failed = failed to read the import file “{file}”: {cause}

# ─── Semantics: functions (`semantic/function.rs`) ───────────────────────────
se-126.unknown-function-attribute = unknown function attribute '{name}': allowed are 'inline' (substitute the body at the call site) and 'noinline' (do not substitute)
se-127.inline-on-extern = the 'inline' attribute on an external function: it has no body, there is nothing to substitute — its call stays a call in the generated code
se-022.function-needs-name-lower = a function in a definition must have a name
se-118.function-without-body = the local function '{name}' is declared without a body: the body is required (an external function is declared as 'extern fn')

# ─── Semantics: the q(m, n) type (`semantic/type_node/type_fixed.rs`) ────────
se-057.unknown-type-constructor = unknown type constructor '{ctor}(…, …)'; the only parametric type is the fixed-point 'q(m, n)'
se-057.fixed-format-invalid = fixed-point 'q({m}, {n})': {what} (m ≥ 1, n ≥ 1, m + n ≤ 64 are required)
se-104.fixed-modifier-unknown = only the 'sat' modifier (saturation instead of wrapping) may follow the fixed-point format 'q({m}, {n})', got '{other}'
se-058.fixed-literal-unrepresentable = the literal '{literal}' is not representable in fixed-point 'q({m}, {n})': {why}

# ─── Semantics: model specialisation (`semantic/specialize.rs`) ──────────────
se-087.specialize-with-nested = specialising model '{name}' with nested models is not supported: set the parameters of the nested models at their own instantiations, or build with --parameters=assign
se-086.specialize-without-parent = the model being specialised has no parent in the tree
se-086.specialize-name-taken = the specialisation name '{name}' is already taken by a model — rename it: the code generation namespace is shared, and renaming the specialisation silently would hide the clash in the output
se-086.specialize-parameter-not-found = parameter '{name}' not found in the model being specialised

# ─── Semantics: adopting imported declarations (`semantic/import/adopt.rs`) ──
se-005.variable-declared-in-included = variable '{name}' is already declared (the included file '{file}' declares it too)
se-108.type-declared-in-included = a type named '{name}' is already declared: the included file                      '{file}' declares it too
se-108.type-declared-in-included-note = the declaration of type '{name}' in the included file is here
se-009.function-defined-in-included = function '{name}' is already defined (the included file '{file}' defines it too)
se-074.imported-model-needs-declarations = the imported model uses declarations of the included file that were not imported with it: {list}. Add them to the import list

# ─── Semantics: constant evaluation (`semantic/const_eval/mod.rs`) ───────────
se-083.not-a-constant-expression = the expression is not evaluated at compile time: {reason}
se-121.duration-cast-overflow = casting a duration to a signed {bits}-bit type overflows it: {value} ms do not fit
se-121.cast-overflow = the 'as' cast overflows the signed {bits}-bit type: the value {value} does not fit, and signed overflow is a program error (undefined behaviour in C). Take a wider type or an unsigned one — wrapping is defined there
# ─── Semantics: structures (`semantic/validate/structs.rs`) ──────────────────
se-115.struct-without-fields = structure '{name}' is declared without fields: a structure must have at least one field (add a field or drop the declaration). An empty record is invalid per the C standard (6.7.2.1) and is rejected by the ST compiler
se-040.struct-duplicate-field = structure '{name}' has a duplicate field '{field}'
se-041.struct-field-unknown-type = field '{field}' of structure '{name}' refers to the unknown type '{ty}'

# ─── Semantics: transition non-determinism (`semantic/validate/nondeterminism.rs`)
se-037.unconditional-transitions = {prefix}: {count} unconditional transition(s) — non-deterministic behaviour
se-042.same-condition = {prefix}: transitions to '{first}' and '{second}' have the same condition — guaranteed non-deterministic behaviour
se-042.conditions-may-overlap = {prefix}: the conditions of transitions to '{first}' and '{second}' may hold at once — a possible overlap

# ─── Semantics: time in the tree (`semantic/time_ast.rs`) ────────────────────
se-067.clock-declared-twice-imported = the clock rate is declared twice and differently: {previous} Hz and {hertz} Hz (the second one in an included file) — only the author knows which is the real one
se-067.clock-declared-twice = the clock rate is declared twice and differently: {previous} Hz and {hertz} Hz — only the author knows which is the real one
se-068.after-outside-ref = an 'after' delay is allowed only in a 'ref' transition condition; in {place}\nit has no source state to count from

# ─── Semantics: model implementation (`semantic/extend.rs`) ──────────────────
se-001.model-not-found = model '{name}' not found
se-081.implementation-form = a model implementation is given by a model name, a composition '+'/'|' or an instantiation 'M(parameter := value)'
se-101.implementation-with-own-state = model '{model}' is declared with an implementation ('= …') and also has its own state '{name}': a model cannot be both a composition and an automaton. Drop the implementation and declare it as a state ('start Name = …;'), or drop the own states
se-101.own-state-note = the state '{name}' is here

# ─── Semantics: type registry (`semantic/type_registry.rs`) ──────────────────
se-107.builtin-type-name = the name '{name}' belongs to a built-in type of the language and cannot be redeclared: pick another name (otherwise the built-in type becomes unavailable in the whole file)
se-108.type-already-declared-here = a type named '{name}' is already declared
se-108.type-already-declared-imported = a type named '{name}' is already declared — it came from an included file
se-108.first-type-declaration-note = the first declaration of type '{name}' is here

# ─── Semantics: unused declarations (`semantic/unused.rs`) ───────────────────
se-036.variable-unused = variable '{name}' is declared but never used

# ─── Semantics: implicit truthiness (`semantic/validate/implicit_bool.rs`) ───
se-037.implicit-bool = {prefix}: the transition condition {verb} '{target}' contains {summary} — an explicit comparison is recommended (for example, '!= 0')

# ─── Semantics: assignment position (`semantic/validate/assignment_position.rs`)
se-095.assignment-in-value-position = {what} stands where a value is computed: in this language assignment is a statement, not an expression. Writing a port or a variable must be a separate statement: first `target := value;`, then use of the target

# ─── Semantics: a model as an implementation (`semantic/validate/implemented.rs`)
se-106.model-without-states = model '{name}' has no states, so it cannot be an implementation: there is nothing to run in it. Add a start state to it ('start Name {{ … }}') or drop it from the implementation

# ─── Semantics: name collisions (`semantic/validate/name_collisions.rs`) ─────
se-100.state-named-as-model = state '{name}' has the same name as the model '{model}' it implements: the state name and the model name land in one target namespace, and none of the targets translates such input. Rename the state

# ─── Semantics: literal range (`semantic/validate/literal_range.rs`) ─────────
se-089.literal-out-of-range = the literal {value} does not fit the type '{ty}' {what}: [{lo}, {hi}] is allowed

# ─── Semantics: nesting depth (`semantic/validate/depth.rs`) ─────────────────
se-062.nesting-too-deep = the nesting limit ({limit}) is exceeded: an expression, a condition or a statement is nested too deeply

# ─── Semantics: constant conditions (`semantic/validate/constant_conditions.rs`)
se-047.condition-always-true = the transition condition always holds — the transition is unconditional
se-047.condition-always-false = the transition condition never holds — the transition is unreachable

# ─── Semantics: aggregate length (`semantic/validate/aggregate_length.rs`) ───
se-123.aggregate-length-mismatch = {what}: {expected} elements are declared, {got} are given in the initialiser. This used to mean different things: the reference kept the extra ones, the 'rust' target printed invalid code, and 'st' lost the initialiser without a word

# ─── Semantics: call evaluation (`semantic/const_eval/call.rs`) ──────────────
se-084.function-not-constant = the function '{name}' is not evaluated at compile time: {reason}

# ─── Semantics: the subject of a condition (`semantic/condition/base.rs`) ────
cond.base-field = field '{name}'
cond.base-indexable = an indexable value

# ─── Semantics: target-addressed inserts (`semantic/target_block.rs`) ────────
se-129.assembly-unknown-target = the assembly insert is addressed to an unknown target '{name}': {hint}
se-129.assembly-target-is-a-mode = '{mode}' is a build mode, not an output language: the same language is printed by the target '{base}'. Write "{base}"
se-129.assembly-known-targets = allowed names: {known}

# ─── Semantics: who includes the library (`semantic/import/importers.rs`) ────
import.importers-note = this library is included by: {listed}
import.importers-more =  (and {count} more)

# ─── Semantics: built-in functions (`semantic/builtin.rs`) ───────────────────
se-004.unknown-function = unknown function '{name}'

# ─── Semantics: composite port split (`semantic/condition/port_split.rs`) ────
se-119.struct-not-declared = structure '{name}' is not declared: the port is not split

# ─── Semantics: subjects of check messages ───────────────────────────────────
what.array = array '{name}'
what.struct-of-type = structure '{name}' of type '{ty}'
what.assignment-named = the assignment '{name}'
what.assignment = the assignment
what.variable = variable '{name}'
what.state = state '{name}'
what.model-state = model '{model}', state '{state}'
what.model-prefix = model '{name}': 
note.model-declared-here = model '{name}' is declared here
note.model-used-here = model '{name}' is used here
se-010.state-unreachable = state '{name}' is unreachable from the start state

# ─── Semantics: subjects of implicit truthiness (`validate/implicit_bool.rs`) ─
bool.number-literal = the numeric literal {value}
bool.rational-literal = the rational literal {sign}{value}
bool.string-literal = a string literal
bool.variable-unknown-type = a variable (unknown type)
bool.variable-of-type = variable '{name}' of type {ty}
bool.add = arithmetic addition
bool.subtract = arithmetic subtraction
bool.bitand = bitwise AND
bool.bitor = bitwise OR
bool.expression = an expression
bool.array-element = the array element '{name}[{index}]'
bool.bit-access = a bit-field access
bool.numeric-expression = a numeric expression

# ─── Semantics: reasons for non-constness (`semantic/const_eval/`) ──────────
const.op-on-durations = the operation '{op}' on durations is not defined
const.op-on-booleans = the operation '{op}' on booleans is not defined
const.op-not-evaluated = the operation '{op}' is not evaluated at compile time
const.name-not-declared = the name '{name}' is not declared in scope
const.name-is-a-variable = '{name}' is a variable: its value is known only within a tick
const.name-is-a-port = '{name}' is a port: its value arrives from outside at run time
const.declaration-unresolved = the declaration '{name}' is unresolved
const.function-not-found = the function '{name}' is not found in scope
const.no-initializer = '{name}' has no initialiser: its value is unknown at compile time
const.not-a-condition = a value of kind “{kind}” cannot be a condition

# ─── Semantics: notes and message parts ──────────────────────────────────────
time.hertz-suffix = , {hertz} Hz
note.imported-into = '{what}' is imported into '{where}'
note.imported-here = '{what}' is imported here
note.first-assignment = the first assignment of '{name}'
what.variable-upper = variable '{name}'
what.field-upper = field '{name}'
what.indexable-upper = an indexable value

# ─── Semantics: constant delay (`semantic/condition/after_const.rs`) ─────────
after.undeclared = the 'after' delay: the constant '{name}' is not declared — 'after' takes a duration literal (after 3m), a tick literal (after 3t), the name of a duration constant, or a constant expression over durations (after (BASE + 30s))
after.not-const = the 'after' delay: '{name}' is a {kind}, and the delay needs a constant of type duration (const {name} := 3m;); a value known only within a tick cannot be substituted by the compiler
after.wrong-type = the 'after' delay: the constant '{name}' has type {ty}, and the delay needs a duration
after.not-literal = the 'after' delay: the value of the constant '{name}' does not reduce to a duration literal — a literal (const {name} := 3m;), the name of another such constant, or their sum and difference are allowed
after.bare-number = the 'after' delay: the number {value} without a time unit — a duration combines only with a duration (write {value}s, {value}ms or {value}us)
after.function-call = the 'after' delay: a call to the function '{name}' is not allowed in a delay expression — its value is unknown to the compiler
after.not-constant-form = the 'after' delay: {what} is not allowed in a delay expression — duration literals, duration constants, parentheses and the operators '+' and '-' are allowed
after.negative = the 'after' delay: the expression yields {nanos} ns — there is no negative delay
after.overflow = the 'after' delay: overflow while computing the duration (nanoseconds do not fit a 64-bit integer)
after.cycle = the 'after' delay: evaluation deeper than {depth} levels does not end in a duration literal — the constants probably refer to each other
after.subject = the 'after' delay
after.form-nested-after = a nested 'after' delay
after.form-not = a logical negation
after.form-bitwise = a bitwise operation
after.form-comparison = a comparison
after.form-bit-access = a bit access
after.form-array-element = an array element access
after.form-rational = a rational number
after.form-bool = a boolean literal
after.form-address-cell = an address cell access
after.form-string = a string
kind.variable = a variable
kind.port = a port
kind.unresolved-declaration = an unresolved declaration

# ─── Semantics: assignment target (`semantic/validate/assignment_place.rs`) ─
se-111.not-a-place = the left-hand side of the assignment is {kind}, not a storage location: you can write to a variable, a structure field, an array element or slice, a single bit, a port or a '#ADDRESS' cell
se-112.constant-target = the target of the write is the constant '{name}': a constant denotes a value, not storage. Declare it as 'var {name}: …' if the value must change
place.call = a function call
place.named-call = a call with named arguments
place.code-block = a code block
place.literal = a literal
place.address-literal = an address literal
place.aggregate-literal = an aggregate literal
place.type-name = a type name
place.model = a model
place.named-condition = a named condition
place.parameter-list = a parameter list
place.empty = an empty expression
place.not = a logical negation
place.bitwise-not = a bitwise negation
place.unary-plus = a unary plus
place.negate = a sign change
place.cast = a type cast
place.arithmetic = an arithmetic expression
place.bitwise = a bitwise expression
place.comparison = a comparison
place.logical = a logical expression
place.conditional = a conditional operator
place.assignment = an assignment

# ─── Semantics: why a call is not constant (`semantic/const_eval/call.rs`) ───
const.fn-extern = declared as extern — its value comes from external code at run time
const.fn-builtin = a built-in function of the language is not executed at compile time
const.fn-arity = arguments given: {given}, parameters declared: {declared}
const.fn-no-body = the function has no body
const.fn-return-without-value = a 'return' without a value was executed — there is nothing to assign
const.fn-no-return = the body ended without a 'return' — there is no value
const.fn-parameter-without-name = a parameter is declared without a name
const.assign-target-name = a name must stand to the left of ':=' in constant evaluation
const.not-a-local-function = '{name}' is not a local function name: the model state cannot be changed at compile time
const.expression-statement = an expression statement without an assignment is meaningless in constant evaluation
const.statement-unsupported = the statement is not supported in constant evaluation
const.local-values-only = only local values are declared inside a function
const.declaration-without-name = a declaration without a name
kind.integer = an integer
kind.boolean = a boolean
kind.duration = a duration
kind.rational = a rational
kind.aggregate = an aggregate

# ─── Semantics: why an expression is not constant (`const_eval/mod.rs`) ──────
const.steps-exhausted = the evaluation step limit is exceeded
const.depth-exhausted = the evaluation depth limit is exceeded (a cycle of definitions is possible)
const.negate-not-applicable = a unary minus does not apply to a value of kind “{kind}”
const.bitwise-not-not-applicable = a bitwise NOT does not apply to a value of kind “{kind}”
const.not-not-applicable = a logical NOT does not apply to a value of kind “{kind}”
const.expression-form = this form of expression is not evaluated at compile time
const.rational-op-inexact = the operation '{op}' on rationals is not performed exactly at compile time: the representation of a rational is chosen by build flags (--float-as-q / --float-embedded), and q rounding is set by the simulator reference — computing it here would give a value the simulator does not produce. Write a ready literal
const.rational-mixed = the operation '{op}' on a rational and “{kind}” is not performed at compile time
const.mixed-kinds = the operation '{op}' on values of different kinds: “{left}” and “{right}”
const.division-by-zero = division by zero
const.remainder-by-zero = remainder of a division by zero
const.shift-out-of-range = a shift is defined only for 0..63 bits
const.exponent-out-of-range = the exponent is negative or wider than 32 bits
const.not-a-constant-name = '{name}' is not a constant: its value is known only within a tick
const.constant-value-not-evaluated = the constant value is not evaluated at compile time
const.cast-needs-integer = a cast is evaluated only over an integer value
const.cast-type-not-evaluated = a cast to this type is not evaluated at compile time: the rule for changing the value (truncation, wrapping, q scaling) is set by the reference
const.cast-changes-value = the cast would change the value {value} (type range: {min}..={max}), and the rule for the change is set by the reference — it cannot be computed at compile time

# ─── Semantics: why q(m, n) is not representable (`type_node/type_fixed.rs`) ─
fixed.m-too-small = integer bits m < 1 (m includes the sign bit)
fixed.n-too-small = fractional bits n < 1
fixed.width-too-large = total width m + n > 64
fixed.not-a-number = not a number
fixed.bad-exponent = an invalid exponent
fixed.exponent-too-large = the exponent is too large
fixed.literal-too-large = the literal is too large
fixed.not-exact = not exactly representable (the fraction is not a multiple of 2⁻ⁿ)
fixed.out-of-range = out of the type range
fixed.expression-placeholder = <expression>

# ─── Semantics: time profiles (`semantic/duration.rs`) ───────────────────────
time.profile-clock = clock
time.profile-ticks = ticks
time.unit-ms = ms
time.unit-ticks = ticks
time.clock-quantum = the quantum of the “clock” profile is 1 ms; express the duration as a whole number of milliseconds

# ─── Semantics: places where 'after' is not allowed (`semantic/time_ast.rs`) ─
after-place.guard-formula = a Guard formula
after-place.named-condition = a named condition
after-place.invariant = an invariant
after-place.state-invariant = a state invariant

# ─── Semantics: miscellaneous ────────────────────────────────────────────────
import.no-search-paths = (no search paths given)
bool.verb-next = next to
bool.verb-to = to
se-053.recursion-forbidden = function recursion is forbidden: the call chain {chain}

# ─── Semantics: type mixing and aliases ──────────────────────────────────────
se-065.duration-mixed = mixing the types '{left}' and '{right}' in arithmetic is forbidden: a duration              combines only with a duration
se-039.type-alias-cycle = the type alias '{name}' forms a cyclic dependency

# ─── Semantics: function body inlining (`semantic/inline/`) ──────────────────
se-128.inline-refusal = the function '{name}' is marked with the 'inline' attribute, but its body does not reduce to a substitution: {why}
inline.no-return = the body has no 'return' at all, and there is nowhere to take the value from
inline.return-in-open-loop = the 'return' stands inside a loop whose iteration count is not known in advance: substitution suppresses the body by an exit flag, and such a loop would become infinite (in a 'for' with a counter from literal to literal the return is substituted)
inline.no-default-value = the result type has no initial value to declare the temporary with (arrays and structures are not included here)

# ─── Semantics: internal refusals (`semantic/internal.rs` and neighbours) ────
se-119.no-diagnostic = {what} failed without leaving a diagnostic
se-119.function-parameter-without-type = a function parameter without a type
se-119.function-node-undefined = the function node is undefined
se-119.named-block-without-body = a named block without a body
se-081.implementation-not-a-composition = the implementation expression does not reduce to a composition of models
what.anonymous-model = <anonymous>
minimap.model-needs-start = the model must have a start state

# ─── Target c: unsupported construct (`generator/c/c_unsupported.rs`) ────────
cc-022.refusal = {what} is not translated to C by the 'c' target
cc-022.refusal-with-reason = {what} is not translated to C by the 'c' target: {reason}
cc-022.node-model = a model reference in an expression
cc-022.node-array-slice = an array slice
cc-022.node-code-block = a code block in expression position
cc-022.node-named-function = a named function as a value
cc-022.node-parameter-list = a parameter list in expression position
cc-022.node-type = a type in expression position
cc-022.node-address = an address literal in an expression
cc-022.node-builtin = the built-in function '{name}'
cc-022.node-unknown-builtin = an unknown built-in function
cc-022.node-bit-beyond-vector = a bit beyond the bit vector
cc-022.node-wide-bit-vector = the operation '{op}' on a bit vector wider than 64 bits
cc-022.why-model = accessing a sub-model variable through the model name is not supported by the language in any consumer: a sub-model value is read through its port
cc-022.why-array-slice = C has no slice operation, and a slice in Takt has no owning type
cc-022.why-builtin = it serves debugging and generates no code
cc-022.why-bit-beyond-vector = a vector has no bits beyond its declared width, and access past the word array is undefined behaviour in the generated firmware
cc-022.why-wide-bit-vector = a vector wider than 64 bits is represented as an array of words, and no such operation on words exists; the reference does not support it either (SIM-005) — work with individual bits or split the vector into fields

# ─── Target c: unresolved node (`generator/c/c_unresolved.rs`) ───────────────
cc-023.refusal = {what}: the node did not pass semantic lowering and is not translated to C
cc-023.node-condition = an unresolved condition
cc-023.node-expression = an unresolved expression
cc-023.node-statement = an unresolved statement
cc-023.node-empty-expression = an empty expression
cc-023.node-variable = an unresolved variable
cc-023.node-port-owner = an unresolved port owner ({where})
cc-023.node-function-named = an unresolved function definition '{name}'
cc-023.node-function = an unresolved function definition
cc-023.node-const-value = an uncomputed constant value '{name}'

# ─── Target sv: uncovered node and refusals (`generator/sv/sv_expr.rs`) ──────
sv-002.refusal = not translated to SystemVerilog: {what}
sv-002.note = the construct cannot be skipped silently: the generated module would behave differently from the model
sv-005.extern-function = the external function '{name}' (extern fn) is not supported by the 'sv' target: synthesisable RTL has no call to external code. DPI-C is a simulation mechanism unavailable to a synthesiser; the hardware counterpart of an “external function” is a separate module with its own interface, while Takt's extern fn describes neither ports, nor ticks, nor latency, and they cannot be inferred. Use the 'c'/'st'/'rust' target or express the logic in Takt
sv-009.variable-divisor = division or remainder by a variable divisor: synthesisable RTL has no hardware divider, so '/' and '%' expand into a large combinational block (an order of magnitude larger than addition) with a long path — this lowers the achievable clock ceiling. A constant divisor does not cost that (it folds into shifts and multiplication by the reciprocal); if the divisor is constant in essence, move it to a constant
sv-015.after-outside-time = an 'after' delay must be printed through sv_time, not as a condition
sv.what-duration-in-condition = a duration literal in a condition
sv.what-duration = a duration literal
sv.what-unresolved-variable-in-condition = an unresolved variable in a condition
sv.what-unresolved-condition = an unresolved condition
sv.what-rational = a rational literal: synthesisable RTL has no floating point (see SV-003)
sv.what-string = a string literal
sv.what-model-in-condition = a model reference in a condition
sv.what-state-in-condition = a state reference in a condition
sv.what-unresolved-variable = an unresolved variable
sv.what-assign-in-expression = an assignment inside an expression (in SystemVerilog assignment is a statement)
sv.what-empty-expression = an empty expression
sv.what-unresolved-expression = an unresolved expression
sv.what-array-slice = an array slice
sv.what-code-block = a code block in an expression
sv.what-named-call = a call with named arguments
sv.what-type = a type in expression position
sv.what-address = a port address: an address is meaningless for RTL — the signal arrives at a chip pin, not at an address
sv.what-model = a model reference in an expression
sv.what-parameter-list = a parameter list in expression position
sv.what-array-literal = an array literal
sv.what-struct-initializer = a structure initialiser

# ─── Target rust: untranslatable and refusals (`generator/rust/rust_expr.rs`) ─
rs-011.refusal = not translated to Rust: {what}
rs-022.hal-unavailable = {what} requires access to the HAL, but it is unavailable in this scope
rs-017.model-variable-in-function = accessing the model variable '{name}' from a function body is not translated to Rust: a function does not see the model state. Pass the value as a parameter
rs-018.output-port-read = reading the output port '{name}' is not translated to Rust: the HAL trait gives an output port only writing
rs-018.input-port-write = writing to the input port '{name}' is not translated to Rust: the HAL trait gives an input port only reading
rs.what-port-read = reading port '{name}'
rs.what-port-write = writing to port '{name}'
rs.what-function-call = a call to the function '{name}'
rs.what-builtin-debug = the built-in function 'debug'
rs.what-unresolved-variable = an unresolved variable
rs.what-wide-bit-vector = the operation '{op}' on a bit vector wider than 64 bits: it is represented as an array of words, and no such operation on words exists — the reference does not support it either (SIM-005); work with individual bits
rs.what-duration = a duration literal
rs.what-cast = a type cast
rs.what-empty-expression = an empty expression
rs.what-unresolved-expression = an unresolved expression
rs.what-array-slice = an array slice: in Takt it has no owning type, and no_std has no alloc
rs.what-code-block = a code block in expression position
rs.what-named-call = a call with named arguments
rs.what-string = a string literal outside a debug call: no_std has no owned string
rs.what-type = a type in expression position
rs.what-address = an address literal: the rust target does not consume the address map (ports go through the HAL trait)
rs.what-anon-port = an address cell access ('#0x…'): the rust target does not know addresses — address access is given by the 'c-hal', 'st-at' and 'sv-mmio' targets
rs.what-model = a model in expression position
rs.what-empty-function = an empty function definition
rs.what-unresolved-function = an unresolved function
rs.what-debug-non-string = debug with a non-string argument: no_std has no formatting, the HAL method takes a ready string
rs.what-builtin-s-outside = the built-in function S outside the condition 'S(Model) = State'
rs.what-builtin-arity = the built-in function '{name}' with {count} arguments

# ─── Verification: why a check is refused (`verification/verify.rs`) ─────────
verify.unknown-atom = the atom is neither a state name of this model nor a predicate over its data: a typo in the name, or a state of a nested model (states of nested models are out of scope)
verify.predicate-outside-subset = the data predicate is outside the supported subset: it contains arithmetic, a function call, a port access, a bit access or a non-enumerable type (`float`, `q`, `duration`, array, structure) — comparisons and logical connectives over variables are tracked
verify.domain-not-enumerable = the domain of a tracked variable is not enumerable: `float`, `q(m, n)`, `duration`, an array or a structure — the check enumerates values, and there is nothing to enumerate here
verify.size-over-limit = the problem size is over the ceiling: the edge count is `graph edges × (product of domains)²`, and beyond 1 000 000 the check is refused before counting. Narrow the domains of the formula variables (for example `u8` → `enum`/`bit`) or take a property over fewer of them
verify.initial-value-unknown = the initial value of a tracked variable is unknown: the initialiser does not fold to a constant, or the value is outside the domain of its type

# ─── Target st: IEC 61131-3 types (`generator/st/st_type.rs`) ────────────────
st-002.unmapped-type = the type '{shown}' has no representation in IEC 61131-3: {why}
st-002.why-unit = the empty type is not a variable type
st-002.why-inference = type inference is not finished — the variable type is unknown
st-002.why-unsupported = the type is not supported by semantics
st-002.why-address = a port address literal is an internal type, not a variable type
st-002.why-string = the string type of built-in functions is not translated to ST
st-002.why-builtin = an internal type of built-in functions
st-002.why-builtin-numeric = an internal type of built-in functions: the exact width is unknown
st-002.why-integer-width = IEC 61131-3 knows only the widths 8, 16, 32 and 64
st-002.shown-inference = <not inferred>
st-002.shown-unsupported = <unsupported>
st-002.shown-model = <model>
st-002.shown-state = <state>
st-002.shown-numeric = <numeric>
st-007.zero-size-array = a zero-size array ('{ty}') is inexpressible in IEC 61131-3: the range 'ARRAY [0..-1]' is empty
st-008.unresolved-declaration = {kind} '{name}' is not found in the model: a type declaration for ST cannot be built
st.kind-enum = enumeration
st.kind-struct = structure

# ─── Target st: expressions (`generator/st/st_expr.rs`) ──────────────────────
st-011.refusal = not translated to Structured Text: {what}
st.what-duration = a duration literal
st.what-empty-expression = an empty expression
st.what-unresolved-expression = the expression did not pass semantic lowering (Unresolved)
st.what-array-slice = an array slice: IEC 61131-3 has no slice operation
st.what-code-block = a code block as an expression is inexpressible in ST
st.what-named-call = a call with named arguments is inexpressible in ST
st.what-string = a string literal: the ST target does not support strings
st.what-type = a type as an expression
st.what-address = an address literal in value position: an address is a property of placement (`at`, the `address` statement, the map), not a value
st.what-model = a model as an expression
st.what-parameter-list = a parameter list as an expression
st.what-aggregate = an aggregate in value position: IEC 61131-3 has no array value — assign the elements one by one or pass the array as a `VAR_IN_OUT` parameter
st.unresolved-variable-comment = (*unresolved variable*)
st.what-bitwise-unknown-type = a bitwise operation on an operand whose integer type is not determined statically: in IEC 61131-3 such operations need a bit string (BYTE/WORD/DWORD/LWORD), and the width must be known
st.what-bit-access-unknown-type = a bit access on an operand whose type is not determined statically: the width is needed to build the mask
st.what-bit-of-bool = bit {bit} of a single-bit value: in IEC 61131-3 a BOOL has no bits other than zero
st.what-bit-access-type = a bit access on type '{ty}': the mask is built only for IEC integer types (8/16/32/64 bits)
st.what-bit-out-of-width = bit {bit} is outside the width of type '{ty}' ({bits} bits)
st.what-cast-unknown-type = a cast of an operand whose type is not determined statically: the name of the IEC conversion function is built from BOTH types (<FROM>_TO_<TO>)
